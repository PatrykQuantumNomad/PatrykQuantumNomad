---
phase: 99-download-ui-and-colab-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/eda/NotebookActions.astro
  - src/lib/eda/notebooks/notebook-urls.ts
  - src/lib/eda/notebooks/__tests__/notebook-urls.test.ts
  - src/pages/eda/case-studies/[...slug].astro
autonomous: false
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "Every case study page with a notebook shows a download button linking to /downloads/notebooks/{slug}.zip"
    - "Every case study page with a notebook shows an Open in Colab badge linking to Colab GitHub URL"
    - "Case study pages without notebooks (3 advanced slugs) show no notebook actions"
    - "Download button matches CaseStudyDataset button styling (inline-flex, accent bg, white text)"
  artifacts:
    - path: "src/components/eda/NotebookActions.astro"
      provides: "Download button and Colab badge UI"
      min_lines: 20
    - path: "src/lib/eda/notebooks/notebook-urls.ts"
      provides: "Pure functions for URL construction and slug availability"
      exports: ["getDownloadUrl", "getColabUrl", "hasNotebook", "NOTEBOOK_SLUGS"]
    - path: "src/lib/eda/notebooks/__tests__/notebook-urls.test.ts"
      provides: "Unit tests for URL construction and slug filtering"
      min_lines: 30
    - path: "src/pages/eda/case-studies/[...slug].astro"
      provides: "Page template with NotebookActions wired in"
      contains: "NotebookActions"
  key_links:
    - from: "src/components/eda/NotebookActions.astro"
      to: "src/lib/eda/notebooks/notebook-urls.ts"
      via: "imports getDownloadUrl, getColabUrl, hasNotebook"
      pattern: "getDownloadUrl|getColabUrl|hasNotebook"
    - from: "src/pages/eda/case-studies/[...slug].astro"
      to: "src/components/eda/NotebookActions.astro"
      via: "import and render with slug prop"
      pattern: "NotebookActions"
    - from: "src/lib/eda/notebooks/notebook-urls.ts"
      to: "src/lib/eda/notebooks/templates/standard.ts"
      via: "re-exports STANDARD_SLUGS as NOTEBOOK_SLUGS"
      pattern: "STANDARD_SLUGS"
---

<objective>
Create the NotebookActions UI component with download button and Colab badge, and wire it into the case study page template so all pages with notebooks automatically show both actions.

Purpose: Users visiting case study pages need a clear, one-click path to download the notebook ZIP or open it directly in Google Colab for zero-install execution.

Output: NotebookActions.astro component, testable URL helper module, unit tests, and wired page template.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-download-ui-and-colab-integration/99-RESEARCH.md
@src/pages/eda/case-studies/[...slug].astro
@src/components/eda/CaseStudyDataset.astro
@src/lib/eda/notebooks/templates/standard.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/pages/eda/case-studies/[...slug].astro (current structure):
```astro
<!-- slug is extracted at line 23 -->
const slug = page.id.replace('case-studies/', '');

<!-- Layout structure: header -> prose div -> end -->
<header class="mb-10">
  <h1 ...>{page.data.title}</h1>
  <p ...>NIST/SEMATECH Section {page.data.nistSection}</p>
</header>

<div class="prose-foundations ...">
  <Content />
</div>
```

From src/components/eda/CaseStudyDataset.astro (button styling pattern):
```astro
<!-- Primary button style (accent bg, white text) -->
<a class="inline-flex items-center gap-1.5 px-3 py-1.5 text-sm font-medium rounded
         bg-[var(--color-accent)] !text-white hover:opacity-90 transition-opacity !no-underline">

<!-- Secondary button style (border, muted text) -->
<a class="inline-flex items-center gap-1.5 px-3 py-1.5 text-sm font-medium rounded
         border border-[var(--color-border)] !text-[var(--color-text-secondary)]
         hover:!text-[var(--color-accent)] hover:border-[var(--color-accent)] transition-colors !no-underline">
```

From src/lib/eda/notebooks/templates/standard.ts:
```typescript
export const STANDARD_SLUGS: string[] = [
  'normal-random-numbers', 'uniform-random-numbers', 'heat-flow-meter',
  'filter-transmittance', 'cryothermometry', 'fatigue-life', 'standard-resistor',
];
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create notebook URL helpers and NotebookActions component with page template wiring</name>
  <files>src/lib/eda/notebooks/notebook-urls.ts, src/lib/eda/notebooks/__tests__/notebook-urls.test.ts, src/components/eda/NotebookActions.astro, src/pages/eda/case-studies/[...slug].astro</files>
  <behavior>
    - Test 1: getDownloadUrl('normal-random-numbers') returns '/downloads/notebooks/normal-random-numbers.zip'
    - Test 2: getColabUrl('heat-flow-meter') returns 'https://colab.research.google.com/github/PatrykQuantumNomad/PatrykQuantumNomad/blob/main/notebooks/eda/heat-flow-meter.ipynb'
    - Test 3: hasNotebook('normal-random-numbers') returns true (standard slug)
    - Test 4: hasNotebook('beam-deflections') returns false (advanced slug, not yet available)
    - Test 5: hasNotebook('nonexistent-slug') returns false
    - Test 6: NOTEBOOK_SLUGS has exactly 7 entries matching STANDARD_SLUGS
    - Test 7: getDownloadUrl and getColabUrl work correctly for all 7 standard slugs
  </behavior>
  <action>
    1. Create `src/lib/eda/notebooks/notebook-urls.ts` with pure functions:
       - Import `STANDARD_SLUGS` from `./templates/standard`
       - Export `NOTEBOOK_SLUGS` as a re-export of STANDARD_SLUGS (Phase 100 will extend this)
       - `getDownloadUrl(slug: string): string` returns `/downloads/notebooks/${slug}.zip`
       - `getColabUrl(slug: string): string` returns `https://colab.research.google.com/github/PatrykQuantumNomad/PatrykQuantumNomad/blob/main/notebooks/eda/${slug}.ipynb`
       - `hasNotebook(slug: string): boolean` returns `NOTEBOOK_SLUGS.includes(slug)`

    2. Create `src/lib/eda/notebooks/__tests__/notebook-urls.test.ts` with tests per behavior spec above.

    3. Create `src/components/eda/NotebookActions.astro`:
       - Import `getDownloadUrl`, `getColabUrl`, `hasNotebook` from the helper module
       - Props interface: `{ slug: string }`
       - Early return (render nothing) if `!hasNotebook(slug)`
       - Render a `<section>` with `class="my-6 flex flex-wrap items-center gap-3"` containing:
         a. Download button: `<a>` with `href={getDownloadUrl(slug)}`, `download` attribute, styled with the CaseStudyDataset primary button pattern (`inline-flex items-center gap-1.5 px-3 py-1.5 text-sm font-medium rounded bg-[var(--color-accent)] !text-white hover:opacity-90 transition-opacity !no-underline`), download icon SVG (24x24 Heroicons outline: arrow-down-tray path `M3 16.5v2.25A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75V16.5M16.5 12L12 16.5m0 0L7.5 12m4.5 4.5V3`), label "Download Notebook"
         b. Colab badge: `<a>` with `href={getColabUrl(slug)}`, `target="_blank"`, `rel="noopener noreferrer"`, `class="inline-flex items-center !no-underline"`, containing `<img src="https://colab.research.google.com/assets/colab-badge.svg" alt="Open in Colab" width="117" height="20" />`

    4. Modify `src/pages/eda/case-studies/[...slug].astro`:
       - Add import: `import NotebookActions from '../../../components/eda/NotebookActions.astro';`
       - Place `<NotebookActions slug={slug} />` between the closing `</header>` tag and the opening `<div class="prose-foundations ...">` tag. This ensures the component is OUTSIDE the prose wrapper to avoid CSS conflicts (per research pitfall 4).

    IMPORTANT: Place NotebookActions OUTSIDE the prose-foundations div to prevent prose CSS from overriding button styles.
  </action>
  <verify>
    <automated>npx vitest run src/lib/eda/notebooks/__tests__/notebook-urls.test.ts -x && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <done>URL helper tests pass for all 7 standard slugs, NotebookActions component renders download button and Colab badge, page template includes the component between header and prose div, and `astro build` succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of download button and Colab badge on case study page</name>
  <files>src/components/eda/NotebookActions.astro, src/pages/eda/case-studies/[...slug].astro</files>
  <action>
    Human visually verifies the NotebookActions component renders correctly on case study pages.
    Run `npx astro dev` and inspect the download button and Colab badge placement, styling, and conditional rendering.
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -3</automated>
  </verify>
  <done>User confirms download button and Colab badge render correctly on standard case study pages and are absent on advanced case study pages.</done>
  <what-built>NotebookActions component with download button and Colab badge, wired into all case study pages. Pages with standard notebooks (7 of 10) show both actions; the 3 advanced pages show nothing.</what-built>
  <how-to-verify>
    1. Run `npx astro dev` and visit http://localhost:4321/eda/case-studies/normal-random-numbers/
    2. Verify a "Download Notebook" button appears between the page header and the content
    3. Verify the button has accent background color with white text and a download icon
    4. Verify an "Open in Colab" badge (orange badge image) appears next to the download button
    5. Click the "Open in Colab" badge -- it should open a new tab (Colab may show file-not-found until .ipynb files are pushed to GitHub, which is expected)
    6. Visit http://localhost:4321/eda/case-studies/beam-deflections/ -- verify NO notebook actions appear (advanced slug)
    7. Visit http://localhost:4321/eda/case-studies/ceramic-strength/ -- verify NO notebook actions appear (advanced slug)
    8. Verify buttons are visually consistent with the existing "Download CSV" button style in the Dataset section further down the page
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/eda/notebooks/__tests__/notebook-urls.test.ts` passes all tests
- `npx astro build` succeeds with zero errors
- `grep -l 'Download Notebook' dist/eda/case-studies/normal-random-numbers/index.html` finds the button
- `grep -l 'colab-badge.svg' dist/eda/case-studies/normal-random-numbers/index.html` finds the badge
- `grep -c 'Download Notebook' dist/eda/case-studies/beam-deflections/index.html` returns 0 (no button for advanced slug)
- `npx vitest run` full suite passes
</verification>

<success_criteria>
- NotebookActions component renders download button + Colab badge for 7 standard slugs
- NotebookActions renders nothing for 3 advanced slugs
- Download button matches CaseStudyDataset styling pattern
- Colab badge uses official Google badge SVG with correct alt text
- Component is placed outside prose wrapper to avoid CSS conflicts
- URL helper functions are tested and correct
- Build succeeds with zero errors
- User visually approves the UI
</success_criteria>

<output>
After completion, create `.planning/phases/99-download-ui-and-colab-integration/99-02-SUMMARY.md`
</output>
