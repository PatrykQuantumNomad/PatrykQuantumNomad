---
phase: 87-guide-specific-components
plan: 03
type: execute
wave: 2
depends_on: ["87-01"]
files_modified:
  - src/components/guide/MiddlewareStackDiagram.astro
  - src/components/guide/BuilderPatternDiagram.astro
  - src/components/guide/JwtAuthFlowDiagram.astro
  - src/components/guide/DeploymentTopology.tsx
  - src/components/guide/topology/TopologyNode.tsx
  - src/components/guide/topology/deployment-topology.css
autonomous: false
requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-04]

must_haves:
  truths:
    - "MiddlewareStackDiagram renders the 8-layer middleware stack as a build-time SVG via PlotFigure"
    - "BuilderPatternDiagram renders the Builder pattern composition as a build-time SVG via PlotFigure"
    - "JwtAuthFlowDiagram renders the JWT auth flow with 3 validation modes as a build-time SVG via PlotFigure"
    - "DeploymentTopology renders an interactive React Flow diagram with app, Postgres, Redis, and reverse proxy nodes"
    - "DeploymentTopology supports pan, zoom, and drag via React Flow Controls"
  artifacts:
    - path: "src/components/guide/MiddlewareStackDiagram.astro"
      provides: "Astro wrapper for DIAG-01 SVG"
      min_lines: 5
    - path: "src/components/guide/BuilderPatternDiagram.astro"
      provides: "Astro wrapper for DIAG-02 SVG"
      min_lines: 5
    - path: "src/components/guide/JwtAuthFlowDiagram.astro"
      provides: "Astro wrapper for DIAG-03 SVG"
      min_lines: 5
    - path: "src/components/guide/DeploymentTopology.tsx"
      provides: "Interactive React Flow deployment topology diagram (DIAG-04)"
      min_lines: 60
    - path: "src/components/guide/topology/TopologyNode.tsx"
      provides: "Custom React Flow node for deployment services"
      min_lines: 15
    - path: "src/components/guide/topology/deployment-topology.css"
      provides: "Dark-theme CSS overrides for React Flow"
      min_lines: 10
  key_links:
    - from: "src/components/guide/MiddlewareStackDiagram.astro"
      to: "src/lib/guides/svg-diagrams/index.ts"
      via: "import { generateMiddlewareStack } from '../../lib/guides/svg-diagrams'"
      pattern: "import.*generateMiddlewareStack.*from.*svg-diagrams"
    - from: "src/components/guide/MiddlewareStackDiagram.astro"
      to: "src/components/eda/PlotFigure.astro"
      via: "import PlotFigure from '../eda/PlotFigure.astro'"
      pattern: "import PlotFigure from.*PlotFigure"
    - from: "src/components/guide/DeploymentTopology.tsx"
      to: "@xyflow/react"
      via: "import { ReactFlow, Controls, Background } from '@xyflow/react'"
      pattern: "import.*ReactFlow.*from.*@xyflow/react"
    - from: "src/components/guide/DeploymentTopology.tsx"
      to: "@dagrejs/dagre"
      via: "import dagre from '@dagrejs/dagre'"
      pattern: "import dagre from.*dagre"
---

<objective>
Create 3 Astro wrapper components for the build-time SVG diagrams (using generators from Plan 01) and the interactive React Flow deployment topology diagram for the Docker chapter.

Purpose: These are the embeddable components that guide MDX pages will import directly. The Astro wrappers call SVG generators at build time (zero client JS), while DeploymentTopology provides interactive pan/zoom/drag for the deployment architecture view.
Output: 3 Astro diagram wrappers + 1 interactive React Flow component with custom node and dark-theme CSS.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-guide-specific-components/87-RESEARCH.md
@.planning/phases/87-guide-specific-components/87-01-SUMMARY.md

@src/components/eda/PlotFigure.astro
@src/components/tools/compose-results/DependencyGraph.tsx
@src/components/tools/compose-results/ServiceNode.tsx
@src/components/tools/compose-results/dependency-graph.css
</context>

<interfaces>
<!-- Key types and contracts from Plan 01 (SVG generators) and existing codebase. -->

From src/lib/guides/svg-diagrams/index.ts (created by Plan 01):
```typescript
export { generateMiddlewareStack } from './middleware-stack';
export { generateBuilderPattern } from './builder-pattern';
export { generateJwtAuthFlow } from './jwt-auth-flow';
export { DIAGRAM_PALETTE, type DiagramConfig } from './diagram-base';
```

From src/components/eda/PlotFigure.astro (existing):
```typescript
interface Props {
  svg: string;
  caption?: string;
  maxWidth?: string;  // defaults to '720px'
}
```

From @xyflow/react (installed v12.10.1):
```typescript
import { ReactFlow, Controls, Background, BackgroundVariant, MarkerType } from '@xyflow/react';
import { Handle, Position, type NodeProps, type Node } from '@xyflow/react';
```

From @dagrejs/dagre (installed v2.0.4):
```typescript
import dagre from '@dagrejs/dagre';
const g = new dagre.graphlib.Graph().setDefaultEdgeLabel(() => ({}));
g.setGraph({ rankdir: 'TB', nodesep: 60, ranksep: 100 });
```

From src/components/tools/compose-results/ServiceNode.tsx (pattern to follow):
```typescript
export type ServiceNodeData = { label: string; image?: string; };
export type ServiceNodeType = Node<ServiceNodeData, 'service'>;
export function ServiceNode({ data }: NodeProps<ServiceNodeType>) { ... }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create 3 Astro diagram wrappers and React Flow deployment topology</name>
  <files>
    src/components/guide/MiddlewareStackDiagram.astro,
    src/components/guide/BuilderPatternDiagram.astro,
    src/components/guide/JwtAuthFlowDiagram.astro,
    src/components/guide/DeploymentTopology.tsx,
    src/components/guide/topology/TopologyNode.tsx,
    src/components/guide/topology/deployment-topology.css
  </files>
  <action>
    **Part A: 3 Astro SVG Wrappers**

    Each wrapper follows the same minimal pattern (like NormalRandomPlots.astro):

    `MiddlewareStackDiagram.astro`:
    ```astro
    ---
    import PlotFigure from '../eda/PlotFigure.astro';
    import { generateMiddlewareStack } from '../../lib/guides/svg-diagrams';
    const svg = generateMiddlewareStack();
    ---
    <PlotFigure svg={svg} caption="Request flow through the 8-layer middleware stack" maxWidth="720px" />
    ```

    `BuilderPatternDiagram.astro`:
    ```astro
    ---
    import PlotFigure from '../eda/PlotFigure.astro';
    import { generateBuilderPattern } from '../../lib/guides/svg-diagrams';
    const svg = generateBuilderPattern();
    ---
    <PlotFigure svg={svg} caption="Builder pattern: setup_*() method composition and create_app() factory" maxWidth="780px" />
    ```

    `JwtAuthFlowDiagram.astro`:
    ```astro
    ---
    import PlotFigure from '../eda/PlotFigure.astro';
    import { generateJwtAuthFlow } from '../../lib/guides/svg-diagrams';
    const svg = generateJwtAuthFlow();
    ---
    <PlotFigure svg={svg} caption="JWT authentication: 3 validation modes (shared secret, static key, JWKS)" maxWidth="720px" />
    ```

    **Part B: DeploymentTopology React Flow Component**

    `topology/TopologyNode.tsx`:
    - Export `TopologyNodeData` type: `{ label: string; subtitle?: string; icon: 'app' | 'db' | 'cache' | 'proxy' }`
    - Export `TopologyNodeType` = `Node<TopologyNodeData, 'topology'>`
    - Export `TopologyNode` function component following ServiceNode pattern:
      - Container div with rounded-lg border, dark-theme CSS variable colors
      - Target Handle at top, Source Handle at bottom
      - Display icon (simple SVG shape or emoji based on icon type: app=gear, db=cylinder, cache=lightning, proxy=shield), label in bold, subtitle in secondary text
      - Width 200px for consistent dagre layout

    `topology/deployment-topology.css`:
    - Follow dependency-graph.css pattern exactly:
      - `.deployment-topology .react-flow` with `--xy-background-color: transparent` and theme variable overrides
      - Control button styles with surface/border colors
      - Attribution hidden
      - Arrow marker color override

    `DeploymentTopology.tsx`:
    - Import ReactFlow, Controls, Background, BackgroundVariant, MarkerType from `@xyflow/react`
    - Import `@xyflow/react/dist/style.css` (CRITICAL -- without this, React Flow has no styling)
    - Import dagre from `@dagrejs/dagre`
    - Import TopologyNode and types from `./topology/TopologyNode`
    - Import `./topology/deployment-topology.css`

    Module-level constants (avoid re-registration):
    - `nodeTypes = { topology: TopologyNode }`
    - `NODE_WIDTH = 200`, `NODE_HEIGHT = 70`

    Static data (hardcoded, not data-driven):
    - 4 nodes:
      - { id: 'proxy', label: 'Caddy', subtitle: 'Reverse Proxy', icon: 'proxy' }
      - { id: 'app', label: 'FastAPI App', subtitle: 'Uvicorn Workers', icon: 'app' }
      - { id: 'postgres', label: 'PostgreSQL', subtitle: 'Primary Database', icon: 'db' }
      - { id: 'redis', label: 'Redis', subtitle: 'Cache & Rate Limits', icon: 'cache' }
    - 3 edges:
      - proxy -> app (label: "HTTP/HTTPS")
      - app -> postgres (label: "asyncpg")
      - app -> redis (label: "aioredis")

    Layout function (same dagre pattern as DependencyGraph.tsx):
    - Create dagre graph with `rankdir: 'TB'`, `nodesep: 80`, `ranksep: 120`
    - Set nodes with NODE_WIDTH/NODE_HEIGHT
    - Set edges
    - Run dagre.layout(g)
    - Map positioned nodes

    Render:
    - Outer div with className `deployment-topology` and fixed height `h-[350px] lg:h-[450px]`
    - ReactFlow with nodes, edges, nodeTypes, fitView, fitViewOptions padding 0.4
    - proOptions: `{ hideAttribution: true }`
    - Controls with `showInteractive={false}` at bottom-right
    - Background with Dots variant
    - Edge marker: MarkerType.ArrowClosed with color `'#888'`
    - nodesConnectable={false}, nodesDraggable={true}

    Export default the component (for lazy loading in MDX).

    NOTE: In MDX pages (Phase 88), this component will be imported as:
    `import DeploymentTopology from '../../components/guide/DeploymentTopology'`
    and used as `<DeploymentTopology client:visible />`
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -5</automated>
  </verify>
  <done>3 Astro diagram wrappers exist and produce SVG at build time. DeploymentTopology.tsx renders 4 nodes and 3 edges with dagre layout, dark theme CSS, custom nodes, and React Flow controls. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all 4 diagrams</name>
  <files>src/data/guides/fastapi-production/pages/00-builder-pattern.mdx</files>
  <action>
    The executor should temporarily add all 4 diagram components to the builder-pattern MDX stub page for visual verification, then revert changes after user approval. The executor adds imports and renders each component, starts dev server, and presents URLs to user.
  </action>
  <what-built>Complete set of guide-specific components: 3 build-time SVG architecture diagrams (middleware stack, builder pattern, JWT auth flow) and 1 interactive React Flow deployment topology.</what-built>
  <how-to-verify>
    1. Run `npx astro dev` and navigate to `/guides/fastapi-production/builder-pattern/`
    2. The page should load without errors (it uses GuideLayout from Phase 86)
    3. To visually verify the SVG diagrams, temporarily add them to the builder-pattern stub page:
       - Open `src/data/guides/fastapi-production/pages/00-builder-pattern.mdx`
       - Add imports for the 3 diagram components and render them
       - Verify each diagram renders with correct labels, arrows, and theme colors
       - The middleware stack should show 8 labeled layers with downward flow
       - The builder pattern should show setup_*() methods and create_app() factory
       - The JWT flow should show 3 branching paths (Shared Secret, Static Key, JWKS)
    4. For DeploymentTopology, add it to the MDX with `client:visible`:
       ```mdx
       import DeploymentTopology from '../../components/guide/DeploymentTopology'
       <DeploymentTopology client:visible />
       ```
       - Verify 4 nodes (Caddy, FastAPI App, PostgreSQL, Redis) render
       - Verify 3 edges with labels (HTTP/HTTPS, asyncpg, aioredis) render
       - Verify pan, zoom, and drag work
       - Verify dark theme matches site aesthetic
    5. After verification, revert the MDX changes (diagrams will be properly placed in Phase 88)
  </how-to-verify>
  <verify>
    <automated>npx astro dev -- visual verification by user</automated>
  </verify>
  <done>User approves visual appearance of all 4 diagrams. MDX changes reverted.</done>
  <resume-signal>Type "approved" or describe any visual issues with the diagrams</resume-signal>
</task>

</tasks>

<verification>
```bash
npx vitest run src/lib/guides/ --reporter=verbose && npx astro build 2>&1 | tail -5
```
All tests from Plan 01 and Plan 02 still pass, and build succeeds with all new components.
</verification>

<success_criteria>
- 3 Astro diagram wrappers each call their respective generator and render via PlotFigure
- maxWidth props match each diagram's viewBox width (720px for middleware/JWT, 780px for builder)
- DeploymentTopology renders 4 service nodes (Caddy, FastAPI App, PostgreSQL, Redis) with dagre layout
- 3 edges with protocol labels (HTTP/HTTPS, asyncpg, aioredis) connect the nodes correctly
- Dark-theme CSS overrides match the compose-results/dependency-graph.css pattern
- React Flow provides interactive pan/zoom/drag controls
- Astro build succeeds with all new components
- User visually approves all 4 diagrams
</success_criteria>

<output>
After completion, create `.planning/phases/87-guide-specific-components/87-03-SUMMARY.md`
</output>
