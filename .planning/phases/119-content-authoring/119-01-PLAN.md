---
phase: 119-content-authoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/blog/dark-code.mdx
autonomous: true
requirements: [CONT-01, CONT-02, CONT-04, CONT-05, CONT-06, CONT-08, CONT-09, INTG-05]
must_haves:
  truths:
    - "dark-code.mdx exists with valid frontmatter that passes astro build"
    - "OpeningStatement component renders the bold opening claim"
    - "TldrSummary renders 3-5 bullet summary of the essay argument"
    - "Act 1 contains ~1500 words across 4 sections following the outline structure"
    - "StatHighlight components render key statistics (4x clones, refactoring collapse, 23.7% vulns, 5-day exploit, 17% comprehension, 31% defect)"
    - "TermDefinition component renders the formal Dark Code definition"
    - "GFM footnote syntax renders correctly in the MDX build"
  artifacts:
    - path: "src/data/blog/dark-code.mdx"
      provides: "MDX blog post with frontmatter, imports, opening, TL;DR, and Act 1"
      contains: "OpeningStatement"
  key_links:
    - from: "src/data/blog/dark-code.mdx"
      to: "src/components/blog/OpeningStatement.astro"
      via: "import statement"
      pattern: "import OpeningStatement from"
    - from: "src/data/blog/dark-code.mdx"
      to: "src/components/blog/StatHighlight.astro"
      via: "import statement"
      pattern: "import StatHighlight from"
    - from: "src/data/blog/dark-code.mdx"
      to: "src/components/blog/TermDefinition.astro"
      via: "import statement"
      pattern: "import TermDefinition from"
---

<objective>
Create the dark-code.mdx file with complete frontmatter, component imports, OpeningStatement, TldrSummary, and the full Act 1 (~1500 words) including all StatHighlight and TermDefinition component uses. Verify GFM footnote rendering with a build test.

Purpose: Establishes the MDX file, validates the GFM footnote assumption (highest technical risk), and delivers the essay's opening act with its data-driven alarm. Act 1 is the densest section with 6 StatHighlights and the formal TermDefinition -- getting these right first ensures components work in context before the remaining acts are written.

Output: `src/data/blog/dark-code.mdx` containing frontmatter through end of Act 1 (Section 1.4), plus footnote definitions for all citations used in Act 1.
</objective>

<execution_context>
@/Users/patrykattc/work/git/PatrykQuantumNomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/PatrykQuantumNomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-source-verification-and-outline/outline.md
@.planning/phases/117-source-verification-and-outline/sources.md
@.planning/phases/119-content-authoring/119-RESEARCH.md

<interfaces>
<!-- Component interfaces verified from codebase -->

From src/components/blog/StatHighlight.astro -- Props: stat (string), label (string), source (optional string)

From src/components/blog/TermDefinition.astro -- Props: term (string), pronunciation (optional string). Uses slot for definition content (markdown supported with blank lines around content).

From src/components/blog/OpeningStatement.astro -- Slot-based, renders centered text in not-prose div. Pattern: `<OpeningStatement>Text here</OpeningStatement>`

From src/components/blog/TldrSummary.astro -- Aside with title prop (default "TL;DR"), supports markdown in slot. Requires blank lines after opening tag and before closing tag.

Blog frontmatter schema (from content.config.ts): title (string), description (string), publishedDate (coerce date), updatedDate (optional date), tags (string array), draft (boolean, default false), coverImage (optional string)

Import path convention: `import X from '../../components/blog/X.astro';`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MDX file with frontmatter, imports, OpeningStatement, and TldrSummary</name>
  <files>src/data/blog/dark-code.mdx</files>
  <action>
Create `src/data/blog/dark-code.mdx` with the following content in this exact order:

**1. Frontmatter** (YAML between triple-dash delimiters):
- title: "Dark Code: The Silent Rot AI Accelerated and No One Is Measuring"
- description: "AI coding assistants have accelerated a pre-existing problem: codebases filling with code no one understands, no one owns, and no one can safely change. A framework for measuring how much of your codebase has gone dark."
- publishedDate: 2026-04-14
- tags: ["code-quality", "ai-coding-assistant", "technical-debt", "security", "devops", "architecture"]
- coverImage: "/images/dark-code-cover.svg"
- draft: true

(per CONT-01, INTG-05: 5 existing tags + 1 new "technical-debt")

**2. Component imports** (one blank line after frontmatter closing delimiter):

    import OpeningStatement from '../../components/blog/OpeningStatement.astro';
    import TldrSummary from '../../components/blog/TldrSummary.astro';
    import StatHighlight from '../../components/blog/StatHighlight.astro';
    import TermDefinition from '../../components/blog/TermDefinition.astro';

**3. OpeningStatement** (per CONT-05): Write a bold, provocative opening claim. Use the outline's guidance: the essay's thesis is that codebases are filling with code no one understands, owns, or can safely change. The statement should be one to two sentences, declarative, confident. Example tone from outline: "Your codebase is full of code that no one understands, no one owns, and no one can safely change." Make it punchy and quotable. Do NOT use footnotes inside the component tag.

**4. TldrSummary** (per CONT-06): Write 3-5 bullet points summarizing the essay's core argument. Use the TL;DR content from the outline as the guide:
- AI assistants have accelerated a pre-existing problem (code no one understands/owns/changes)
- Key statistics: 4x clone growth, refactoring collapse, 17% comprehension drop
- Introduction of "The Dark Code Spectrum" framework
- The defense: measure, own, refactor, or accept consequences

Include blank lines after `<TldrSummary>` and before `</TldrSummary>` for MDX parsing. Use bold markdown for emphasis within bullets.

**5. GFM footnote test:** After the TldrSummary, add a single test paragraph with one GFM footnote reference `[^1]` and its definition at the bottom of the file `[^1]: [test](url)`. This tests the assumption A1 from research. This test content will be replaced by Act 1 prose in Task 2.

**CRITICAL: First paragraph after TldrSummary** gets the drop cap CSS treatment (`.prose > p:first-of-type::first-letter`). The first word must be strong and intentional -- do NOT start with a quote, number, or parenthetical. Start with a clear narrative word.

Run `npm run build` after creating the file. If the build passes and footnotes render (no errors), the GFM footnote assumption is confirmed. If footnotes fail, switch to hyperlinked text citations and document the deviation.

**Citation URLs:** Every URL used must be copy-pasted from `.planning/phases/117-source-verification-and-outline/sources.md`. Do NOT type URLs from memory.

**Voice and tone:** Professional but approachable, first person where appropriate, confident, occasionally provocative. Match the voice described in the outline: "Authority with edge."
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>dark-code.mdx exists with valid frontmatter (6 tags including 1 new), 4 component imports, OpeningStatement with bold claim, TldrSummary with 3-5 bullets, and a GFM footnote test that builds successfully. No build errors.</done>
</task>

<task type="auto">
  <name>Task 2: Write Act 1 prose (~1500 words) with StatHighlights, TermDefinition, and footnotes</name>
  <files>src/data/blog/dark-code.mdx</files>
  <action>
Replace the GFM footnote test content (from Task 1) with the full Act 1 prose. Keep everything from Task 1 (frontmatter, imports, OpeningStatement, TldrSummary) intact. Write Act 1 directly after the TldrSummary.

**CRITICAL: First paragraph** immediately after TldrSummary gets the drop cap. Ensure it starts with a strong intentional word (not a number, quote, or parenthetical). This paragraph should set the scene and transition from the TL;DR into the essay's argument.

Write the four sections of Act 1 following the outline exactly:

**Section 1.1: "Four Times the Clones, One-Tenth the Refactoring" (~500w)**
- Lead with the 4x clone growth stat from Source #7 (GitClear 2025)
- Follow with the refactoring collapse from 25% to <10% (Source #7)
- Use Source #16 (GitClear 2023) as the "before" baseline showing acceleration is AI-era specific
- Components to insert (each on its own line with blank lines above/below):
  - `<StatHighlight stat="4x" label="Increase in code clones since AI coding assistants went mainstream" source="GitClear 2025" />`
  - `<StatHighlight stat="<10%" label="Refactoring as a share of all code changes, down from 25%" source="GitClear 2025" />`
- Use GFM footnote references in prose (e.g., `[^1]`, `[^2]`) pointing to Sources #7 and #16

**Section 1.2: "One in Four Lines of AI Code Ships with Exploitable Vulnerabilities" (~400w)**
- Escalate from code quality to security
- Source #2: 23.7% more vulnerabilities, 89% junior acceptance rate
- Source #38: 25.1% of AI code has exploitable vulns
- Source #6: 48,175 CVEs, 5-day exploit window
- Components:
  - `<StatHighlight stat="23.7%" label="More security vulnerabilities in AI-generated code" source="ResearchGate 2024" />`
  - `<StatHighlight stat="5 days" label="Average time from vulnerability disclosure to active exploitation" source="Mondoo 2026" />`
- Footnotes referencing Sources #2, #38, #6

**Section 1.3: "The Comprehension Crisis No One Is Talking About" (~350w)**
- AI degrades developer comprehension, not just code quality
- Source #25: Anthropic RCT, 17% lower comprehension in 52-engineer study
- Source #15: SpinRoot "First Law" -- codebases grow exponentially
- Components:
  - `<StatHighlight stat="17%" label="Lower comprehension scores for AI-assisted developers" source="Anthropic 2025" />`
  - TermDefinition for "Dark Code" (per CONT-09). Use the definition from the outline/research: "Code that executes in production but is not understood by any current team member. It compiles, it ships, it runs -- and no one on the team can explain what it does, why it exists, or what breaks if it changes." Extend with the "shadow inventory" framing from the research code example. Include `pronunciation="/dark kohd/"`.
- Footnotes referencing Sources #25, #15

**Section 1.4: "Technical Debt Is Compounding -- Literally" (~250w)**
- Short, punchy closer to Act 1
- Source #41: 31% defect increase per standard deviation of tech debt, 14% quarterly compound rate
- Component:
  - `<StatHighlight stat="31%" label="Defect density increase per standard deviation of technical debt" source="American Impact Review 2026" />`
- Footnote referencing Source #41
- End with a transition sentence that implies the need for a systematic measurement framework (leading into Act 2)

**Footnote definitions:** Place ALL footnote definitions at the very end of the file, after all prose. One blank line before the footnote block. Each on its own line, e.g.:
`[^1]: [GitClear: AI Copilot Code Quality 2025](https://www.gitclear.com/ai_assistant_code_quality_2025_research)`

**All citation URLs** must be copied from sources.md. The expected footnotes for Act 1 are Sources #7, #16, #2, #38, #6, #25, #15, #41 (8 unique sources, likely 8-10 footnote references since some may be cited multiple times within Act 1).

**Anti-patterns to avoid:**
- Do NOT put footnote references `[^n]` inside JSX component tags (StatHighlight, TermDefinition, etc.)
- Do NOT scatter footnote definitions throughout the file -- all at the end
- Blank lines MUST surround component tags
- Components MUST be at top level of MDX (not inside blockquotes, lists, etc.)

**Word budget:** Act 1 total should be ~1500 words. Use the section targets as guidelines (500+400+350+250). Do not exceed 1725 words (15% over budget).

Run `npm run build` after writing. Verify no errors.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Act 1 is complete with ~1500 words across 4 sections, 6 StatHighlight components, 1 TermDefinition component, 8-10 GFM footnote references, and all footnote definitions at the end of the file. Build passes with no errors. The first paragraph after TldrSummary starts with a strong word suitable for drop cap rendering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External URLs in footnotes | Citation URLs link to third-party sites; verified in Phase 117 |
| MDX build pipeline | User-authored MDX content processed by Astro/remark-gfm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01 | Tampering | Footnote URLs | mitigate | All URLs copy-pasted from verified sources.md; no URLs typed from memory |
| T-119-02 | Information Disclosure | Draft flag | accept | Post set to draft: true; not publicly visible until Phase 120 sets draft: false |
</threat_model>

<verification>
1. `npm run build` completes with zero errors
2. dark-code.mdx exists at src/data/blog/ with correct frontmatter schema
3. GFM footnotes render (no footnote-related build errors)
4. All component imports resolve (OpeningStatement, TldrSummary, StatHighlight, TermDefinition)
5. Word count for Act 1 is approximately 1500 words (check via build output reading time or manual count)
</verification>

<success_criteria>
- dark-code.mdx builds successfully with Astro
- Frontmatter has all required fields: title, description, publishedDate, tags (6), coverImage, draft: true
- OpeningStatement renders a bold, quotable opening claim
- TldrSummary renders 3-5 summary bullets
- Act 1 has 4 headed sections matching outline titles
- 6 StatHighlight components used with correct stat/label/source props
- 1 TermDefinition with "Dark Code" term, pronunciation, and definition
- 8-10 GFM footnote references in prose, definitions at end of file
- All citation URLs match sources.md exactly
- First prose paragraph starts with a strong word (not number/quote/parenthetical)
</success_criteria>

<output>
After completion, create `.planning/phases/119-content-authoring/119-01-SUMMARY.md`
</output>
