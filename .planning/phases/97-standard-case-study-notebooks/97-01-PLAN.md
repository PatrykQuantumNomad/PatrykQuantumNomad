---
phase: 97-standard-case-study-notebooks
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/eda/notebooks/templates/standard.ts
  - src/lib/eda/notebooks/templates/sections/intro.ts
  - src/lib/eda/notebooks/templates/sections/setup.ts
  - src/lib/eda/notebooks/templates/sections/data-loading.ts
  - src/lib/eda/notebooks/templates/sections/summary-stats.ts
  - src/lib/eda/notebooks/templates/sections/four-plot.ts
  - src/lib/eda/notebooks/templates/sections/individual-plots.ts
  - src/lib/eda/notebooks/templates/sections/hypothesis-tests.ts
  - src/lib/eda/notebooks/templates/sections/test-summary.ts
  - src/lib/eda/notebooks/templates/sections/interpretation.ts
  - src/lib/eda/notebooks/templates/sections/conclusions.ts
  - src/lib/eda/notebooks/__tests__/standard-notebook.test.ts
autonomous: true
requirements: [NBSTD-01, NBSTD-02, NBSTD-03, NBSTD-04, NBSTD-05, NBSTD-06, NBSTD-07]

must_haves:
  truths:
    - "buildStandardNotebook(slug) produces valid nbformat v4.5 JSON for all 7 standard slugs"
    - "Each notebook contains interleaved markdown and code cells in correct section order"
    - "Data loading cells include row-count assertions matching each config.expectedRows"
    - "All cell IDs are unique within each notebook and match ^[a-zA-Z0-9]+$ pattern"
    - "Notebooks use THEME_SETUP_CODE and DEPENDENCY_CHECK_CODE from Phase 96 theme.ts"
    - "Multi-value-per-line datasets (RANDN, RANDU, SOULEN) generate flatten() code"
    - "Multi-column datasets (DZIUBA1) generate column names in read_fwf()"
    - "4-plot cells use QUANTUM_COLORS for Quantum Explorer theme"
    - "Individual plot cells produce 4 separate code cells (run sequence, lag, histogram, probability)"
  artifacts:
    - path: "src/lib/eda/notebooks/templates/standard.ts"
      provides: "buildStandardNotebook(slug) entry point and STANDARD_SLUGS constant"
      exports: ["buildStandardNotebook", "STANDARD_SLUGS"]
    - path: "src/lib/eda/notebooks/templates/sections/intro.ts"
      provides: "Title + background + goals markdown cells"
      exports: ["buildIntro"]
    - path: "src/lib/eda/notebooks/templates/sections/setup.ts"
      provides: "Dependency check + theme + imports cells"
      exports: ["buildSetup"]
    - path: "src/lib/eda/notebooks/templates/sections/data-loading.ts"
      provides: "Data load with Colab fallback + preview + assertion"
      exports: ["buildDataLoading"]
    - path: "src/lib/eda/notebooks/templates/sections/summary-stats.ts"
      provides: "Summary statistics computation + display cell"
      exports: ["buildSummaryStats"]
    - path: "src/lib/eda/notebooks/templates/sections/four-plot.ts"
      provides: "4-plot code cell (run sequence, lag, histogram, probability)"
      exports: ["buildFourPlot"]
    - path: "src/lib/eda/notebooks/templates/sections/individual-plots.ts"
      provides: "Individual plot cells (4 separate code cells)"
      exports: ["buildIndividualPlots"]
    - path: "src/lib/eda/notebooks/templates/sections/hypothesis-tests.ts"
      provides: "Stub: location, variation, randomness, distribution, outlier test cells"
      exports: ["buildHypothesisTests"]
    - path: "src/lib/eda/notebooks/templates/sections/test-summary.ts"
      provides: "Stub: test summary table computation + display"
      exports: ["buildTestSummary"]
    - path: "src/lib/eda/notebooks/templates/sections/interpretation.ts"
      provides: "Stub: case-study-specific interpretation narrative"
      exports: ["buildInterpretation"]
    - path: "src/lib/eda/notebooks/templates/sections/conclusions.ts"
      provides: "Stub: key findings + next steps markdown"
      exports: ["buildConclusions"]
    - path: "src/lib/eda/notebooks/__tests__/standard-notebook.test.ts"
      provides: "Structure, data loading, and visualization tests for all 7 notebooks"
      min_lines: 80
  key_links:
    - from: "src/lib/eda/notebooks/templates/standard.ts"
      to: "src/lib/eda/notebooks/registry/index.ts"
      via: "getCaseStudyConfig(slug)"
      pattern: "getCaseStudyConfig"
    - from: "src/lib/eda/notebooks/templates/standard.ts"
      to: "src/lib/eda/notebooks/notebook.ts"
      via: "createNotebook(cells)"
      pattern: "createNotebook"
    - from: "src/lib/eda/notebooks/templates/sections/*.ts"
      to: "src/lib/eda/notebooks/cells.ts"
      via: "markdownCell() and codeCell() factories"
      pattern: "markdownCell|codeCell"
    - from: "src/lib/eda/notebooks/templates/sections/setup.ts"
      to: "src/lib/eda/notebooks/theme.ts"
      via: "THEME_SETUP_CODE and DEPENDENCY_CHECK_CODE imports"
      pattern: "THEME_SETUP_CODE|DEPENDENCY_CHECK_CODE"
---

<objective>
Build the standard notebook template skeleton with `buildStandardNotebook(slug)` entry point, 6 fully-implemented section builders (intro through individual-plots), 4 stub section builders (hypothesis-tests through conclusions), and structural tests.

Purpose: Establishes the end-to-end notebook generation pipeline and implements the visualization-heavy first half of each notebook. Plan 02 fills in the analysis and narrative content.

Output: 12 TypeScript files (1 entry point + 6 full sections + 4 stubs + 1 test file) producing structurally valid notebooks with complete setup, data loading, and visualization sections.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-notebook-foundation/96-01-SUMMARY.md
@.planning/phases/96-notebook-foundation/96-02-SUMMARY.md
@.planning/phases/97-standard-case-study-notebooks/97-RESEARCH.md

@src/lib/eda/notebooks/types.ts
@src/lib/eda/notebooks/cells.ts
@src/lib/eda/notebooks/notebook.ts
@src/lib/eda/notebooks/theme.ts
@src/lib/eda/notebooks/registry/types.ts
@src/lib/eda/notebooks/registry/index.ts
@src/lib/eda/notebooks/registry/normal-random-numbers.ts
@src/lib/eda/notebooks/registry/uniform-random-numbers.ts
@src/lib/eda/notebooks/registry/heat-flow-meter.ts
@src/lib/eda/notebooks/registry/filter-transmittance.ts
@src/lib/eda/notebooks/registry/cryothermometry.ts
@src/lib/eda/notebooks/registry/fatigue-life.ts
@src/lib/eda/notebooks/registry/standard-resistor.ts

<interfaces>
<!-- Key types and contracts from Phase 96 that this plan builds on. -->

From src/lib/eda/notebooks/types.ts:
```typescript
export type MultilineString = string | string[];
export type CellId = string;
export interface MarkdownCell { id: CellId; cell_type: 'markdown'; metadata: Record<string, unknown>; source: MultilineString; }
export interface CodeCell { id: CellId; cell_type: 'code'; metadata: Record<string, unknown>; source: MultilineString; outputs: Output[]; execution_count: number | null; }
export type Cell = MarkdownCell | CodeCell | RawCell;
export interface NotebookV4 { nbformat: 4; nbformat_minor: 5; metadata: NotebookMetadata; cells: Cell[]; }
```

From src/lib/eda/notebooks/cells.ts:
```typescript
export function cellId(slug: string, index: number): string;
export function markdownCell(slug: string, index: number, source: string[]): MarkdownCell;
export function codeCell(slug: string, index: number, source: string[]): CodeCell;
```

From src/lib/eda/notebooks/notebook.ts:
```typescript
export function createNotebook(cells: Cell[]): NotebookV4;
```

From src/lib/eda/notebooks/registry/types.ts:
```typescript
export interface CaseStudyConfig {
  slug: string; title: string; nistSection: string; dataFile: string;
  skipRows: number; expectedRows: number; columns: string[];
  responseVariable: string;
  expectedStats: { mean: number; stdDev: number; min: number; max: number; median?: number; };
  nistUrl: string; githubRawUrl: string;
  plotTitles: { fourPlot: string; runSequence: string; lagPlot: string; histogram: string; probabilityPlot: string; };
  testParams?: { targetMean?: number; alpha?: number; };
  modelParams?: { type: 'sinusoidal' | 'ar1'; [key: string]: unknown; };
  doeFactors?: { name: string; column: string; levels: (string | number)[]; }[];
  valuesPerLine?: number;
  description?: string;
}
```

From src/lib/eda/notebooks/registry/index.ts:
```typescript
export const CASE_STUDY_REGISTRY: Record<string, CaseStudyConfig>;
export const ALL_CASE_STUDY_SLUGS: string[];
export function getCaseStudyConfig(slug: string): CaseStudyConfig | undefined;
```

From src/lib/eda/notebooks/theme.ts:
```typescript
export const QUANTUM_COLORS: Record<string, string>;
export const QUANTUM_PALETTE: string[];
export const THEME_SETUP_CODE: string[];
export const DEPENDENCY_CHECK_CODE: string[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Template skeleton with setup, data loading, and visualization sections</name>
  <files>
    src/lib/eda/notebooks/__tests__/standard-notebook.test.ts,
    src/lib/eda/notebooks/templates/standard.ts,
    src/lib/eda/notebooks/templates/sections/intro.ts,
    src/lib/eda/notebooks/templates/sections/setup.ts,
    src/lib/eda/notebooks/templates/sections/data-loading.ts,
    src/lib/eda/notebooks/templates/sections/summary-stats.ts,
    src/lib/eda/notebooks/templates/sections/four-plot.ts,
    src/lib/eda/notebooks/templates/sections/individual-plots.ts,
    src/lib/eda/notebooks/templates/sections/hypothesis-tests.ts,
    src/lib/eda/notebooks/templates/sections/test-summary.ts,
    src/lib/eda/notebooks/templates/sections/interpretation.ts,
    src/lib/eda/notebooks/templates/sections/conclusions.ts
  </files>
  <behavior>
    Structure tests (all 7 slugs via it.each):
    - buildStandardNotebook(slug) returns object with nbformat: 4, nbformat_minor: 5
    - Cells array is non-empty (at least 15 cells -- stubs contribute fewer)
    - First cell is markdown (title)
    - All cell IDs unique within each notebook
    - All cell IDs match /^[a-zA-Z0-9]+$/
    - No empty source arrays in any cell
    - JSON.stringify(notebook) produces valid JSON without circular refs

    Data loading tests (per slug):
    - normal-random-numbers notebook contains `assert len(df) == 500`
    - uniform-random-numbers notebook contains `assert len(df) == 500`
    - heat-flow-meter notebook contains `assert len(df) == 195`
    - filter-transmittance notebook contains `assert len(df) == 50`
    - cryothermometry notebook contains `assert len(df) == 700`
    - fatigue-life notebook contains `assert len(df) == 101`
    - standard-resistor notebook contains `assert len(df) == 1000`

    Multi-value parsing:
    - normal-random-numbers data loading contains `flatten()`
    - uniform-random-numbers data loading contains `flatten()`
    - cryothermometry data loading contains `flatten()`
    - heat-flow-meter data loading does NOT contain `flatten()`

    Multi-column:
    - standard-resistor data loading contains column names `Month`, `Day`, `Year`, `Resistance`

    Theme/setup:
    - Every notebook contains `QUANTUM_COLORS` in a code cell source
    - Every notebook contains `!pip install` in a code cell source
    - Every notebook contains `import numpy as np` in a code cell source

    Visualization:
    - Every notebook contains `probplot` (probability plot in 4-plot)
    - Every notebook contains `plt.subplots(2, 2` (4-plot grid)
    - Every notebook contains `Run Sequence` in a markdown cell
    - Every notebook has at least 4 code cells after the 4-plot section (individual plots)
  </behavior>
  <action>
    **RED phase:** Create `standard-notebook.test.ts` with all tests listed in the behavior block. Use `describe` blocks:
    - `describe('buildStandardNotebook structure')` -- structural tests via `it.each` over all 7 slugs
    - `describe('data loading')` -- per-slug row assertions and parsing method tests
    - `describe('setup and theme')` -- theme/dependency/import cell content
    - `describe('visualization sections')` -- 4-plot and individual plot presence

    Helper function in tests: `getAllSource(notebook)` that concatenates all cell sources into a single string for easy content searching. Also `getCodeSources(notebook)` and `getMarkdownSources(notebook)` for type-specific searching.

    Create ALL 12 source files. The first 7 files (standard.ts + intro through individual-plots) as stubs initially. The last 4 section builders (hypothesis-tests, test-summary, interpretation, conclusions) as stubs that will remain stubs in this plan (Plan 02 fills them in). All stubs follow the same pattern: export a function with the correct signature that returns a minimal cell array with a section header markdown cell.

    Run tests -- they MUST fail. Commit: `test(97-01): add failing tests for standard notebook template`.

    **GREEN phase:** Implement the 6 full section builders while leaving the 4 complex sections as stubs.

    **intro.ts** -- `buildIntro(slug, startIndex, config)`:
    - Markdown cell: `# {config.title}` line, blank line, `*NIST/SEMATECH e-Handbook of Statistical Methods, Section {config.nistSection}*` line, blank line, Open in Colab badge (markdown image link using `https://colab.research.google.com/github/PatrykQuantumNomad/PatrykQuantumNomad/blob/main/notebooks/eda/{slug}.ipynb`)
    - Markdown cell: `## Background` header, config.description, blank line, `## Analysis Goals` header, bullet list of analysis steps (4-plot, individual plots, hypothesis tests, interpretation), blank line, `**Data Source:** [{config.dataFile}]({config.nistUrl})` link

    **setup.ts** -- `buildSetup(slug, startIndex, config)`:
    - Code cell: spread `DEPENDENCY_CHECK_CODE` from theme.ts (import it directly)
    - Code cell: spread `THEME_SETUP_CODE` from theme.ts
    - Code cell: additional imports -- `import numpy as np`, `import pandas as pd`, `from scipy import stats`, `from scipy.stats import linregress, bartlett, levene, anderson, shapiro`, `from scipy.stats import t as t_dist`, `import os`, `import urllib.request`

    **data-loading.ts** -- `buildDataLoading(slug, startIndex, config)`:
    - Markdown cell explaining data loading with Colab fallback strategy
    - Code cell with THREE conditional paths based on config properties (NOT slug branching):
      1. If `config.valuesPerLine` is defined: `pd.read_fwf()` into raw df, `df_raw.values.flatten()`, `data[~np.isnan(data)]` to remove NaN padding, wrap in `pd.DataFrame({'{config.responseVariable}': data})`
      2. If `config.columns.length > 1`: `pd.read_fwf()` with `names=[{columns joined}]`
      3. Else: `pd.read_fwf()` with single column name `names=['{config.responseVariable}']`
      All paths preceded by: `DATA_FILE = '{config.dataFile}'`, `GITHUB_URL = '{config.githubRawUrl}'`, `if not os.path.exists(DATA_FILE):` block with `urllib.request.urlretrieve(GITHUB_URL, DATA_FILE)`.
      All paths followed by: `assert len(df) == {config.expectedRows}`, `print(f'Dataset shape: {df.shape}')`, `df.head(10)`
      After DataFrame created: `data = df['{config.responseVariable}'].values` to extract the numeric array used by all subsequent cells.

    **summary-stats.ts** -- `buildSummaryStats(slug, startIndex, config)`:
    - Markdown cell: `## Summary Statistics` header with explanation
    - Code cell computing: `np.mean(data)`, `np.std(data, ddof=1)`, `np.min(data)`, `np.max(data)`, `np.median(data)`, `stats.skew(data)`, `stats.kurtosis(data)`. Print formatted table. Include reference comparison: `print('\\nReference (NIST):')`, `print(f'  Mean: {config.expectedStats.mean}')`, etc. Use `np.isclose(computed, expected, rtol=1e-2)` for pass/fail indicators.

    **four-plot.ts** -- `buildFourPlot(slug, startIndex, config)`:
    - Markdown cell: `## 4-Plot Analysis` header, explanation of what each quadrant shows (run sequence for location shift, lag plot for autocorrelation, histogram for distribution shape, probability plot for normality)
    - Code cell: `fig, axes = plt.subplots(2, 2, figsize=(12, 10))` with `fig.suptitle('{config.plotTitles.fourPlot}')`. Four subplots exactly as shown in 97-RESEARCH.md Code Examples section: run sequence (upper left), lag plot (upper right), histogram (lower left), normal probability plot (lower right). Use QUANTUM_COLORS dict for all colors. Call `stats.probplot(data, dist='norm', plot=axes[1, 1])`. End with `plt.tight_layout()` and `plt.show()`.
    - Markdown cell: Interpretation guidance -- what patterns to look for in each quadrant

    **individual-plots.ts** -- `buildIndividualPlots(slug, startIndex, config)`:
    - Markdown cell: `## Individual Plots` header with explanation of detailed examination
    - Code cell: Run sequence plot (full-size `figsize=(12, 5)`) with mean line (`axhline`), title from `config.plotTitles.runSequence`. Plot data points with QUANTUM_COLORS accent, mean line with teal color.
    - Code cell: Lag plot (`figsize=(8, 8)`) with `plt.scatter(data[:-1], data[1:])`, diagonal reference line, title from `config.plotTitles.lagPlot`.
    - Code cell: Histogram (`figsize=(10, 6)`) with `bins='auto'`, `sns.histplot()` for histogram + optional KDE overlay, title from `config.plotTitles.histogram`.
    - Code cell: Normal probability plot (`figsize=(8, 8)`) with `stats.probplot(data, dist='norm', plot=plt)`, title from `config.plotTitles.probabilityPlot`.

    **hypothesis-tests.ts** (STUB for now):
    - Return 2 cells: markdown header `## Hypothesis Tests` + code cell with `# Hypothesis tests will be added in Plan 02` comment and placeholder print.

    **test-summary.ts** (STUB for now):
    - Return 2 cells: markdown header `## Test Summary` + code cell with placeholder print `print('Test summary will be computed after hypothesis tests.')`.

    **interpretation.ts** (STUB for now):
    - Return 1 cell: markdown `## Interpretation` + `*Detailed interpretation will be added in Plan 02.*`

    **conclusions.ts** (STUB for now):
    - Return 1 cell: markdown `## Conclusions` + `*Conclusions will be added in Plan 02.*`

    Run tests -- they MUST pass. Commit: `feat(97-01): implement standard notebook template with visualization sections`.

    **REFACTOR (if needed):** Extract data loading code builder into a helper function. Ensure consistent Python string quoting (TypeScript double quotes wrapping Python single quotes). Run tests -- MUST pass. Commit: `refactor(97-01): clean up notebook template code`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/eda/notebooks/__tests__/standard-notebook.test.ts</automated>
  </verify>
  <done>
    - buildStandardNotebook() produces valid nbformat v4.5 notebooks for all 7 standard slugs
    - All notebooks have 15+ cells with correct section ordering
    - Data loading cells contain correct row-count assertions for each case study
    - Multi-value datasets (RANDN, RANDU, SOULEN) generate flatten() code
    - Multi-column datasets (DZIUBA1) generate column names in read_fwf()
    - Theme setup imports THEME_SETUP_CODE and DEPENDENCY_CHECK_CODE from Phase 96
    - 4-plot cells generate correct subplot grid with QUANTUM_COLORS
    - Individual plot cells produce 4 separate full-size plots
    - Hypothesis-tests, test-summary, interpretation, conclusions exist as stubs (compilable, structurally valid)
    - All cell IDs unique and alphanumeric across every notebook
    - All tests pass
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/eda/notebooks/__tests__/standard-notebook.test.ts` -- all tests green
2. `npx vitest run` -- full test suite passes (no regressions in Phase 96 tests)
3. For each of the 7 slugs, `buildStandardNotebook(slug)` produces object with nbformat: 4, nbformat_minor: 5
4. Spot check: `JSON.stringify(buildStandardNotebook('normal-random-numbers'), null, 2)` produces valid JSON
</verification>

<success_criteria>
- buildStandardNotebook(slug) works for all 7 standard case study slugs
- Each notebook produces 15+ cells in correct section order (will grow to 25+ after Plan 02)
- All 7 data loading cells contain correct row-count assertions
- 4-plot and individual plot sections fully implemented with QUANTUM_COLORS theming
- Stub sections compile and produce structurally valid placeholder cells
- All tests pass (new standard-notebook tests and existing Phase 96 tests)
</success_criteria>

<output>
After completion, create `.planning/phases/97-standard-case-study-notebooks/97-01-SUMMARY.md`
</output>
