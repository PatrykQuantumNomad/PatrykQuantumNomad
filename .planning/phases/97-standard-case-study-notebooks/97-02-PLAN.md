---
phase: 97-standard-case-study-notebooks
plan: 02
type: tdd
wave: 2
depends_on: ["97-01"]
files_modified:
  - src/lib/eda/notebooks/templates/sections/hypothesis-tests.ts
  - src/lib/eda/notebooks/templates/sections/test-summary.ts
  - src/lib/eda/notebooks/templates/sections/interpretation.ts
  - src/lib/eda/notebooks/templates/sections/conclusions.ts
  - src/lib/eda/notebooks/__tests__/standard-notebook.test.ts
autonomous: true
requirements: [NBSTD-01, NBSTD-02, NBSTD-03, NBSTD-04, NBSTD-05, NBSTD-06, NBSTD-07]

must_haves:
  truths:
    - "Each notebook contains location test using scipy.stats.linregress"
    - "Each notebook contains manual runs test implementation (no statsmodels import)"
    - "Each notebook contains lag-1 autocorrelation with critical value 2/sqrt(N)"
    - "Filter-transmittance and standard-resistor skip distribution/outlier tests with explanation"
    - "Normal-random-numbers contains Anderson-Darling and Grubbs tests"
    - "Fatigue-life contains Weibull/Gamma distribution comparison with probability plots"
    - "Each notebook has case-study-specific interpretation narrative"
    - "Each notebook has test summary table collecting all results"
    - "Each notebook has conclusions with key findings and next steps"
    - "All 7 notebooks have 25+ cells after completion"
  artifacts:
    - path: "src/lib/eda/notebooks/templates/sections/hypothesis-tests.ts"
      provides: "Location, variation, randomness, distribution, outlier test cells with conditional skipping"
      exports: ["buildHypothesisTests"]
      min_lines: 100
    - path: "src/lib/eda/notebooks/templates/sections/test-summary.ts"
      provides: "Test summary table collecting all hypothesis test results"
      exports: ["buildTestSummary"]
      min_lines: 30
    - path: "src/lib/eda/notebooks/templates/sections/interpretation.ts"
      provides: "Per-slug interpretation narratives for all 7 case studies"
      exports: ["buildInterpretation"]
      min_lines: 60
    - path: "src/lib/eda/notebooks/templates/sections/conclusions.ts"
      provides: "Key findings + next steps parameterized from config"
      exports: ["buildConclusions"]
      min_lines: 20
    - path: "src/lib/eda/notebooks/__tests__/standard-notebook.test.ts"
      provides: "Expanded tests covering hypothesis tests, interpretation, and per-notebook NBSTD requirements"
      min_lines: 150
  key_links:
    - from: "src/lib/eda/notebooks/templates/sections/hypothesis-tests.ts"
      to: "src/lib/eda/notebooks/cells.ts"
      via: "markdownCell() and codeCell() for test cells"
      pattern: "markdownCell|codeCell"
    - from: "src/lib/eda/notebooks/templates/sections/hypothesis-tests.ts"
      to: "Generated Python code"
      via: "scipy.stats calls and manual test implementations"
      pattern: "linregress|bartlett|levene|anderson|shapiro|runs_test|grubbs"
    - from: "src/lib/eda/notebooks/templates/sections/interpretation.ts"
      to: "src/lib/eda/notebooks/registry/types.ts"
      via: "CaseStudyConfig.slug for content map lookup"
      pattern: "config\\.slug"
---

<objective>
Complete the 4 stub section builders from Plan 01 (hypothesis-tests, test-summary, interpretation, conclusions) with full statistical analysis content, per-study interpretation narratives, and expand tests to validate all 7 specific NBSTD requirements.

Purpose: Transforms the notebook template from structural skeleton to complete analysis pipeline. After this plan, every standard case study notebook contains the full EDA workflow: data loading, visualization, hypothesis testing, and interpretation.

Output: 4 fully-implemented section builders + expanded test file covering all 7 NBSTD requirements.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-standard-case-study-notebooks/97-RESEARCH.md
@.planning/phases/97-standard-case-study-notebooks/97-01-SUMMARY.md

@src/lib/eda/notebooks/types.ts
@src/lib/eda/notebooks/cells.ts
@src/lib/eda/notebooks/registry/types.ts
@src/lib/eda/notebooks/registry/index.ts
@src/lib/eda/notebooks/templates/standard.ts
@src/lib/eda/notebooks/templates/sections/hypothesis-tests.ts
@src/lib/eda/notebooks/templates/sections/test-summary.ts
@src/lib/eda/notebooks/templates/sections/interpretation.ts
@src/lib/eda/notebooks/templates/sections/conclusions.ts
@src/lib/eda/notebooks/__tests__/standard-notebook.test.ts

<interfaces>
<!-- Contracts established by Plan 01 that this plan implements against. -->

From src/lib/eda/notebooks/templates/standard.ts (Plan 01):
```typescript
export function buildStandardNotebook(slug: string): NotebookV4;
export const STANDARD_SLUGS: readonly string[];  // 7 standard slugs
```

Section builder signature (all section builders follow this):
```typescript
export function build{Section}(
  slug: string,
  startIndex: number,
  config: CaseStudyConfig,
): Cell[];
```

The 4 stub section builders from Plan 01 that this plan replaces:
- hypothesis-tests.ts: currently returns 2 placeholder cells
- test-summary.ts: currently returns 2 placeholder cells
- interpretation.ts: currently returns 1 placeholder cell
- conclusions.ts: currently returns 1 placeholder cell

After replacement, total cells per notebook should be 25-35 (up from ~15 with stubs).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement hypothesis tests, test summary, interpretation, and conclusions</name>
  <files>
    src/lib/eda/notebooks/__tests__/standard-notebook.test.ts,
    src/lib/eda/notebooks/templates/sections/hypothesis-tests.ts,
    src/lib/eda/notebooks/templates/sections/test-summary.ts,
    src/lib/eda/notebooks/templates/sections/interpretation.ts,
    src/lib/eda/notebooks/templates/sections/conclusions.ts
  </files>
  <behavior>
    Hypothesis test content tests (expand existing test file):
    - Every notebook contains `linregress` in a code cell source (location test)
    - Every notebook contains `def runs_test(data):` in a code cell source (manual implementation)
    - Every notebook does NOT contain `statsmodels` in any cell source
    - Every notebook contains `2 / np.sqrt` or `2/np.sqrt` in code (autocorrelation critical value)
    - normal-random-numbers contains `anderson` in code cell (distribution test applied)
    - normal-random-numbers contains `grubbs` or `Grubbs` in code cell (outlier test applied)
    - uniform-random-numbers contains `anderson` in code cell (randomness OK, distribution applied)
    - heat-flow-meter contains `anderson` in code cell (mild non-randomness, tests still applied)
    - filter-transmittance does NOT contain `anderson` in code cell (skipped: severe autocorrelation)
    - filter-transmittance contains `autocorrelation` and `skipped` or `omitted` in cell source
    - standard-resistor does NOT contain `anderson` in code cell (skipped: severe autocorrelation)
    - standard-resistor contains `autocorrelation` and `skipped` or `omitted` in cell source
    - cryothermometry contains `anderson` in code cell (discrete but randomness OK)
    - fatigue-life contains `weibull` or `Weibull` in code cell (distribution comparison)
    - fatigue-life contains `gamma` or `Gamma` in code cell (distribution comparison)
    - Every notebook contains `bartlett` or `levene` in code cell (variation test)

    Test summary:
    - Every notebook contains a cell with `Test Summary` in source
    - Every notebook contains `Location` and `Variation` and `Randomness` in a code cell (summary table columns)

    Interpretation per-study content:
    - normal-random-numbers: interpretation contains `assumptions` and `satisfied` or `pass`
    - uniform-random-numbers: interpretation contains `non-normal` or `non-normality`
    - heat-flow-meter: interpretation contains `non-random` or `randomness`
    - filter-transmittance: interpretation contains `autocorrelation` and `serial`
    - cryothermometry: interpretation contains `discrete` and `integer`
    - fatigue-life: interpretation contains `right-skewed` or `skewed` and `Weibull` or `reliability`
    - standard-resistor: interpretation contains `drift` and `non-stationary` or `trend`

    Conclusions:
    - Every notebook contains `## Conclusions` or `## Key Findings` in markdown
    - Every notebook contains config.nistUrl in a markdown cell (reference link)

    Cell count:
    - Every notebook has at least 25 cells (was 15+ with stubs, now 25+ with full content)
  </behavior>
  <action>
    **RED phase:** Expand `standard-notebook.test.ts` with all tests described in the behavior block. Add new `describe` blocks:
    - `describe('hypothesis tests')` -- scipy call presence, runs test, conditional skipping, distribution comparison
    - `describe('test summary')` -- table presence and content
    - `describe('per-study interpretation (NBSTD-01 through NBSTD-07)')` -- slug-specific narrative content
    - `describe('conclusions')` -- key findings and references
    - `describe('cell count')` -- minimum 25 cells per notebook

    Reuse the helper functions from Plan 01 tests (getAllSource, getCodeSources, getMarkdownSources).

    Run tests -- new tests MUST fail (stubs produce placeholder content). Commit: `test(97-02): add failing tests for hypothesis tests, interpretation, and conclusions`.

    **GREEN phase:** Replace all 4 stub section builders with full implementations:

    **hypothesis-tests.ts** -- `buildHypothesisTests(slug, startIndex, config)`:

    Returns 6-8 cells depending on whether distribution/outlier tests are included. The conditional inclusion is handled at PYTHON RUNTIME (not TypeScript build time) to keep the template truly parameterized. The generated Python code uses if/else blocks based on computed autocorrelation values.

    Cell layout:
    1. Markdown: `## Hypothesis Tests` header + explanation of the 5-test framework (location, variation, randomness, distribution, outlier) with alpha=0.05 significance level.

    2. Code cell -- Location test: Linear regression of response vs run order.
       ```python
       x = np.arange(1, len(data) + 1)
       slope, intercept, r_value, p_value, std_err = linregress(x, data)
       t_stat_loc = slope / std_err
       critical_t = t_dist.ppf(0.975, df=len(data)-2)
       ```
       Print formatted results: slope, t-statistic, critical value, p-value, conclusion.
       Store `location_result = 'Fail' if abs(t_stat_loc) > critical_t else 'Pass'`.

    3. Code cell -- Variation test: Split data into lag-4 groups (4 equal-ish groups based on observation order). Use `scipy.stats.bartlett(*groups)` as default. Print test statistic, p-value, conclusion.
       Store `variation_result = 'Fail' if p_value_var < 0.05 else 'Pass'`.

    4. Code cell -- Randomness tests (two in one cell):
       a. Manual runs test (Wald-Wolfowitz) -- define `runs_test(data)` function inline exactly as in 97-RESEARCH.md. Compute z-statistic, compare against +/-1.96 critical value.
       b. Lag-1 autocorrelation: `r1 = np.corrcoef(data[:-1], data[1:])[0,1]`, critical value `2 / np.sqrt(len(data))`. Print both results.
       Store `randomness_result`, `r1_value`.
       Store `severe_autocorrelation = abs(r1) > 0.5`.

    5. Code cell -- Distribution test (CONDITIONAL at Python runtime):
       ```python
       if severe_autocorrelation:
           print("Distribution test SKIPPED: severe autocorrelation detected")
           print(f"(lag-1 autocorrelation r1 = {r1:.4f}, |r1| > 0.5)")
           print("Distribution tests assume independent observations.")
           distribution_result = 'Skipped'
       else:
           # Anderson-Darling test
           ad_stat, ad_critical, ad_sig = anderson(data, dist='norm')
           # Shapiro-Wilk test
           sw_stat, sw_p = shapiro(data)
           ...
           distribution_result = 'Fail' if (reject) else 'Pass'
       ```
       For fatigue-life: add distribution comparison section AFTER the standard tests (NOT instead of). Generate Weibull, Gamma, and log-normal probability plots using `stats.probplot(data, dist=stats.weibull_min(...))` etc. Compute AIC/BIC for each distribution fit. Print comparison table. This extension is added when `config.slug === 'fatigue-life'` -- this is the ONE acceptable place for slug branching because the distribution comparison is unique pedagogical content, not a structural variation.

    6. Code cell -- Outlier test (CONDITIONAL, same guard):
       ```python
       if severe_autocorrelation:
           print("Outlier test SKIPPED: severe autocorrelation detected")
           outlier_result = 'Skipped'
       else:
           # Grubbs' test (manual implementation)
           def grubbs_test(data, alpha=0.05):
               n = len(data)
               mean_val = np.mean(data)
               std_val = np.std(data, ddof=1)
               G = np.max(np.abs(data - mean_val)) / std_val
               t_crit = t_dist.ppf(1 - alpha/(2*n), n-2)
               G_crit = ((n-1)/np.sqrt(n)) * np.sqrt(t_crit**2 / (n - 2 + t_crit**2))
               return G, G_crit, G > G_crit
           ...
       ```

    **test-summary.ts** -- `buildTestSummary(slug, startIndex, config)`:
    - Markdown cell: `## Test Summary`
    - Code cell: Collect all test results (stored in variables from hypothesis-tests cells) into a summary. Use a list-of-lists approach to build the table:
      ```python
      results = [
          ['Location (drift)', f'{t_stat_loc:.4f}', f'+/- {critical_t:.4f}', f'{p_value_loc:.6f}', location_result],
          ['Variation', ...],
          ['Randomness (Runs)', ...],
          ['Randomness (Autocorrelation)', ...],
      ]
      if not severe_autocorrelation:
          results.append(['Distribution (Anderson-Darling)', ...])
          results.append(['Distribution (Shapiro-Wilk)', ...])
          results.append(['Outliers (Grubbs)', ...])
      ```
      Print as formatted table using pandas DataFrame or manual column formatting.

    **interpretation.ts** -- `buildInterpretation(slug, startIndex, config)`:
    - Markdown cell with case-study-specific interpretation. Use a `Record<string, string[]>` map keyed by slug, each value being an array of markdown lines (2-3 paragraphs). Content for each slug:

    **normal-random-numbers:** "The Normal Random Numbers dataset serves as a baseline case study. All four underlying assumptions -- fixed location, fixed variation, randomness, and normal distribution -- are satisfied. The 4-plot shows a stable run sequence centered near zero, a circular lag plot indicating no autocorrelation, a bell-shaped histogram, and a linear normal probability plot. All hypothesis tests pass at the 0.05 significance level, confirming the data are consistent with a N(0,1) process."

    **uniform-random-numbers:** "The Uniform Random Numbers dataset demonstrates detection of non-normality while other assumptions hold. Location and variation are constant, and randomness is confirmed. However, the histogram reveals a characteristic flat (rectangular) shape rather than bell-shaped, and the normal probability plot shows a distinct S-curve. The Anderson-Darling test strongly rejects normality. This is the expected behavior for uniform(0,1) data."

    **heat-flow-meter:** "The Heat Flow Meter dataset reveals mild departures from randomness in otherwise well-behaved data. The run sequence plot shows subtle structure, and the lag plot exhibits slight clustering rather than the circular scatter expected from random data. The runs test detects non-randomness at the 0.05 level, suggesting an underlying physical process influences consecutive measurements."

    **filter-transmittance:** "The Filter Transmittance dataset is dominated by extreme positive autocorrelation (lag-1 autocorrelation r1 approximately 0.94). The lag plot shows a tight linear band rather than circular scatter, and the run sequence plot reveals strong serial dependence. The runs test confirms severe non-randomness. Because the independence assumption is violated, distribution and outlier tests are not applicable and are omitted."

    **cryothermometry:** "The Cryothermometry dataset consists of discrete integer-valued voltage counts, which creates distinctive artifacts in the analysis. The histogram shows discrete spikes rather than a smooth bell curve, and the normal probability plot exhibits a step-like pattern. Despite the discreteness, the underlying process appears stationary with constant location and variation."

    **fatigue-life:** "The Fatigue Life dataset is right-skewed with a long upper tail, characteristic of lifetime and reliability data. The normal probability plot curves away from the theoretical line, and the Anderson-Darling test rejects normality. Distribution comparison shows that Weibull and Birnbaum-Saunders distributions provide substantially better fits than the normal distribution, with lower AIC/BIC values."

    **standard-resistor:** "The Standard Resistor dataset exhibits a triple assumption failure: systematic drift in location (positive trend over the 5-year measurement period), non-constant variation (variance changes across time periods), and extreme positive autocorrelation (lag-1 r1 approximately 0.97). The 4-plot immediately reveals these issues: an upward-trending run sequence, a tight linear lag plot, and a histogram that reflects the drift rather than the underlying noise distribution."

    **conclusions.ts** -- `buildConclusions(slug, startIndex, config)`:
    - Markdown cell: `## Conclusions and Next Steps` header, key findings as bullet list (3-5 points drawn from the template pattern: which assumptions passed/failed, the most notable finding), then `### Further Reading` with link to `config.nistUrl` and suggestion to explore the NIST handbook for deeper analysis.

    Run tests -- they MUST pass. Commit: `feat(97-02): implement hypothesis tests, interpretation, and conclusions`.

    **REFACTOR (if needed):** Clean up hypothesis test code generation, ensure variable names are consistent between hypothesis-tests.ts and test-summary.ts (the test summary reads variables set in earlier cells). Run tests -- MUST pass. Commit: `refactor(97-02): clean up hypothesis test and summary code`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/eda/notebooks/__tests__/standard-notebook.test.ts</automated>
  </verify>
  <done>
    - All 7 notebooks contain location test (linregress), variation test (bartlett/levene), randomness tests (manual runs + lag-1 autocorrelation)
    - No notebook contains `statsmodels` import
    - Filter-transmittance and standard-resistor skip distribution/outlier tests with autocorrelation explanation
    - Normal-random-numbers, uniform-random-numbers, heat-flow-meter, cryothermometry include Anderson-Darling and Grubbs tests
    - Fatigue-life includes Weibull/Gamma/log-normal distribution comparison with probability plots
    - Each notebook has test summary table collecting all results
    - Each notebook has case-study-specific interpretation (normal: all pass, uniform: non-normal, heat-flow: mild non-randomness, filter: autocorrelation, cryo: discrete, fatigue: right-skewed, resistor: triple failure)
    - Each notebook has conclusions with key findings and NIST reference link
    - All 7 notebooks have 25+ cells
    - All tests pass (full suite including Plan 01 structural tests)
    - NBSTD-01 through NBSTD-07 all validated by per-study content tests
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/eda/notebooks/__tests__/standard-notebook.test.ts` -- all tests green
2. `npx vitest run` -- full suite passes (Phase 96 + Phase 97 Plan 01 + Plan 02)
3. Per-notebook verification for all 7 slugs:
   - buildStandardNotebook(slug) has nbformat: 4, nbformat_minor: 5
   - Cell count >= 25
   - Contains location, variation, and randomness tests
   - Contains distribution/outlier tests OR autocorrelation skip message
   - Contains interpretation specific to that case study
   - Contains conclusions with NIST URL
4. No `statsmodels` import in any generated cell
5. Autocorrelation critical value uses `2/sqrt(N)` not `1.96/sqrt(N)`
</verification>

<success_criteria>
- All 7 NBSTD requirements validated: each notebook contains the full analysis pipeline
- Hypothesis tests cover all 5 categories with correct conditional logic
- Manual runs test and Grubbs test implementations present (no statsmodels dependency)
- Distribution comparison (fatigue-life) includes Weibull/Gamma probability plots
- Per-study interpretation narratives accurately describe each case study's findings
- All tests pass across the full suite
</success_criteria>

<output>
After completion, create `.planning/phases/97-standard-case-study-notebooks/97-02-SUMMARY.md`
</output>
