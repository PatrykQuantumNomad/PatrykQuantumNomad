---
phase: 113-lower-impact-chapter-updates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/guides/claude-code/pages/context-management.mdx
autonomous: true
requirements: [UPD-02]

must_haves:
  truths:
    - "Ch2 documents autoMemoryDirectory setting with JSON config example"
    - "Ch2 mentions PostCompact hook with cross-reference to Ch8 Hooks"
    - "Ch2 contains real hyperlinks to Ch8 Hooks for PostCompact"
    - "No deprecated terms appear"
  artifacts:
    - path: "src/data/guides/claude-code/pages/context-management.mdx"
      provides: "Updated Ch2 with autoMemoryDirectory and PostCompact hook mention"
      contains: "autoMemoryDirectory"
  key_links:
    - from: "context-management.mdx"
      to: "hooks.mdx"
      via: "inline cross-reference for PostCompact"
      pattern: "/guides/claude-code/hooks/"
    - from: "context-management.mdx"
      to: "models-and-costs.mdx"
      via: "next-chapter link in Further Reading"
      pattern: "/guides/claude-code/models-and-costs/"
---

<objective>
Update Ch2 (Project Context & Memory) with autoMemoryDirectory setting and PostCompact hook mention. This is the smallest update in the phase -- two targeted additions to existing sections.

Purpose: Ch2 covers context and memory. autoMemoryDirectory customizes memory storage, PostCompact is an integration point after compaction.
Output: Updated context-management.mdx with two new features.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-lower-impact-chapter-updates/113-RESEARCH.md
@.planning/research/FEATURES-claude-code-guide-refresh.md
@.planning/research/STACK.md

@src/data/guides/claude-code/pages/context-management.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read existing Ch2 and feature sources</name>
  <files>src/data/guides/claude-code/pages/context-management.mdx</files>
  <action>
Read the full existing context-management.mdx (446 lines) to understand current structure, imports, and sections.

Also read .planning/research/STACK.md for:
- #15 autoMemoryDirectory (v2.1.74) -- custom directory for auto-memory storage
- #11 PostCompact hook (v2.1.76) -- fires after context compaction

And .planning/research/FEATURES-claude-code-guide-refresh.md for:
- Feature #42 (/init flow opt-in preview, CLAUDE_CODE_NEW_INIT=1)

Note specifically:
- Current "Auto-Memory System" section location and content (where to add autoMemoryDirectory)
- Current "Context Window Management" section (where /compact is documented -- where to mention PostCompact)
- Current CLAUDE.md File section (where to mention /init flow briefly)
- All cross-references to other chapters (Ch3, Ch8 -- must stay valid)
- Component imports: CodeBlock, TerminalRecording
  </action>
  <verify>File read successfully. Locations for all three additions identified.</verify>
  <done>Full understanding of Ch2 structure with specific insertion points for autoMemoryDirectory, PostCompact, and /init</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Ch2 context-management.mdx</name>
  <files>src/data/guides/claude-code/pages/context-management.mdx</files>
  <action>
Write the COMPLETE updated chapter as a full MDX file replacement. Do NOT do surgical edits -- write the chapter fresh using the existing file as structural reference.

**Frontmatter updates:**
- Bump `lastVerified` and `updatedDate` to 2026-04-12
- Add "autoMemoryDirectory" to `keywords` array
- `description` likely adequate as-is (already covers auto-memory and context management)

**Structural approach: ADDITIVE (minimal changes)**

Keep the existing section flow. Three small additions:

1. **autoMemoryDirectory** -- Add to the Auto-Memory System section. One paragraph explaining: by default Claude Code stores auto-memory in `~/.claude/projects/`. The `autoMemoryDirectory` setting lets you specify a custom directory. Use case: shared team memory location, or keeping memory alongside the project. Include a CodeBlock:
   ```json
   {
     "autoMemoryDirectory": "/path/to/custom/memory"
   }
   ```
   with title="settings.json" and a caption explaining when to use it. Keep it brief -- 1 paragraph + 1 CodeBlock.

2. **PostCompact hook** -- Add a brief mention in the Context Window Management section alongside /compact documentation. One paragraph: "Claude Code fires a `PostCompact` hook event after compaction completes, letting you trigger custom scripts -- for example, logging what was compacted or notifying external systems. See [Hooks & Lifecycle Automation](/guides/claude-code/hooks/) for full hook documentation." Do NOT deep-dive into hooks -- that belongs in Ch8. Just note the integration point exists.

3. **New /init flow** -- Brief mention in the CLAUDE.md File section or as a subsection. The opt-in preview (`CLAUDE_CODE_NEW_INIT=1`) provides a multi-phase interactive init that sets up CLAUDE.md, skills, and hooks. Keep it to 2-3 sentences since it is an opt-in preview. Include the env var in a short inline code snippet.

**Cross-references to Ch12-14:**
- If auto-memory or CLAUDE.md section mentions skills/plugins, add link to [Plugins](/guides/claude-code/plugins/)
- PostCompact mention links to [Hooks](/guides/claude-code/hooks/) (Ch8, already exists)
- Only add Ch12-14 links where natural -- do not force them

**Style rules (from Phase 111 decisions -- MANDATORY):**
- No "What's New" callouts
- No "(formerly X)" parentheticals
- Complete CodeBlock examples with code, lang, title props
- JSON only for config examples
- Professional but approachable tone

**Deprecation verification:**
- No /tag, /vim
- No ANTHROPIC_SMALL_FAST_MODEL
- No stale references
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -5</automated>
Verify:
1. File builds without MDX errors
2. "autoMemoryDirectory" appears in the chapter with a CodeBlock config example
3. "PostCompact" mentioned with a link to Ch8 Hooks
4. No deprecated terms
5. All imports (CodeBlock, TerminalRecording) still used
  </verify>
  <done>
- Ch2 documents autoMemoryDirectory with JSON config example
- Ch2 mentions PostCompact hook with cross-reference to Ch8
- Ch2 mentions /init flow (opt-in preview)
- All deprecated terms absent
- File compiles without errors
- ~470-490 lines (expanded from 446)
  </done>
</task>

</tasks>

<verification>
1. Build: `npx astro build` passes without errors on context-management.mdx
2. Content: grep for "autoMemoryDirectory", "PostCompact" in the rewritten file
3. Cross-refs: grep for "/guides/claude-code/hooks/" to verify PostCompact link
4. Deprecation: grep for "/tag", "/vim", "ANTHROPIC_SMALL_FAST_MODEL" returns zero matches
</verification>

<success_criteria>
- Ch2 documents autoMemoryDirectory setting with JSON config example
- Ch2 mentions PostCompact hook with cross-reference to Ch8
- No deprecated terms
- File compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/113-lower-impact-chapter-updates/113-02-SUMMARY.md`
</output>
