---
phase: 113-lower-impact-chapter-updates
plan: 07
type: execute
wave: 2
depends_on: ["113-01", "113-02", "113-03", "113-04", "113-05", "113-06"]
files_modified:
  - src/data/guides/claude-code/guide.json
autonomous: true
requirements: [UPD-01, UPD-02, UPD-05, UPD-06, UPD-09, UPD-10]

must_haves:
  truths:
    - "guide.json description for Ch5 mentions Channels and server mode"
    - "guide.json description for Ch9 mentions persistent memory, initialPrompt, and sparsePaths"
    - "No deprecated terms appear in any of the 6 updated chapter files"
    - "All 6 chapters build clean with npx astro build"
    - "All cross-references to Ch12-14 are real hyperlinks (not text-only)"
  artifacts:
    - path: "src/data/guides/claude-code/guide.json"
      provides: "Updated chapter descriptions matching expanded content"
      contains: "Channels"
  key_links:
    - from: "guide.json"
      to: "all 6 chapter MDX files"
      via: "slug and description matching"
---

<objective>
Update guide.json chapter descriptions for Ch5 and Ch9 (whose scope expanded significantly), then run a cross-chapter verification sweep across all 6 updated chapters to catch deprecated terms, broken cross-references, and text-only Ch12-14 links.

Purpose: guide.json descriptions appear on the landing page and in OG images. Stale descriptions for chapters whose scope expanded confuse users. The sweep ensures all 6 chapters are clean.
Output: Updated guide.json and clean verification pass.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-lower-impact-chapter-updates/113-RESEARCH.md

@src/data/guides/claude-code/guide.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update guide.json chapter descriptions</name>
  <files>src/data/guides/claude-code/guide.json</files>
  <action>
Read the current guide.json and update chapter descriptions that no longer reflect the expanded scope:

**Ch5 (remote-and-headless) -- MUST update:**
Current: "Run Claude Code from any device via remote control, automate with headless mode in CI/CD pipelines, and schedule recurring tasks with cron."
Updated: Add Channels and server mode. Example: "Run Claude Code from any device via remote control with multi-session server mode, interact through Telegram, Discord, and iMessage channels, automate with headless mode in CI/CD, and schedule recurring tasks."

**Ch9 (worktrees) -- MUST update:**
Current: "Parallelize development across isolated git worktree branches using built-in and custom subagents with AGENT.md persona definitions."
Updated: Add memory, initialPrompt, sparsePaths. Example: "Parallelize development across isolated git worktree branches using built-in and custom subagents with AGENT.md personas, persistent memory, initialPrompt auto-submit, and sparse-checkout for monorepos."

**Ch1, Ch2, Ch6, Ch10 -- VERIFY but likely no update needed:**
Read each current description and compare to the updated chapter scope. Only update if the description is materially inaccurate. Do NOT change descriptions just to add minor features.

**Rules:**
- Descriptions should be concise (1-2 sentences)
- Include SEO-relevant keywords naturally
- Match the voice of existing descriptions
- Keep guide.json as valid JSON
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && node -e "JSON.parse(require('fs').readFileSync('src/data/guides/claude-code/guide.json','utf8')); console.log('Valid JSON')"</automated>
Verify:
1. guide.json is valid JSON
2. Ch5 description mentions "Channels" or "channels"
3. Ch9 description mentions "memory" or "sparsePaths"
4. All 14 chapter entries still present
  </verify>
  <done>
- guide.json Ch5 description updated with Channels and server mode
- guide.json Ch9 description updated with memory, initialPrompt, sparsePaths
- guide.json is valid JSON with all 14 chapter entries
  </done>
</task>

<task type="auto">
  <name>Task 2: Cross-chapter verification sweep</name>
  <files>src/data/guides/claude-code/pages/introduction.mdx, src/data/guides/claude-code/pages/context-management.mdx, src/data/guides/claude-code/pages/remote-and-headless.mdx, src/data/guides/claude-code/pages/mcp.mdx, src/data/guides/claude-code/pages/worktrees.mdx, src/data/guides/claude-code/pages/agent-teams.mdx</files>
  <action>
Run a verification sweep across all 6 updated chapter files. This is a read-and-verify task -- only fix issues if found.

**Check 1: Deprecated terms sweep**
Grep all 6 files for deprecated terms that should not appear:
- `/tag` (removed command)
- `/vim` (removed command)
- `ANTHROPIC_SMALL_FAST_MODEL` (replaced by ANTHROPIC_DEFAULT_HAIKU_MODEL)
- `"5 permission modes"` (should be 6)
- `thinking summaries` described as "on by default" in interactive (now off)

**Check 2: Cross-references to Ch12-14 are real hyperlinks**
Grep all 6 files for mentions of Plugins, Agent SDK, Computer Use chapters. Verify they use real hyperlink format:
- `[Plugins](/guides/claude-code/plugins/)` -- not "the Plugins chapter" without link
- `[Agent SDK](/guides/claude-code/agent-sdk/)` -- not "see the Agent SDK chapter"
- `[Computer Use](/guides/claude-code/computer-use/)` -- not text-only mention

**Check 3: Internal cross-references still valid**
Verify all `/guides/claude-code/SLUG/` links in each file point to existing chapter slugs in guide.json. The valid slugs are: introduction, context-management, models-and-costs, environment, remote-and-headless, mcp, custom-skills, hooks, worktrees, agent-teams, security, plugins, agent-sdk, computer-use.

**Check 4: Component imports**
Verify each file's imports match what is actually used in the body. Common issue: import left over from removed section, or component used without import.

**Check 5: Build verification**
Run `npx astro build` and verify all 6 chapter pages build without errors.

If any issues found, fix them directly in the affected files.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -5</automated>
Verify:
1. Build passes
2. Zero deprecated terms across all 6 files
3. All Ch12-14 mentions use real hyperlinks
4. All cross-reference slugs are valid
5. All component imports match usage
  </verify>
  <done>
- All 6 chapters verified clean of deprecated terms
- All Ch12-14 references use real hyperlinks
- All internal cross-references point to valid slugs
- All component imports match usage
- Full build passes without errors
  </done>
</task>

</tasks>

<verification>
1. Build: `npx astro build` passes clean
2. guide.json: valid JSON, Ch5 mentions Channels, Ch9 mentions memory/sparsePaths
3. Deprecated: grep across all 6 files for /tag, /vim, ANTHROPIC_SMALL_FAST_MODEL returns zero
4. Hyperlinks: all Ch12-14 references in all 6 files are real links (not text-only)
5. Slugs: all /guides/claude-code/ links point to valid slugs from guide.json
</verification>

<success_criteria>
- guide.json descriptions updated for Ch5 and Ch9
- Zero deprecated terms across all 6 updated chapters
- All Ch12-14 references are real hyperlinks
- All cross-references valid
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/113-lower-impact-chapter-updates/113-07-SUMMARY.md`
</output>
