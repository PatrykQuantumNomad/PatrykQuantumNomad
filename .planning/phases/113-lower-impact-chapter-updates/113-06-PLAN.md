---
phase: 113-lower-impact-chapter-updates
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/guides/claude-code/pages/agent-teams.mdx
autonomous: true
requirements: [UPD-10]

must_haves:
  truths:
    - "Ch10 documents /agents UI with Running tab showing active teammates"
    - "Ch10 documents initialPrompt for team agents (auto-submit first turn)"
    - "Ch10 documents dynamic agents via --agents flag with cross-ref to Ch9"
    - "Ch10 documents --teammate-mode flag (auto|in-process|tmux)"
    - "Ch10 contains real hyperlinks to Ch12 Plugins (plugin agents as teammates)"
    - "No deprecated terms appear"
  artifacts:
    - path: "src/data/guides/claude-code/pages/agent-teams.mdx"
      provides: "Updated Ch10 with /agents UI, initialPrompt, dynamic agents, --teammate-mode"
      contains: "initialPrompt"
  key_links:
    - from: "agent-teams.mdx"
      to: "worktrees.mdx"
      via: "cross-reference for full subagent/frontmatter docs"
      pattern: "/guides/claude-code/worktrees/"
    - from: "agent-teams.mdx"
      to: "plugins.mdx"
      via: "inline cross-reference for plugin agents"
      pattern: "/guides/claude-code/plugins/"
    - from: "agent-teams.mdx"
      to: "security.mdx"
      via: "next-chapter or inline reference"
      pattern: "/guides/claude-code/security/"
---

<objective>
Update Ch10 (Agent Teams & Advanced Orchestration) with /agents UI updates (Running/Library tabs), initialPrompt for team agents, dynamic agents via --agents flag, --teammate-mode flag, and agent frontmatter fields for teams.

Purpose: Ch10 covers multi-agent coordination. These are incremental capability additions to an otherwise stable feature.
Output: Updated agent-teams.mdx with current capabilities.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-lower-impact-chapter-updates/113-RESEARCH.md
@.planning/research/FEATURES-claude-code-guide-refresh.md
@.planning/research/STACK.md

@src/data/guides/claude-code/pages/agent-teams.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read existing Ch10 and feature sources</name>
  <files>src/data/guides/claude-code/pages/agent-teams.mdx</files>
  <action>
Read the full existing agent-teams.mdx (319 lines) to understand current structure, imports, sections, and existing coverage.

Also read .planning/research/STACK.md for:
- #13 /agents tabbed layout (v2.1.98): Running tab, Library tab
- #13 initialPrompt (v2.1.83): auto-submit first turn when spawned as teammate
- #13 --agents flag: inline JSON agent definitions at launch for team lead
- #13 --teammate-mode flag: auto|in-process|tmux
- #13 effort, maxTurns, disallowedTools for plugin agents (v2.1.78)

And .planning/research/FEATURES-claude-code-guide-refresh.md for:
- Feature #15 (initialPrompt details)
- Feature #18 (--agents flag details)
- Feature #38 (/agents UI details)

Note specifically:
- Current Research Preview blockquote at the top (verify if still accurate -- assume still research preview)
- Current "Starting a Team" section (where /agents UI updates go)
- Current "Display Modes" section (where --teammate-mode flag goes)
- Current "Team Architecture" section (where initialPrompt mention fits)
- Cross-references to Ch9 Worktrees (must stay valid)
- Component imports: CodeBlock, AgentTeamsDiagram
  </action>
  <verify>File read successfully. Insertion points identified for all additions.</verify>
  <done>Full understanding of Ch10 structure with specific locations for /agents UI, initialPrompt, dynamic agents, and --teammate-mode</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Ch10 agent-teams.mdx</name>
  <files>src/data/guides/claude-code/pages/agent-teams.mdx</files>
  <action>
Write the COMPLETE updated chapter as a full MDX file replacement. Do NOT do surgical edits -- write the chapter fresh using the existing file as structural reference.

**Frontmatter updates:**
- Bump `lastVerified` and `updatedDate` to 2026-04-12
- Add "initialPrompt", "dynamic agents", "teammate mode" to `keywords` array
- `description` likely adequate as-is (already covers coordination and communication)

**Structural approach: ADDITIVE (moderate)**

Keep the existing section flow. Update relevant existing sections:

1. **Research Preview blockquote** -- Keep it. Update date from "March 2026" to "April 2026" and verify the env var is still `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`. The feature is assumed to still be research preview.

2. **/agents UI updates** -- Update the "Starting a Team" section or "Display Modes" section:
   - Running tab shows active teammates with their current status
   - Library tab shows available agents from AGENT.md files and plugins
   - Cross-ref to [Worktrees & Subagent Delegation](/guides/claude-code/worktrees/) where /agents is documented more fully for individual subagent management

3. **initialPrompt for team agents** -- Add to "Team Architecture" or "Starting a Team":
   - Team lead can use agents with `initialPrompt` frontmatter -- the agent auto-submits a first turn when spawned as a teammate
   - Use case: agents that immediately start working on a predefined task without manual prompting
   - Brief CodeBlock example showing an AGENT.md with initialPrompt used in a team context
   - Cross-ref to Ch9 for full initialPrompt documentation

4. **Dynamic agents (--agents flag)** -- Add brief mention to team setup:
   - Lead can spawn teammates using dynamically-defined agent JSON via `--agents` flag instead of requiring AGENT.md files on disk
   - Session-only (definitions not persisted)
   - Cross-ref to [Worktrees](/guides/claude-code/worktrees/) for full --agents documentation
   - Keep brief -- the detail belongs in Ch9

5. **--teammate-mode flag** -- Update "Display Modes" section:
   - `--teammate-mode auto` (default: auto-selects based on terminal capabilities)
   - `--teammate-mode in-process` (teammates run in same process)
   - `--teammate-mode tmux` (teammates get separate tmux panes for visibility)
   - CodeBlock example:
     ```bash
     claude --teammate-mode tmux
     ```

6. **Agent frontmatter fields for teams** -- Brief mention that AGENT.md files used by teammates support additional fields:
   - `effort` -- set the effort level for the teammate
   - `maxTurns` -- limit how many turns the teammate can take
   - `disallowedTools` -- restrict which tools the teammate can use
   - These are documented in Ch9's frontmatter reference -- cross-ref rather than duplicate

7. **Cross-references to Ch12-14:**
   - [Plugins](/guides/claude-code/plugins/) -- plugin agents can serve as teammates
   - [Worktrees](/guides/claude-code/worktrees/) -- for subagent details, --agents flag, initialPrompt depth

**Style rules (MANDATORY):**
- No "What's New" callouts
- No "(formerly X)" parentheticals
- Complete CodeBlock examples with code, lang, title, caption props
- JSON only for config, YAML only for AGENT.md frontmatter
- Professional but approachable tone
- Research Preview uses blockquote format (matching existing Ch10 pattern)

**Deprecation verification:**
- No /tag, /vim
- No ANTHROPIC_SMALL_FAST_MODEL
- No "5 permission modes" (should be 6 if mentioned)
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -5</automated>
Verify:
1. File builds without MDX errors
2. "initialPrompt" documented with team context example
3. "--teammate-mode" documented with auto|in-process|tmux options
4. "--agents" flag mentioned with cross-ref to Ch9
5. /agents UI mentions Running and Library tabs
6. Research Preview blockquote present at top
7. Cross-refs to /guides/claude-code/worktrees/ and /guides/claude-code/plugins/ present
8. AgentTeamsDiagram import still present and used
9. No deprecated terms
  </verify>
  <done>
- Ch10 documents /agents UI with Running/Library tabs
- Ch10 documents initialPrompt for team agents with example
- Ch10 documents dynamic agents via --agents flag
- Ch10 documents --teammate-mode flag (auto/in-process/tmux)
- Ch10 documents effort, maxTurns, disallowedTools for team agents
- Real hyperlinks to Ch12 Plugins, Ch9 Worktrees
- Research Preview blockquote updated
- All deprecated terms absent
- File compiles without errors
- ~360-390 lines (expanded from 319)
  </done>
</task>

</tasks>

<verification>
1. Build: `npx astro build` passes without errors on agent-teams.mdx
2. Content: grep for "initialPrompt", "teammate-mode", "--agents", "Running.*Library\|Library.*Running" in the rewritten file
3. Cross-refs: grep for "/guides/claude-code/worktrees/", "/guides/claude-code/plugins/" to verify real hyperlinks
4. Research Preview: grep for "Research Preview" blockquote at top
5. Deprecation: grep for "/tag", "/vim", "ANTHROPIC_SMALL_FAST_MODEL" returns zero
</verification>

<success_criteria>
- Ch10 documents /agents UI with Running and Library tabs
- Ch10 documents initialPrompt for team agents
- Ch10 documents dynamic agents via --agents flag
- Ch10 documents --teammate-mode flag
- Real hyperlinks to Ch12 and Ch9
- Research Preview blockquote present
- No deprecated terms
- File compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/113-lower-impact-chapter-updates/113-06-SUMMARY.md`
</output>
