---
phase: 113-lower-impact-chapter-updates
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/guides/claude-code/pages/worktrees.mdx
autonomous: true
requirements: [UPD-09]

must_haves:
  truths:
    - "Ch9 Persistent Memory section documents storage paths (~/.claude/agent-memory/<name>/ and .claude/agent-memory/<name>/)"
    - "Ch9 Persistent Memory section documents MEMORY.md entrypoint and topic files"
    - "Ch9 Persistent Memory section documents 200-line / 25KB loading limit"
    - "Ch9 documents background mode env var CLAUDE_CODE_DISABLE_BACKGROUND_TASKS and Ctrl+B"
    - "Ch9 documents initialPrompt with auto-submit behavior and a CodeBlock example"
    - "Ch9 documents sparsePaths setting for monorepo sparse-checkout in worktree section"
    - "Ch9 documents /agents UI with Running and Library tabs"
    - "Ch9 documents @agent-name mention syntax"
    - "Ch9 documents --agents CLI flag for inline JSON agent definitions"
    - "No deprecated terms appear"
  artifacts:
    - path: "src/data/guides/claude-code/pages/worktrees.mdx"
      provides: "Updated Ch9 with expanded memory, background, initialPrompt, sparsePaths, and /agents UI"
      contains: "sparsePaths"
  key_links:
    - from: "worktrees.mdx"
      to: "agent-teams.mdx"
      via: "next-chapter link in Further Reading"
      pattern: "/guides/claude-code/agent-teams/"
    - from: "worktrees.mdx"
      to: "plugins.mdx"
      via: "inline cross-reference for plugin agents"
      pattern: "/guides/claude-code/plugins/"
    - from: "worktrees.mdx"
      to: "agent-sdk.mdx"
      via: "inline cross-reference for programmatic agents"
      pattern: "/guides/claude-code/agent-sdk/"
---

<objective>
Update Ch9 (Git Worktrees & Subagent Delegation) with expanded persistent memory details, background mode improvements, initialPrompt auto-submit behavior, sparsePaths for monorepo worktrees, /agents UI updates, --agents CLI flag, @agent-name mention, and ExitWorktree tool.

Purpose: Ch9 is the subagent reference. These are expansions of existing sections, not greenfield. The chapter already documents memory, background, and initialPrompt -- they need more depth.
Output: Updated worktrees.mdx with expanded sections and new features.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-lower-impact-chapter-updates/113-RESEARCH.md
@.planning/research/FEATURES-claude-code-guide-refresh.md
@.planning/research/STACK.md

@src/data/guides/claude-code/pages/worktrees.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read existing Ch9 and feature sources</name>
  <files>src/data/guides/claude-code/pages/worktrees.mdx</files>
  <action>
Read the full existing worktrees.mdx (435 lines) to understand current structure, imports, sections, and existing coverage of features being expanded.

CRITICAL: Ch9 ALREADY documents memory, background, initialPrompt, and isolation in the frontmatter reference section. The requirement is to EXPAND these, not add from scratch. Identify exactly what is already covered vs what needs adding.

Also read .planning/research/FEATURES-claude-code-guide-refresh.md for:
- Feature #14 (persistent memory: storage paths, MEMORY.md entrypoint, topic files, 200 lines / 25KB limit)
- Feature #15 (initialPrompt: auto-submit first turn, commands/skills processed, prepended to user prompt)
- Feature #17 (background flag: CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1, Ctrl+B foreground-to-background)
- Feature #18 (--agents CLI flag: inline JSON agent definitions, session-only)
- Feature #38 (/agents UI: Running tab, Library tab, Generate with Claude, color picker v2.1.98)

And .planning/research/STACK.md for:
- #17 sparsePaths (v2.1.76) -- worktree.sparsePaths for sparse-checkout in monorepos
- #17 ExitWorktree tool (v2.1.72) -- clean worktree exit
- #13 @agent-name mention (v2.1.89) -- invoke specific subagent by name
- #13 --agents flag details

Note specifically:
- Current Persistent Memory section (lines ~307-334) -- what it already covers, what needs expanding
- Current background execution section (lines ~338-364) -- what it already covers
- Current frontmatter reference section (where initialPrompt is mentioned briefly)
- Current /agents section -- how it describes the management UI
- Current "Where Subagents Live" section (where --agents flag fits)
- Current Git Worktrees section (where sparsePaths fits)
- All cross-references to other chapters
- Component imports: CodeBlock, TerminalRecording
  </action>
  <verify>File read successfully. Gap analysis complete between existing coverage and required expansions.</verify>
  <done>Full understanding of what Ch9 already covers vs what needs expanding, with specific line ranges and sections identified</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Ch9 worktrees.mdx</name>
  <files>src/data/guides/claude-code/pages/worktrees.mdx</files>
  <action>
Write the COMPLETE updated chapter as a full MDX file replacement. Do NOT do surgical edits -- write the chapter fresh using the existing file as structural reference.

**Frontmatter updates:**
- Bump `lastVerified` and `updatedDate` to 2026-04-12
- Update `keywords` to include "sparsePaths", "initialPrompt", "agent memory", "background tasks"
- Update `description` to mention memory and initialPrompt: "Parallelize development across isolated git worktree branches using built-in and custom subagents with AGENT.md persona definitions, persistent memory, initialPrompt auto-submit, and sparse-checkout for monorepos."

**Structural approach: ADDITIVE with section expansions**

Keep the existing section flow. Expand these existing sections:

1. **Persistent Memory section** -- Expand significantly with:
   - Storage paths: user-scoped memory at `~/.claude/agent-memory/<name>/`, project-scoped at `.claude/agent-memory/<name>/`
   - MEMORY.md entrypoint file: the main memory file that the agent reads on startup
   - Topic files: agents can create topic-specific memory files (e.g., `coding-standards.md`, `project-architecture.md`)
   - Loading limits: first 200 lines or 25KB of memory loaded at session start
   - CodeBlock example showing the memory directory structure
   - Keep the existing scope choices (user vs project vs both) but add the storage details

2. **Background execution section** -- Expand with:
   - `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1` env var to disable background execution entirely
   - Permissions in background mode: tools pre-approved upfront run normally; anything else is auto-denied (no human to ask)
   - `Ctrl+B` to send a foreground subagent to background mid-execution
   - Verify what the existing section already covers and do not duplicate

3. **initialPrompt in frontmatter reference** -- Expand from brief mention to a dedicated example:
   - Auto-submits the first turn when the agent runs as main session agent (via `--agent` flag or `agent` setting)
   - Commands and skills referenced in the prompt are processed
   - Prepended to any user-provided prompt
   - CodeBlock example showing an AGENT.md with initialPrompt:
     ```yaml
     ---
     name: code-reviewer
     initialPrompt: "Review all staged changes for security issues and code quality. Use /compact if context gets large."
     ---
     ```

4. **sparsePaths** -- Add to the Git Worktrees section as a subsection:
   - `worktree.sparsePaths` setting for sparse-checkout in monorepos
   - When enabled, worktrees only check out specified directories instead of the full repo
   - Use case: large monorepos where each subagent only needs a subset of the codebase
   - CodeBlock example:
     ```json
     {
       "worktree": {
         "sparsePaths": ["packages/frontend", "packages/shared"]
       }
     }
     ```
   - Keep to ~15-20 lines

5. **/agents UI updates** -- Update the existing /agents management section:
   - Running tab shows active subagents with status
   - Library tab shows available agents from AGENT.md files and plugins
   - Generate with Claude: AI-assisted agent creation
   - Color picker for agent identification (v2.1.98)
   - Keep factual, not a walkthrough

6. **--agents CLI flag** -- Expand the existing mention in "Where Subagents Live":
   - `--agents` flag defines subagents inline as JSON at launch
   - Session-only (not persisted to disk)
   - CodeBlock example:
     ```bash
     claude --agents '[{"name": "reviewer", "description": "Reviews code for issues"}]'
     ```

7. **@agent-name mention** -- Add to the section about invoking subagents:
   - Type `@agent-name` in prompt to invoke a specific subagent by name
   - Works with both AGENT.md-defined and built-in agents

8. **ExitWorktree tool** -- Brief mention in worktree section:
   - New tool that cleanly exits a worktree session (cleans up the worktree branch)

9. **Cross-references to Ch12-14:**
   - [Plugins](/guides/claude-code/plugins/) -- mention that plugins can define agents
   - [Agent SDK](/guides/claude-code/agent-sdk/) -- mention that agents can be created programmatically via SDK

**Style rules (MANDATORY):**
- No "What's New" callouts
- No "(formerly X)" parentheticals
- Complete CodeBlock examples with code, lang, title, caption props
- JSON only for config, YAML only for AGENT.md frontmatter examples
- Professional but approachable tone

**Deprecation verification:**
- No /tag, /vim
- No ANTHROPIC_SMALL_FAST_MODEL
- No "5 permission modes" (should be 6 if mentioned)
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -5</automated>
Verify:
1. File builds without MDX errors
2. "sparsePaths" documented with JSON config example
3. "initialPrompt" expanded with AGENT.md YAML example
4. Memory storage paths documented (~/.claude/agent-memory/, .claude/agent-memory/)
5. "CLAUDE_CODE_DISABLE_BACKGROUND_TASKS" documented
6. "@agent-name" mention syntax documented
7. Cross-refs to /guides/claude-code/plugins/ and /guides/claude-code/agent-sdk/ present
8. No deprecated terms
9. Total file is 520-560 lines
  </verify>
  <done>
- Ch9 Persistent Memory expanded with storage paths, MEMORY.md, topic files, 200-line limit
- Ch9 Background expanded with CLAUDE_CODE_DISABLE_BACKGROUND_TASKS, Ctrl+B, permission behavior
- Ch9 initialPrompt expanded with auto-submit behavior and AGENT.md example
- Ch9 sparsePaths documented for monorepo sparse-checkout
- Ch9 /agents UI updated with Running/Library tabs
- Ch9 --agents CLI flag expanded with inline JSON example
- Ch9 @agent-name mention documented
- Ch9 ExitWorktree tool mentioned
- Real hyperlinks to Ch12 Plugins, Ch13 Agent SDK
- All deprecated terms absent
- File compiles without errors
- ~520-560 lines (expanded from 435)
  </done>
</task>

</tasks>

<verification>
1. Build: `npx astro build` passes without errors on worktrees.mdx
2. Content: grep for "sparsePaths", "initialPrompt", "MEMORY.md", "agent-memory", "@agent-name", "ExitWorktree"
3. Cross-refs: grep for "/guides/claude-code/plugins/", "/guides/claude-code/agent-sdk/" to verify real hyperlinks
4. Deprecation: grep for "/tag", "/vim", "ANTHROPIC_SMALL_FAST_MODEL" returns zero
5. Size: wc -l shows 520-560 lines
</verification>

<success_criteria>
- Ch9 Persistent Memory section documents storage paths, MEMORY.md, topic files, 200-line limit
- Ch9 documents background mode env var and Ctrl+B
- Ch9 documents initialPrompt auto-submit with example
- Ch9 documents sparsePaths for monorepo worktrees
- Ch9 documents /agents UI with Running/Library tabs
- Ch9 documents --agents CLI flag and @agent-name
- Real hyperlinks to Ch12 and Ch13
- No deprecated terms
- File compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/113-lower-impact-chapter-updates/113-05-SUMMARY.md`
</output>
