---
phase: 113-lower-impact-chapter-updates
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/guides/claude-code/pages/remote-and-headless.mdx
autonomous: true
requirements: [UPD-05]

must_haves:
  truths:
    - "Ch5 documents Remote Control server mode with --spawn, --capacity, and session modes"
    - "Ch5 has a Channels section covering Telegram/Discord/iMessage with security model"
    - "Ch5 documents --bare flag for scripted/CI contexts"
    - "Ch5 documents --teleport flag for resuming web sessions locally"
    - "Ch5 scheduled tasks section reviewed and confirmed current (/loop, CronCreate/List/Delete, 3-day expiry)"
    - "Ch5 Channels section is 60-80 lines (not bloated)"
    - "Ch5 contains real hyperlinks to Ch12 Plugins (Channels installed as plugins)"
    - "No deprecated terms appear"
  artifacts:
    - path: "src/data/guides/claude-code/pages/remote-and-headless.mdx"
      provides: "Updated Ch5 with server mode, Channels, --bare, --teleport"
      contains: "Channels"
  key_links:
    - from: "remote-and-headless.mdx"
      to: "plugins.mdx"
      via: "inline cross-reference for Channels installation"
      pattern: "/guides/claude-code/plugins/"
    - from: "remote-and-headless.mdx"
      to: "agent-sdk.mdx"
      via: "inline cross-reference as programmatic alternative"
      pattern: "/guides/claude-code/agent-sdk/"
    - from: "remote-and-headless.mdx"
      to: "security.mdx"
      via: "inline cross-reference for enterprise Channels controls"
      pattern: "/guides/claude-code/security/"
    - from: "remote-and-headless.mdx"
      to: "mcp.mdx"
      via: "next-chapter link in Further Reading"
      pattern: "/guides/claude-code/mcp/"
---

<objective>
Update Ch5 (Remote Control & Headless Automation) with Remote Control server mode (--spawn, --capacity), Channels section (Telegram/Discord/iMessage), --bare flag, --teleport flag, and minor additions. This is the largest update in the phase with two significant new sections.

Purpose: Ch5 covers remote and headless usage. Server mode and Channels are substantial new capabilities that expand what users can do with remote sessions.
Output: Updated remote-and-headless.mdx with server mode subsection and Channels section.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-lower-impact-chapter-updates/113-RESEARCH.md
@.planning/research/FEATURES-claude-code-guide-refresh.md
@.planning/research/STACK.md

@src/data/guides/claude-code/pages/remote-and-headless.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read existing Ch5 and feature sources</name>
  <files>src/data/guides/claude-code/pages/remote-and-headless.mdx</files>
  <action>
Read the full existing remote-and-headless.mdx (396 lines) to understand current structure, imports, and sections.

Also read .planning/research/STACK.md for:
- #7 Remote Control server mode (--spawn with modes: same-dir, worktree, session; --capacity N; --remote-control-session-name-prefix)
- #3 Channels (Telegram/Discord/iMessage, --channels flag, sender allowlist, pairing codes, enterprise controls)
- #16 --bare flag (v2.1.81, skips auto-discovery of hooks/LSP/plugins/MCP/auto-memory/CLAUDE.md)
- --teleport flag (resume web session in local terminal)

And .planning/research/FEATURES-claude-code-guide-refresh.md for:
- Feature #5 (Channels details)
- Feature #27 (PR Auto-Fix, web-only)
- Feature #30 (Deep links, claude-cli:// protocol)

Note specifically:
- Current Remote Control section structure (where to add server mode as subsection)
- Current Programmatic Usage section (where --bare fits)
- Current session management section (where --teleport fits)
- Current scheduled tasks section (verify still accurate)
- All cross-references to other chapters (must preserve)
- Component imports: CodeBlock, TerminalRecording
  </action>
  <verify>File read successfully. Clear understanding of where server mode, Channels, and --bare sections go.</verify>
  <done>Full understanding of Ch5 structure with specific insertion points for all new content</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Ch5 remote-and-headless.mdx</name>
  <files>src/data/guides/claude-code/pages/remote-and-headless.mdx</files>
  <action>
Write the COMPLETE updated chapter as a full MDX file replacement. Do NOT do surgical edits -- write the chapter fresh using the existing file as structural reference.

**Frontmatter updates:**
- Bump `lastVerified` and `updatedDate` to 2026-04-12
- Update `keywords` to include "channels", "server mode", "bare flag", "teleport"
- Update `description` to mention Channels and server mode: "Run Claude Code from any device via remote control with multi-session server mode, automate with headless mode in CI/CD pipelines, interact through Telegram/Discord/iMessage channels, and schedule recurring tasks."

**Structural approach: ADDITIVE with two new sections**

Keep the existing section flow. Add new content in these locations:

1. **Server Mode subsection** (under Remote Control section) -- New subsection covering:
   - The `--spawn` flag that starts remote control in server mode
   - Three spawn modes: `same-dir` (all sessions share working directory), `worktree` (each session gets isolated git worktree), `session` (each session gets separate directory)
   - `--capacity N` to limit concurrent sessions (default: 1)
   - `--remote-control-session-name-prefix` for auto-naming sessions
   - Include a CodeBlock showing a typical server mode invocation:
     ```bash
     claude remote-control --spawn worktree --capacity 3
     ```
   - Brief explanation of when to use server mode (persistent service, multiple users connecting)
   - Keep to ~30-40 lines

2. **Channels section** (NEW top-level `##` section, placed after Remote Control and before Programmatic Usage) -- This is the biggest addition:
   - Open with a Research Preview blockquote (matching Ch10 Agent Teams pattern):
     ```
     > **Research Preview:** Channels are experimental and disabled by default...
     ```
   - What channels are: push events from Telegram, Discord, or iMessage into a running Claude Code session (2 paragraphs)
   - Setup flow: Install channel plugin from marketplace, start session with `--channels` flag. CodeBlock example:
     ```bash
     claude --channels
     ```
   - Security model: pairing codes for initial connection, sender allowlist restricts who can send messages. Brief but clear.
   - Enterprise controls: `channelsEnabled` setting in managed-settings.json for organizational control. Cross-ref to [Security](/guides/claude-code/security/) for full enterprise settings.
   - Cross-ref: "Channels are installed as plugins. See [Plugins](/guides/claude-code/plugins/) for installation and marketplace details."
   - **CRITICAL: Keep the Channels section to 60-80 lines total.** Do NOT deep-dive per-platform setup (Telegram vs Discord vs iMessage specifics). Cover the concept, setup, and security model. This is research preview -- may change.

3. **--bare flag** -- Add to the Programmatic Usage section or nearby. One paragraph + CodeBlock:
   ```bash
   claude -p "query" --bare
   ```
   Explain: skips auto-discovery of hooks, LSP, plugins, MCP, auto-memory, CLAUDE.md. Useful for scripted/CI contexts where you want a clean, fast invocation without project-specific configuration loading. v2.1.81.

4. **--teleport flag** -- Brief mention in session management section. One sentence: `--teleport` resumes a web session in your local terminal. Include inline code example.

5. **Minor additions** (one-liners in relevant spots):
   - PR Auto-Fix: brief mention that web sessions can auto-watch CI and fix failures
   - Deep links: `claude-cli://` protocol for multi-line prompts from external tools
   - Cross-ref to [Agent SDK](/guides/claude-code/agent-sdk/) as the programmatic alternative to -p flag

6. **Cross-references to Ch12-14:**
   - [Plugins](/guides/claude-code/plugins/) -- in Channels section (installed as plugins)
   - [Agent SDK](/guides/claude-code/agent-sdk/) -- in Programmatic Usage (SDK is the programmatic alternative)
   - [Security](/guides/claude-code/security/) -- for enterprise Channels controls (channelsEnabled)

**Style rules (MANDATORY):**
- No "What's New" callouts
- No "(formerly X)" parentheticals
- Complete CodeBlock examples with code, lang, title, caption props
- JSON only for config examples
- Research Preview uses blockquote format (matching Ch10 pattern), NOT Callout component
- Professional but approachable tone

**Deprecation verification:**
- No /tag, /vim
- No ANTHROPIC_SMALL_FAST_MODEL
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -5</automated>
Verify:
1. File builds without MDX errors
2. "Channels" section exists with Research Preview blockquote
3. "--spawn" and "--capacity" documented in server mode subsection
4. "--bare" documented with explanation
5. Cross-refs to /guides/claude-code/plugins/, /guides/claude-code/agent-sdk/, /guides/claude-code/security/ present
6. Channels section is under 100 lines (target 60-80)
7. No deprecated terms
8. Total file is 520-570 lines
  </verify>
  <done>
- Ch5 documents server mode with --spawn, --capacity, and three spawn modes
- Ch5 has Channels section (60-80 lines) covering concept, setup, security model, enterprise controls
- Ch5 documents --bare flag for scripted/CI
- Ch5 documents --teleport for web session resumption
- Ch5 scheduled tasks section reviewed and confirmed current
- Real hyperlinks to Ch12 Plugins, Ch13 Agent SDK, Ch11 Security
- All deprecated terms absent
- File compiles without errors
- ~520-570 lines (expanded from 396)
  </done>
</task>

</tasks>

<verification>
1. Build: `npx astro build` passes without errors on remote-and-headless.mdx
2. Content: grep for "Channels", "--spawn", "--bare", "--teleport", "server mode" in the rewritten file
3. Cross-refs: grep for "/guides/claude-code/plugins/", "/guides/claude-code/agent-sdk/" to verify real hyperlinks
4. Scope: wc -l shows 520-570 lines (not bloated)
5. Channels: section is under 100 lines
6. Deprecation: grep for "/tag", "/vim", "ANTHROPIC_SMALL_FAST_MODEL" returns zero
</verification>

<success_criteria>
- Ch5 documents Remote Control server mode with --spawn, --capacity, session modes
- Ch5 has a Channels section (60-80 lines) with security model and enterprise controls
- Ch5 documents --bare flag
- Ch5 documents --teleport flag
- Ch5 scheduled tasks section reviewed and confirmed current
- Real hyperlinks to Ch12, Ch13, Ch11
- No deprecated terms
- File compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/113-lower-impact-chapter-updates/113-03-SUMMARY.md`
</output>
