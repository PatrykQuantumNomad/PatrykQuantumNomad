---
phase: 113-lower-impact-chapter-updates
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/guides/claude-code/pages/mcp.mdx
autonomous: true
requirements: [UPD-06]

must_haves:
  truths:
    - "Ch6 has an Elicitation section explaining MCP server interactive dialogs"
    - "Ch6 documents per-tool result-size caps via _meta['anthropic/maxResultSizeChars']"
    - "Ch6 mentions Computer Use as a built-in MCP server with cross-ref to Ch14"
    - "Ch6 documents MCP OAuth improvements (RFC 9728, Client ID Metadata)"
    - "Ch6 documents MCP env vars (CLAUDE_CODE_MCP_SERVER_NAME, MCP_CONNECTION_NONBLOCKING)"
    - "No deprecated terms appear"
  artifacts:
    - path: "src/data/guides/claude-code/pages/mcp.mdx"
      provides: "Updated Ch6 with elicitation, per-tool caps, OAuth improvements, env vars"
      contains: "elicitation"
  key_links:
    - from: "mcp.mdx"
      to: "computer-use.mdx"
      via: "inline cross-reference for Computer Use as MCP"
      pattern: "/guides/claude-code/computer-use/"
    - from: "mcp.mdx"
      to: "plugins.mdx"
      via: "inline cross-reference for plugin-bundled MCP servers"
      pattern: "/guides/claude-code/plugins/"
    - from: "mcp.mdx"
      to: "custom-skills.mdx"
      via: "next-chapter link in Further Reading"
      pattern: "/guides/claude-code/custom-skills/"
---

<objective>
Update Ch6 (Model Context Protocol) with MCP elicitation section, per-tool result-size caps, Computer Use MCP mention, OAuth improvements, server deduplication, and MCP env vars.

Purpose: Ch6 is the MCP reference. Elicitation is a new capability, per-tool caps give server authors more control, and several incremental improvements need documenting.
Output: Updated mcp.mdx with elicitation section and expanded troubleshooting/configuration.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-lower-impact-chapter-updates/113-RESEARCH.md
@.planning/research/FEATURES-claude-code-guide-refresh.md
@.planning/research/STACK.md

@src/data/guides/claude-code/pages/mcp.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read existing Ch6 and feature sources</name>
  <files>src/data/guides/claude-code/pages/mcp.mdx</files>
  <action>
Read the full existing mcp.mdx (354 lines) to understand current structure, imports, and sections.

Also read .planning/research/STACK.md for:
- #12 MCP elicitation (v2.1.76) -- servers request structured input from users
- Per-tool result-size caps via `_meta["anthropic/maxResultSizeChars"]` in tools/list metadata (v2.1.91, up to 500K chars)
- OAuth improvements: RFC 9728 Protected Resource Metadata (v2.1.85), OAuth Client ID Metadata (v2.1.81)
- Server deduplication: local config wins when same server in multiple scopes (v2.1.84)
- Env vars: CLAUDE_CODE_MCP_SERVER_NAME, CLAUDE_CODE_MCP_SERVER_URL (v2.1.85), MCP_CONNECTION_NONBLOCKING=true for -p mode (v2.1.89)

And .planning/research/FEATURES-claude-code-guide-refresh.md for:
- Feature #2 (Computer Use implemented as built-in MCP server)
- Feature #12 (per-tool result-size _meta)

Note specifically:
- Current MCP Resources section (where to place Elicitation after it)
- Current OAuth section (where to add improvements)
- Current Server Scopes section (where to add deduplication note)
- Current troubleshooting section (where MAX_MCP_OUTPUT_TOKENS is mentioned -- per-tool caps are a related mechanism)
- Component imports: CodeBlock, McpArchitectureDiagram
  </action>
  <verify>File read successfully. Section locations identified for all additions.</verify>
  <done>Full understanding of Ch6 structure with specific insertion points for elicitation, per-tool caps, OAuth, and env vars</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Ch6 mcp.mdx</name>
  <files>src/data/guides/claude-code/pages/mcp.mdx</files>
  <action>
Write the COMPLETE updated chapter as a full MDX file replacement. Do NOT do surgical edits -- write the chapter fresh using the existing file as structural reference.

**Frontmatter updates:**
- Bump `lastVerified` and `updatedDate` to 2026-04-12
- Add "elicitation", "result-size caps" to `keywords` array
- Update `description` to mention elicitation: "Connect Claude Code to external tools using the Model Context Protocol with stdio, HTTP, OAuth, elicitation, and per-tool result-size controls."

**Structural approach: ADDITIVE with one new section**

Keep the existing section flow. Add new content in these locations:

1. **Elicitation section** (NEW `##` section, placed after MCP Resources section) -- The main addition:
   - What elicitation is: MCP servers can request structured input from the user through an interactive dialog during tool execution
   - How it works: server sends an elicitation request specifying the input schema, Claude Code presents it to the user as a form/dialog, user responds, server receives the response and continues
   - When it is used: servers that need runtime configuration, confirmations, or credentials that should not be hardcoded
   - Brief CodeBlock example showing what an elicitation request looks like from the server author perspective (JSON schema for the input)
   - Note: elicitation is automatic -- no user configuration needed. If a connected server requests it, Claude Code handles the UI.
   - Keep to ~30-40 lines

2. **Per-tool result-size caps** -- Add to the troubleshooting section alongside existing MAX_MCP_OUTPUT_TOKENS mention. Explain:
   - Server authors can set per-tool maximum result size via `_meta["anthropic/maxResultSizeChars"]` in the `tools/list` response metadata
   - Up to 500K characters per tool
   - This overrides the global cap for specific tools that legitimately return large results
   - CodeBlock example showing the _meta field in tools/list JSON response
   - Keep to ~15-20 lines

3. **Computer Use as MCP** -- Brief mention in the architecture overview or "What MCP Enables" section. One sentence: "Claude Code's Computer Use feature is implemented as a built-in MCP server, giving it access to screen capture and input tools. See [Computer Use](/guides/claude-code/computer-use/) for setup and usage." Do not elaborate -- that is Ch14's job.

4. **OAuth improvements** -- Update the existing OAuth section with current behavior:
   - RFC 9728 Protected Resource Metadata (v2.1.85) for automatic discovery
   - OAuth Client ID Metadata (v2.1.81)
   - These are incremental improvements -- integrate naturally into the existing OAuth prose

5. **Server deduplication** -- Brief note in Server Scopes section. One sentence: when the same MCP server appears at multiple scopes, the most local configuration wins (project > user > managed).

6. **MCP env vars** -- Add to relevant sections or create a small "Environment Variables" subsection in troubleshooting:
   - `CLAUDE_CODE_MCP_SERVER_NAME` -- override the server name
   - `CLAUDE_CODE_MCP_SERVER_URL` (v2.1.85) -- override server URL
   - `MCP_CONNECTION_NONBLOCKING=true` -- for -p mode, don't block on MCP connection
   - Brief table format preferred

7. **Cross-references to Ch12-14:**
   - [Computer Use](/guides/claude-code/computer-use/) -- in architecture/overview
   - [Plugins](/guides/claude-code/plugins/) -- mention that plugins can bundle MCP servers

**Style rules (MANDATORY):**
- No "What's New" callouts
- No "(formerly X)" parentheticals
- Complete CodeBlock examples with code, lang, title, caption props
- JSON only for config examples
- Professional but approachable tone

**Deprecation verification:**
- No /tag, /vim
- No ANTHROPIC_SMALL_FAST_MODEL
- SSE transport should be noted as deprecated if still mentioned (HTTP replaces it)
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -5</automated>
Verify:
1. File builds without MDX errors
2. "Elicitation" or "elicitation" section exists
3. "_meta" and "maxResultSizeChars" appear for per-tool caps
4. Cross-refs to /guides/claude-code/computer-use/ and /guides/claude-code/plugins/ present
5. McpArchitectureDiagram import still present and used
6. No deprecated terms
7. Total file is 410-440 lines
  </verify>
  <done>
- Ch6 has Elicitation section explaining MCP server interactive dialogs
- Ch6 documents per-tool result-size caps with _meta example
- Ch6 mentions Computer Use as built-in MCP server with Ch14 link
- Ch6 documents OAuth improvements
- Ch6 documents MCP env vars
- Real hyperlinks to Ch14 Computer Use, Ch12 Plugins
- File compiles without errors
- ~410-440 lines (expanded from 354)
  </done>
</task>

</tasks>

<verification>
1. Build: `npx astro build` passes without errors on mcp.mdx
2. Content: grep for "elicitation", "maxResultSizeChars", "Computer Use", "MCP_CONNECTION_NONBLOCKING" in the rewritten file
3. Cross-refs: grep for "/guides/claude-code/computer-use/", "/guides/claude-code/plugins/" to verify real hyperlinks
4. Architecture: McpArchitectureDiagram import present
5. Deprecation: grep for "/tag", "/vim", "ANTHROPIC_SMALL_FAST_MODEL" returns zero
</verification>

<success_criteria>
- Ch6 has Elicitation section
- Ch6 documents per-tool result-size caps
- Ch6 mentions Computer Use as built-in MCP server
- Ch6 documents OAuth improvements and env vars
- Real hyperlinks to Ch14 and Ch12
- No deprecated terms
- File compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/113-lower-impact-chapter-updates/113-04-SUMMARY.md`
</output>
