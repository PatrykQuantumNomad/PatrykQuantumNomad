---
phase: 104-static-landing-page-force-layout
plan: 02
type: execute
wave: 2
depends_on: [104-01]
files_modified:
  - src/components/ai-landscape/GraphLegend.astro
  - src/components/ai-landscape/ConceptList.astro
  - src/pages/ai-landscape/index.astro
autonomous: false
requirements: [GRAPH-07, SITE-01]

must_haves:
  truths:
    - "Visiting /ai-landscape/ renders a page with a static SVG graph visible before any JS loads"
    - "The SVG shows cluster-colored nodes matching the DOT hierarchy colors"
    - "A color-coded legend below the SVG explains cluster colors"
    - "A category-grouped concept list links to all 51 individual /ai-landscape/[slug]/ pages"
    - "The page works in both light and dark mode"
    - "The SVG is horizontally scrollable on mobile viewports"
  artifacts:
    - path: "src/components/ai-landscape/GraphLegend.astro"
      provides: "Color-coded legend explaining cluster colors and edge styles"
      min_lines: 30
    - path: "src/components/ai-landscape/ConceptList.astro"
      provides: "Category-grouped concept list with links to concept pages"
      min_lines: 30
    - path: "src/pages/ai-landscape/index.astro"
      provides: "Landing page assembling SVG, legend, and concept list"
      min_lines: 50
  key_links:
    - from: "src/pages/ai-landscape/index.astro"
      to: "src/lib/ai-landscape/svg-builder.ts"
      via: "imports buildLandscapeSvg"
      pattern: "import.*buildLandscapeSvg.*svg-builder"
    - from: "src/pages/ai-landscape/index.astro"
      to: "src/data/ai-landscape/layout.json"
      via: "imports layout data"
      pattern: "import.*layout\\.json"
    - from: "src/pages/ai-landscape/index.astro"
      to: "src/data/ai-landscape/graph.json"
      via: "imports graph data"
      pattern: "import.*graph\\.json"
    - from: "src/pages/ai-landscape/index.astro"
      to: "src/components/ai-landscape/GraphLegend.astro"
      via: "renders legend component"
      pattern: "<GraphLegend"
    - from: "src/pages/ai-landscape/index.astro"
      to: "src/components/ai-landscape/ConceptList.astro"
      via: "renders concept list component"
      pattern: "<ConceptList"
    - from: "src/components/ai-landscape/ConceptList.astro"
      to: "src/lib/ai-landscape/routes.ts"
      via: "imports conceptUrl for links"
      pattern: "import.*conceptUrl.*routes"
---

<objective>
Build the /ai-landscape/ landing page with GraphLegend, ConceptList components, and the full static SVG graph inline-rendered at build time.

Purpose: This is the user-facing deliverable -- the landing page that renders the pre-computed SVG graph, explains it with a legend, and provides a navigable concept list linking to all 51 concept pages. The static SVG is the first meaningful paint with zero JavaScript required.

Output: GraphLegend.astro, ConceptList.astro, index.astro (landing page)
</objective>

<execution_context>
@/Users/patrykattc/work/git/PatrykQuantumNomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/PatrykQuantumNomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-static-landing-page-force-layout/104-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan consumes. -->

From src/lib/ai-landscape/svg-builder.ts (created in Plan 01):
```typescript
export function buildLandscapeSvg(
  nodes: AiNode[],
  edges: Edge[],
  positions: LayoutPosition[],
  clusters: Cluster[],
  meta: LayoutMeta,
): string;
```

From src/lib/ai-landscape/layout-schema.ts (created in Plan 01):
```typescript
export type LayoutPosition = { id: string; x: number; y: number };
export type LayoutMeta = { width: number; height: number; ticks: number; generated: string };
export type Layout = { meta: LayoutMeta; positions: LayoutPosition[] };
```

From src/lib/ai-landscape/schema.ts (existing):
```typescript
export type AiNode = z.infer<typeof aiNodeSchema>;
export type Edge = z.infer<typeof edgeSchema>;
export type Cluster = z.infer<typeof clusterSchema>;
// Cluster: { id, name, color, darkColor, parentClusterId }
```

From src/lib/ai-landscape/routes.ts (existing):
```typescript
export const AI_LANDSCAPE_BASE = '/ai-landscape/';
export function conceptUrl(slug: string): string;
```

From src/data/ai-landscape/graph.json:
```json
{
  "clusters": [
    { "id": "ai", "name": "Artificial Intelligence (AI)", "color": "#e0f7fa", "darkColor": "#00696e", "parentClusterId": null },
    { "id": "ml", "name": "Machine Learning (ML)", "color": "#c8e6c9", "darkColor": "#2e7d32", "parentClusterId": "ai" },
    { "id": "nn", "name": "Neural Networks (NN)", "color": "#fff9c4", "darkColor": "#827717", "parentClusterId": "ml" },
    { "id": "dl", "name": "Deep Learning (DL)", "color": "#ffecb3", "darkColor": "#e65100", "parentClusterId": "nn" },
    { "id": "genai", "name": "Generative AI (GenAI)", ... },
    { "id": "levels", "name": "Levels of AI", ... },
    { "id": "agentic", "name": "Agentic AI", ... },
    { "id": "agent-frameworks", "name": "Agent Frameworks", ... },
    { "id": "devtools", "name": "Developer Tools", ... }
  ],
  "edges": [ /* 66 edges with source, target, label, type */ ]
}
```

Landing page pattern from src/pages/beauty-index/index.astro:
- Layout wrapper with title, description, ogImage props
- BreadcrumbJsonLd component for structured data
- Hero section: centered text, "2026 Edition" badge, h1, subtitle
- Content sections with max-w-6xl mx-auto px-4 sm:px-6 lg:px-8
- getCollection('aiLandscape') for node data

Edge types for legend:
- hierarchy: solid line (structural parent-child)
- includes: dashed line (set membership)
- Other types (enables, example, relates, etc.): dotted/thin line
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GraphLegend and ConceptList components</name>
  <files>
    src/components/ai-landscape/GraphLegend.astro,
    src/components/ai-landscape/ConceptList.astro
  </files>
  <action>
    1. Create `src/components/ai-landscape/GraphLegend.astro`:

       Props: `clusters: Cluster[]` (from graph.json)

       Structure:
       - Outer section: `class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 mt-12"`
       - h2: "Legend" with `class="text-xl font-heading font-bold mb-4"`
       - Three-column grid: `class="grid grid-cols-1 sm:grid-cols-3 gap-6"`

       Column 1 -- Clusters:
       - h3: "Clusters" with `class="text-sm font-heading font-bold mb-2 text-[var(--color-text-secondary)]"`
       - For each cluster: a flex row with a colored circle (16x16 rounded-full div) using inline style for background color, and the cluster name as text
       - Light mode: use cluster.color for the circle background
       - Dark mode: use a `<span>` with both `style="background:{cluster.color}"` and a `class="dark:hidden"` / `class="hidden dark:inline-block"` pair (one span for light, one for dark using cluster.darkColor)
       - Actually simpler: use a single circle with `style="background:var(--cluster-{id}-color)"` and define those CSS variables. BUT since Astro components are server-rendered, the simplest approach is two spans with Tailwind dark: variant. Use: `<span class="inline-block w-4 h-4 rounded-full dark:hidden" style={`background:${cluster.color}`}></span><span class="hidden dark:inline-block w-4 h-4 rounded-full" style={`background:${cluster.darkColor}`}></span>`

       Column 2 -- Node Shapes:
       - h3: "Node Sizes"
       - Two entries:
         - Large circle (w-6 h-6 rounded-full border-2 border-[var(--color-border)]) + "Root concepts (AI, ML, NN, DL, GenAI)"
         - Small circle (w-4 h-4 rounded-full border-2 border-[var(--color-border)]) + "Individual concepts"

       Column 3 -- Edge Styles:
       - h3: "Connections"
       - Three entries using small inline SVGs or styled divs:
         - Solid line (w-8 h-0.5 bg-[var(--color-border)]) + "Structural hierarchy"
         - Dashed line (w-8 h-0.5 border-t-2 border-dashed border-[var(--color-border)]) + "Set membership"
         - Thin dotted line (w-8 h-0.5 border-t border-dotted border-[var(--color-border)] opacity-50) + "Related concepts"

    2. Create `src/components/ai-landscape/ConceptList.astro`:

       Props: `nodesByCluster: Map<string, AiNode[]>`, `clusters: Cluster[]`

       Import conceptUrl from '../../lib/ai-landscape/routes'

       Structure:
       - Outer section: `class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 mt-16 pb-16"`
       - h2: "Explore by Category" with `class="text-2xl font-heading font-bold mb-8"`
       - Grid: `class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8"`

       For each cluster (iterate clusters array to maintain order):
       - Get nodes for this cluster from nodesByCluster map
       - Skip if no nodes
       - div container with:
         - h3: cluster name with `class="text-lg font-heading font-bold mb-3"` and left border accent using cluster darkColor: `style={`border-left: 4px solid ${cluster.darkColor}; padding-left: 0.75rem`}`
         - Dark mode border: Use a `<style>` scoped block or inline approach. Simplest: use darkColor for the border since it's visible in both modes (dark colors have enough contrast on white backgrounds and stand out on dark backgrounds). This matches the existing pattern.
         - ul: `class="space-y-1.5"`
         - For each node in sorted order (alphabetical by name):
           - li > a with href={conceptUrl(node.slug)}
           - Link class: `class="text-sm text-[var(--color-text-secondary)] hover:text-[var(--color-accent)] transition-colors"`
           - Link text: node.name

    IMPORTANT: Both components are pure Astro (zero JS) -- all rendering is server-side at build time.
    IMPORTANT: Follow existing component patterns: interface Props, const { prop } = Astro.props destructuring.
    IMPORTANT: Use relative imports (../../lib/...) -- no path aliases in this project.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -20</automated>
  </verify>
  <done>
    - GraphLegend.astro renders 9 cluster color entries, node size explanations, and edge style explanations
    - ConceptList.astro renders category-grouped links for all 51 concepts across 9 clusters
    - Both components use Tailwind for styling with dark mode support
    - No TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /ai-landscape/ landing page assembling SVG, legend, and concept list</name>
  <files>src/pages/ai-landscape/index.astro</files>
  <action>
    Create `src/pages/ai-landscape/index.astro` following the beauty-index/index.astro pattern:

    Frontmatter:
    - Import getCollection from 'astro:content'
    - Import graphData from '../../data/ai-landscape/graph.json'
    - Import layoutData from '../../data/ai-landscape/layout.json'
    - Import { buildLandscapeSvg } from '../../lib/ai-landscape/svg-builder'
    - Import { conceptUrl, AI_LANDSCAPE_BASE } from '../../lib/ai-landscape/routes'
    - Import Layout from '../../layouts/Layout.astro'
    - Import BreadcrumbJsonLd from '../../components/BreadcrumbJsonLd.astro'
    - Import GraphLegend from '../../components/ai-landscape/GraphLegend.astro'
    - Import ConceptList from '../../components/ai-landscape/ConceptList.astro'
    - Import type { AiNode } from '../../lib/ai-landscape/schema'

    - const entries = await getCollection('aiLandscape')
    - const allNodes: AiNode[] = entries.map(e => e.data as AiNode)
    - const svgString = buildLandscapeSvg(allNodes, graphData.edges, layoutData.positions, graphData.clusters, layoutData.meta)
    - Group nodes by cluster: `const nodesByCluster = new Map<string, AiNode[]>()` with a for loop

    Template:
    ```
    <Layout
      title="AI Landscape Explorer: Visual Guide to Artificial Intelligence (2026) | Patryk Golabek"
      description="Explore the AI landscape — from machine learning to generative AI, agentic systems, and developer tools. Interactive visual guide with 51 concepts explained simply and technically."
    >
      <BreadcrumbJsonLd crumbs={[
        { name: 'Home', url: 'https://patrykgolabek.dev/' },
        { name: 'AI Landscape', url: 'https://patrykgolabek.dev/ai-landscape/' },
      ]} />

      <!-- Hero -->
      <section class="text-center max-w-4xl mx-auto py-16 pb-12 px-4">
        <span class="inline-block text-xs font-mono text-[var(--color-accent)] border border-[var(--color-accent)]/30 rounded-full px-3 py-1 mb-4">2026 Edition</span>
        <h1 class="text-4xl sm:text-5xl font-heading font-bold">AI Landscape Explorer</h1>
        <p class="text-lg text-[var(--color-text-secondary)] mt-4 max-w-2xl mx-auto">
          A visual guide to 51 key concepts in artificial intelligence — from foundational
          machine learning to cutting-edge agentic systems and developer tools.
        </p>
      </section>

      <!-- Static SVG Graph (first meaningful paint, no JS) -->
      <section class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 overflow-x-auto" aria-label="AI landscape visualization">
        <Fragment set:html={svgString} />
      </section>

      <!-- Legend -->
      <GraphLegend clusters={graphData.clusters} />

      <!-- Category-Grouped Concept List -->
      <ConceptList nodesByCluster={nodesByCluster} clusters={graphData.clusters} />
    </Layout>
    ```

    SEO considerations:
    - Title includes "AI Landscape Explorer", "Visual Guide", "Artificial Intelligence", "2026"
    - Description includes "machine learning", "generative AI", "agentic systems", "developer tools"
    - BreadcrumbJsonLd for structured data
    - SVG section has aria-label for accessibility
    - SVG container has overflow-x: auto for mobile horizontal scroll
    - Static SVG is inline HTML (not <img>) so it is indexable and supports CSS dark mode
    - Concept list provides text-based navigation alternative for mobile and accessibility

    IMPORTANT: Use `<Fragment set:html={svgString} />` to inject the SVG string as raw HTML -- do NOT use `innerHTML` or dangerouslySetInnerHTML.
    IMPORTANT: The SVG must be the first substantial visual content after the hero -- no JS dependencies for first paint.
    IMPORTANT: Follow the beauty-index pattern for Layout props, BreadcrumbJsonLd usage, and section spacing.
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - /ai-landscape/ page builds successfully with astro build
    - Page contains inline SVG with cluster-colored nodes
    - Page contains GraphLegend with cluster colors, node sizes, edge styles
    - Page contains ConceptList with all 51 concepts grouped by 9 clusters
    - BreadcrumbJsonLd renders structured data
    - SVG container is horizontally scrollable on narrow viewports
    - Page works in both light and dark mode
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of AI Landscape landing page</name>
  <action>
    Human verifies the complete /ai-landscape/ landing page visually.

    What was built: Complete /ai-landscape/ landing page with pre-computed force-directed SVG graph, color-coded legend, and category-grouped concept list. The SVG renders 51 AI concept nodes across 9 color-coded clusters with 66 connecting edges. Dark mode is fully supported.

    How to verify:
    1. Run `npm run dev` (or `npx astro dev`)
    2. Visit http://localhost:4321/ai-landscape/
    3. Verify the SVG graph is visible with colored nodes -- check that clusters are spatially grouped (AI concepts on the left, Levels/Agentic on the right, etc.)
    4. Toggle dark mode -- all node colors should switch to their dark variants
    5. Scroll down to the Legend section -- verify it shows 9 cluster colors, node size explanations, and edge style explanations
    6. Scroll to the "Explore by Category" section -- verify all 9 clusters are listed with clickable concept links
    7. Click any concept link (e.g., "Machine Learning") -- verify it navigates to /ai-landscape/machine-learning/
    8. Resize browser to mobile width (~375px) -- the SVG should be horizontally scrollable, and the concept list should stack to single column
    9. If cluster positioning looks wrong (nodes from different clusters overlapping), note which clusters are problematic -- the force parameters in generate-layout.mjs can be tuned
  </action>
  <verify>Type "approved" to complete the phase, or describe any visual issues (e.g., "clusters overlap", "text too small", "colors wrong in dark mode") for adjustment</verify>
  <done>User has visually verified the landing page in light mode, dark mode, and mobile viewport</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/ai-landscape` -- all tests pass (layout-schema + svg-builder + existing tests)
- `npx astro build` -- builds successfully with /ai-landscape/ page in dist output
- `ls dist/ai-landscape/index.html` -- landing page HTML exists
- `grep -c "svg" dist/ai-landscape/index.html` -- confirms SVG is inline in HTML
- `grep -c "ai-cluster-" dist/ai-landscape/index.html` -- confirms cluster CSS classes present
</verification>

<success_criteria>
- /ai-landscape/ renders a static SVG with 51 cluster-colored nodes and 66 edges
- Legend explains cluster colors, node sizes, and edge styles
- Concept list groups all 51 concepts by category with links to individual pages
- Page builds without errors and works in light/dark mode
- SVG is first meaningful paint -- no JavaScript required
- Visual checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/104-static-landing-page-force-layout/104-02-SUMMARY.md`
</output>
