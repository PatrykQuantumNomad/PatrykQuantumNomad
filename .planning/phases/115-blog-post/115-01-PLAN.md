---
phase: 115-blog-post
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/blog/claude-code-guide.mdx
  - public/images/claude-code-guide-refresh-cover.svg
autonomous: true
requirements: [BLOG-03, BLOG-04]

must_haves:
  truths:
    - "Old blog post displays an update callout banner at the top of the body"
    - "Old blog post links to the new post URL /blog/claude-code-guide-refresh/"
    - "Old blog post frontmatter includes updatedDate 2026-04-12"
    - "A branded cover SVG exists at the correct path for the OG image pipeline"
  artifacts:
    - path: "public/images/claude-code-guide-refresh-cover.svg"
      provides: "Branded cover image for new blog post"
      contains: "viewBox=\"0 0 1200 690\""
    - path: "src/data/blog/claude-code-guide.mdx"
      provides: "Updated old post with callout banner"
      contains: "Callout"
  key_links:
    - from: "src/data/blog/claude-code-guide.mdx"
      to: "/blog/claude-code-guide-refresh/"
      via: "Callout component link"
      pattern: "claude-code-guide-refresh"
    - from: "public/images/claude-code-guide-refresh-cover.svg"
      to: "src/pages/open-graph/[...slug].png.ts"
      via: "coverImage frontmatter in new post (Plan 02)"
      pattern: "claude-code-guide-refresh-cover"
---

<objective>
Update the existing "The Context Window Is the Product" blog post with an update callout banner linking to the new refresh post, and create a branded cover SVG image for the new post.

Purpose: BLOG-03 requires returning readers of the original post to discover the updated guide. BLOG-04 requires a branded OG image for social media sharing of the new post.
Output: Modified old blog post with Callout component, new cover SVG at public/images/
</objective>

<execution_context>
@/Users/patrykattc/work/git/PatrykQuantumNomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/PatrykQuantumNomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-blog-post/115-RESEARCH.md

@src/data/blog/claude-code-guide.mdx
@public/images/claude-code-guide-cover.svg
@src/components/blog/Callout.astro

<interfaces>
<!-- Callout component API (from src/components/blog/Callout.astro) -->
<!-- Props: type ("info" | "important" | "tip" | "warning"), title (string) -->
<!-- Usage: <Callout type="important" title="...">markdown content</Callout> -->
<!-- Import: import Callout from '../../components/blog/Callout.astro'; -->

<!-- Blog frontmatter schema (from src/content.config.ts) -->
<!-- Required: title, description, publishedDate, tags -->
<!-- Optional: updatedDate, coverImage, draft, externalUrl, source -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add update callout banner to existing blog post</name>
  <files>src/data/blog/claude-code-guide.mdx</files>
  <action>
Modify `src/data/blog/claude-code-guide.mdx` with two changes:

1. **Add `updatedDate` to frontmatter:** Insert `updatedDate: 2026-04-12` after the `publishedDate: 2026-03-15` line. This ensures the `article:modified_time` meta tag appears in SEO output.

2. **Add Callout import and banner after frontmatter:** The old post currently has NO component imports. Insert the following immediately after the closing `---` of the frontmatter and BEFORE the first paragraph ("I have watched dozens..."):

```mdx
import Callout from '../../components/blog/Callout.astro';

<Callout type="important" title="This guide has been updated">

The Claude Code Guide has grown from 11 to 14 chapters with major rewrites across every existing chapter. Three new topics cover Plugins, Agent SDK, and Computer Use. Read the full update: [Claude Code Guide Refresh](/blog/claude-code-guide-refresh/).

</Callout>
```

IMPORTANT: The Callout content must have blank lines before and after the markdown text inside the component tags -- this is required for MDX to parse the markdown correctly. Do NOT modify any other content in the file. The rest of the post body remains exactly as-is.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && grep -c "updatedDate: 2026-04-12" src/data/blog/claude-code-guide.mdx && grep -c "import Callout" src/data/blog/claude-code-guide.mdx && grep -c "claude-code-guide-refresh" src/data/blog/claude-code-guide.mdx</automated>
  </verify>
  <done>Old blog post frontmatter includes updatedDate 2026-04-12. Callout component is imported and rendered at the top of the body with type="important", linking to /blog/claude-code-guide-refresh/. No other content in the file is modified.</done>
</task>

<task type="auto">
  <name>Task 2: Create branded cover SVG for the refresh blog post</name>
  <files>public/images/claude-code-guide-refresh-cover.svg</files>
  <action>
Create `public/images/claude-code-guide-refresh-cover.svg` following the exact visual language of the existing `public/images/claude-code-guide-cover.svg` but distinguished as an "update/refresh" post.

Specifications:
- **Dimensions:** viewBox="0 0 1200 690", width="1200", height="690" (matches existing cover and OG pipeline expectations)
- **Background:** Same dark radial gradient (#1a1a2e center to #0f0f23 edges)
- **Accent colors:** Same amber gradient (#D97706 to #F59E0B)
- **Fonts:** Helvetica/Arial sans-serif for headings, Georgia/Times serif for italics (same as existing cover -- these are SVG text elements, not embedded fonts)
- **Corner accents:** Same decorative amber lines in all four corners

Content layout (differentiated from original):
- **Top:** "CLAUDE CODE GUIDE" in small caps with letter spacing (replacing "THE CONTEXT WINDOW IS THE PRODUCT")
- **Subtitle:** "What changed in the April 2026 refresh" in italic serif
- **Center:** A visual element showing the update -- three stacked amber-bordered cards/blocks labeled "3 New Chapters", "5 Major Rewrites", "Updated Cheatsheet" arranged vertically with subtle amber glow. Each card should be approximately 300px wide, 50px tall, with rounded corners (rx=6), amber border (stroke #D97706), dark fill, and white text.
- **Below cards:** "14 CHAPTERS | ZERO TO HERO" in small caps (updating from the original's "11 CHAPTERS")
- **Bottom:** Same amber accent line and "patrykgolabek.dev/guides/claude-code" attribution
- **Decorative:** Subtle amber dashed arrows pointing from left ("FROM 11") to right ("TO 14") flanking the cards, conveying the growth narrative

Keep the SVG clean and simple -- no embedded images, no external font references, no scripts. The OG pipeline (satori + sharp) processes this SVG and resizes to 1088x400 with `fit: 'contain'`, so the design must remain legible at that smaller size.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && test -f public/images/claude-code-guide-refresh-cover.svg && grep -c 'viewBox="0 0 1200 690"' public/images/claude-code-guide-refresh-cover.svg && grep -c "14 CHAPTERS" public/images/claude-code-guide-refresh-cover.svg</automated>
  </verify>
  <done>Cover SVG exists at public/images/claude-code-guide-refresh-cover.svg with 1200x690 dimensions, dark background, amber accents matching the existing cover's visual language, and content reflecting the guide refresh (14 chapters, 3 new topics, cheatsheet).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static content only | No user input, no API calls, no dynamic data -- pure static content files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | T (Tampering) | SVG file | accept | SVG is static, served from public/, no script elements. Astro build pipeline sanitizes. Low risk. |
| T-115-02 | I (Info Disclosure) | Blog post content | accept | All content is intentionally public (blog post). No secrets or PII involved. |
</threat_model>

<verification>
1. `grep "updatedDate" src/data/blog/claude-code-guide.mdx` shows `updatedDate: 2026-04-12`
2. `grep "import Callout" src/data/blog/claude-code-guide.mdx` confirms component import
3. `test -f public/images/claude-code-guide-refresh-cover.svg` confirms SVG exists
4. SVG contains `viewBox="0 0 1200 690"` and references 14 chapters
</verification>

<success_criteria>
- Old blog post has updatedDate in frontmatter and a Callout banner at the top linking to the new post
- Cover SVG exists at the correct path with proper dimensions and branded styling
</success_criteria>

<output>
After completion, create `.planning/phases/115-blog-post/115-01-SUMMARY.md`
</output>
