---
phase: 129-tiers-2-3-managed-graph-rag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "../rag-architecture-patterns/pyproject.toml"
  - "../rag-architecture-patterns/.env.example"
  - "../rag-architecture-patterns/.gitignore"
  - "../rag-architecture-patterns/tier-2-managed/requirements.txt"
  - "../rag-architecture-patterns/tier-3-graph/requirements.txt"
autonomous: true
requirements: [TIER-02, TIER-03]
user_setup:
  - service: openrouter
    why: "Tier 3 LightRAG entity-extraction + answer LLM + embeddings all route through OpenRouter (~$1 one-time ingest, ~$0.005/query)"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "https://openrouter.ai/keys"
    dashboard_config:
      - task: "Confirm key set in local .env (gitignored). Already required by Tier 1 (Phase 128)."
        location: "Local .env file"
  - service: gemini
    why: "Tier 2 Gemini File Search managed indexing + retrieval (Gemini-native, cannot route via OpenRouter); ~$0.075 one-time index, ~$0.0014/query"
    env_vars:
      - name: GEMINI_API_KEY
        source: "https://aistudio.google.com/app/apikey"
    dashboard_config:
      - task: "Already required by Phase 127 — confirm still in .env"
        location: "Local .env file"

must_haves:
  truths:
    - "`uv pip install --dry-run -e \".[tier-2]\"` resolves cleanly with NO new top-level deps (google-genai>=1.73,<2 from [shared] already covers file_search_stores)"
    - "`uv pip install --dry-run -e \".[tier-3]\"` resolves cleanly with lightrag-hku==1.4.15 (exact pin), openai>=1.50,<3, pymupdf>=1.27,<2"
    - "tests/test_tier_requirements.py::test_lockfile_does_not_contain_deprecated_sdk still passes — lightrag-hku 1.4.15 transitive deps do NOT pull `google-generativeai`"
    - ".env.example documents OPENROUTER_API_KEY as REQUIRED for Tier 1 + Tier 3, GEMINI_API_KEY as REQUIRED for Phase 127 smoke + Tier 2"
    - ".gitignore covers `lightrag_storage/` (already does via `lightrag_*/`) and `tier-2-managed/.store_id` (NEW entry)"
    - "tier-2-managed/requirements.txt and tier-3-graph/requirements.txt remain `-e ..[tier-N]` mirrors (single source of truth in pyproject)"
    - "shared/embeddings.py and shared/llm.py are NOT modified (Phase 127 contract preserved; Phase 128 smoke test still passes)"
  artifacts:
    - path: "../rag-architecture-patterns/pyproject.toml"
      provides: "Concrete tier-2 (no new deps) and tier-3 (lightrag-hku==1.4.15, openai, pymupdf) extras"
      contains: "lightrag-hku==1.4.15"
    - path: "../rag-architecture-patterns/.env.example"
      provides: "Updated comment block: OPENROUTER_API_KEY required for Tier 1 + Tier 3"
      contains: "Tier 3"
    - path: "../rag-architecture-patterns/.gitignore"
      provides: "tier-2-managed/.store_id sidecar gitignored"
      contains: "tier-2-managed/.store_id"
  key_links:
    - from: "tier-2-managed/requirements.txt"
      to: "pyproject.toml [project.optional-dependencies.tier-2]"
      via: "editable install of parent extras"
      pattern: "-e \\.\\.\\[tier-2\\]"
    - from: "tier-3-graph/requirements.txt"
      to: "pyproject.toml [project.optional-dependencies.tier-3]"
      via: "editable install of parent extras"
      pattern: "-e \\.\\.\\[tier-3\\]"
---

<objective>
Lock the dep contract for Tiers 2 and 3 in a single pyproject edit (file ownership conflict prevents parallelizing). Tier 2 needs NO new top-level deps — `google-genai>=1.73,<2` from `[shared]` already exposes `client.file_search_stores.*` (added in v1.49.0, verified). Tier 3 needs `lightrag-hku==1.4.15` (exact pin — releases every 2-7 days), `openai>=1.50,<3` (for `lightrag.llm.openai_complete_if_cache` OpenRouter routing), and `pymupdf>=1.27,<2` (Tier 3 owns its own PDF extractor per Pitfall 11 to avoid cross-tier coupling). Update `.env.example` to mark `OPENROUTER_API_KEY` as required for Tier 3, and add `tier-2-managed/.store_id` to `.gitignore` so the cached File Search store handle never leaks.

Purpose: Single early plan locks both tiers' deps so Plans 02 (Tier 2 store) and 03 (Tier 3 rag) can install in parallel from Wave 2 onward. No code yet — pure config.

Output: Updated `pyproject.toml`, `.env.example`, `.gitignore`. Tier requirements.txt files validated unchanged. Lockfile-guard test still passes.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-tiers-2-3-managed-graph-rag/129-RESEARCH.md

<interfaces>
<!-- Locked decisions from 129-RESEARCH.md "User Constraints" + "Standard Stack" -->

Tier 2 extras shape (NO new deps; google-genai already in [shared] >=1.73 covers file_search_stores added in v1.49):

```toml
tier-2 = [
  "rag-architecture-patterns[shared]",
]
```

Tier 3 extras shape (verbatim from 129-RESEARCH.md "Standard Stack"):

```toml
tier-3 = [
  "rag-architecture-patterns[shared]",
  "lightrag-hku==1.4.15",
  "openai>=1.50,<3",
  "pymupdf>=1.27,<2",
]
```

Pinning rationale (verified via `pip3 index versions` 2026-04-26 in 129-RESEARCH.md):
- `lightrag-hku==1.4.15` EXACT pin (Pitfall 9 — LightRAG ships dot releases every 2-7 days; floating constraint risks silent breakage). Released 2026-04-19.
- `openai>=1.50,<3` — same range as Tier 1 (Phase 128 Plan 01). LightRAG's `lightrag.llm.openai` module imports the `openai` Python SDK; pinning here ensures version alignment.
- `pymupdf>=1.27,<2` — Tier 3 OWNS its own PDF extractor (Pitfall 11 — avoids cross-tier coupling on `tier_1_naive.ingest.extract_pages`). Same range as `[tier-1]` and `[curation]`.

.env.example update (only the OPENROUTER_API_KEY comment block changes; the empty value line stays):

```
# REQUIRED for Tier 1 (chat + embeddings) and Tier 3 (LightRAG entity-extraction + answer LLM + embeddings via the OpenRouter unified gateway).
# OpenRouter proxies OpenAI, Anthropic, Google, Meta, and others behind a single
# OpenAI-compatible API — Tier 1 uses `openai/text-embedding-3-small` for the
# vector index and a configurable chat model (default `google/gemini-2.5-flash`,
# override at the CLI with `--model`). Tier 3 uses the same defaults — the
# graph-extraction LLM is also configurable via `--model`. Pricing matches each
# underlying provider 1:1.
#   Obtain at: https://openrouter.ai/keys
#   Browse models at: https://openrouter.ai/models
```

.gitignore addition (append once, after the existing `chroma_db/` and `lightrag_*/` entries):

```
tier-2-managed/.store_id
```

`lightrag_storage/` is ALREADY covered by the existing `lightrag_*/` glob (verified via `grep` in research). Do NOT add a duplicate.

DO NOT touch:
- shared/embeddings.py (Gemini-only by Phase 127 contract; smoke test depends on it).
- shared/llm.py (Gemini-only by Phase 127 contract; Tier 2 uses it).
- shared/config.py (`openrouter_api_key: SecretStr | None` already declared correctly from Phase 128).
- Any other [tier-N] extras (tier-1 stays as-is; tier-4/tier-5 stay as `["rag-architecture-patterns[shared]"]` stubs).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend [tier-3] extras with lightrag-hku==1.4.15, openai, pymupdf and verify dry-run resolution for both tiers</name>
  <files>
    ../rag-architecture-patterns/pyproject.toml
    ../rag-architecture-patterns/tier-2-managed/requirements.txt
    ../rag-architecture-patterns/tier-3-graph/requirements.txt
  </files>
  <action>
All work happens in `../rag-architecture-patterns/`. NOT in this profile repo.

1. Read `pyproject.toml` to confirm the current shape:
   - `tier-2 = ["rag-architecture-patterns[shared]"]` (stub)
   - `tier-3 = ["rag-architecture-patterns[shared]"]` (stub)
   - `[shared]` already has `google-genai>=1.73,<2` (Tier 2 inherits)

2. Replace ONLY the `tier-3 = [...]` line with the concrete dep list (verbatim from `<interfaces>` above):

   ```toml
   tier-3 = [
     "rag-architecture-patterns[shared]",
     "lightrag-hku==1.4.15",
     "openai>=1.50,<3",
     "pymupdf>=1.27,<2",
   ]
   ```

   LEAVE `tier-2 = ["rag-architecture-patterns[shared]"]` UNCHANGED — Tier 2 needs no new deps because `google-genai>=1.73` from `[shared]` already exposes `client.file_search_stores.*` (added in SDK v1.49). Document this in the commit message so future readers don't think it was forgotten.

   Do NOT modify any other tier extras (tier-1 stays as Phase 128 left it; tier-4/tier-5 stay as stubs). Do NOT touch [shared], [curation], [dependency-groups], [build-system], or [tool.*] sections.

3. Confirm `tier-2-managed/requirements.txt` and `tier-3-graph/requirements.txt` already contain `-e ..[tier-2]` / `-e ..[tier-3]` respectively (they should, from Phase 127). If they do, leave them untouched — the `-e ..[tier-N]` line picks up the new deps automatically. If for some reason either differs, restore to:

   ```
   # Tier 2 (Managed RAG / Gemini File Search) — mirrors [project.optional-dependencies.tier-2] from ../pyproject.toml.
   -e ..[tier-2]
   ```

   ```
   # Tier 3 (Graph RAG / LightRAG) — mirrors [project.optional-dependencies.tier-3] from ../pyproject.toml.
   -e ..[tier-3]
   ```

4. Verify each tier's pyproject parses and resolves cleanly using uv dry-run:

   ```
   cd ../rag-architecture-patterns
   uv pip install --dry-run -e ".[tier-2]" --python python3.11 2>&1 | tail -20
   uv pip install --dry-run -e ".[tier-3]" --python python3.11 2>&1 | tail -30
   ```

   - For tier-2: expect uv to print a resolved plan listing `google-genai`, `pydantic`, `rich`, `tiktoken`, `python-dotenv` (no new packages vs [shared]).
   - For tier-3: expect uv to resolve `lightrag-hku==1.4.15` exactly + `openai>=1.50` + `pymupdf>=1.27` + transitive deps (`networkx`, `nano-vectordb`, `tenacity`, `numpy`, etc.).

   If either errors with "no matching distribution" for any pin, INVESTIGATE — do NOT loosen blindly. Pins were verified against PyPI on 2026-04-26.

5. Re-run the lockfile-guard test to ensure `lightrag-hku`'s transitive deps do NOT pull the deprecated `google-generativeai` package (per Phase 127 lockfile guard):

   ```
   uv run pytest tests/test_tier_requirements.py -q
   ```

   The test enforces:
   - `[tier-2]` and `[tier-3]` extras exist in pyproject (PASS expected).
   - `tier-N/requirements.txt` references the parent (`-e ..[tier-N]`) (PASS expected).
   - `uv.lock` does NOT contain `google-generativeai` (research confirms LightRAG's pyproject does NOT depend on it; this should still PASS).

   If the test fails, halt and report — that's a research-validation gap that needs investigation before any code lands.

6. Stage and commit ONLY the changed files (per Phase 127 Pitfall 5 — never `git add -A`):

   ```
   git add pyproject.toml tier-2-managed/requirements.txt tier-3-graph/requirements.txt
   git commit -m "deps(129-01): extend [tier-3] extras (lightrag-hku==1.4.15, openai, pymupdf); [tier-2] needs no new deps"
   git push origin main
   ```

   (If the two `requirements.txt` files were not modified, drop them from `git add` — only commit what actually changed.)

Decisions referenced (129-RESEARCH.md): Standard Stack section; Pitfall 9 (exact LightRAG pin); Pitfall 11 (Tier 3 owns its own pymupdf to avoid cross-tier coupling).

DO NOT actually install Tier 3 deps in this task — Plans 02-05 will install when they need them. This task is config-only.
  </action>
  <verify>
    <automated>
cd ../rag-architecture-patterns &amp;&amp; \
  test -f pyproject.toml &amp;&amp; \
  grep -q 'lightrag-hku==1.4.15' pyproject.toml &amp;&amp; \
  grep -A4 'tier-3 = \[' pyproject.toml | grep -q 'openai&gt;=1.50' &amp;&amp; \
  grep -A4 'tier-3 = \[' pyproject.toml | grep -q 'pymupdf&gt;=1.27' &amp;&amp; \
  grep -A2 'tier-2 = \[' pyproject.toml | grep -q 'rag-architecture-patterns\[shared\]' &amp;&amp; \
  test -f tier-2-managed/requirements.txt &amp;&amp; \
  test -f tier-3-graph/requirements.txt &amp;&amp; \
  grep -q '\-e \.\.\[tier-2\]' tier-2-managed/requirements.txt &amp;&amp; \
  grep -q '\-e \.\.\[tier-3\]' tier-3-graph/requirements.txt &amp;&amp; \
  uv pip install --dry-run -e ".[tier-2]" --python python3.11 &gt; /dev/null 2&gt;&amp;1 &amp;&amp; \
  uv pip install --dry-run -e ".[tier-3]" --python python3.11 &gt; /dev/null 2&gt;&amp;1 &amp;&amp; \
  uv run pytest tests/test_tier_requirements.py -q &gt; /dev/null 2&gt;&amp;1
    </automated>
  </verify>
  <done>
    `pyproject.toml [tier-3]` lists lightrag-hku==1.4.15, openai>=1.50,<3, pymupdf>=1.27,<2.
    `pyproject.toml [tier-2]` unchanged (intentionally — google-genai is in [shared]).
    `uv pip install --dry-run -e ".[tier-2]"` and `uv pip install --dry-run -e ".[tier-3]"` both resolve.
    `tests/test_tier_requirements.py` passes (no `google-generativeai` in lockfile).
    Tier requirements.txt files unchanged (already mirror via `-e ..[tier-N]`).
    Commit pushed to origin/main.
  </done>
</task>

<task type="auto">
  <name>Task 2: Promote OPENROUTER_API_KEY in .env.example (Tier 1 + Tier 3) and add tier-2-managed/.store_id to .gitignore</name>
  <files>
    ../rag-architecture-patterns/.env.example
    ../rag-architecture-patterns/.gitignore
  </files>
  <action>
All work happens in `../rag-architecture-patterns/`.

1. Read the current `.env.example`. The OPENROUTER_API_KEY comment block currently reads (Phase 128 vintage):

   ```
   # REQUIRED for Tier 1+ (chat + embeddings via the OpenRouter unified gateway).
   # OpenRouter proxies OpenAI, Anthropic, Google, Meta, and others behind a single
   # OpenAI-compatible API — Tier 1 uses `openai/text-embedding-3-small` for the
   # vector index and a configurable chat model (default `google/gemini-2.5-flash`,
   # override at the CLI with `--model`). Pricing matches each underlying provider 1:1.
   #   Obtain at: https://openrouter.ai/keys
   #   Browse models at: https://openrouter.ai/models
   ```

2. Replace ONLY that comment block with the updated text that explicitly names Tier 3 (verbatim from `<interfaces>` above):

   ```
   # REQUIRED for Tier 1 (chat + embeddings) and Tier 3 (LightRAG entity-extraction + answer LLM + embeddings via the OpenRouter unified gateway).
   # OpenRouter proxies OpenAI, Anthropic, Google, Meta, and others behind a single
   # OpenAI-compatible API — Tier 1 uses `openai/text-embedding-3-small` for the
   # vector index and a configurable chat model (default `google/gemini-2.5-flash`,
   # override at the CLI with `--model`). Tier 3 uses the same defaults — the
   # graph-extraction LLM is also configurable via `--model`. Pricing matches each
   # underlying provider 1:1.
   #   Obtain at: https://openrouter.ai/keys
   #   Browse models at: https://openrouter.ai/models
   ```

3. Leave the empty-default key line `OPENROUTER_API_KEY=` UNCHANGED — placeholder filled in local (gitignored) `.env`. Do NOT inject a real key. Leave every other line (GEMINI_API_KEY block, OPENAI_API_KEY block, S2_API_KEY block, DEFAULT_*, DATASET_ROOT) UNCHANGED.

4. Update `.gitignore`. Find the existing block that contains `chroma_db/` and `lightrag_*/`. Append `tier-2-managed/.store_id` to that block (or right after it — placement is cosmetic). Final state should contain:

   ```
   chroma_db/
   lightrag_*/
   tier-2-managed/.store_id
   ```

   Do NOT add `lightrag_storage/` separately — the existing `lightrag_*/` glob already covers it (verified). If `tier-2-managed/.store_id` is somehow already in `.gitignore` (it shouldn't be), confirm and skip.

5. Update `tests/test_env_example.py` ONLY if it asserts on the literal "REQUIRED for Tier 1+" string. Read the file first:

   ```
   grep -n 'OPENROUTER_API_KEY\|REQUIRED for Tier' tests/test_env_example.py
   ```

   If a test asserts the old "REQUIRED for Tier 1+" line, update it to "REQUIRED for Tier 1 (chat + embeddings) and Tier 3" to match. If no such assertion exists (likely — test suite tends to assert key NAMES, not comment text), leave the file alone.

6. Run the env-example test to confirm:

   ```
   cd ../rag-architecture-patterns
   uv run pytest tests/test_env_example.py -q
   ```

   Should pass.

7. Stage and commit:

   ```
   git add .env.example .gitignore tests/test_env_example.py
   git commit -m "docs(129-01): promote OPENROUTER_API_KEY for Tier 3; gitignore tier-2-managed/.store_id"
   git push origin main
   ```

   (Drop `tests/test_env_example.py` from `git add` if not modified.)

Decisions referenced (129-RESEARCH.md "User Constraints"): Tier 3 uses OpenRouter for narrative continuity with Phase 128 pivot; Tier 2 uses Gemini direct (File Search is Gemini-native); Pattern 1 (store-id caching) requires `.store_id` to be gitignored.
  </action>
  <verify>
    <automated>
cd ../rag-architecture-patterns &amp;&amp; \
  test -f .env.example &amp;&amp; \
  test -f .gitignore &amp;&amp; \
  grep -q '^OPENROUTER_API_KEY=' .env.example &amp;&amp; \
  grep -q 'Tier 3' .env.example &amp;&amp; \
  grep -q 'LightRAG' .env.example &amp;&amp; \
  grep -q '^GEMINI_API_KEY=' .env.example &amp;&amp; \
  grep -q '^chroma_db/' .gitignore &amp;&amp; \
  grep -q '^lightrag_\*/' .gitignore &amp;&amp; \
  grep -q '^tier-2-managed/\.store_id' .gitignore &amp;&amp; \
  uv run pytest tests/test_env_example.py -q &gt; /dev/null 2&gt;&amp;1
    </automated>
  </verify>
  <done>
    .env.example OPENROUTER_API_KEY comment now names Tier 1 AND Tier 3 explicitly.
    .gitignore now ignores `tier-2-managed/.store_id` (in addition to existing `chroma_db/` and `lightrag_*/`).
    GEMINI_API_KEY block, OPENAI_API_KEY block, S2_API_KEY block, DEFAULT_*/DATASET_ROOT lines unchanged.
    `tests/test_env_example.py` passes.
    Commit pushed to origin/main.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run from inside `../rag-architecture-patterns/`:

```
# 1. Tier-3 extras lock in lightrag-hku, openai, pymupdf
grep -A4 'tier-3 = \[' pyproject.toml | grep -q 'lightrag-hku==1.4.15'
grep -A4 'tier-3 = \[' pyproject.toml | grep -q 'openai>=1.50'
grep -A4 'tier-3 = \[' pyproject.toml | grep -q 'pymupdf>=1.27'

# 2. Tier-2 extras unchanged (google-genai inherited from [shared])
grep -A2 'tier-2 = \[' pyproject.toml | head -3

# 3. Both tiers' dry-run resolves
uv pip install --dry-run -e ".[tier-2]" --python python3.11 > /dev/null
uv pip install --dry-run -e ".[tier-3]" --python python3.11 > /dev/null

# 4. .env.example mentions Tier 3
grep -q 'Tier 3' .env.example

# 5. .gitignore covers tier-2 store-id sidecar
grep -q '^tier-2-managed/\.store_id' .gitignore

# 6. shared/embeddings.py and shared/llm.py NOT touched
git log --oneline -1 shared/embeddings.py | grep -v '129-'
git log --oneline -1 shared/llm.py | grep -v '129-'

# 7. lockfile guard + env tests still pass
uv run pytest tests/test_tier_requirements.py tests/test_env_example.py tests/test_pricing.py -q
```
</verification>

<success_criteria>
- `pyproject.toml [tier-3]` lists `lightrag-hku==1.4.15`, `openai>=1.50,<3`, `pymupdf>=1.27,<2`
- `pyproject.toml [tier-2]` unchanged (google-genai>=1.73 from [shared] is sufficient)
- `uv pip install --dry-run -e ".[tier-2]"` and `uv pip install --dry-run -e ".[tier-3]"` both resolve cleanly
- `tests/test_tier_requirements.py` passes (lockfile guard against `google-generativeai`)
- `.env.example` documents OPENROUTER_API_KEY as REQUIRED for Tier 1 + Tier 3 (comment text)
- `.gitignore` ignores `tier-2-managed/.store_id`
- `shared/embeddings.py` and `shared/llm.py` are NOT modified (Phase 127 contract intact)
- All changes committed and pushed to origin/main with atomic commits
</success_criteria>

<output>
After completion, create `.planning/phases/129-tiers-2-3-managed-graph-rag/129-01-SUMMARY.md` documenting:
- Final `[tier-2]` and `[tier-3]` extras lists (verbatim)
- `uv pip install --dry-run` resolution output for both tiers (last 10 lines each)
- Confirmation that lockfile guard test passes (`uv.lock` has no `google-generativeai`)
- Confirmation that shared/llm.py and shared/embeddings.py untouched (`git log --oneline -1`)
- Any pin adjustments needed (none expected — research verified all pins on 2026-04-26)
</output>
