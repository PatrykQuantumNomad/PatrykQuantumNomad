---
phase: 89-seo-og-images-and-site-integration
plan: 03
type: execute
wave: 2
depends_on:
  - 89-01
files_modified:
  - src/data/blog/fastapi-production-guide.mdx
  - src/layouts/GuideLayout.astro
autonomous: true
requirements:
  - SITE-03

must_haves:
  truths:
    - "A companion blog post exists at /blog/fastapi-production-guide/"
    - "The blog post links to all 11 guide chapter pages"
    - "Every guide chapter page links back to the companion blog post"
    - "The blog post renders correctly with proper frontmatter, tags, and description"
  artifacts:
    - path: "src/data/blog/fastapi-production-guide.mdx"
      provides: "Companion blog post with links to all 11 chapters"
      contains: "guides/fastapi-production"
    - path: "src/layouts/GuideLayout.astro"
      provides: "Back-link to companion blog post on all chapter pages"
      contains: "blog/fastapi-production-guide"
  key_links:
    - from: "src/data/blog/fastapi-production-guide.mdx"
      to: "/guides/fastapi-production/*"
      via: "markdown links to all 11 chapter URLs"
      pattern: "guides/fastapi-production/"
    - from: "src/layouts/GuideLayout.astro"
      to: "/blog/fastapi-production-guide/"
      via: "anchor tag in layout"
      pattern: "blog/fastapi-production-guide"
---

<objective>
Author a companion blog post for the FastAPI Production Guide and establish bidirectional cross-links between the blog post and all 11 guide chapter pages.

Purpose: The blog post serves as a narrative entry point and SEO asset that introduces the guide to blog readers. Bidirectional links strengthen the internal link graph and ensure readers can navigate between the blog overview and the detailed guide chapters in either direction.

Output: One MDX blog post file and a back-link addition to GuideLayout.astro.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-seo-og-images-and-site-integration/89-RESEARCH.md
@.planning/phases/89-seo-og-images-and-site-integration/89-01-SUMMARY.md
@src/layouts/GuideLayout.astro
@src/data/blog/fastapi-production-guide.mdx
@src/lib/guides/routes.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/layouts/GuideLayout.astro (after Plan 01 modifications):
```typescript
// Plan 01 added GuideJsonLd and BreadcrumbJsonLd imports and rendering
// Plan 03 adds a back-link to the companion blog post
interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  ogImageAlt?: string;
  guideTitle: string;
  guideSlug: string;
  chapters: Array<{ slug: string; title: string }>;
  currentSlug: string;
  chapterTitle: string;
}
```

Blog post frontmatter schema (from src/content.config.ts):
```typescript
{
  title: string;
  description: string;
  publishedDate: Date;     // z.coerce.date()
  tags: string[];
  draft: boolean;
  externalUrl?: string;    // omit for local posts
  source?: string;         // omit for local posts
  coverImage?: string;     // optional
}
```

Guide chapter slugs (from guide.json chapters array):
```
builder-pattern, middleware, authentication, observability, database,
docker, testing, health-checks, security-headers, rate-limiting, caching
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author companion blog post with links to all 11 guide chapters</name>
  <files>src/data/blog/fastapi-production-guide.mdx</files>
  <action>
Create `src/data/blog/fastapi-production-guide.mdx` as a standard blog post MDX file.

**Frontmatter:**
```yaml
---
title: "FastAPI Production Guide: What Your AI Agent Inherits"
description: "A companion overview of the FastAPI Production Guide covering 11 production concerns -- middleware, authentication, observability, database, Docker, testing, health checks, security headers, rate limiting, and caching."
publishedDate: 2026-03-08
tags: ["fastapi", "python", "production", "kubernetes", "docker", "ai-agent", "devops"]
draft: false
---
```

**Content structure (approximately 300-400 lines of MDX):**

1. **Opening section** -- Frame the narrative: "When an AI agent generates a FastAPI endpoint, what production infrastructure is already in place?" Introduce the guide as a walkthrough of the fastapi-template repository.

2. **Why a Production Guide?** -- Brief explanation of why production FastAPI is more than `uvicorn main:app`. Reference the AI agent angle: the template handles 11 production concerns so agents write business logic, not boilerplate.

3. **The 11 Production Concerns** -- One subsection per chapter, each with:
   - A heading (## Middleware, ## Authentication, etc.)
   - 2-3 sentences summarizing what the chapter covers
   - A link to the guide chapter page: `[Read the full Middleware chapter](/guides/fastapi-production/middleware/)`
   - Use ALL 11 chapter slugs: builder-pattern, middleware, authentication, observability, database, docker, testing, health-checks, security-headers, rate-limiting, caching

4. **How to Use This Guide** -- Brief section on reading order (start with Builder Pattern to understand the factory, then any chapter of interest). Link back to the landing page: `[FastAPI Production Guide](/guides/fastapi-production/)`.

5. **Closing** -- "The goal is simple: your AI agent inherits production-grade infrastructure. This guide shows you exactly what that means."

**Important:** Use standard markdown links (not MDX components) for the chapter links. Do NOT import any components -- keep it pure MDX prose with standard markdown syntax. Use existing blog conventions (no explicit imports needed, the blog layout handles everything).

**Tone:** Professional, concise, informative. Match the existing blog tone from other companion posts (Dockerfile Best Practices, Database Compass methodology). First person where appropriate.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npm run build 2>&1 | tail -10 && echo "--- Blog post exists ---" && test -f dist/blog/fastapi-production-guide/index.html && echo "YES" && echo "--- Chapter links ---" && grep -c "guides/fastapi-production/" dist/blog/fastapi-production-guide/index.html</automated>
  </verify>
  <done>Blog post builds at /blog/fastapi-production-guide/ with valid frontmatter. Contains links to all 11 guide chapter pages plus the landing page. Renders correctly with reading time, tags, and TOC.</done>
</task>

<task type="auto">
  <name>Task 2: Add companion blog post back-link to GuideLayout</name>
  <files>src/layouts/GuideLayout.astro</files>
  <action>
Modify `src/layouts/GuideLayout.astro` to add a back-link to the companion blog post. This fulfills the "bidirectional" requirement of SITE-03 without editing 11 individual MDX files.

Add a small callout/link ABOVE the GuideChapterNav component (before the prev/next navigation) inside the main content div:

```astro
<!-- Companion blog post back-link -->
<aside class="mt-10 pt-6 border-t border-[var(--color-border)]">
  <p class="text-sm text-[var(--color-text-secondary)]">
    For a high-level overview of all 11 production concerns, read the companion blog post:
    <a href="/blog/fastapi-production-guide/" class="text-[var(--color-accent)] hover:underline font-medium">
      FastAPI Production Guide: What Your AI Agent Inherits
    </a>
  </p>
</aside>
```

Place this AFTER the `<slot />` (main content) and BEFORE the `<GuideChapterNav>` component. This ensures the back-link appears at the bottom of every chapter page's content without modifying any MDX files.

Do NOT add this to the landing page (index.astro) -- only chapter pages use GuideLayout, and the landing page uses Layout directly.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npm run build 2>&1 | tail -5 && echo "--- Back-link on chapter pages ---" && grep -c "blog/fastapi-production-guide" dist/guides/fastapi-production/builder-pattern/index.html && grep -c "blog/fastapi-production-guide" dist/guides/fastapi-production/authentication/index.html && grep -c "blog/fastapi-production-guide" dist/guides/fastapi-production/caching/index.html</automated>
  </verify>
  <done>All 11 guide chapter pages contain a back-link to /blog/fastapi-production-guide/. The link appears between the chapter content and the prev/next navigation. Landing page is unaffected. Full build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. Blog post exists at dist/blog/fastapi-production-guide/index.html
3. Blog post contains links to all 11 chapter pages: `grep -c "guides/fastapi-production/" dist/blog/fastapi-production-guide/index.html` returns 12+ (11 chapters + landing)
4. All chapter pages link back to blog: `grep -c "blog/fastapi-production-guide" dist/guides/fastapi-production/middleware/index.html` returns 1+
5. Verify bidirectional: blog -> chapters AND chapters -> blog both have live links
</verification>

<success_criteria>
- Companion blog post renders at /blog/fastapi-production-guide/ with correct title, description, tags, and publishedDate
- Blog post contains markdown links to all 11 guide chapter pages and the landing page
- All 11 guide chapter pages contain a back-link to the companion blog post via GuideLayout
- Blog post appears in blog listing, RSS feed, and sitemap automatically (existing infrastructure handles this)
- Full production build passes
</success_criteria>

<output>
After completion, create `.planning/phases/89-seo-og-images-and-site-integration/89-03-SUMMARY.md`
</output>
