---
phase: 89-seo-og-images-and-site-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Header.astro
  - src/pages/guides/index.astro
  - src/pages/index.astro
  - src/pages/llms.txt.ts
  - src/pages/llms-full.txt.ts
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-06

must_haves:
  truths:
    - "A 'Guides' link appears in the site header navigation and points to /guides/"
    - "A /guides/ index page exists listing the FastAPI Production Guide"
    - "A callout card on the homepage links to the FastAPI Production Guide"
    - "LLMs.txt includes a FastAPI Production Guide section with all 11 chapter links"
    - "LLMs-full.txt includes detailed guide content"
  artifacts:
    - path: "src/components/Header.astro"
      provides: "10th nav item for Guides"
      contains: "Guides"
    - path: "src/pages/guides/index.astro"
      provides: "Guides hub page listing available guides"
    - path: "src/pages/index.astro"
      provides: "Homepage with FastAPI guide callout card"
      contains: "fastapi-production"
    - path: "src/pages/llms.txt.ts"
      provides: "LLMs.txt with guide section"
      contains: "FastAPI Production Guide"
    - path: "src/pages/llms-full.txt.ts"
      provides: "LLMs-full.txt with guide section"
      contains: "FastAPI Production Guide"
  key_links:
    - from: "src/components/Header.astro"
      to: "/guides/"
      via: "navLinks array entry"
      pattern: "href.*guides"
    - from: "src/pages/index.astro"
      to: "/guides/fastapi-production/"
      via: "anchor tag in card"
      pattern: "guides/fastapi-production"
    - from: "src/pages/llms.txt.ts"
      to: "guidePages collection"
      via: "getCollection('guidePages')"
      pattern: "getCollection.*guidePages"
---

<objective>
Add the "Guides" header nav link with a proper /guides/ hub page, create a homepage callout card for the FastAPI Production Guide, and add guide entries to both LLMs.txt endpoints.

Purpose: These site integration touchpoints make the guide discoverable from every page (header nav), from the homepage (callout card), and from LLM crawlers (LLMs.txt). Without them the guide exists but is isolated.

Output: Header nav updated, /guides/index.astro created, homepage card added, llms.txt and llms-full.txt updated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-seo-og-images-and-site-integration/89-RESEARCH.md
@src/components/Header.astro
@src/pages/index.astro
@src/pages/llms.txt.ts
@src/pages/llms-full.txt.ts
@src/lib/guides/routes.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/components/Header.astro (current navLinks):
```typescript
const navLinks = [
  { href: '/', label: 'Home' },
  { href: '/blog/', label: 'Blog' },
  { href: '/beauty-index/', label: 'Beauty Index' },
  { href: '/db-compass/', label: 'DB Compass' },
  { href: '/eda/', label: 'EDA' },
  { href: '/tools/', label: 'Tools' },
  { href: '/projects/', label: 'Projects' },
  { href: '/about/', label: 'About' },
  { href: '/contact/', label: 'Contact' },
];
```

From src/pages/index.astro (Reference Guides section, lines 161-227):
```astro
<!-- 3-column grid with Beauty Index, DB Compass, EDA cards -->
<section class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
  <h2 class="text-2xl sm:text-3xl font-heading font-bold mb-8" data-word-reveal>Reference Guides</h2>
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
    <!-- 3 existing cards -->
  </div>
</section>
```

From src/lib/guides/routes.ts:
```typescript
export const GUIDE_ROUTES = { landing: '/guides/fastapi-production/', guides: '/guides/' } as const;
export function guidePageUrl(guideSlug: string, chapterSlug: string): string;
```

From src/pages/llms.txt.ts (EDA section pattern, lines 145-190):
```typescript
'## EDA Visual Encyclopedia',
'',
'Interactive visual encyclopedia...',
'',
'- [EDA Visual Encyclopedia](https://patrykgolabek.dev/eda/): Landing page...',
'',
...edaTechniques.sort(...).map(t => `- [${t.data.title}](url): ${t.data.description}`),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Guides header nav link, create /guides/ hub page, and add homepage callout card</name>
  <files>src/components/Header.astro, src/pages/guides/index.astro, src/pages/index.astro</files>
  <action>
1. Modify `src/components/Header.astro`:
   - Add `{ href: '/guides/', label: 'Guides' }` to the `navLinks` array AFTER the EDA entry (position 6, between EDA and Tools). This keeps content-oriented items grouped together.
   - The array becomes 10 items. The existing desktop nav uses `gap-6` in a `max-w-6xl` container. If layout feels tight at `md` breakpoint, reduce to `gap-4` or `gap-5` -- but do this only if needed after visual inspection. The mobile menu uses full-width block links and handles additional items gracefully.

2. Create `src/pages/guides/index.astro` as a minimal hub page:
   - Import `Layout` from `../../layouts/Layout.astro`.
   - Import `getCollection` from `astro:content`.
   - Load guide metadata: `const [guideMeta] = await getCollection('guides');`
   - Use Layout with title "Guides | Patryk Golabek" and description "Production-grade technical guides covering FastAPI, Kubernetes, and cloud-native architecture."
   - Content: A heading "Guides", a brief intro paragraph ("In-depth production guides that walk through real-world configurations and battle-tested patterns."), and a single card linking to `/guides/fastapi-production/` showing the guide title, description, and "11 chapters" badge.
   - Style the card matching the homepage Reference Guides section pattern (card-hover, rounded-lg, border, etc.).
   - This page future-proofs for additional guides without being over-engineered.

3. Modify `src/pages/index.astro`:
   - In the "Reference Guides" section, add a 4th card linking to `/guides/fastapi-production/` AFTER the EDA card.
   - The card structure should match existing cards but without an image/SVG in the header area (use a simple colored gradient or accent background instead, similar to how other sections handle text-only cards).
   - Card content:
     - Meta line: `<p class="meta-mono text-[var(--color-accent)]">Production Guide</p>`
     - Title: `<h3>FastAPI Production Guide</h3>`
     - Description: "11 chapters covering middleware, authentication, observability, database, Docker, testing, and more. Everything your AI agent inherits."
     - CTA: `<span>Read the guide &rarr;</span>`
   - The grid currently uses `lg:grid-cols-3`. With 4 cards, change to `lg:grid-cols-4` or keep `lg:grid-cols-3` (which naturally wraps 4th card to next row as 3+1). Per research recommendation, keep `lg:grid-cols-3` -- the 4th card wraps naturally and the `sm:grid-cols-2` handles 2+2 on smaller screens.
   - Add `data-reveal` and `data-tilt` attributes to match existing cards.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npm run build 2>&1 | tail -5 && echo "--- Nav check ---" && grep -c "Guides" dist/index.html && echo "--- Guides hub ---" && test -f dist/guides/index.html && echo "guides/index.html exists" && echo "--- Homepage card ---" && grep -c "fastapi-production" dist/index.html</automated>
  </verify>
  <done>"Guides" appears in header nav on all pages. /guides/ hub page renders with FastAPI Production Guide card. Homepage has a 4th card in Reference Guides section linking to /guides/fastapi-production/. Full build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add FastAPI Production Guide section to LLMs.txt and LLMs-full.txt</name>
  <files>src/pages/llms.txt.ts, src/pages/llms-full.txt.ts</files>
  <action>
1. Modify `src/pages/llms.txt.ts`:
   - Add imports: `import { guidePageUrl } from '../lib/guides/routes';` at the top with existing imports.
   - Add collection fetch: `const [guideMeta] = await getCollection('guides');` and `const guidePages = await getCollection('guidePages');` alongside existing collection fetches.
   - Add a new section AFTER the EDA Visual Encyclopedia section and BEFORE the citation section. Insert these lines into the `lines` array:
     ```
     '## FastAPI Production Guide',
     '',
     'Production-ready FastAPI template guide covering 11 chapters on middleware, authentication, observability, database, Docker, testing, health checks, security headers, rate limiting, and caching.',
     '',
     `- [FastAPI Production Guide](https://patrykgolabek.dev/guides/fastapi-production/): ${guideMeta.data.description}`,
     '',
     ...guidePages
       .sort((a, b) => a.data.order - b.data.order)
       .map(p => `- [${p.data.title}](https://patrykgolabek.dev${guidePageUrl(guideMeta.data.slug, p.data.slug)}): ${p.data.description}`),
     '',
     ```
   - Also add a citation example at the bottom: `'Example: "The FastAPI Production Guide by Patryk Golabek (patrykgolabek.dev/guides/fastapi-production/) covers 11 production concerns for the FastAPI template."'`
   - Update the "All ... data is licensed" line to include guide content: append "FastAPI Production Guide," to the list.

2. Modify `src/pages/llms-full.txt.ts`:
   - Add the same imports and collection fetches as llms.txt.ts.
   - Add a guide section with more detail than the slim version. Include the same chapter listing with descriptions, plus a brief summary of what the guide covers.
   - Follow the existing pattern in llms-full.txt.ts where sections have more verbose descriptions than their llms.txt counterparts.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npm run build 2>&1 | tail -5 && echo "--- llms.txt check ---" && grep -c "FastAPI Production Guide" dist/llms.txt && echo "--- llms-full.txt check ---" && grep -c "FastAPI Production Guide" dist/llms-full.txt && echo "--- chapter links ---" && grep -c "guides/fastapi-production/" dist/llms.txt</automated>
  </verify>
  <done>llms.txt contains FastAPI Production Guide section with landing page link and all 11 chapter links sorted by order. llms-full.txt contains the same plus additional context. Citation example added. Both files build successfully.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. Header nav shows "Guides" link on all pages: `grep "Guides" dist/index.html`
3. /guides/ hub page exists: `test -f dist/guides/index.html`
4. Homepage has FastAPI guide card: `grep "fastapi-production" dist/index.html`
5. LLMs.txt has guide section: `grep "FastAPI Production Guide" dist/llms.txt`
6. LLMs-full.txt has guide section: `grep "FastAPI Production Guide" dist/llms-full.txt`
7. LLMs.txt has all 11 chapter links: `grep -c "guides/fastapi-production/" dist/llms.txt` returns 12+ (landing + 11 chapters)
</verification>

<success_criteria>
- "Guides" link in header nav between "EDA" and "Tools" on every page
- /guides/ hub page renders with card linking to FastAPI Production Guide
- Homepage Reference Guides section has 4th card for FastAPI Production Guide
- llms.txt includes guide section with landing + 11 chapter links
- llms-full.txt includes guide section with additional context
- Full production build passes
</success_criteria>

<output>
After completion, create `.planning/phases/89-seo-og-images-and-site-integration/89-02-SUMMARY.md`
</output>
