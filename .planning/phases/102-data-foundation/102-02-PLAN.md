---
phase: 102-data-foundation
plan: 02
type: execute
wave: 2
depends_on: ["102-01"]
files_modified:
  - src/data/ai-landscape/nodes.json
autonomous: true

must_haves:
  truths:
    - "~26 concept nodes exist in nodes.json covering AI subfields, ML types, NN, and DL clusters"
    - "Every node has a simple description (100-150 words, plain English, no jargon)"
    - "Every node has a technical description (100-150 words, Wikipedia-level, accurate terminology)"
    - "Every node has a whyItMatters sentence connecting to real-world impact"
    - "Parent nodes include examples arrays with grouped products/frameworks/techniques"
    - "All nodes pass Zod schema validation"
  artifacts:
    - path: "src/data/ai-landscape/nodes.json"
      provides: "First ~26 concept nodes (AI, ML, NN, DL clusters)"
      contains: "artificial-intelligence"
  key_links:
    - from: "src/data/ai-landscape/nodes.json"
      to: "src/lib/ai-landscape/schema.ts"
      via: "Zod validation at build time"
      pattern: "aiNodeSchema"
    - from: "src/data/ai-landscape/nodes.json"
      to: "src/data/ai-landscape/graph.json"
      via: "Node slugs match edge source/target values"
      pattern: "artificial-intelligence"
---

<objective>
Author the first batch of AI landscape concept nodes (~26 nodes) covering the AI, ML, NN, and DL clusters with two-tier educational content, whyItMatters statements, and grouped examples.

Purpose: This is the first of two content authoring plans. These clusters represent the foundational hierarchy of AI concepts (AI > ML > NN > DL) plus their subfields and architectures. Writing by cluster ensures consistent depth and tone within each conceptual group.

Output: nodes.json populated with ~26 validated concept nodes
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-data-foundation/102-CONTEXT.md
@.planning/phases/102-data-foundation/102-RESEARCH.md
@.planning/phases/102-data-foundation/102-01-SUMMARY.md
@src/lib/ai-landscape/schema.ts
@src/data/ai-landscape/graph.json
@public/images/ai_landscape.dot
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author AI and ML cluster nodes (~16 nodes)</name>
  <files>src/data/ai-landscape/nodes.json</files>
  <action>
Replace the empty array stub in `src/data/ai-landscape/nodes.json` with concept node data. This task covers the **AI cluster** (10 nodes) and **ML cluster** (6 nodes).

**Content guidelines (LOCKED per CONTEXT.md):**
- **Simple tier:** Informed layperson level. Plain English, assumes basic tech literacy but no AI expertise. 100-150 words. NO jargon -- if you must use a technical term, define it inline.
- **Technical tier:** Wikipedia-level. Accurate terminology, brief history, key characteristics. Assumes some CS knowledge. 100-150 words.
- **whyItMatters:** 1-2 sentences connecting abstract concept to real-world impact using recognizable examples (ChatGPT, self-driving cars, Netflix recommendations, Siri, Google Translate, etc.).

**AI Cluster nodes (10):**

1. `artificial-intelligence` -- AI_Node. Root node, cluster "ai", parentId null. No examples.
2. `natural-language-processing` -- NLP. Cluster "ai", parentId "artificial-intelligence". No examples.
3. `computer-vision` -- CV. Cluster "ai", parentId "artificial-intelligence". No examples.
4. `robotics` -- Robotics. Cluster "ai", parentId "artificial-intelligence". No examples.
5. `expert-systems` -- ExpertSystems. Cluster "ai", parentId "artificial-intelligence". No examples.
6. `planning-scheduling` -- PlanningScheduling. Cluster "ai", parentId "artificial-intelligence". No examples.
7. `knowledge-representation` -- KnowledgeRep. Cluster "ai", parentId "artificial-intelligence". No examples.
8. `speech-recognition` -- SpeechRec. Cluster "ai", parentId "artificial-intelligence". No examples.
9. `search-optimization` -- SearchOptimization. Cluster "ai", parentId "artificial-intelligence". No examples.
10. `ai-safety-ethics` -- AI_Ethics. Cluster "ai", parentId "artificial-intelligence". No examples.

**ML Cluster nodes (6):**

11. `machine-learning` -- ML_Node. Cluster "ml", parentId "artificial-intelligence". No examples.
12. `supervised-learning` -- Supervised. Cluster "ml", parentId "machine-learning". Examples: Decision Trees ("Classification/regression via branching rules"), SVM ("Finds optimal boundary between data classes"), Linear Regression ("Predicts continuous values from input features"), Logistic Regression ("Predicts probability of binary outcomes"), Ensemble Methods ("Combines multiple models for better accuracy -- e.g., Random Forest, XGBoost").
13. `unsupervised-learning` -- Unsupervised. Cluster "ml", parentId "machine-learning". Examples: K-Means Clustering ("Groups data into clusters by similarity"), PCA ("Reduces data dimensions while preserving key patterns").
14. `reinforcement-learning` -- Reinforcement. Cluster "ml", parentId "machine-learning". No examples.
15. `semi-supervised-learning` -- SemiSupervised. Cluster "ml", parentId "machine-learning". No examples.
16. `self-supervised-learning` -- SelfSupervised. Cluster "ml", parentId "unsupervised-learning". No examples.

**For each node, set:**
- `id` and `slug` to the same kebab-case value (listed above)
- `name` to the display name from the DOT file label
- `cluster` to the cluster id
- `parentId` as specified
- `dotNodeId` to the original DOT identifier
- `simpleDescription`, `technicalDescription`, `whyItMatters` -- author fresh content per the guidelines
- `examples` -- populated array for supervised-learning and unsupervised-learning, empty array `[]` for others

**Tone check:** After writing each cluster's nodes, re-read all simple descriptions together to verify consistent depth. The simple tier should feel like explaining AI to a curious friend who works in marketing.
  </action>
  <verify>Run `npx vitest run src/lib/ai-landscape/__tests__/schema.test.ts` and `node -e "const n = require('./src/data/ai-landscape/nodes.json'); console.log('nodes:', n.length); n.forEach(x => { const sw = x.simpleDescription.split(/\s+/).length; const tw = x.technicalDescription.split(/\s+/).length; if(sw < 80 || tw < 80) console.log('SHORT:', x.id, 'simple:', sw, 'tech:', tw); });"` -- all 16 nodes present, no short descriptions.</verify>
  <done>16 concept nodes for AI and ML clusters exist in nodes.json with two-tier content, whyItMatters, and grouped examples where applicable. All pass Zod validation.</done>
</task>

<task type="auto">
  <name>Task 2: Author NN and DL cluster nodes (~10 nodes)</name>
  <files>src/data/ai-landscape/nodes.json</files>
  <action>
Append concept nodes for the **NN cluster** (1 node) and **DL cluster** (10 nodes) to the existing nodes.json array.

**NN Cluster (1 node):**

17. `neural-networks` -- NN_Node. Cluster "nn", parentId "machine-learning". Examples: Perceptron ("Simplest neural network -- single layer, binary classification"), MLP ("Multiple layers of connected neurons -- the classic 'deep' architecture"), Backpropagation ("The algorithm that trains neural networks by propagating errors backward"), Activation Functions ("Mathematical gates (ReLU, sigmoid, tanh) that control neuron output"), SOM ("Self-Organizing Maps -- unsupervised networks that create low-dimensional representations").

**DL Cluster (10 nodes):**

18. `deep-learning` -- DL_Node. Cluster "dl", parentId "neural-networks". Examples: DBN ("Deep Belief Networks -- layered probabilistic models, historically important for pre-training"), Boltzmann Machines ("Energy-based networks that learn probability distributions over inputs").
19. `convolutional-neural-networks` -- CNN. Cluster "dl", parentId "deep-learning". No examples.
20. `recurrent-neural-networks` -- RNN. Cluster "dl", parentId "deep-learning". Examples: LSTM ("Long Short-Term Memory -- solves vanishing gradient with gated memory cells"), GRU ("Gated Recurrent Unit -- streamlined LSTM alternative with fewer parameters").
21. `transformers` -- Transformers. Cluster "dl", parentId "deep-learning". No examples.
22. `autoencoders` -- Autoencoders. Cluster "dl", parentId "deep-learning". No examples.
23. `foundation-models` -- FoundationModels. Cluster "dl", parentId "deep-learning". Examples: BERT ("Bidirectional Encoder Representations from Transformers -- pioneered pre-training for NLP tasks").
24. `deep-reinforcement-learning` -- DeepRL. Cluster "dl", parentId "deep-learning". No examples.
25. `mixture-of-experts` -- MoE. Cluster "dl", parentId "deep-learning". No examples.
26. `multimodal-ai` -- Multimodal. Cluster "dl", parentId "deep-learning". No examples.
27. `reasoning-models` -- ReasoningModels. Cluster "dl", parentId "deep-learning". Examples: o1/o3 ("OpenAI's chain-of-thought reasoners that 'think' before answering"), DeepSeek R1 ("Open-source reasoning model matching proprietary performance"), Extended Thinking ("Anthropic's approach giving Claude extended internal reasoning time").

**Same content guidelines as Task 1.** Author simpleDescription, technicalDescription, and whyItMatters for all nodes.

**Consistency check:** Re-read all DL architecture descriptions together. Transformers, CNN, RNN, and Autoencoders should be at a comparable depth. The technical tier should distinguish them by architecture (attention vs. convolution vs. recurrence vs. encoder-decoder).
  </action>
  <verify>Run `node -e "const n = require('./src/data/ai-landscape/nodes.json'); console.log('nodes:', n.length); const clusters = {}; n.forEach(x => { clusters[x.cluster] = (clusters[x.cluster]||0)+1; }); console.log(clusters);"` -- confirms ~26 total nodes across ai, ml, nn, dl clusters.</verify>
  <done>~26 concept nodes exist in nodes.json covering AI, ML, NN, and DL clusters. All have two-tier content at 100-150 words each, whyItMatters statements, and grouped examples. All pass Zod validation.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/ai-landscape/__tests__/schema.test.ts` -- all schema tests still pass
2. `node -e "const n = require('./src/data/ai-landscape/nodes.json'); console.log('Total nodes:', n.length); const ids = n.map(x=>x.id); const dupes = ids.filter((id,i) => ids.indexOf(id) !== i); if(dupes.length) console.log('DUPES:', dupes); else console.log('No duplicates');"` -- ~26 nodes, no duplicates
3. Word count spot check: `node -e "const n = require('./src/data/ai-landscape/nodes.json'); n.slice(0,5).forEach(x => console.log(x.id, 'simple:', x.simpleDescription.split(/\s+/).length, 'tech:', x.technicalDescription.split(/\s+/).length));"` -- each tier 80-170 words
4. `npx astro build 2>&1 | tail -5` -- build succeeds with ~26 aiLandscape entries
</verification>

<success_criteria>
- nodes.json contains ~26 concept nodes for AI, ML, NN, DL clusters
- Every node has simpleDescription of 100-150 words in plain English
- Every node has technicalDescription of 100-150 words with accurate terminology
- Every node has a whyItMatters sentence
- Parent nodes (supervised-learning, unsupervised-learning, neural-networks, deep-learning, recurrent-neural-networks, foundation-models, reasoning-models) have populated examples arrays
- All nodes pass Zod validation
- Astro build succeeds with aiLandscape collection returning ~26 entries
</success_criteria>

<output>
After completion, create `.planning/phases/102-data-foundation/102-02-SUMMARY.md`
</output>
