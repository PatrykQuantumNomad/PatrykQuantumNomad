---
phase: 91-svg-diagram-generators
plan: 03
type: execute
wave: 2
depends_on: ["91-01"]
files_modified:
  - src/lib/guides/svg-diagrams/agent-teams.ts
  - src/lib/guides/svg-diagrams/index.ts
  - src/lib/guides/svg-diagrams/__tests__/agent-teams.test.ts
  - src/components/guide/AgentTeamsDiagram.astro
autonomous: true
requirements:
  - DIAG-05

must_haves:
  truths:
    - "The Agent Teams diagram shows the lead agent, teammates, shared task list, and mailbox communication system"
    - "The diagram clearly indicates experimental/research preview status via visual treatment and text label"
    - "The diagram shows task states (pending, in progress, completed) in the shared task list"
    - "The diagram renders correctly in dark and light themes without hardcoded hex colors"
    - "The diagram uses a unique marker ID prefix (team-arrow) to avoid SVG defs collisions"
  artifacts:
    - path: "src/lib/guides/svg-diagrams/agent-teams.ts"
      provides: "DIAG-05 generator function"
      exports: ["generateAgentTeams"]
    - path: "src/lib/guides/svg-diagrams/__tests__/agent-teams.test.ts"
      provides: "Unit tests for DIAG-05"
      contains: "Team Lead"
    - path: "src/components/guide/AgentTeamsDiagram.astro"
      provides: "Astro wrapper for DIAG-05"
      contains: "PlotFigure"
  key_links:
    - from: "src/lib/guides/svg-diagrams/agent-teams.ts"
      to: "src/lib/guides/svg-diagrams/diagram-base.ts"
      via: "import diamondNode, groupBox, DIAGRAM_PALETTE, diagramSvgOpen, roundedRect, arrowLine, arrowMarkerDef, textLabel"
      pattern: "import.*from.*diagram-base"
    - from: "src/components/guide/AgentTeamsDiagram.astro"
      to: "src/lib/guides/svg-diagrams/index.ts"
      via: "import generateAgentTeams from barrel"
      pattern: "import.*generateAgentTeams.*from.*svg-diagrams"
---

<objective>
Create the Agent Teams architecture diagram and run full regression verification for all Phase 91 diagrams.

Purpose: The Agent Teams diagram (DIAG-05) visualizes the experimental multi-agent orchestration feature -- team lead, teammates, shared task list, and mailbox messaging. It must prominently convey the "research preview" status since readers should not treat it as a stable production feature. This is the final diagram in Phase 91, so the plan also includes full regression to ensure all 5 diagrams and existing FastAPI diagrams work together.

Output: 1 SVG generator function, 1 vitest test file, 1 Astro wrapper component, updated barrel exports, full test suite green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-svg-diagram-generators/91-RESEARCH.md
@.planning/phases/91-svg-diagram-generators/91-01-SUMMARY.md

@src/lib/guides/svg-diagrams/diagram-base.ts
@src/lib/guides/svg-diagrams/index.ts
@src/lib/guides/svg-diagrams/jwt-auth-flow.ts
@src/lib/guides/svg-diagrams/__tests__/jwt-auth-flow.test.ts
@src/components/guide/JwtAuthFlowDiagram.astro

<interfaces>
<!-- diagram-base.ts after Plan 01 extensions -->
From src/lib/guides/svg-diagrams/diagram-base.ts (after Plan 01):
```typescript
export const DIAGRAM_PALETTE: { text, textSecondary, accent, accentSecondary, border, surface, surfaceAlt };
export interface DiagramConfig { width: number; height: number; fontFamily?: string; }
export function diagramSvgOpen(config: DiagramConfig, ariaLabel: string): string;
export function roundedRect(x, y, w, h, opts?): string;
export function arrowLine(x1, y1, x2, y2, markerId): string;
export function arrowMarkerDef(id: string): string;
export function textLabel(x, y, text, opts?): string;
export function curvedPath(x1, y1, cx, cy, x2, y2, markerId?): string;
export function diamondNode(cx, cy, size, opts?): string;
export function groupBox(x, y, w, h, title, opts?): string;
```

Astro wrapper pattern:
```astro
---
import PlotFigure from '../eda/PlotFigure.astro';
import { generateJwtAuthFlow } from '../../lib/guides/svg-diagrams';
const svg = generateJwtAuthFlow();
---
<PlotFigure svg={svg} caption="..." maxWidth="720px" />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Agent Teams architecture diagram (DIAG-05)</name>
  <files>
    src/lib/guides/svg-diagrams/agent-teams.ts,
    src/lib/guides/svg-diagrams/__tests__/agent-teams.test.ts,
    src/components/guide/AgentTeamsDiagram.astro,
    src/lib/guides/svg-diagrams/index.ts
  </files>
  <behavior>
    - Test: generateAgentTeams returns valid SVG (starts with &lt;svg, ends with &lt;/svg&gt;)
    - Test: SVG includes role="img" and aria-label accessibility attributes
    - Test: SVG contains "Team Lead" label
    - Test: SVG contains "Teammate" label (one or more teammate nodes)
    - Test: SVG contains "Shared Task List" or "Task List" label
    - Test: SVG contains "Mailbox" label
    - Test: SVG contains task state labels: "pending", "in progress", "completed" (or visual equivalent)
    - Test: SVG contains "Research Preview" or "Experimental" text (experimental status indicator)
    - Test: SVG contains arrow marker definition with team- prefix
    - Test: SVG uses CSS custom properties and no hardcoded hex colors
  </behavior>
  <action>
    Create `agent-teams.ts` following the established pattern:

    - Export `generateAgentTeams(): string`
    - Use MARKER_ID = 'team-arrow' (unique prefix)
    - The ENTIRE diagram should be wrapped in a dashed groupBox with title "Research Preview" to visually convey the experimental status. This is the research recommendation from 91-RESEARCH.md.

    Layout: Multi-agent orchestration topology:

    **Top center:** "Team Lead" large rounded rect (accent border, bold label) -- sub-label: "Main session, coordinates work"

    **Middle row -- Teammates** (2-3 teammate nodes in a horizontal row):
    - Each teammate is a rounded rect with "Teammate 1", "Teammate 2", "Teammate 3" labels
    - Sub-label per teammate: "Separate context"
    - Bidirectional arrows between Team Lead and each Teammate

    **Center panel -- Shared Task List** (groupBox with solid border):
    - 3 small task items showing different states:
      - Task with "pending" state indicator (textSecondary color)
      - Task with "in progress" state indicator (accent color)
      - Task with "completed" state indicator (accentSecondary or textSecondary with checkmark)
    - Label: "File-lock-based claiming"
    - Arrows from teammates to/from task list

    **Right side -- Mailbox** (rounded rect or groupBox):
    - "Mailbox" label
    - Sub-labels: "Message one", "Broadcast all"
    - Arrows connecting mailbox to teammates (showing inter-agent messaging)

    **Bottom note:** Small text label: "Teammates communicate directly (unlike subagents which only report back)"

    **Experimental indicator:** The outer dashed groupBox + "Research Preview" title in the SVG itself, plus the caption will reinforce this.

    - Canvas size: ~720x500, responsive via viewBox
    - aria-label: "Agent teams architecture (research preview): team lead, teammates, shared task list, and mailbox communication"

    Create test file at `__tests__/agent-teams.test.ts`.

    Create `AgentTeamsDiagram.astro` wrapper:
    - Import from barrel, render with PlotFigure
    - Caption: "Agent teams (research preview): lead agent orchestrates teammates via shared task list and mailbox messaging"
    - maxWidth="720px"

    Update `index.ts` -- add `export { generateAgentTeams } from './agent-teams';`
  </action>
  <verify>
    <automated>npx vitest run src/lib/guides/svg-diagrams/__tests__/agent-teams.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - generateAgentTeams() returns valid SVG with team lead, teammates, shared task list, mailbox
    - "Research Preview" or "Experimental" text is present in SVG
    - Task states (pending, in progress, completed) are visually shown
    - All tests pass including no-hardcoded-hex assertion
    - AgentTeamsDiagram.astro wrapper exists
    - index.ts exports generateAgentTeams
  </done>
</task>

<task type="auto">
  <name>Task 2: Full regression verification and build validation</name>
  <files>
    src/lib/guides/svg-diagrams/index.ts
  </files>
  <action>
    **Step 1: Run full diagram test suite.** All 8 test files must pass (3 existing FastAPI + 5 new Claude Code):
    ```
    npx vitest run src/lib/guides/svg-diagrams/__tests__/ --reporter=verbose
    ```

    **Step 2: Verify index.ts barrel exports** are complete -- should have exactly 9 generator exports:
    - generateMiddlewareStack (existing)
    - generateBuilderPattern (existing)
    - generateJwtAuthFlow (existing)
    - generateNfrDiagram (existing)
    - generateAgenticLoop (Plan 01)
    - generateHookLifecycle (Plan 01)
    - generatePermissionModel (Plan 02)
    - generateMcpArchitecture (Plan 02)
    - generateAgentTeams (this plan)

    If any exports are missing (e.g., Plan 02 ran in parallel and has not yet added its exports), add them now.

    **Step 3: Run production build** to confirm all Astro wrappers render without errors:
    ```
    npx astro build 2>&1 | tail -30
    ```
    Build must succeed with zero errors.

    **Step 4: Verify all 5 Astro wrapper components exist** in src/components/guide/:
    - AgenticLoopDiagram.astro
    - HookLifecycleDiagram.astro
    - PermissionModelDiagram.astro
    - McpArchitectureDiagram.astro
    - AgentTeamsDiagram.astro

    **Step 5: Verify unique marker IDs** -- grep all 9 diagram generators for MARKER_ID and confirm all are unique:
    - agentic-arrow, hook-arrow, perm-arrow, mcp-arrow, team-arrow (new)
    - Plus existing: mw-arrow, builder-arrow, jwt-arrow, nfr-arrow (or similar)
  </action>
  <verify>
    <automated>npx vitest run src/lib/guides/svg-diagrams/__tests__/ --reporter=verbose && npx astro build 2>&1 | grep -E "(error|Error|✓|pages)" | tail -10</automated>
  </verify>
  <done>
    - All 8 diagram test files pass (3 existing + 5 new)
    - index.ts has 9 generator exports
    - Production build succeeds with zero errors
    - All 5 new Astro wrapper components exist
    - All marker IDs are unique across all 9 diagrams
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/guides/svg-diagrams/__tests__/ --reporter=verbose` -- all 8 test files pass
2. `npx astro build` -- succeeds with zero errors
3. All 5 new Astro wrapper components exist in src/components/guide/
4. All 9 barrel exports present in index.ts
5. No duplicate marker IDs across any diagram generators
</verification>

<success_criteria>
- Agent Teams diagram shows lead, teammates, shared task list, and mailbox with "Research Preview" label
- Task states (pending, in progress, completed) visible in shared task list
- Full regression: all 8 test files pass, production build succeeds
- All 9 generators exported from index.ts barrel
- All 5 Claude Code Astro wrappers exist and render through PlotFigure
- Phase 91 complete: 5 SVG architecture diagrams available as Astro components
</success_criteria>

<output>
After completion, create `.planning/phases/91-svg-diagram-generators/91-03-SUMMARY.md`
</output>
