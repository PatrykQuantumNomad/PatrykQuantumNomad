---
phase: 90-infrastructure-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/guides/schema.ts
  - src/lib/guides/routes.ts
  - src/lib/guides/__tests__/schema.test.ts
  - src/lib/guides/__tests__/routes.test.ts
  - src/content.config.ts
  - src/data/guides/claude-code/guide.json
  - src/data/guides/claude-code/pages/introduction.mdx
  - astro.config.mjs
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "Both FastAPI and Claude Code content collections load at build time with zero errors"
    - "guide.json for Claude Code validates against the extended guideMetaSchema"
    - "The lastVerified field is accepted as an optional date in guide page frontmatter"
    - "The sitemap builder iterates all guide directories dynamically, not just FastAPI"
    - "A stub MDX chapter exists for Claude Code so the collection is not empty"
  artifacts:
    - path: "src/lib/guides/schema.ts"
      provides: "Extended guidePageSchema with lastVerified, extended guideMetaSchema with accentColor and optional templateRepo/versionTag"
      contains: "lastVerified"
    - path: "src/lib/guides/routes.ts"
      provides: "URL builder functions without hardcoded GUIDE_ROUTES.landing constant"
      exports: ["guidePageUrl", "guideLandingUrl", "GUIDE_ROUTES"]
    - path: "src/content.config.ts"
      provides: "claudeCodePages and claudeCodeGuide collection definitions"
      contains: "claudeCodePages"
    - path: "src/data/guides/claude-code/guide.json"
      provides: "Claude Code guide metadata with 11 chapters and accent color"
      contains: "claude-code"
    - path: "src/data/guides/claude-code/pages/introduction.mdx"
      provides: "Stub chapter page for pipeline validation"
      contains: "introduction"
    - path: "astro.config.mjs"
      provides: "Dynamic multi-guide sitemap builder iterating src/data/guides/*"
      contains: "readdirSync"
  key_links:
    - from: "src/content.config.ts"
      to: "src/lib/guides/schema.ts"
      via: "import guidePageSchema, guideMetaSchema"
      pattern: "import.*guidePageSchema.*guideMetaSchema.*from.*schema"
    - from: "src/content.config.ts"
      to: "src/data/guides/claude-code/guide.json"
      via: "file() loader"
      pattern: "file.*claude-code.*guide\\.json"
    - from: "src/content.config.ts"
      to: "src/data/guides/claude-code/pages"
      via: "glob() loader"
      pattern: "glob.*claude-code.*pages"
    - from: "astro.config.mjs"
      to: "src/data/guides/*/guide.json"
      via: "readdirSync loop"
      pattern: "readdirSync.*guides"
---

<objective>
Extend the guide schema, register Claude Code content collections, create guide.json metadata, scaffold a stub chapter, and refactor the sitemap builder for multi-guide support.

Purpose: This is the foundation plan. All subsequent plans depend on the collections being registered and the schema supporting multi-guide metadata (accent color, optional templateRepo, lastVerified). The sitemap builder must iterate all guides dynamically.

Output: Extended schema, two new collections in content.config.ts, Claude Code guide.json with 11 chapters, a stub introduction.mdx, and a multi-guide sitemap builder.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-infrastructure-refactoring/90-RESEARCH.md

@src/lib/guides/schema.ts
@src/lib/guides/routes.ts
@src/lib/guides/__tests__/schema.test.ts
@src/lib/guides/__tests__/routes.test.ts
@src/content.config.ts
@astro.config.mjs
@src/data/guides/fastapi-production/guide.json

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/guides/schema.ts (CURRENT — will be modified):
```typescript
export const guidePageSchema = z.object({
  title: z.string(),
  description: z.string(),
  order: z.number().int().min(0),
  slug: z.string(),
});

export const guideMetaSchema = z.object({
  id: z.string(),
  title: z.string(),
  description: z.string(),
  slug: z.string(),
  templateRepo: z.string().url(),     // CURRENTLY REQUIRED — make optional
  versionTag: z.string(),              // CURRENTLY REQUIRED — make optional
  publishedDate: z.coerce.date().optional(),
  requirements: z.record(z.string(), z.string()).optional(),
  chapters: z.array(z.object({ slug: z.string(), title: z.string() })),
});

export type GuidePage = z.infer<typeof guidePageSchema>;
export type GuideMeta = z.infer<typeof guideMetaSchema>;
```

From src/lib/guides/routes.ts (CURRENT — GUIDE_ROUTES.landing to be removed):
```typescript
export const GUIDE_ROUTES = {
  landing: '/guides/fastapi-production/',   // REMOVE this line
  guides: '/guides/',                       // KEEP
} as const;
export function guidePageUrl(guideSlug: string, chapterSlug: string): string;
export function guideLandingUrl(guideSlug: string): string;
```

From src/content.config.ts (CURRENT — will add two collections):
```typescript
export const collections = { blog, languages, dbModels, edaTechniques, edaDistributions, edaPages, guidePages, guides };
// guidePages: glob for ./src/data/guides/fastapi-production/pages
// guides: file for src/data/guides/fastapi-production/guide.json
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend guide schemas, clean up routes, and update tests</name>
  <files>src/lib/guides/schema.ts, src/lib/guides/routes.ts, src/lib/guides/__tests__/schema.test.ts, src/lib/guides/__tests__/routes.test.ts</files>
  <behavior>
    - guidePageSchema accepts an object with optional `lastVerified` date field
    - guidePageSchema still rejects missing title, description, order, slug
    - guideMetaSchema accepts an object without `templateRepo` (optional)
    - guideMetaSchema accepts an object without `versionTag` (optional)
    - guideMetaSchema accepts an object with `accentColor` string
    - guideMetaSchema accepts chapters with optional `description` string
    - guideMetaSchema still rejects missing id, title, description, slug, chapters
    - guideMetaSchema still validates templateRepo as URL when present
    - Existing FastAPI-style validMeta (with templateRepo + versionTag) still passes
    - GUIDE_ROUTES no longer has a `landing` property
    - GUIDE_ROUTES still has `guides` property
    - guidePageUrl and guideLandingUrl still work correctly
  </behavior>
  <action>
    1. Modify `src/lib/guides/schema.ts`:
       - Add `lastVerified: z.coerce.date().optional()` to guidePageSchema
       - Change `templateRepo: z.string().url()` to `templateRepo: z.string().url().optional()`
       - Change `versionTag: z.string()` to `versionTag: z.string().optional()`
       - Add `accentColor: z.string().optional()` to guideMetaSchema
       - Add `description: z.string().optional()` to the chapters array object schema

    2. Modify `src/lib/guides/routes.ts`:
       - Remove the `landing` property from GUIDE_ROUTES. Keep only `guides: '/guides/'`
       - Keep guidePageUrl and guideLandingUrl unchanged

    3. Update `src/lib/guides/__tests__/schema.test.ts`:
       - Add test: guidePageSchema accepts object with lastVerified date string
       - Add test: guidePageSchema accepts object without lastVerified (backward compat)
       - Add test: guideMetaSchema accepts object without templateRepo
       - Add test: guideMetaSchema accepts object without versionTag
       - Add test: guideMetaSchema accepts object with accentColor
       - Add test: guideMetaSchema accepts chapters with description
       - Add test: guideMetaSchema still validates templateRepo as URL when present (existing test should still pass)
       - Ensure existing FastAPI validMeta test still passes (backward compat proof)

    4. Update `src/lib/guides/__tests__/routes.test.ts`:
       - Remove the test for `GUIDE_ROUTES.landing`
       - Keep the test for `GUIDE_ROUTES.guides`
       - Keep all guidePageUrl and guideLandingUrl tests
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx vitest run src/lib/guides/__tests__/schema.test.ts src/lib/guides/__tests__/routes.test.ts --reporter=verbose</automated>
  </verify>
  <done>Schema accepts lastVerified, optional templateRepo/versionTag, accentColor, and chapter descriptions. GUIDE_ROUTES.landing removed. All tests pass including backward-compatible FastAPI validMeta.</done>
</task>

<task type="auto">
  <name>Task 2: Register Claude Code collections, create guide.json, scaffold stub chapter, and refactor sitemap builder</name>
  <files>src/content.config.ts, src/data/guides/claude-code/guide.json, src/data/guides/claude-code/pages/introduction.mdx, astro.config.mjs</files>
  <action>
    1. Create directory `src/data/guides/claude-code/pages/` if it does not exist.

    2. Create `src/data/guides/claude-code/guide.json` — an array with one object:
       ```json
       [
         {
           "id": "claude-code",
           "title": "Claude Code Guide",
           "description": "The definitive zero-to-hero guide for Claude Code -- from first agentic interaction through multi-agent orchestration, by a cloud-native architect with 17+ years of experience.",
           "slug": "claude-code",
           "accentColor": "#D97706",
           "publishedDate": "2026-03-15",
           "requirements": {
             "claude-code": "latest",
             "node": "18+"
           },
           "chapters": [
             { "slug": "introduction", "title": "Introduction & Getting Started", "description": "The agentic loop, installation across all interfaces, core tools, and your first checkpoint" },
             { "slug": "context-management", "title": "Project Context & Memory", "description": "CLAUDE.md hierarchy, auto-memory, context rot prevention, and .claudeignore" },
             { "slug": "models-and-costs", "title": "Models, Cost Economics & Permissions", "description": "Model selection, effort levels, pricing tiers, and the permission system" },
             { "slug": "environment", "title": "Environment Sandboxing & Customization", "description": "Configuration scopes, sandboxed execution, and status line customization" },
             { "slug": "remote-and-headless", "title": "Remote Control & Headless Automation", "description": "Remote control, headless mode, cron scheduling, and HTTP proxy configuration" },
             { "slug": "mcp", "title": "Model Context Protocol", "description": "Connecting external tools, transport modes, tool search, and troubleshooting" },
             { "slug": "custom-skills", "title": "Custom Skills", "description": "SKILL.md anatomy, slash commands vs auto-invocation, and the skill creator" },
             { "slug": "hooks", "title": "Hooks & Lifecycle Automation", "description": "Lifecycle events, exit codes, and prompt/agent hooks" },
             { "slug": "worktrees", "title": "Git Worktrees & Subagent Delegation", "description": "Parallel development branches and custom agent personas" },
             { "slug": "agent-teams", "title": "Agent Teams & Advanced Orchestration", "description": "Team architecture, shared tasks, and dependency tracking (research preview)" },
             { "slug": "security", "title": "Security & Enterprise Administration", "description": "Vulnerability scanning, managed settings, and plugin governance" }
           ]
         }
       ]
       ```

    3. Create `src/data/guides/claude-code/pages/introduction.mdx` — a minimal stub chapter:
       ```mdx
       ---
       title: "Introduction & Getting Started"
       description: "The agentic loop, installation across all interfaces, core tools, and your first checkpoint"
       order: 0
       slug: "introduction"
       ---

       Content coming in Phase 93.
       ```

    4. Modify `src/content.config.ts`:
       - Add after the existing `guidePages` and `guides` definitions:
         ```typescript
         const claudeCodePages = defineCollection({
           loader: glob({ pattern: '**/*.mdx', base: './src/data/guides/claude-code/pages' }),
           schema: guidePageSchema,
         });

         const claudeCodeGuide = defineCollection({
           loader: file('src/data/guides/claude-code/guide.json'),
           schema: guideMetaSchema,
         });
         ```
       - Add `claudeCodePages` and `claudeCodeGuide` to the collections export:
         ```typescript
         export const collections = { blog, languages, dbModels, edaTechniques, edaDistributions, edaPages, guidePages, guides, claudeCodePages, claudeCodeGuide };
         ```

    5. Refactor `astro.config.mjs` `buildContentDateMap()`:
       - Replace the hardcoded FastAPI guide.json read (lines 47-59) with a dynamic loop:
         ```javascript
         // Guide pages: iterate all guide directories
         try {
           const guidesDir = './src/data/guides';
           for (const entry of readdirSync(guidesDir, { withFileTypes: true })) {
             if (!entry.isDirectory()) continue;
             const jsonPath = `${guidesDir}/${entry.name}/guide.json`;
             try {
               const meta = JSON.parse(readFileSync(jsonPath, 'utf-8'));
               const guideDate = meta[0]?.publishedDate;
               const guideSlug = meta[0]?.slug;
               if (guideDate && guideSlug) {
                 const iso = new Date(guideDate).toISOString();
                 map.set(`${SITE}/guides/${guideSlug}/`, iso);
                 for (const ch of meta[0].chapters ?? []) {
                   map.set(`${SITE}/guides/${guideSlug}/${ch.slug}/`, iso);
                 }
               }
             } catch { /* non-fatal -- guide.json may not parse */ }
           }
         } catch { /* non-fatal */ }
         ```
       - This removes the hardcoded `/guides/fastapi-production/faq/` entry. FastAPI's FAQ page is a static .astro file and doesn't need a sitemap date entry (it will still be included in the sitemap, just without lastmod — same as other static pages).

    6. Verify both collections are populated by running `npx astro build` and checking for zero errors. The Claude Code collection should have 1 page (introduction.mdx) and the FastAPI collection should still have all its pages.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx vitest run src/lib/guides/__tests__/ --reporter=verbose && npx astro build 2>&1 | tail -20</automated>
  </verify>
  <done>content.config.ts exports claudeCodePages and claudeCodeGuide collections. guide.json validates with 11 chapters and accent color #D97706. Stub introduction.mdx exists. astro.config.mjs iterates all guide directories. `npx astro build` succeeds with zero errors and both guides appear in build output.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/guides/__tests__/ --reporter=verbose` -- all schema and route tests pass
2. `npx astro build` succeeds with zero errors
3. `ls dist/guides/fastapi-production/*/index.html | wc -l` returns the same count as before refactoring (14+ files: landing, 14 chapters, FAQ)
4. `test -f dist/guides/claude-code/index.html` -- Claude Code landing page generates (will be empty/404 until Plan 03 creates it, but the collection should be loadable)
</verification>

<success_criteria>
- Extended schema passes all new and existing tests
- GUIDE_ROUTES.landing removed; route utilities still work
- Two new collections (claudeCodePages, claudeCodeGuide) registered in content.config.ts
- Claude Code guide.json has 11 chapters with descriptions, accent color #D97706, no templateRepo
- Stub introduction.mdx exists in claude-code/pages/
- astro.config.mjs sitemap builder dynamically iterates all guide directories
- Full production build succeeds with zero errors
- FastAPI guide still generates all pages (regression check)
</success_criteria>

<output>
After completion, create `.planning/phases/90-infrastructure-refactoring/90-01-SUMMARY.md`
</output>
