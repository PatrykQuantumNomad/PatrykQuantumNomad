---
phase: 54-case-studies-reference-landing-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/eda/reference/[...slug].astro
  - src/data/eda/pages/reference/analysis-questions.mdx
  - src/data/eda/pages/reference/techniques-by-category.mdx
  - src/data/eda/pages/reference/distribution-tables.mdx
  - src/data/eda/pages/reference/related-distributions.mdx
autonomous: true
requirements: [REF-01, REF-02, REF-03, REF-04]

must_haves:
  truths:
    - "All 4 reference pages render at /eda/reference/{slug}/ with NIST section citations"
    - "Analysis questions page lists the core EDA questions with explanations"
    - "Techniques by category page provides a taxonomy with cross-links to technique pages"
    - "Distribution tables page provides critical value lookup tables"
    - "Related distributions page explains PDF/CDF/PPF relationships with cross-links to distribution pages"
  artifacts:
    - path: "src/pages/eda/reference/[...slug].astro"
      provides: "Dynamic route for 4 reference MDX pages"
      contains: "getStaticPaths"
    - path: "src/data/eda/pages/reference/analysis-questions.mdx"
      provides: "Core EDA analysis questions reference"
      min_lines: 50
    - path: "src/data/eda/pages/reference/techniques-by-category.mdx"
      provides: "Technique taxonomy by problem category"
      min_lines: 60
    - path: "src/data/eda/pages/reference/distribution-tables.mdx"
      provides: "Statistical distribution critical value tables"
      min_lines: 80
    - path: "src/data/eda/pages/reference/related-distributions.mdx"
      provides: "Distribution relationship reference"
      min_lines: 50
  key_links:
    - from: "src/pages/eda/reference/[...slug].astro"
      to: "edaPages collection"
      via: "getCollection + filter by category === 'reference'"
      pattern: "category.*reference"
    - from: "src/data/eda/pages/reference/techniques-by-category.mdx"
      to: "/eda/techniques/{slug}/"
      via: "markdown cross-links to technique pages"
      pattern: "/eda/techniques/"
    - from: "src/data/eda/pages/reference/related-distributions.mdx"
      to: "/eda/distributions/{slug}/"
      via: "markdown cross-links to distribution pages"
      pattern: "/eda/distributions/"
---

<objective>
Create the reference dynamic route and populate all 4 reference MDX files with lookup tables, technique taxonomies, and cross-linked reference content.

Purpose: Reference pages serve as the EDA encyclopedia's index -- providing quick-lookup tables for critical values, organized technique taxonomies for problem-driven navigation, and relationship maps between distributions. They tie the encyclopedia together with dense cross-links.

Output: 4 renderable reference pages at /eda/reference/{slug}/ with tables, taxonomies, and cross-links to technique and distribution pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-case-studies-reference-landing-page/54-RESEARCH.md
@src/pages/eda/foundations/[...slug].astro
@src/data/eda/pages/reference/analysis-questions.mdx
@src/lib/eda/routes.ts
@src/content.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reference dynamic route</name>
  <files>src/pages/eda/reference/[...slug].astro</files>
  <action>
Create `src/pages/eda/reference/[...slug].astro` following the exact pattern from `src/pages/eda/foundations/[...slug].astro`.

Key differences from foundations route:
1. Filter `edaPages` by `category === 'reference'` (not 'foundations')
2. Strip `reference/` prefix from `page.id` for slug extraction
3. Breadcrumb: Home > EDA > Reference > [Title]
4. BreadcrumbJsonLd with reference breadcrumb path

Import the same components:
```typescript
import { getCollection, render } from 'astro:content';
import EDALayout from '../../../layouts/EDALayout.astro';
import EdaBreadcrumb from '../../../components/eda/EdaBreadcrumb.astro';
import BreadcrumbJsonLd from '../../../components/BreadcrumbJsonLd.astro';
```

getStaticPaths: filter by `p.data.category === 'reference'`, slug = `page.id.replace('reference/', '')`.

Use `max-w-3xl` container, `py-16` padding, `prose-foundations` class for MDX styling.

Add table styling to the prose-foundations class div. Reference pages are table-heavy, so include:
```
[&_table]:w-full [&_table]:text-sm [&_table]:mb-6
[&_th]:text-left [&_th]:p-2 [&_th]:border-b [&_th]:border-[var(--color-border)] [&_th]:text-[var(--color-text-primary)] [&_th]:font-medium
[&_td]:p-2 [&_td]:border-b [&_td]:border-[var(--color-border)]/30 [&_td]:text-[var(--color-text-secondary)]
```
These extend the existing prose-foundations selectors.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -20</automated>
    <manual>Build completes and 4 reference routes are generated</manual>
  </verify>
  <done>Dynamic route generates 4 reference pages from existing MDX stubs without build errors</done>
</task>

<task type="auto">
  <name>Task 2: Populate all 4 reference MDX files with tables, taxonomies, and cross-links</name>
  <files>
    src/data/eda/pages/reference/analysis-questions.mdx
    src/data/eda/pages/reference/techniques-by-category.mdx
    src/data/eda/pages/reference/distribution-tables.mdx
    src/data/eda/pages/reference/related-distributions.mdx
  </files>
  <action>
Populate each of the 4 existing MDX stub files with rich reference content. Preserve existing frontmatter exactly. Replace the `{/* Content will be populated in Phase 54 */}` comment.

**1. analysis-questions.mdx** (REF-01, NIST Section 1.3.2):
List the core EDA analysis questions that drive exploratory analysis. Structure:

```
## The Core EDA Questions

[Brief intro: EDA begins by asking fundamental questions about the data]

### 1. What is a good model for this data?
[Explanation: distributional characterization, PDF fitting]

### 2. What are good estimates of location?
[Mean, median, mode -- when each is appropriate]

### 3. What are good estimates of spread?
[Standard deviation, range, IQR -- robustness considerations]

### 4. What is a good estimate of percentiles?
[Percentile estimation, interpolation methods]

### 5. Is the data random?
[Autocorrelation, runs test, lag plot diagnostics]

### 6. Is the assumed distribution correct?
[Goodness-of-fit, probability plots, PPCC]

### 7. Does the model fit the data?
[Residual analysis, adequacy checks]
```

Include cross-links to relevant technique pages (e.g., "[histogram](/eda/techniques/histogram/) for distributional shape", "[probability plot](/eda/techniques/normal-probability-plot/) for normality testing").

**2. techniques-by-category.mdx** (REF-02, NIST Section 1.3.4):
Organize all EDA techniques by the type of problem they solve. Use markdown tables with cross-links:

| Problem Category | Techniques |
|------------------|------------|
| Distributional characterization | [Histogram](/eda/techniques/histogram/), [Box Plot](/eda/techniques/box-plot/), [Normal Probability Plot](/eda/techniques/normal-probability-plot/) |
| Location assessment | [Mean](/eda/quantitative/mean/), [Median](/eda/quantitative/median/) |
| ...etc |

Group into 7 categories from NIST 1.3.4: Distributional, Location, Spread, Randomness, Model Adequacy, Outlier Detection, Time Series. Each category should list relevant graphical AND quantitative techniques with markdown links.

**3. distribution-tables.mdx** (REF-03, NIST Section 1.3.6.7):
Provide critical value reference tables. Create markdown tables for:
- Normal distribution critical values (z-table excerpt: common alpha levels 0.10, 0.05, 0.025, 0.01, 0.005)
- t-distribution critical values (for df = 1, 2, 5, 10, 20, 30, 60, 120, infinity)
- Chi-square critical values (common df and alpha levels)
- F-distribution critical values (selected numerator/denominator df pairs)

Include brief descriptions of when each table is used. Cross-link to distribution detail pages: "[Normal Distribution](/eda/distributions/normal/)", "[t Distribution](/eda/distributions/t/)", etc.

Tables should use realistic statistical values (reference standard statistics textbooks / NIST tables).

**4. related-distributions.mdx** (REF-04, NIST Sections 1.3.6.2-5):
Explain the mathematical relationships between distributions:

```
## Distribution Functions
[Define PDF, CDF, PPF (quantile), survival function, hazard function]

## Key Relationships
### Normal Family
[Normal -> Chi-square (sum of squares), Normal -> t (ratio), Normal -> F (ratio of chi-squares)]

### Exponential Family
[Exponential -> Gamma (generalization), Exponential -> Weibull (generalization)]

### Limiting Distributions
[Binomial -> Normal (large n), Poisson -> Normal (large lambda), t -> Normal (large df)]
```

Include cross-links to each distribution page: "[Normal](/eda/distributions/normal/)", "[Chi-Square](/eda/distributions/chi-square/)", etc.

IMPORTANT:
- Each file should be 50-120 lines of content.
- Use markdown tables extensively (reference pages are table-heavy).
- Cross-links use correct route patterns from routes.ts: `/eda/techniques/{slug}/`, `/eda/quantitative/{slug}/`, `/eda/distributions/{slug}/`.
- Keep prose concise and reference-oriented (lookup-friendly, not essay-style).
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -20</automated>
    <manual>Verify 4 reference pages render with tables and cross-links</manual>
  </verify>
  <done>All 4 reference MDX files contain structured reference content with cross-links to technique and distribution pages. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npx astro build` completes without errors
2. Build output includes 4 reference routes under `/eda/reference/`
3. Each reference page has dense cross-links to technique and distribution pages
4. Tables render correctly in markdown output
</verification>

<success_criteria>
- All 4 reference pages render at their defined URLs
- Cross-links from reference pages to technique/distribution pages use correct URLs
- Tables display properly with header rows and data rows
- Build produces zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/54-case-studies-reference-landing-page/54-02-SUMMARY.md`
</output>
