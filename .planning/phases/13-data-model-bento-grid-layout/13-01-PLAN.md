---
phase: 13-data-model-bento-grid-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/projects.ts
  - src/pages/llms-full.txt.ts
autonomous: true

must_haves:
  truths:
    - "Each project entry has a technologies array, featured flag, status field, and gridSize hint"
    - "TypeScript compiles without errors after interface extension"
    - "2 projects are marked as featured (kps-graph-agent, kps-cluster-deployment)"
    - "Grid distribution is 3 large, 8 medium, 5 small across 16 projects"
    - "llms-full.txt.ts renders technologies for each project instead of just the primary language"
  artifacts:
    - path: "src/data/projects.ts"
      provides: "Extended Project interface and all 16 project entries with new fields"
      contains: "technologies"
    - path: "src/pages/llms-full.txt.ts"
      provides: "Updated LLMs.txt with technology data"
      contains: "technologies"
  key_links:
    - from: "src/data/projects.ts"
      to: "src/pages/projects/index.astro"
      via: "import { projects, categories }"
      pattern: "from.*projects"
    - from: "src/data/projects.ts"
      to: "src/pages/llms-full.txt.ts"
      via: "import { projects, categories }"
      pattern: "from.*projects"
---

<objective>
Extend the Project data model with technologies, featured, status, and gridSize fields, and populate all 16 project entries with accurate data.

Purpose: The bento grid layout (Plan 02) needs gridSize to determine column spans, featured to separate hero projects, technologies for badge display, and status for badge styling. This plan makes the data available.
Output: Updated `src/data/projects.ts` with extended interface and complete data for all 16 projects.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-data-model-bento-grid-layout/13-RESEARCH.md
@src/data/projects.ts
@src/pages/llms-full.txt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Project interface and populate all 16 entries</name>
  <files>src/data/projects.ts</files>
  <action>
    Extend the existing `Project` interface in `src/data/projects.ts` with these new fields:

    ```typescript
    technologies: string[];                              // DATA-01
    featured?: boolean;                                  // DATA-02 (optional, defaults to false)
    status: 'active' | 'archived' | 'experimental';     // DATA-03
    gridSize: 'large' | 'medium' | 'small';              // DATA-04
    ```

    Keep the existing `language` field for backward compatibility -- it remains the primary language.

    Update ALL 16 project entries with the new fields using these exact assignments from the research:

    **AI/ML and LLM Agents (7 projects):**
    - kps-graph-agent: technologies ['Python', 'LangGraph', 'Neo4j'], featured: true, status: 'active', gridSize: 'large'
    - kps-assistant: technologies ['Python', 'LLM', 'FastAPI'], status: 'active', gridSize: 'medium'
    - kps-assistant-support: technologies ['HCL', 'Terraform', 'Kubernetes'], status: 'active', gridSize: 'small'
    - kubert-langflow: technologies ['Python', 'Langflow', 'LLM'], status: 'active', gridSize: 'medium'
    - kps-langflow: technologies ['HCL', 'Terraform', 'Kubernetes'], status: 'active', gridSize: 'small'
    - tekstack-assistant-library: technologies ['Python', 'LLM', 'SDK'], status: 'active', gridSize: 'medium'
    - financial-data-extractor: technologies ['Python', 'NLP', 'Data Extraction'], status: 'active', gridSize: 'medium'

    **Kubernetes and Infrastructure (6 projects):**
    - kps-cluster-deployment: technologies ['HCL', 'Terraform', 'Kubernetes', 'GCP'], featured: true, status: 'active', gridSize: 'large'
    - kps-infra-management: technologies ['HCL', 'Terraform', 'Terragrunt'], status: 'active', gridSize: 'medium'
    - kps-basic-package: technologies ['HCL', 'Terraform'], status: 'active', gridSize: 'small'
    - kps-observability-package: technologies ['HCL', 'Terraform', 'Prometheus', 'Grafana'], status: 'active', gridSize: 'medium'
    - kps-charts: technologies ['Go Template', 'Helm', 'Kubernetes'], status: 'active', gridSize: 'medium'
    - kps-images: technologies ['Docker', 'CI/CD', 'Shell'], status: 'active', gridSize: 'small'

    **Platform and DevOps Tooling (2 projects):**
    - kps-lobe-chat: technologies ['TypeScript', 'Kubernetes', 'Docker'], status: 'active', gridSize: 'medium'
    - jobs: technologies ['Python', 'Scraping', 'Automation'], status: 'experimental', gridSize: 'small'

    **Security and Networking (1 project):**
    - networking-tools: technologies ['Shell', 'Docker', 'Nmap', 'Metasploit'], status: 'active', gridSize: 'large'

    Totals to verify: 2 featured, 3 large, 8 medium, 5 small, 15 active + 1 experimental.
  </action>
  <verify>Run `npx astro check` or `npx tsc --noEmit` -- zero type errors. Count projects: 16 total, 2 featured, 3 large/8 medium/5 small.</verify>
  <done>All 16 project entries have technologies, status, and gridSize fields. 2 entries have featured: true. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update llms-full.txt.ts to use technologies field</name>
  <files>src/pages/llms-full.txt.ts</files>
  <action>
    In `src/pages/llms-full.txt.ts`, update the project rendering loop (around line 89) to show the technologies array instead of just the primary language.

    Change from:
    ```
    lines.push(`- ${project.name} (${project.language}): ${project.description}`);
    ```

    To:
    ```
    lines.push(`- ${project.name} (${project.technologies.join(', ')}): ${project.description}`);
    ```

    This makes the LLMs.txt output richer with the full technology stack per project, improving AI discoverability. Keep the rest of the file unchanged.
  </action>
  <verify>Run `npx astro build` -- build succeeds. Check the generated llms-full.txt output contains technology arrays (e.g., "Python, LangGraph, Neo4j" instead of just "Python").</verify>
  <done>llms-full.txt.ts renders comma-separated technologies for each project. Build succeeds without errors.</done>
</task>

</tasks>

<verification>
- `npx astro build` completes without errors
- `src/data/projects.ts` exports 16 projects with all new fields populated
- Featured count: exactly 2 (kps-graph-agent, kps-cluster-deployment)
- gridSize distribution: 3 large, 8 medium, 5 small
- Status distribution: 15 active, 1 experimental
- llms-full.txt output shows technologies arrays
</verification>

<success_criteria>
DATA-01: Every project has a technologies array with 2-4 items
DATA-02: 2 projects have featured: true
DATA-03: Every project has a status field (active or experimental)
DATA-04: Every project has a gridSize hint (large, medium, or small)
Build passes with zero type errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-data-model-bento-grid-layout/13-01-SUMMARY.md`
</output>
