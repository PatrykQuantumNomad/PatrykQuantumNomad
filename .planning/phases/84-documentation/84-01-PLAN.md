---
phase: 84-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md, scripts/test-validator-hooks.sh]
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-04, DOC-05]

must_haves:
  truths:
    - "README.md contains an Agent Skills section with a working npx skills add install command"
    - "All 4 skills are listed in README with descriptions and individual --skill install example"
    - "README includes benchmark highlight stats (98.8% pass rate, +42.4% improvement)"
    - "GitHub Actions Workflow Validator appears in the Interactive Tools table"
    - "No stale public/skills/ references remain in non-planning repo files"
  artifacts:
    - path: "README.md"
      provides: "Agent Skills section, GHA Validator in Interactive Tools, benchmark stats"
      contains: "npx skills add PatrykQuantumNomad/PatrykQuantumNomad"
    - path: "scripts/test-validator-hooks.sh"
      provides: "Updated hook paths referencing skills/ instead of public/skills/"
      contains: "$REPO_ROOT/skills/"
  key_links:
    - from: "README.md Agent Skills section"
      to: "skills.sh listing"
      via: "npx skills add command and skills.sh hyperlinks"
      pattern: "npx skills add PatrykQuantumNomad"
    - from: "README.md Interactive Tools table"
      to: "GHA Validator tool page"
      via: "Try it and Blog links"
      pattern: "patrykgolabek.dev/tools/gha-validator"
    - from: "scripts/test-validator-hooks.sh"
      to: "skills/ directory at repo root"
      via: "REPO_ROOT/skills/ path references"
      pattern: "REPO_ROOT/skills/"
---

<objective>
Update README.md to showcase 4 DevOps validator agent skills with install commands, benchmark stats, and GHA Validator in the Interactive Tools table. Clean up stale public/skills/ path references in scripts.

Purpose: This is the final phase of v1.14 (DevOps Skills Publishing). It makes the published skills discoverable from the GitHub profile README and ensures all repo references point to the canonical skills/ directory.
Output: Updated README.md with Agent Skills section and GHA Validator row; updated test-validator-hooks.sh with corrected paths.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-documentation/84-RESEARCH.md
@README.md
@scripts/test-validator-hooks.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GHA Validator to Interactive Tools and Agent Skills section to README</name>
  <files>README.md</files>
  <action>
Edit README.md to make 3 changes:

**Change 1 (DOC-04): Add GHA Validator row to Interactive Tools table.**
Insert a new row in the existing Interactive Tools table (lines 32-36). Insert it in alphabetical order between Dockerfile Analyzer and Kubernetes Manifest Analyzer:

```markdown
| GitHub Actions Workflow Validator | 48 rules | [Try it](https://patrykgolabek.dev/tools/gha-validator/) · [Blog](https://patrykgolabek.dev/blog/github-actions-best-practices/) |
```

Note: Use 48 rules (matching the blog post and codebase), NOT 46 (which is the SKILL.md frontmatter count).

**Change 2 (DOC-01, DOC-02, DOC-03): Add Agent Skills section between Interactive Tools and Recent Writing.**
Insert a new `## Agent Skills` section AFTER the Interactive Tools section and BEFORE the `## Recent Writing` section. Use this exact content:

```markdown
## Agent Skills

DevOps validator skills for AI coding agents (Claude Code, Cursor, Copilot, Codex, and 17+ more). Install via [skills.sh](https://skills.sh):

\```bash
npx skills add PatrykQuantumNomad/PatrykQuantumNomad
\```

| Skill | Rules | Description |
|-------|-------|-------------|
| [dockerfile-analyzer](https://skills.sh/PatrykQuantumNomad/PatrykQuantumNomad/dockerfile-analyzer) | 46 | Dockerfile security, efficiency, and best-practice analysis |
| [compose-validator](https://skills.sh/PatrykQuantumNomad/PatrykQuantumNomad/compose-validator) | 52 | Docker Compose security, semantic, and schema validation |
| [gha-validator](https://skills.sh/PatrykQuantumNomad/PatrykQuantumNomad/gha-validator) | 46 | GitHub Actions workflow security and best-practice analysis |
| [k8s-analyzer](https://skills.sh/PatrykQuantumNomad/PatrykQuantumNomad/k8s-analyzer) | 67 | Kubernetes manifest security, reliability, and RBAC analysis |

Install a single skill:

\```bash
npx skills add PatrykQuantumNomad/PatrykQuantumNomad --skill dockerfile-analyzer
\```

**Benchmarked:** 98.8% pass rate across all skills (+42.4% improvement over baseline without skills). Tested with Claude Haiku 4.5.
```

IMPORTANT anti-patterns to avoid:
- Do NOT use verbose SKILL.md descriptions. Use the short 10-15 word summaries shown above.
- Do NOT link to raw GitHub SKILL.md files. Link to skills.sh pages.
- Do NOT list all 4 individual install commands. Show ONE example with `--skill dockerfile-analyzer`.
- Use 46 rules for gha-validator in the Agent Skills table (matching SKILL.md frontmatter), NOT 48.
- Place the section AFTER Interactive Tools and BEFORE Recent Writing.
  </action>
  <verify>
    <automated>grep -q "npx skills add PatrykQuantumNomad" README.md && grep -q "98.8%" README.md && grep -q "42.4%" README.md && grep -q "GitHub Actions Workflow Validator" README.md && grep -c "skills add.*--skill" README.md | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README.md contains: (1) GHA Validator row in Interactive Tools with 48 rules, Try it and Blog links; (2) Agent Skills section with npx install command, 4-skill table with descriptions and skills.sh links, single --skill example, and benchmark callout showing 98.8% pass rate and +42.4% improvement</done>
</task>

<task type="auto">
  <name>Task 2: Update test script paths and verify no stale public/skills/ references</name>
  <files>scripts/test-validator-hooks.sh</files>
  <action>
**Change 1 (DOC-05): Update 4 hook path references in scripts/test-validator-hooks.sh.**
Replace `public/skills/` with `skills/` on lines 7-10:

Line 7: `COMPOSE_HOOK="$REPO_ROOT/skills/compose-validator/hooks/validate-compose.sh"`
Line 8: `DOCKERFILE_HOOK="$REPO_ROOT/skills/dockerfile-analyzer/hooks/validate-dockerfile.sh"`
Line 9: `K8S_HOOK="$REPO_ROOT/skills/k8s-analyzer/hooks/validate-k8s.sh"`
Line 10: `GHA_HOOK="$REPO_ROOT/skills/gha-validator/hooks/validate-gha.sh"`

**Change 2: Verify no stale references remain.**
After editing, run a grep across all non-planning repo files to confirm zero remaining `public/skills/` references. The grep should exclude `.planning/` (historical records) and `node_modules/`.

IMPORTANT: Do NOT update any files in `.planning/` -- those are historical records of past state and should not be rewritten.

**Change 3: Run the test script to verify it still works.**
Execute `bash scripts/test-validator-hooks.sh` to confirm all tests pass with the updated paths. The skills/ directory at repo root contains the actual hook scripts, so paths should resolve correctly.
  </action>
  <verify>
    <automated>grep -r "public/skills/" --include="*.sh" --include="*.md" --include="*.ts" --include="*.js" --include="*.json" /Users/patrykattc/work/git/PatrykQuantumNomad/ | grep -v ".planning/" | grep -v "node_modules/" | wc -l | tr -d ' ' | grep -q "^0$" && bash /Users/patrykattc/work/git/PatrykQuantumNomad/scripts/test-validator-hooks.sh > /dev/null 2>&1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>scripts/test-validator-hooks.sh references skills/ (not public/skills/); grep finds zero stale public/skills/ references in non-planning files; test script runs with all tests passing (PASS count > 0, FAIL count = 0)</done>
</task>

</tasks>

<verification>
Run all 5 DOC requirement checks:

1. DOC-01: `grep -q "npx skills add PatrykQuantumNomad" README.md` (exit 0)
2. DOC-02: `grep -c "skills add.*--skill" README.md` returns >= 1
3. DOC-03: `grep -q "98.8%" README.md && grep -q "42.4%" README.md` (exit 0)
4. DOC-04: `grep -q "GitHub Actions Workflow Validator" README.md` (exit 0)
5. DOC-05: `grep -r "public/skills/" --include="*.sh" --include="*.md" --include="*.ts" . | grep -v ".planning/" | grep -v "node_modules/" | wc -l` returns 0

Final gate: `bash scripts/test-validator-hooks.sh` passes with 0 failures.
</verification>

<success_criteria>
- README.md has Agent Skills section between Interactive Tools and Recent Writing
- README.md Interactive Tools table has 4 rows (Compose, Dockerfile, GHA, K8s)
- All 4 skills listed in Agent Skills table with skills.sh links and correct rule counts
- Benchmark callout shows 98.8% pass rate and +42.4% improvement
- scripts/test-validator-hooks.sh uses skills/ paths (not public/skills/)
- Test script passes with 0 failures
- Zero stale public/skills/ references in non-planning repo files
</success_criteria>

<output>
After completion, create `.planning/phases/84-documentation/84-01-SUMMARY.md`
</output>
