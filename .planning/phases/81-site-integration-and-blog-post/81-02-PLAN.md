---
phase: 81-site-integration-and-blog-post
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/blog/github-actions-best-practices.mdx
  - src/pages/blog/[slug].astro
autonomous: true
requirements:
  - BLOG-01
  - BLOG-02
  - BLOG-03

must_haves:
  truths:
    - "Blog post at /blog/github-actions-best-practices/ renders with full content covering GitHub Actions best practices"
    - "Blog post links to individual rule documentation pages (e.g., /tools/gha-validator/rules/ga-c001/)"
    - "Blog post has a CTA linking to the tool page at /tools/gha-validator/"
    - "Blog post page has FAQPageJsonLd structured data with 3 GHA-specific FAQ items"
    - "Blog post page has aboutDataset linking to the GHA Validator SoftwareApplication"
    - "Blog post page has articleSection set to 'GitHub Actions Security'"
  artifacts:
    - path: "src/data/blog/github-actions-best-practices.mdx"
      provides: "Companion blog post covering GitHub Actions best practices and tool architecture"
      min_lines: 200
    - path: "src/pages/blog/[slug].astro"
      provides: "Blog template with isGhaPost check for FAQPageJsonLd and aboutDataset"
      contains: "isGhaPost"
  key_links:
    - from: "src/data/blog/github-actions-best-practices.mdx"
      to: "/tools/gha-validator/"
      via: "inline CTA link in blog content"
      pattern: "tools/gha-validator"
    - from: "src/data/blog/github-actions-best-practices.mdx"
      to: "/tools/gha-validator/rules/"
      via: "rule documentation links in blog content"
      pattern: "tools/gha-validator/rules/ga-"
    - from: "src/pages/blog/[slug].astro"
      to: "FAQPageJsonLd"
      via: "conditional rendering for GHA post"
      pattern: "ghaFAQ.*length.*FAQPageJsonLd"
---

<objective>
Create the companion blog post covering GitHub Actions best practices and tool architecture, with bidirectional cross-links to the tool page and individual rule documentation pages. Wire the blog post into the [slug].astro template with GHA-specific FAQPageJsonLd, aboutDataset, and articleSection.

Purpose: Drive organic traffic to the GitHub Actions Validator through a comprehensive best-practices guide that targets high-value search keywords (github actions best practices, github actions security, workflow yaml linter).
Output: Published MDX blog post at /blog/github-actions-best-practices/ with structured data integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-site-integration-and-blog-post/81-RESEARCH.md
@src/pages/blog/[slug].astro
@src/data/blog/kubernetes-manifest-best-practices.mdx
@src/lib/tools/gha-validator/rules/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create companion blog post MDX</name>
  <files>src/data/blog/github-actions-best-practices.mdx</files>
  <action>
Create `src/data/blog/github-actions-best-practices.mdx` following the `kubernetes-manifest-best-practices.mdx` pattern. Target 2000-3000 words.

Frontmatter:
```yaml
---
title: "GitHub Actions Best Practices"
description: "Production-tested guide to writing secure GitHub Actions workflows. 48 rules across security, semantic correctness, best practices, and style with fix examples."
publishedDate: 2026-03-04
tags: ["github-actions", "ci-cd", "devops", "security", "yaml", "automation"]
draft: false
---
```

Component imports (use relative paths from src/data/blog/):
```
import OpeningStatement from '../../components/blog/OpeningStatement.astro';
import TldrSummary from '../../components/blog/TldrSummary.astro';
import KeyTakeaway from '../../components/blog/KeyTakeaway.astro';
import Callout from '../../components/blog/Callout.astro';
```

Content structure:

1. **OpeningStatement**: Bold hook about how most GitHub Actions workflows in production have preventable security issues.

2. **TldrSummary**: Bullet list summarizing 48 rules, 6 categories, two-pass analysis, scoring system, 100% client-side, link to tool.

3. **Introduction**: Why GitHub Actions security matters. Context about CI/CD supply-chain attacks. Mention building the validator after reviewing hundreds of pipelines over 17 years.

4. **How the Validator Works**: Explain two-pass architecture -- Pass 1 (instant schema + custom rules) and Pass 2 (actionlint WASM for deep semantic analysis). Mention deduplication of overlapping diagnostics. Category-weighted scoring with letter grades.

5. **Security Rules (GA-C001 through GA-C010)**: The most critical category. Cover:
   - Pinning actions to SHA ([GA-C001](/tools/gha-validator/rules/ga-c001/)) with before/after code examples
   - Permissions ([GA-C003](/tools/gha-validator/rules/ga-c003/), [GA-C004](/tools/gha-validator/rules/ga-c004/)) -- explicit least-privilege
   - Script injection ([GA-C005](/tools/gha-validator/rules/ga-c005/)) with dangerous `${{ github.event }}` contexts
   - pull_request_target ([GA-C006](/tools/gha-validator/rules/ga-c006/)) pitfalls
   - Hardcoded secrets ([GA-C007](/tools/gha-validator/rules/ga-c007/))
   - Use KeyTakeaway for "Top 3 Security Rules"

6. **Best Practice Rules (GA-B001 through GA-B008)**: Cover:
   - Timeout-minutes ([GA-B001](/tools/gha-validator/rules/ga-b001/))
   - Concurrency groups ([GA-B002](/tools/gha-validator/rules/ga-b002/))
   - Step naming ([GA-B003](/tools/gha-validator/rules/ga-b003/), [GA-B004](/tools/gha-validator/rules/ga-b004/))
   - Outdated actions ([GA-B007](/tools/gha-validator/rules/ga-b007/))

7. **Semantic Rules (GA-L001 through GA-L018)**: Explain actionlint WASM integration. Cover key semantic checks:
   - Invalid expressions ([GA-L001](/tools/gha-validator/rules/ga-l001/))
   - Unknown context properties ([GA-L005](/tools/gha-validator/rules/ga-l005/))
   - Type mismatches ([GA-L004](/tools/gha-validator/rules/ga-l004/))
   - Note GA-L017 (shellcheck) and GA-L018 (pyflakes) browser WASM limitations

8. **Style Rules (GA-F001 through GA-F004)**: Brief coverage:
   - Job ordering ([GA-F001](/tools/gha-validator/rules/ga-f001/))
   - Quoting consistency ([GA-F002](/tools/gha-validator/rules/ga-f002/))
   - Workflow name ([GA-F004](/tools/gha-validator/rules/ga-f004/))

9. **Schema Rules (GA-S001 through GA-S008)**: Brief coverage of structural validation.

10. **Scoring Methodology**: Category weights -- Security 35%, Semantic 20%, Best Practice 20%, Schema 15%, Style 10%. Diminishing returns formula. Letter grade thresholds. Use Callout for weight breakdown.

11. **Getting Started**: CTA paragraph linking to [GitHub Actions Workflow Validator](/tools/gha-validator/). Mention the tool is 100% client-side, code never leaves the browser. Mention browsing all 48 rules starting from [GA-C001](/tools/gha-validator/rules/ga-c001/).

All rule references MUST be lowercase in URLs: `/tools/gha-validator/rules/ga-c001/` (not `GA-C001`).
Each rule link uses the format `[GA-C001](/tools/gha-validator/rules/ga-c001/)`.
Do NOT create code fences with triple backticks inside MDX component children (use `<pre>` or inline code instead).
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Blog post file exists at src/data/blog/github-actions-best-practices.mdx
    - Blog post has correct frontmatter (title, description, publishedDate, tags, draft: false)
    - Blog post is 2000+ words covering all 6 rule categories
    - Blog post links to /tools/gha-validator/ (tool CTA)
    - Blog post links to 15+ individual rule documentation pages with lowercase URLs
    - Blog post uses OpeningStatement, TldrSummary, KeyTakeaway, Callout components
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire blog post into [slug].astro with structured data</name>
  <files>src/pages/blog/[slug].astro</files>
  <action>
Update `src/pages/blog/[slug].astro` to add GHA blog post integration, following the exact pattern used for isK8sPost, isComposePost, and isDockerfilePost.

1. Add `isGhaPost` constant after the existing `isDockerfilePost` line (around line 43):
   ```
   const isGhaPost = post.id === 'github-actions-best-practices';
   ```

2. Update `articleSection` chain (around line 44-48) -- add before the final `: undefined`:
   ```
   : isGhaPost ? 'GitHub Actions Security'
   ```

3. Update `aboutDataset` chain (around line 49-57) -- add before the final `: undefined`:
   ```
   : isGhaPost
     ? { type: 'SoftwareApplication', name: 'GitHub Actions Workflow Validator', url: 'https://patrykgolabek.dev/tools/gha-validator/' }
   ```

4. Add `ghaFAQ` array after the `dockerfileFAQ` block (around line 130):
   ```typescript
   const ghaFAQ = isGhaPost ? [
     {
       question: 'What is a GitHub Actions workflow validator?',
       answer: 'A GitHub Actions workflow validator checks your .github/workflows/*.yml files for schema compliance, security vulnerabilities, semantic errors, and best-practice violations. The GitHub Actions Validator at patrykgolabek.dev/tools/gha-validator/ runs 48 rules with two-pass analysis including actionlint WASM. It runs 100% in the browser.',
     },
     {
       question: 'How do I secure my GitHub Actions workflows?',
       answer: 'Pin all third-party actions to full SHA commits instead of mutable tags. Set explicit permissions with least-privilege scopes instead of write-all. Never use untrusted input in run: scripts to prevent script injection. Avoid pull_request_target with checkout of PR code. Use environment secrets instead of hardcoded credentials.',
     },
     {
       question: 'What is a free online GitHub Actions linter?',
       answer: 'The GitHub Actions Workflow Validator at patrykgolabek.dev/tools/gha-validator/ is a free online tool that checks 48 rules across schema, security, semantic, best-practice, style, and actionlint categories. It uses two-pass analysis with actionlint WASM for deep semantic checking. 100% browser-based.',
     },
   ] : [];
   ```

5. Add FAQPageJsonLd rendering after the existing `dockerfileFAQ` conditional (around line 208):
   ```
   {ghaFAQ.length > 0 && <FAQPageJsonLd items={ghaFAQ} />}
   ```

Maintain exact formatting and style conventions of the existing file. Each new block follows the identical pattern of its predecessor (isDockerfilePost pattern).
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - [slug].astro has isGhaPost = post.id === 'github-actions-best-practices'
    - articleSection returns 'GitHub Actions Security' for GHA post
    - aboutDataset returns SoftwareApplication with GHA Validator URL for GHA post
    - ghaFAQ array has 3 FAQ items for GHA post
    - FAQPageJsonLd renders conditionally for GHA post
    - Build succeeds with the blog post rendering at /blog/github-actions-best-practices/
  </done>
</task>

</tasks>

<verification>
1. `npx astro build` completes without errors
2. Blog post renders at /blog/github-actions-best-practices/ with full content
3. Blog post page source contains FAQPageJsonLd with 3 GHA-specific questions
4. Blog post page source contains aboutDataset with SoftwareApplication type
5. Blog post page source contains articleSection "GitHub Actions Security"
6. Blog post body contains links to /tools/gha-validator/ (tool CTA)
7. Blog post body contains links to /tools/gha-validator/rules/ga-c001/ and other rule pages
8. Blog post OG image auto-generated via existing [...slug].png.ts catch-all route
</verification>

<success_criteria>
All 3 BLOG-* requirements are satisfied: comprehensive companion blog post exists covering GitHub Actions best practices and tool architecture (BLOG-01), bidirectional cross-links exist between blog post and tool page (BLOG-02), blog post links to individual rule documentation pages (BLOG-03).
</success_criteria>

<output>
After completion, create `.planning/phases/81-site-integration-and-blog-post/81-02-SUMMARY.md`
</output>
