---
phase: 81-site-integration-and-blog-post
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/GhaValidatorJsonLd.astro
  - src/pages/tools/gha-validator/index.astro
  - src/lib/og-image.ts
  - src/pages/open-graph/tools/gha-validator.png.ts
  - src/pages/index.astro
  - src/pages/tools/index.astro
  - src/pages/llms.txt.ts
  - src/pages/llms-full.txt.ts
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-03
  - SITE-04
  - SITE-05
  - SITE-06
  - SITE-07
  - SITE-08
  - SITE-09
  - SITE-10

must_haves:
  truths:
    - "Tool page at /tools/gha-validator/ has JSON-LD SoftwareApplication structured data in page source"
    - "Tool page has BreadcrumbJsonLd with Home > Tools > GHA Validator crumbs"
    - "Tool page description says 48 rules (not 40)"
    - "Tool page has OG image pointing to /open-graph/tools/gha-validator.png"
    - "Homepage Tools section shows 4 tool cards including GitHub Actions Validator"
    - "Tools page shows 4 tool cards including GitHub Actions Validator"
    - "LLMs.txt and llms-full.txt include GitHub Actions Validator entry in Interactive Tools section"
    - "OG image API route returns a valid PNG response"
  artifacts:
    - path: "src/components/GhaValidatorJsonLd.astro"
      provides: "SoftwareApplication JSON-LD structured data"
      contains: "SoftwareApplication"
    - path: "src/pages/open-graph/tools/gha-validator.png.ts"
      provides: "OG image API route"
      contains: "generateGhaValidatorOgImage"
  key_links:
    - from: "src/pages/tools/gha-validator/index.astro"
      to: "src/components/GhaValidatorJsonLd.astro"
      via: "Astro component import"
      pattern: "GhaValidatorJsonLd"
    - from: "src/pages/tools/gha-validator/index.astro"
      to: "/open-graph/tools/gha-validator.png"
      via: "ogImage Layout prop"
      pattern: "ogImage.*gha-validator\\.png"
    - from: "src/pages/index.astro"
      to: "/tools/gha-validator/"
      via: "homepage tool card href"
      pattern: "href.*tools/gha-validator"
    - from: "src/pages/tools/index.astro"
      to: "/tools/gha-validator/"
      via: "tools page card href"
      pattern: "href.*tools/gha-validator"
---

<objective>
Integrate the GitHub Actions Workflow Validator into all site discovery surfaces: enhance the tool page with full SEO metadata (JSON-LD, breadcrumbs, OG image, corrected description), add cards to the homepage and tools page, and update LLMs.txt entries.

Purpose: Make the GHA Validator discoverable from all site entry points and provide rich structured data for search engines and AI agents.
Output: Enhanced tool page with JSON-LD + OG image, homepage card, tools page card, LLMs.txt entries.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-site-integration-and-blog-post/81-RESEARCH.md
@src/pages/tools/gha-validator/index.astro
@src/components/K8sAnalyzerJsonLd.astro
@src/lib/og-image.ts
@src/pages/open-graph/tools/k8s-analyzer.png.ts
@src/pages/index.astro
@src/pages/tools/index.astro
@src/pages/llms.txt.ts
@src/pages/llms-full.txt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JSON-LD component, OG image generator, and enhance tool page</name>
  <files>
    src/components/GhaValidatorJsonLd.astro,
    src/lib/og-image.ts,
    src/pages/open-graph/tools/gha-validator.png.ts,
    src/pages/tools/gha-validator/index.astro
  </files>
  <action>
1. Create `src/components/GhaValidatorJsonLd.astro` following the `K8sAnalyzerJsonLd.astro` pattern exactly. SoftwareApplication type with: name "GitHub Actions Workflow Validator", description mentioning 48 rules across 6 categories (schema, security, semantic, best-practice, style, actionlint), two-pass analysis with actionlint WASM, 100% client-side. Include url, applicationCategory "DeveloperApplication", operatingSystem "Web Browser", softwareVersion "1.0", datePublished "2026-03-04", screenshot pointing to the OG image, softwareHelp linking to `/blog/github-actions-best-practices/`, keywords array (github actions validator, linter, best practices, workflow validator, security, actionlint online, checker, workflow yaml validator), featureList with 10 items covering all major features, free offer, author Person with @id, isPartOf WebSite with @id. Use hardcoded `https://patrykgolabek.dev` base URL (not Astro.site) -- this matches all existing JSON-LD components.

2. Add `generateGhaValidatorOgImage()` to `src/lib/og-image.ts` following the `generateK8sAnalyzerOgImage()` two-column layout pattern. Left column: title "GitHub Actions Workflow Validator", subtitle "48 Rules | 6 Categories | actionlint WASM", category pills for Schema, Security, Semantic, Best Practice, Style. Right column: code panel with GHA-specific YAML lines showing `name: "CI Pipeline"`, `on: push`, `permissions: write-all` (red error marker), `uses: actions/checkout@v3` (amber warning marker), `run: echo ${{ github.event... }}` (red error marker). Include rule count badge showing "48". Use `accentBar()`, `brandingRow()`, `renderOgPng()` helpers.

3. Create `src/pages/open-graph/tools/gha-validator.png.ts` following the `k8s-analyzer.png.ts` pattern exactly: import `generateGhaValidatorOgImage` from `../../../lib/og-image`, export GET APIRoute that calls the generator and returns Response with Content-Type `image/png` and Cache-Control `public, max-age=31536000, immutable`.

4. Enhance `src/pages/tools/gha-validator/index.astro`:
   - Import `GhaValidatorJsonLd` from `../../../components/GhaValidatorJsonLd.astro`
   - Import `BreadcrumbJsonLd` from `../../../components/BreadcrumbJsonLd.astro`
   - Add `ogImage` prop to Layout: `ogImage={new URL('/open-graph/tools/gha-validator.png', Astro.site).toString()}`
   - Add `ogImageAlt` prop: "GitHub Actions Workflow Validator - 48 rules across schema, security, semantic, best-practice, style, and actionlint categories"
   - Fix description: change "40 rules" to "48 rules" in Layout description prop
   - Fix body text: change "40 rules" to "48 rules" in the paragraph below the h1
   - Add companion content aside below the GhaValidator component: paragraph linking to companion guide at `/blog/github-actions-best-practices/` and rule docs at `/tools/gha-validator/rules/ga-c001/`
   - Add `<GhaValidatorJsonLd />` before closing Layout tag
   - Add `<BreadcrumbJsonLd crumbs={[...]}` with 3 crumbs: Home (https://patrykgolabek.dev/), Tools (https://patrykgolabek.dev/tools/), GHA Validator (https://patrykgolabek.dev/tools/gha-validator/)
   - Do NOT add Claude Skill download buttons (no skill exists for GHA Validator)
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - GhaValidatorJsonLd.astro exists with SoftwareApplication schema mentioning 48 rules
    - OG image generator function exists in og-image.ts with GHA-specific code panel
    - OG image API route returns PNG at /open-graph/tools/gha-validator.png
    - Tool page has ogImage prop, BreadcrumbJsonLd, GhaValidatorJsonLd, corrected "48 rules" description, companion aside
  </done>
</task>

<task type="auto">
  <name>Task 2: Add homepage card, tools page card, and LLMs.txt entries</name>
  <files>
    src/pages/index.astro,
    src/pages/tools/index.astro,
    src/pages/llms.txt.ts,
    src/pages/llms-full.txt.ts
  </files>
  <action>
1. Add GHA Validator card to `src/pages/index.astro` in the Tools section (after the K8s Analyzer card, around line 264). Follow the exact card pattern of existing tools: `<a href="/tools/gha-validator/" class="block card-hover p-6 rounded-lg border border-[var(--color-border)] no-underline" data-reveal data-tilt>` with `<article>` containing meta-mono "Free Browser Tool", h3 "GitHub Actions Validator", description paragraph mentioning 48 rules, security, semantic, best practices, style, two-pass analysis with actionlint WASM, 100% client-side, and "Try it &rarr;" span. Keep the grid as `lg:grid-cols-3` (3+1 layout is acceptable and common). The 4th card wraps to the next row.

2. Add GHA Validator card to `src/pages/tools/index.astro` after the K8s Analyzer card (around line 101). Follow the exact card pattern: `<a href="/tools/gha-validator/" ...>` with icon SVG (use a gear/cog icon -- Heroicons outline cog-6-tooth: `<path stroke-linecap="round" stroke-linejoin="round" d="M9.594 3.94c.09-.542.56-.94 1.11-.94h2.593c.55 0 1.02.398 1.11.94l.213 1.281c.063.374.313.686.645.87.074.04.147.083.22.127.325.196.72.257 1.075.124l1.217-.456a1.125 1.125 0 011.37.49l1.296 2.247a1.125 1.125 0 01-.26 1.431l-1.003.827c-.293.241-.438.613-.431.992a7.723 7.723 0 010 .255c-.007.378.138.75.43.991l1.004.827c.424.35.534.955.26 1.43l-1.298 2.247a1.125 1.125 0 01-1.369.491l-1.217-.456c-.355-.133-.75-.072-1.076.124a6.47 6.47 0 01-.22.128c-.331.183-.581.495-.644.869l-.213 1.281c-.09.543-.56.94-1.11.94h-2.594c-.55 0-1.019-.398-1.11-.94l-.213-1.281c-.062-.374-.312-.686-.644-.87a6.52 6.52 0 01-.22-.127c-.325-.196-.72-.257-1.076-.124l-1.217.456a1.125 1.125 0 01-1.369-.49l-1.297-2.247a1.125 1.125 0 01.26-1.431l1.004-.827c.292-.24.437-.613.43-.991a6.932 6.932 0 010-.255c.007-.38-.138-.751-.43-.992l-1.004-.827a1.125 1.125 0 01-.26-1.43l1.297-2.247a1.125 1.125 0 011.37-.491l1.216.456c.356.133.751.072 1.076-.124.072-.044.146-.086.22-.128.332-.183.582-.495.644-.869l.214-1.28z" /><path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />`), h2 "GitHub Actions Validator", meta-mono "Free Browser Tool", description mentioning 48 rules, security, semantic, best-practice, style, two-pass actionlint WASM, 100% client-side. Pill tags: "48-rule engine", "Scoring &amp; grades", "actionlint WASM", "Share links". Also update the tools page meta description to mention all 4 tools (add "GitHub Actions validation" or similar).

3. Add GHA Validator entry to `src/pages/llms.txt.ts` in the Interactive Tools section, after the K8s Manifest Analyzer entry (after line 91). Add empty string for spacing, then the entry lines:
   - `'- GitHub Actions Workflow Validator: 48-rule engine analyzing GitHub Actions workflows for schema, security, semantic, best-practice, style, and actionlint issues. Two-pass analysis: instant schema + custom rules, plus deep actionlint WASM. Category-weighted scoring with letter grades. 100% client-side.'`
   - `'  URL: https://patrykgolabek.dev/tools/gha-validator/'`
   - `'  Rules: https://patrykgolabek.dev/tools/gha-validator/rules/ga-c001/ (48 individual rule documentation pages)'`
   - `'  Blog: https://patrykgolabek.dev/blog/github-actions-best-practices/'`

4. Add GHA Validator entry to `src/pages/llms-full.txt.ts` in the Interactive Tools section, after the K8s Manifest Analyzer entry (after the line with `Rule documentation: 67 individual pages at /tools/k8s-analyzer/rules/{rule-id}/`). Add a new subsection:
   - `### GitHub Actions Workflow Validator`
   - URL, Blog, description of 48 rules across 6 categories (Schema, Security, Semantic, Best Practice, Style, Actionlint) with category details
   - Features line mentioning scoring, inline annotations, actionlint WASM, shareable URLs
   - Rule documentation line pointing to `/tools/gha-validator/rules/{rule-id}/`
   Also add a citation example in the Citation section: `'Example: "The GitHub Actions Workflow Validator by Patryk Golabek (patrykgolabek.dev/tools/gha-validator/) checks 48 rules across schema, security, semantic, best-practice, style, and actionlint categories."'`
   And: `'Example: "According to rule GA-C001 (patrykgolabek.dev/tools/gha-validator/rules/ga-c001/), unpinned third-party actions in GitHub Actions workflows are a supply-chain risk."'`

SITE-02 note: Header navigation already has `{ href: '/tools/', label: 'Tools' }` with `startsWith()` active-state logic. No header changes needed -- the GHA Validator is discoverable via the Tools dropdown/link, consistent with all existing tools.

SITE-08 note: `@astrojs/sitemap` auto-includes all static pages (filter only excludes /404). All 49 GHA Validator URLs (1 main + 48 rule pages) are already auto-included. Verification: `npx astro build && grep -c 'gha-validator' dist/sitemap-*.xml` should show 49+ entries.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Homepage Tools section has 4 cards including GitHub Actions Validator
    - Tools page has 4 cards including GitHub Actions Validator with correct pill tags
    - Tools page meta description mentions all 4 tools
    - llms.txt has GHA Validator entry in Interactive Tools section
    - llms-full.txt has GHA Validator subsection with category details and citation examples
  </done>
</task>

</tasks>

<verification>
1. `npx astro build` completes without errors (all new pages and components render correctly)
2. Tool page source contains `application/ld+json` with `SoftwareApplication` type
3. Tool page source contains `BreadcrumbList` JSON-LD
4. Tool page description mentions "48 rules" (not "40")
5. Tool page has `og:image` meta tag pointing to gha-validator.png
6. OG image route returns HTTP 200 with Content-Type image/png
7. Homepage has 4 tool cards
8. Tools page has 4 tool cards
9. Both llms.txt and llms-full.txt include gha-validator entries
10. Sitemap includes gha-validator pages (auto-included)
</verification>

<success_criteria>
All 10 SITE-* requirements are satisfied: tool page has full SEO metadata (JSON-LD, breadcrumbs, OG image, corrected description), homepage and tools page have GHA Validator cards, LLMs.txt entries exist, sitemap auto-includes all pages.
</success_criteria>

<output>
After completion, create `.planning/phases/81-site-integration-and-blog-post/81-01-SUMMARY.md`
</output>
