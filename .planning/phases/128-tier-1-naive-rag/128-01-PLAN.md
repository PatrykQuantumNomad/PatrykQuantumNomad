---
phase: 128-tier-1-naive-rag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "../rag-architecture-patterns/pyproject.toml"
  - "../rag-architecture-patterns/.env.example"
  - "../rag-architecture-patterns/tier-1-naive/requirements.txt"
  - "../rag-architecture-patterns/.gitignore"
autonomous: true
requirements: [TIER-01]
user_setup:
  - service: openai
    why: "Tier 1 embeddings call OpenAI text-embedding-3-small (~$0.011 one-time ingest)"
    env_vars:
      - name: OPENAI_API_KEY
        source: "https://platform.openai.com/api-keys"
    dashboard_config:
      - task: "Create an API key with default permissions; copy into local .env (gitignored)"
        location: "OpenAI dashboard"

must_haves:
  truths:
    - "uv pip install -e \".[tier-1]\" pulls chromadb, openai, pymupdf cleanly without conflict"
    - "tier-1-naive/requirements.txt mirrors the [tier-1] extras (`-e ..[tier-1]`) — no duplication"
    - ".env.example documents OPENAI_API_KEY as REQUIRED for Tier 1 (embeddings) — comment text reflects this"
    - "chroma_db/ remains gitignored (already in .gitignore from Phase 127); confirm presence; no new entry needed"
    - "shared/embeddings.py is NOT modified (smoke test depends on Gemini-only contract)"
  artifacts:
    - path: "../rag-architecture-patterns/pyproject.toml"
      provides: "Concrete tier-1 dep list (chromadb, openai, pymupdf) extending [shared]"
      contains: "chromadb>=1.5.8"
    - path: "../rag-architecture-patterns/.env.example"
      provides: "Promoted OPENAI_API_KEY comment marking Tier 1 requirement"
      contains: "REQUIRED for Tier 1"
    - path: "../rag-architecture-patterns/tier-1-naive/requirements.txt"
      provides: "Tier 1 dep mirror (unchanged content; verified resolves after pyproject update)"
      contains: "-e ..[tier-1]"
  key_links:
    - from: "tier-1-naive/requirements.txt"
      to: "pyproject.toml [project.optional-dependencies.tier-1]"
      via: "editable install of parent extras"
      pattern: "-e \\.\\.\\[tier-1\\]"
---

<objective>
Promote Tier 1's pyproject extras from a stub (`["rag-architecture-patterns[shared]"]`) to a concrete dependency list (chromadb, openai, pymupdf) and update `.env.example` so OPENAI_API_KEY is documented as REQUIRED for Tier 1. This plan does NOT touch `shared/embeddings.py` (Gemini-only contract from Phase 127) and does NOT install packages — it only edits config files so Plans 02-04 can install their tools.

Purpose: Lock the dep contract before any tier-1 code lands, so Plans 02 (ingest/chunking — needs pymupdf + tiktoken) and Plan 03 (embed/store — needs chromadb + openai) can install in parallel with confidence.

Output: Updated `pyproject.toml`, `.env.example`, and untouched-but-validated `tier-1-naive/requirements.txt`. No Python code yet.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-tier-1-naive-rag/128-RESEARCH.md

<interfaces>
<!-- Locked decisions from 128-RESEARCH.md "User Constraints" -->

Tier 1 extras shape (verbatim from 128-RESEARCH.md "Standard Stack"):
```toml
tier-1 = [
  "rag-architecture-patterns[shared]",
  "chromadb>=1.5.8,<2",
  "openai>=1.50,<3",
  "pymupdf>=1.27,<2",
]
```

Pinning rationale (verified via `pip3 index versions` 2026-04-26):
- chromadb 1.5.8 latest; major version 2.x not yet released; pin <2 for safety.
- openai 2.32 latest; SDK 1.x is also valid (Tier 1 uses only `client.embeddings.create`, stable across 1.x and 2.x).
- pymupdf 1.27 already pinned in [curation]; lift to [tier-1] for the same version surface.

.env.example update (only the OPENAI_API_KEY comment block changes; the empty-default key line stays as-is):
```
# REQUIRED for Tier 1 (embeddings: text-embedding-3-small), Tier 2 (Managed File Search), Tier 5 (Agentic).
#   Obtain at: https://platform.openai.com/api-keys
```

DO NOT touch:
- shared/embeddings.py (Gemini-only by Phase 127 contract; smoke test depends on it).
- shared/config.py (`openai_api_key: SecretStr | None` already declared correctly).
- chroma_db/ entry in .gitignore (already present from Phase 127).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend [tier-1] extras with chromadb, openai, pymupdf and verify dry-run resolution</name>
  <files>
    ../rag-architecture-patterns/pyproject.toml
    ../rag-architecture-patterns/tier-1-naive/requirements.txt
  </files>
  <action>
All work happens in `../rag-architecture-patterns/`. NOT in this profile repo.

1. Read the current `pyproject.toml` to confirm the existing shape: the `[project.optional-dependencies]` block has `tier-1 = ["rag-architecture-patterns[shared]"]` as a stub.

2. Replace ONLY the `tier-1 = [...]` line with the concrete tier-1 dep list (verbatim from `<interfaces>` above):
   ```toml
   tier-1 = [
     "rag-architecture-patterns[shared]",
     "chromadb>=1.5.8,<2",
     "openai>=1.50,<3",
     "pymupdf>=1.27,<2",
   ]
   ```
   Do NOT modify any other tier extras (tier-2..tier-5 stay as `["rag-architecture-patterns[shared]"]`). Do NOT touch [shared], [curation], [dependency-groups], or [tool.*] sections.

3. Confirm `tier-1-naive/requirements.txt` already contains `-e ..[tier-1]` (it should, from Phase 127). If it does, leave it untouched — the `-e ..[tier-1]` line picks up the new deps automatically. If for some reason it differs, restore it to exactly:
   ```
   # Tier 1 (Naive RAG) — mirrors [project.optional-dependencies.tier-1] from ../pyproject.toml.
   # Concrete tier-specific deps (chromadb, openai, pymupdf) live in pyproject.toml [tier-1] (Phase 128 Plan 01).
   -e ..[tier-1]
   ```

4. Verify the pyproject parses and resolves with the new deps using a dry-run:
   ```
   cd ../rag-architecture-patterns
   uv pip install --dry-run -e ".[tier-1]" --python python3.11 2>&1 | tail -20
   ```
   Expect uv to print a resolved plan listing chromadb, openai, pymupdf among the would-install packages. If it errors with "no matching distribution" for any pin, INVESTIGATE the pin — do NOT loosen blindly. The pins were verified against PyPI on 2026-04-26.

5. Stage and commit ONLY the two changed files (per Phase 127 Pitfall 5 — never `git add -A`):
   ```
   git add pyproject.toml tier-1-naive/requirements.txt
   git commit -m "deps(128-01): extend [tier-1] extras with chromadb, openai, pymupdf"
   git push origin main
   ```

Decisions referenced (128-RESEARCH.md): Option A embedding-vendor reconciliation — TIER-01 says "OpenAI embeddings"; Tier 1 keeps Gemini for chat (`shared/llm`) and adds OpenAI only for embeddings. shared/embeddings.py stays Gemini-only.

DO NOT actually install the deps in this task — Plans 02-04 will install when they need them. This task is config-only.
  </action>
  <verify>
    <automated>
cd ../rag-architecture-patterns &amp;&amp; \
  test -f pyproject.toml &amp;&amp; \
  grep -q 'chromadb&gt;=1.5.8' pyproject.toml &amp;&amp; \
  grep -q 'openai&gt;=1.50' pyproject.toml &amp;&amp; \
  grep -q 'pymupdf&gt;=1.27' pyproject.toml &amp;&amp; \
  test -f tier-1-naive/requirements.txt &amp;&amp; \
  grep -q '\-e \.\.\[tier-1\]' tier-1-naive/requirements.txt &amp;&amp; \
  uv pip install --dry-run -e ".[tier-1]" --python python3.11 &gt; /dev/null 2&gt;&amp;1
    </automated>
  </verify>
  <done>
    pyproject.toml [tier-1] now lists chromadb, openai, pymupdf with verified pins.
    `uv pip install --dry-run -e ".[tier-1]"` resolves without errors.
    tier-1-naive/requirements.txt unchanged (already mirrors via `-e ..[tier-1]`).
    Commit pushed to origin/main.
  </done>
</task>

<task type="auto">
  <name>Task 2: Promote OPENAI_API_KEY in .env.example from optional to REQUIRED for Tier 1</name>
  <files>
    ../rag-architecture-patterns/.env.example
  </files>
  <action>
All work happens in `../rag-architecture-patterns/`.

1. Read the current `.env.example`. The comment block above OPENAI_API_KEY currently reads:
   ```
   # OPTIONAL: OPENAI_API_KEY is needed for Tier 2 (Managed File Search) and Tier 5 (Agentic).
   #   Obtain at: https://platform.openai.com/api-keys
   ```

2. Replace ONLY that comment block with the promoted text (verbatim from `<interfaces>` above):
   ```
   # REQUIRED for Tier 1 (embeddings: text-embedding-3-small), Tier 2 (Managed File Search), Tier 5 (Agentic).
   #   Obtain at: https://platform.openai.com/api-keys
   ```

3. Leave the empty-default key line `OPENAI_API_KEY=` UNCHANGED — it's a placeholder that the user fills in their local (gitignored) `.env`. Do NOT inject a real key.

4. Leave every other line (GEMINI_API_KEY block, S2_API_KEY block, DEFAULT_*, DATASET_ROOT) UNCHANGED.

5. Update `tests/test_env_example.py` ONLY if it asserts on the literal "OPTIONAL" string for OpenAI. Read the file first:
   ```
   grep -n 'OPENAI_API_KEY' tests/test_env_example.py
   ```
   If a test asserts `OPTIONAL: OPENAI_API_KEY`, update the assertion to `REQUIRED for Tier 1` to match the new comment. If no such assertion exists, leave the file alone.

6. Run the env-example test to confirm:
   ```
   cd ../rag-architecture-patterns
   uv run pytest tests/test_env_example.py -q
   ```
   Should pass. If the test still asserts "OPTIONAL" and you didn't update it, fix and retry.

7. Stage and commit:
   ```
   git add .env.example tests/test_env_example.py
   git commit -m "docs(128-01): promote OPENAI_API_KEY to REQUIRED for Tier 1 in .env.example"
   git push origin main
   ```
   (If `tests/test_env_example.py` was not modified, drop it from `git add`.)

Decisions referenced (128-RESEARCH.md "User Constraints"): "Updating .env.example to mark OPENAI_API_KEY as REQUIRED for tier-1, tier-2, tier-5 — promote the comment, do not re-key." `shared/config.py` already has `openai_api_key: SecretStr | None` — Tier 1's runtime guard (Plan 04) handles the None-check; no schema change needed.
  </action>
  <verify>
    <automated>
cd ../rag-architecture-patterns &amp;&amp; \
  test -f .env.example &amp;&amp; \
  grep -q '^OPENAI_API_KEY=' .env.example &amp;&amp; \
  grep -q 'REQUIRED for Tier 1' .env.example &amp;&amp; \
  ! grep -q '^# OPTIONAL: OPENAI_API_KEY' .env.example &amp;&amp; \
  grep -q 'GEMINI_API_KEY=your_gemini_api_key_here' .env.example &amp;&amp; \
  uv run pytest tests/test_env_example.py -q &gt; /dev/null 2&gt;&amp;1
    </automated>
  </verify>
  <done>
    .env.example comment for OPENAI_API_KEY now marks it REQUIRED for Tier 1.
    GEMINI_API_KEY block, S2_API_KEY block, and DEFAULT_*/DATASET_ROOT lines unchanged.
    tests/test_env_example.py passes (updated only if it asserted on the old comment).
    Commit pushed to origin/main.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run from inside `../rag-architecture-patterns/`:

```
# 1. Tier-1 extras lock in chromadb + openai + pymupdf
grep -A4 'tier-1 = \[' pyproject.toml | grep -q 'chromadb'
grep -A4 'tier-1 = \[' pyproject.toml | grep -q 'openai'
grep -A4 'tier-1 = \[' pyproject.toml | grep -q 'pymupdf'

# 2. tier-1 dry-run resolves
uv pip install --dry-run -e ".[tier-1]" --python python3.11 > /dev/null

# 3. .env.example promoted comment
grep -q 'REQUIRED for Tier 1' .env.example
! grep -q 'OPTIONAL: OPENAI_API_KEY' .env.example

# 4. shared/embeddings.py NOT touched
git log --oneline -1 shared/embeddings.py | grep -v '128-'

# 5. existing tests still pass
uv run pytest tests/test_env_example.py tests/test_pricing.py -q
```
</verification>

<success_criteria>
- `pyproject.toml [tier-1]` lists chromadb>=1.5.8,<2, openai>=1.50,<3, pymupdf>=1.27,<2
- `uv pip install --dry-run -e ".[tier-1]"` resolves cleanly
- `.env.example` documents OPENAI_API_KEY as REQUIRED for Tier 1 (comment text); empty default value preserved
- `tier-1-naive/requirements.txt` still contains `-e ..[tier-1]` and now transitively pulls the new deps
- `shared/embeddings.py` is NOT modified (Gemini-only contract intact)
- All changes committed and pushed to origin/main
</success_criteria>

<output>
After completion, create `.planning/phases/128-tier-1-naive-rag/128-01-SUMMARY.md` documenting:
- Final `[tier-1]` extras list (verbatim)
- `uv pip install --dry-run` resolution output (last 10 lines)
- Confirmation that shared/embeddings.py was not modified (`git log --oneline -1 shared/embeddings.py`)
- Any pin adjustments needed (none expected — research verified all pins on 2026-04-26)
</output>
