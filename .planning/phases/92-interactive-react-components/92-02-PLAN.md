---
phase: 92-interactive-react-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/guides/interactive-data/hook-event-data.ts
  - src/lib/guides/interactive-data/__tests__/hook-event-data.test.ts
  - src/components/guide/HookEventVisualizer.tsx
  - src/components/guide/hook-events/HookEventNode.tsx
  - src/components/guide/hook-events/HookCategoryNode.tsx
  - src/components/guide/hook-events/HookDetailPanel.tsx
  - src/components/guide/hook-events/hook-events.css
autonomous: true
requirements: [INTV-02]

must_haves:
  truths:
    - "The hook event visualizer renders all 18 hook events organized in 3 categories (Session 2, Loop 12, Standalone Async 4)"
    - "Clicking an event node reveals a detail panel below the canvas showing its handler types, payload fields, and configuration example"
    - "PreToolUse is visually distinguished with an accent indicator and CAN BLOCK label"
    - "Clicking the same event again (or Close button) dismisses the detail panel"
    - "The component uses CSS custom properties for dark/light theme support"
    - "The component is exported as default for client:visible lazy hydration in MDX"
  artifacts:
    - path: "src/lib/guides/interactive-data/hook-event-data.ts"
      provides: "Node/edge definitions and payload data for 18 hook events"
      exports: ["rawNodes", "rawEdges", "detailContent"]
    - path: "src/lib/guides/interactive-data/__tests__/hook-event-data.test.ts"
      provides: "Data integrity tests for hook event data"
      contains: "PreToolUse"
    - path: "src/components/guide/HookEventVisualizer.tsx"
      provides: "Main React Flow interactive component for hook events"
      exports: ["default"]
      min_lines: 50
    - path: "src/components/guide/hook-events/HookEventNode.tsx"
      provides: "Custom event node with PreToolUse blocking indicator"
      exports: ["HookEventNode"]
    - path: "src/components/guide/hook-events/HookCategoryNode.tsx"
      provides: "Category header node for event grouping"
      exports: ["HookCategoryNode"]
    - path: "src/components/guide/hook-events/HookDetailPanel.tsx"
      provides: "Payload detail panel shown on event click"
      exports: ["HookDetailPanel"]
    - path: "src/components/guide/hook-events/hook-events.css"
      provides: "React Flow theme overrides using CSS custom properties"
      contains: "hook-event-visualizer"
  key_links:
    - from: "src/components/guide/HookEventVisualizer.tsx"
      to: "src/lib/guides/interactive-data/hook-event-data.ts"
      via: "import { rawNodes, rawEdges, detailContent }"
      pattern: "from.*interactive-data/hook-event-data"
    - from: "src/components/guide/HookEventVisualizer.tsx"
      to: "src/components/guide/hook-events/HookEventNode.tsx"
      via: "nodeTypes registration at module scope"
      pattern: "nodeTypes.*=.*event.*HookEventNode"
    - from: "src/components/guide/HookEventVisualizer.tsx"
      to: "src/components/guide/hook-events/HookDetailPanel.tsx"
      via: "conditional render based on selectedNodeId state"
      pattern: "selectedNodeId.*HookDetailPanel"
---

<objective>
Build the interactive hook event visualizer (INTV-02) -- a React Flow event sequence where readers click events to reveal handler types, payload structures, and configuration examples.

Purpose: Transforms the static SVG hook lifecycle from Phase 91 into an interactive experience where readers explore all 18 hook events and their payload schemas hands-on.
Output: HookEventVisualizer.tsx component ready for MDX embedding via client:visible, with data integrity tests.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-interactive-react-components/92-RESEARCH.md

# Reference pattern -- follow this structure exactly
@src/components/guide/DeploymentTopology.tsx
@src/components/guide/topology/TopologyNode.tsx
@src/components/guide/topology/deployment-topology.css

<interfaces>
<!-- Existing React Flow pattern to replicate. Executor should follow this structure. -->

From src/components/guide/DeploymentTopology.tsx:
```typescript
// Module-level nodeTypes to avoid re-registration
const nodeTypes = { topology: TopologyNode };

// Static data with dagre layout
const rawNodes: Node<TopologyNodeData>[] = [...];
const rawEdges: Edge[] = [...];
const { nodes, edges } = layoutGraph(rawNodes, rawEdges);

// Default export for client:visible
export default DeploymentTopology;
```

From src/components/guide/topology/TopologyNode.tsx:
```typescript
export type TopologyNodeData = {
  label: string;
  subtitle?: string;
  icon: 'app' | 'db' | 'cache' | 'proxy';
};

export type TopologyNodeType = Node<TopologyNodeData, 'topology'>;

export function TopologyNode({ data }: NodeProps<TopologyNodeType>) {
  // Handle top + bottom, CSS custom property colors
}
```

From src/components/guide/topology/deployment-topology.css:
```css
.deployment-topology .react-flow {
  --xy-background-color: transparent;
  --xy-node-color: var(--color-text-primary, #e0e0e0);
  --xy-edge-stroke: var(--color-text-secondary, #888);
  /* ... */
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create hook event data module with tests</name>
  <files>
    src/lib/guides/interactive-data/hook-event-data.ts,
    src/lib/guides/interactive-data/__tests__/hook-event-data.test.ts
  </files>
  <behavior>
    - rawNodes has nodes for all 18 events plus 3 category header nodes = 21 total event/category nodes (plus any structural nodes like a session-start entry)
    - Session Events category has exactly 2 events: SessionStart, SessionEnd
    - Loop Events category has exactly 12 events: UserPromptSubmit, PreToolUse, PermissionRequest, PostToolUse, PostToolUseFailure, Notification, SubagentStart, SubagentStop, Stop, TeammateIdle, TaskCompleted, PreCompact
    - Standalone Async Events category has exactly 4 events: InstructionsLoaded, ConfigChange, WorktreeCreate, WorktreeRemove
    - PreToolUse node has canBlock: true in its data
    - detailContent has entries for all 18 event node IDs
    - Each detailContent entry has title, description, handlerTypes array, and configExample string
    - All edge sources and targets reference valid node IDs
  </behavior>
  <action>
    Create `src/lib/guides/interactive-data/` directory if it does not already exist (Plan 92-01 may create it first if executed in parallel, use mkdir -p).

    Write tests FIRST in `src/lib/guides/interactive-data/__tests__/hook-event-data.test.ts`:
    - Test total event nodes count (18 events)
    - Test Session Events: exactly SessionStart, SessionEnd
    - Test Loop Events: exactly the 12 events listed above
    - Test Standalone Async Events: exactly the 4 events listed above
    - Test PreToolUse node has canBlock: true
    - Test all edge sources/targets exist in node IDs
    - Test detailContent has entry for each of the 18 event node IDs
    - Test each detailContent entry has title (string), description (string), handlerTypes (array of strings with at least one item), configExample (string)

    Run tests (MUST FAIL -- RED).

    Then implement `src/lib/guides/interactive-data/hook-event-data.ts`:
    - Export `rawNodes: Node[]` with the event sequence structure:
      - 3 category header nodes (type: 'category'): session-events, loop-events, standalone-events
      - 18 event nodes (type: 'event') organized under their categories
      - PreToolUse event includes `canBlock: true` in its data
      - All event nodes have data: { label, category, canBlock? }
    - Export `rawEdges: Edge[]` connecting:
      - Category headers to their first event
      - Sequential events within each category (vertical flow)
      - Use MarkerType.ArrowClosed on edges
      - Style with stroke color #888 using CSS custom property fallbacks
    - Export `detailContent: Record<string, EventDetail>` with payload info for each of the 18 events:
      - title: event name
      - description: when the event fires and what it does
      - handlerTypes: which handler types are applicable (command, HTTP, prompt, agent)
      - fields: key event-specific fields (e.g., PreToolUse has tool_name, tool_input)
      - configExample: JSON string showing a sample hooks configuration for that event
      - blockingInfo: for PreToolUse only -- explain exit code 2 = block with message
    - Include common fields note: all events receive session context
    - All positions set to {x:0, y:0} -- dagre handles layout

    Run tests (MUST PASS -- GREEN).
  </action>
  <verify>
    <automated>npx vitest run src/lib/guides/interactive-data/__tests__/hook-event-data.test.ts --reporter=verbose</automated>
  </verify>
  <done>Tests pass confirming 18 event nodes across 3 categories, PreToolUse has canBlock:true, all events have detailContent with title/description/handlerTypes/configExample fields.</done>
</task>

<task type="auto">
  <name>Task 2: Build HookEventVisualizer component with custom nodes and detail panel</name>
  <files>
    src/components/guide/HookEventVisualizer.tsx,
    src/components/guide/hook-events/HookEventNode.tsx,
    src/components/guide/hook-events/HookCategoryNode.tsx,
    src/components/guide/hook-events/HookDetailPanel.tsx,
    src/components/guide/hook-events/hook-events.css
  </files>
  <action>
    Create `src/components/guide/hook-events/` directory.

    **HookEventNode.tsx** -- Custom event node:
    - Export type `EventNodeData = { label: string; category: 'session' | 'loop' | 'standalone'; canBlock?: boolean }`
    - Export type `EventNodeType = Node<EventNodeData, 'event'>`
    - Accept `{ data, selected }` from NodeProps<EventNodeType>
    - Render rectangular node with left border colored by category: session=#3b82f6 (blue), loop=#8b5cf6 (violet), standalone=#f59e0b (amber)
    - When `data.canBlock` is true, show a small "CAN BLOCK" badge in accent color (var(--color-accent, #c44b20)) -- matches Phase 91 convention
    - Show accent ring-2 when `selected` is true
    - Handle top (target) + Handle bottom (source)
    - Cursor pointer to indicate clickability
    - Width ~180px to fit event names
    - CSS custom properties for all colors

    **HookCategoryNode.tsx** -- Category header node:
    - Export type `CategoryNodeData = { label: string; count: number }`
    - Export type `CategoryNodeType = Node<CategoryNodeData, 'category'>`
    - Accept `{ data }` from NodeProps<CategoryNodeType>
    - Render wider node (~200px) with bold label and event count badge
    - Dashed border to distinguish from event nodes
    - Background var(--color-surface, #1a1a2e)
    - Handle bottom (source) only -- categories are entry points
    - NOT selectable/clickable (no detail content for categories)

    **HookDetailPanel.tsx** -- Event detail panel below canvas:
    - Accept `{ content, onClose }` props where content has: title, description, handlerTypes, fields, configExample, blockingInfo?
    - Render below the ReactFlow canvas (sibling, not inside flow)
    - Layout sections:
      1. Title + Close button row
      2. Description paragraph
      3. Handler Types as inline badges (command, HTTP, prompt, agent)
      4. Event-Specific Fields as key-value grid
      5. Configuration Example in a `<pre><code>` block with monospace font
      6. Blocking Info section (only for PreToolUse) with accent highlight
    - CSS custom properties for all colors
    - Close button with aria-label="Close detail panel"

    **hook-events.css** -- Theme overrides:
    - Follow deployment-topology.css pattern exactly
    - Scope to `.hook-event-visualizer .react-flow`
    - Set --xy-background-color: transparent
    - Set --xy-node-color, --xy-edge-stroke, --xy-edge-stroke-selected
    - Style controls buttons with theme colors
    - Hide attribution
    - Style arrow markers

    **HookEventVisualizer.tsx** -- Main component:
    - Import ReactFlow, Controls, Background, BackgroundVariant from @xyflow/react
    - Import '@xyflow/react/dist/style.css'
    - Import dagre for layout
    - Import custom nodes (HookEventNode, HookCategoryNode), detail panel, data, CSS
    - Define `nodeTypes = { event: HookEventNode, category: HookCategoryNode }` at MODULE SCOPE (not inside component)
    - Define layoutGraph function matching DeploymentTopology.tsx pattern
    - Use dagre with rankdir: 'TB', nodesep: 40, ranksep: 80 (tighter spacing for 21+ nodes)
    - Pre-compute layout at module scope
    - Component uses `useState<string | null>(null)` for selectedNodeId
    - `onNodeClick` callback:
      - If clicked node is type 'category', ignore (no detail for categories)
      - Otherwise toggle selectedNodeId
    - Wrap ReactFlow in div with className="hook-event-visualizer h-[500px] lg:h-[600px] rounded-lg overflow-hidden border border-[var(--color-border,rgba(255,255,255,0.1))]"
    - Taller height (500px/600px) because 21+ nodes need more vertical space than the 8-node permission tree
    - ReactFlow props: fitView, fitViewOptions={{ padding: 0.2 }}, proOptions={{ hideAttribution: true }}, minZoom=0.2, maxZoom=2, nodesDraggable, nodesConnectable={false}, elementsSelectable
    - Controls with showInteractive={false} position="bottom-right"
    - Background with Dots variant
    - Render HookDetailPanel below canvas when selectedNodeId is truthy and maps to a detailContent entry
    - Export default for client:visible compatibility

    Verify production build succeeds and all tests pass.
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -10</automated>
  </verify>
  <done>HookEventVisualizer component builds without errors. 18 events render in 3 categories. PreToolUse has CAN BLOCK badge. Detail panel shows handler types, payload fields, and config example on event click. All 353+ existing tests still pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/guides/interactive-data/__tests__/hook-event-data.test.ts --reporter=verbose` -- all data integrity tests pass
2. `npx vitest run --reporter=verbose` -- full suite (353+ tests) passes, no regressions
3. `npx astro build` -- production build succeeds with HookEventVisualizer compiled
4. Verify component file exists and exports default: `grep -n "export default" src/components/guide/HookEventVisualizer.tsx`
5. Verify nodeTypes defined at module scope: `grep -n "const nodeTypes" src/components/guide/HookEventVisualizer.tsx`
6. Verify CSS custom properties used (no hardcoded hex): `grep "var(--color" src/components/guide/hook-events/hook-events.css`
7. Verify PreToolUse CAN BLOCK indicator: `grep -n "CAN BLOCK\|canBlock" src/components/guide/hook-events/HookEventNode.tsx`
</verification>

<success_criteria>
- Hook event data module has 18 event nodes across 3 categories, with passing tests
- HookEventVisualizer.tsx follows DeploymentTopology.tsx pattern: module-scope nodeTypes, dagre layout, default export
- PreToolUse is visually distinguished with CAN BLOCK badge in accent color
- Custom nodes use CSS custom properties for theme support
- Detail panel shows handler types, payload fields, and config example
- Production build succeeds with zero errors
- No test regressions (353+ tests pass)
</success_criteria>

<output>
After completion, create `.planning/phases/92-interactive-react-components/92-02-SUMMARY.md`
</output>
