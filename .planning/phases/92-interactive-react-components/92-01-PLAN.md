---
phase: 92-interactive-react-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/guides/interactive-data/permission-flow-data.ts
  - src/lib/guides/interactive-data/__tests__/permission-flow-data.test.ts
  - src/components/guide/PermissionFlowExplorer.tsx
  - src/components/guide/permission-flow/PermissionDecisionNode.tsx
  - src/components/guide/permission-flow/PermissionResultNode.tsx
  - src/components/guide/permission-flow/PermissionDetailPanel.tsx
  - src/components/guide/permission-flow/permission-flow.css
autonomous: true
requirements: [INTV-01]

must_haves:
  truths:
    - "The permission flow explorer renders an interactive decision tree with entry node, 3 decision diamonds, 4 result nodes, and labeled Yes/No edges"
    - "Clicking a node highlights it with an accent ring and reveals a detail panel below the canvas showing evaluation context"
    - "Clicking the same node again (or Close button) dismisses the detail panel"
    - "The component uses CSS custom properties for dark/light theme support"
    - "The component is exported as default for client:visible lazy hydration in MDX"
  artifacts:
    - path: "src/lib/guides/interactive-data/permission-flow-data.ts"
      provides: "Node/edge definitions and detail content for permission evaluation tree"
      exports: ["rawNodes", "rawEdges", "detailContent"]
    - path: "src/lib/guides/interactive-data/__tests__/permission-flow-data.test.ts"
      provides: "Data integrity tests for permission flow data"
      contains: "deny-check"
    - path: "src/components/guide/PermissionFlowExplorer.tsx"
      provides: "Main React Flow interactive component"
      exports: ["default"]
      min_lines: 50
    - path: "src/components/guide/permission-flow/PermissionDecisionNode.tsx"
      provides: "Custom diamond-shaped decision node for deny/ask/allow checks"
      exports: ["PermissionDecisionNode"]
    - path: "src/components/guide/permission-flow/PermissionResultNode.tsx"
      provides: "Custom result node for BLOCKED/Prompt/ALLOWED/Default outcomes"
      exports: ["PermissionResultNode"]
    - path: "src/components/guide/permission-flow/PermissionDetailPanel.tsx"
      provides: "Detail panel rendered below canvas on node click"
      exports: ["PermissionDetailPanel"]
    - path: "src/components/guide/permission-flow/permission-flow.css"
      provides: "React Flow theme overrides using CSS custom properties"
      contains: "permission-flow-explorer"
  key_links:
    - from: "src/components/guide/PermissionFlowExplorer.tsx"
      to: "src/lib/guides/interactive-data/permission-flow-data.ts"
      via: "import { rawNodes, rawEdges, detailContent }"
      pattern: "from.*interactive-data/permission-flow-data"
    - from: "src/components/guide/PermissionFlowExplorer.tsx"
      to: "src/components/guide/permission-flow/PermissionDecisionNode.tsx"
      via: "nodeTypes registration at module scope"
      pattern: "nodeTypes.*=.*decision.*PermissionDecisionNode"
    - from: "src/components/guide/PermissionFlowExplorer.tsx"
      to: "src/components/guide/permission-flow/PermissionDetailPanel.tsx"
      via: "conditional render based on selectedNodeId state"
      pattern: "selectedNodeId.*PermissionDetailPanel"
---

<objective>
Build the interactive permission flow explorer (INTV-01) -- a React Flow decision tree where readers click nodes to reveal deny/ask/allow evaluation context.

Purpose: Transforms the static SVG permission model from Phase 91 into an interactive experience where readers explore the permission evaluation chain hands-on.
Output: PermissionFlowExplorer.tsx component ready for MDX embedding via client:visible, with data integrity tests.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-interactive-react-components/92-RESEARCH.md

# Reference pattern -- follow this structure exactly
@src/components/guide/DeploymentTopology.tsx
@src/components/guide/topology/TopologyNode.tsx
@src/components/guide/topology/deployment-topology.css

<interfaces>
<!-- Existing React Flow pattern to replicate. Executor should follow this structure. -->

From src/components/guide/DeploymentTopology.tsx:
```typescript
// Module-level nodeTypes to avoid re-registration
const nodeTypes = { topology: TopologyNode };

// Static data with dagre layout
const rawNodes: Node<TopologyNodeData>[] = [...];
const rawEdges: Edge[] = [...];
const { nodes, edges } = layoutGraph(rawNodes, rawEdges);

// Default export for client:visible
export default DeploymentTopology;
```

From src/components/guide/topology/TopologyNode.tsx:
```typescript
export type TopologyNodeData = {
  label: string;
  subtitle?: string;
  icon: 'app' | 'db' | 'cache' | 'proxy';
};

export type TopologyNodeType = Node<TopologyNodeData, 'topology'>;

export function TopologyNode({ data }: NodeProps<TopologyNodeType>) {
  // Handle top + bottom, CSS custom property colors
}
```

From src/components/guide/topology/deployment-topology.css:
```css
.deployment-topology .react-flow {
  --xy-background-color: transparent;
  --xy-node-color: var(--color-text-primary, #e0e0e0);
  --xy-edge-stroke: var(--color-text-secondary, #888);
  /* ... */
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create permission flow data module with tests</name>
  <files>
    src/lib/guides/interactive-data/permission-flow-data.ts,
    src/lib/guides/interactive-data/__tests__/permission-flow-data.test.ts
  </files>
  <behavior>
    - rawNodes has exactly 8 nodes: 1 entry (Tool Call Request), 3 decision diamonds (deny-check, ask-check, allow-check), 4 results (blocked, prompt-user, allowed, default-ask)
    - rawEdges has exactly 9 edges connecting the decision tree with Yes/No labels on decision branches
    - detailContent has entries for all 8 node IDs
    - Each detailContent entry has title (string), description (string), and details (array with at least 2 items)
    - Each detail item has key (string) and value (string)
    - All edge sources and targets reference valid node IDs
    - Decision nodes have type 'decision', result nodes have type 'result', entry node has type 'input'
  </behavior>
  <action>
    Create `src/lib/guides/interactive-data/` directory.

    Write tests FIRST in `src/lib/guides/interactive-data/__tests__/permission-flow-data.test.ts`:
    - Test rawNodes length is 8
    - Test node types: 1 input, 3 decision, 4 result
    - Test rawEdges length is 9
    - Test all edge sources/targets exist in node IDs
    - Test detailContent has entry for every node ID
    - Test each detailContent entry has title, description, details array with >= 2 items

    Run tests (MUST FAIL -- RED).

    Then implement `src/lib/guides/interactive-data/permission-flow-data.ts`:
    - Export `rawNodes: Node[]` with the 8-node decision tree structure from the research INTV-01 Domain Data section:
      - entry: "Tool Call Request" (type: input)
      - deny-check: "Deny rules match?" (type: decision, outcome: deny)
      - ask-check: "Ask rules match?" (type: decision, outcome: ask)
      - allow-check: "Allow rules match?" (type: decision, outcome: allow)
      - blocked: "BLOCKED" (type: result, outcome: deny)
      - prompt-user: "Prompt User" (type: result, outcome: ask)
      - allowed: "ALLOWED" (type: result, outcome: allow)
      - default-ask: "Default: Ask" (type: result, outcome: ask)
    - Export `rawEdges: Edge[]` with 9 edges connecting the tree:
      - entry -> deny-check
      - deny-check -> blocked (Yes)
      - deny-check -> ask-check (No)
      - ask-check -> prompt-user (Yes)
      - ask-check -> allow-check (No)
      - allow-check -> allowed (Yes)
      - allow-check -> default-ask (No)
    - Export `detailContent: Record<string, DetailContent>` with evaluation context for each node:
      - deny-check: rule syntax, evaluation order (1st), on-match behavior, example
      - ask-check: prompt behavior, scope options (session/project), evaluation order (2nd)
      - allow-check: auto-approve behavior, common use cases, evaluation order (3rd)
      - blocked: rejection behavior, no user override possible
      - prompt-user: approval options (Allow Once/Session/Project/Deny)
      - allowed: no interaction needed, trust level
      - default-ask: fallback behavior when no rules match
      - entry: what constitutes a tool call (file read, bash, write, etc.)
    - Include supplementary data: tool tiers, settings precedence, permission modes
    - All positions set to {x:0, y:0} -- dagre handles layout
    - Use MarkerType.ArrowClosed on all edges

    Run tests (MUST PASS -- GREEN).
  </action>
  <verify>
    <automated>npx vitest run src/lib/guides/interactive-data/__tests__/permission-flow-data.test.ts --reporter=verbose</automated>
  </verify>
  <done>Tests pass confirming 8 nodes (1 input + 3 decision + 4 result), 9 edges with valid references, and detailContent entries for all nodes with title/description/details fields.</done>
</task>

<task type="auto">
  <name>Task 2: Build PermissionFlowExplorer component with custom nodes and detail panel</name>
  <files>
    src/components/guide/PermissionFlowExplorer.tsx,
    src/components/guide/permission-flow/PermissionDecisionNode.tsx,
    src/components/guide/permission-flow/PermissionResultNode.tsx,
    src/components/guide/permission-flow/PermissionDetailPanel.tsx,
    src/components/guide/permission-flow/permission-flow.css
  </files>
  <action>
    Create `src/components/guide/permission-flow/` directory.

    **PermissionDecisionNode.tsx** -- Custom diamond-shaped decision node:
    - Export type `DecisionNodeData = { label: string; description: string; outcome: 'deny' | 'ask' | 'allow' }`
    - Export type `DecisionNodeType = Node<DecisionNodeData, 'decision'>`
    - Accept `{ data, selected }` from NodeProps<DecisionNodeType>
    - Render a rectangular container with a decorative diamond SVG background (NOT clip-path -- per Pitfall 5 in research, clip-path clips handles)
    - Show accent ring-2 when `selected` is true
    - Use CSS custom properties for all colors: var(--color-accent), var(--color-border), var(--color-surface-alt), var(--color-text-primary)
    - Handle top (target) + Handle bottom (source) with accent color
    - Cursor pointer to indicate clickability
    - Width ~140px to fit "Deny rules match?" text

    **PermissionResultNode.tsx** -- Custom result/outcome node:
    - Export type `ResultNodeData = { label: string; description: string; outcome: 'deny' | 'ask' | 'allow' }`
    - Export type `ResultNodeType = Node<ResultNodeData, 'result'>`
    - Accept `{ data, selected }` from NodeProps<ResultNodeType>
    - Render rectangular node with left border colored by outcome: deny=#ef4444, ask=#f59e0b, allow=#22c55e
    - Show accent ring-2 when selected
    - Handle top only (target -- these are leaf nodes, except default-ask which is also a terminal)
    - CSS custom properties for theming

    **PermissionDetailPanel.tsx** -- Detail panel below canvas:
    - Accept `{ content: { title, description, details }, onClose }` props
    - Render below the ReactFlow canvas (sibling, not inside flow)
    - Show title, description, and key-value details in a 2-column grid
    - Close button with aria-label="Close detail panel"
    - CSS custom properties: var(--color-surface), var(--color-border), var(--color-text-primary), var(--color-text-secondary)
    - Smooth CSS transition-all for appearance

    **permission-flow.css** -- Theme overrides:
    - Follow deployment-topology.css pattern exactly
    - Scope to `.permission-flow-explorer .react-flow`
    - Set --xy-background-color: transparent
    - Set --xy-node-color, --xy-edge-stroke, --xy-edge-stroke-selected
    - Style controls buttons with theme colors
    - Hide attribution
    - Style arrow markers

    **PermissionFlowExplorer.tsx** -- Main component:
    - Import ReactFlow, Controls, Background, BackgroundVariant from @xyflow/react
    - Import '@xyflow/react/dist/style.css'
    - Import dagre for layout
    - Import custom nodes, detail panel, data, CSS
    - Define `nodeTypes = { decision: PermissionDecisionNode, result: PermissionResultNode }` at MODULE SCOPE (not inside component -- Pitfall 1)
    - Define layoutGraph function matching DeploymentTopology.tsx pattern (dagre TB layout, nodesep: 60, ranksep: 100)
    - Pre-compute layout: `const { nodes, edges } = layoutGraph(rawNodes, rawEdges)`
    - Use NODE_WIDTH=160, NODE_HEIGHT=60 for decision nodes, allow dagre to handle sizing
    - Component uses `useState<string | null>(null)` for selectedNodeId
    - `onNodeClick` callback toggles selectedNodeId (click same node deselects)
    - Wrap ReactFlow in div with className="permission-flow-explorer h-[400px] lg:h-[500px] rounded-lg overflow-hidden border border-[var(--color-border,rgba(255,255,255,0.1))]"
    - ReactFlow props: fitView, fitViewOptions={{ padding: 0.3 }}, proOptions={{ hideAttribution: true }}, minZoom=0.3, maxZoom=2, nodesDraggable, nodesConnectable={false}, elementsSelectable
    - Controls with showInteractive={false} position="bottom-right"
    - Background with Dots variant
    - Render PermissionDetailPanel below canvas when selectedNodeId is truthy
    - Export default for client:visible compatibility

    Verify production build succeeds with the new component compiled.
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -10</automated>
  </verify>
  <done>PermissionFlowExplorer component builds without errors. Custom decision/result nodes render with theme-aware colors. Detail panel shows on node click with evaluation context. All 353+ existing tests still pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/guides/interactive-data/__tests__/permission-flow-data.test.ts --reporter=verbose` -- all data integrity tests pass
2. `npx vitest run --reporter=verbose` -- full suite (353+ tests) passes, no regressions
3. `npx astro build` -- production build succeeds with PermissionFlowExplorer compiled
4. Verify component file exists and exports default: `grep -n "export default" src/components/guide/PermissionFlowExplorer.tsx`
5. Verify nodeTypes defined at module scope: `grep -n "const nodeTypes" src/components/guide/PermissionFlowExplorer.tsx`
6. Verify CSS custom properties used (no hardcoded hex): `grep "var(--color" src/components/guide/permission-flow/permission-flow.css`
</verification>

<success_criteria>
- Permission flow data module has 8 nodes, 9 edges, and detail content for all nodes, with passing tests
- PermissionFlowExplorer.tsx follows DeploymentTopology.tsx pattern: module-scope nodeTypes, dagre layout, default export
- Custom nodes use CSS custom properties for theme support (no hardcoded hex except in fallback values)
- Detail panel renders below canvas with close button
- Production build succeeds with zero errors
- No test regressions (353+ tests pass)
</success_criteria>

<output>
After completion, create `.planning/phases/92-interactive-react-components/92-01-SUMMARY.md`
</output>
