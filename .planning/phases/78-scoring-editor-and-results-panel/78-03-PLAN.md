---
phase: 78-scoring-editor-and-results-panel
plan: 03
type: execute
wave: 3
depends_on: ["78-01", "78-02"]
files_modified:
  - src/components/tools/GhaResultsPanel.tsx
  - src/components/tools/gha-results/GhaViolationList.tsx
  - src/components/tools/gha-results/GhaCategoryBreakdown.tsx
  - src/components/tools/gha-results/GhaEmptyState.tsx
  - src/components/tools/GhaValidator.tsx
  - src/pages/tools/gha-validator/index.astro
autonomous: true
requirements: [SCORE-04, UI-05, UI-06, UI-07, UI-08, UI-09, UI-10]

must_haves:
  truths:
    - "Tabbed results panel shows score gauge, category breakdown, and violation list"
    - "SVG score gauge displays overall score with letter grade"
    - "Per-category sub-scores shown with colored progress bars and weights"
    - "Violations grouped by category with expandable details and rule ID links"
    - "Clicking a violation scrolls editor to that line"
    - "Clean workflow shows congratulatory empty state with perfect score"
    - "Layout is responsive: stacked on mobile, side-by-side on desktop"
    - "React island loads with client:only='react' directive"
    - "Graph tab placeholder exists for Phase 79"
  artifacts:
    - path: "src/components/tools/GhaResultsPanel.tsx"
      provides: "Tabbed results panel with score, categories, violations"
      exports: ["default"]
      min_lines: 80
    - path: "src/components/tools/gha-results/GhaViolationList.tsx"
      provides: "Category-grouped expandable violation list"
      exports: ["GhaViolationList"]
      min_lines: 60
    - path: "src/components/tools/gha-results/GhaCategoryBreakdown.tsx"
      provides: "Category sub-scores with progress bars"
      exports: ["GhaCategoryBreakdown"]
      min_lines: 40
    - path: "src/components/tools/gha-results/GhaEmptyState.tsx"
      provides: "No issues found congratulatory state"
      exports: ["GhaEmptyState"]
      min_lines: 20
    - path: "src/components/tools/GhaValidator.tsx"
      provides: "Top-level React island wrapper"
      exports: ["default"]
    - path: "src/pages/tools/gha-validator/index.astro"
      provides: "Astro page at /tools/gha-validator/"
      contains: "client:only"
  key_links:
    - from: "src/components/tools/GhaResultsPanel.tsx"
      to: "src/stores/ghaValidatorStore.ts"
      via: "useStore(ghaResult)"
      pattern: "useStore.*ghaResult"
    - from: "src/components/tools/GhaResultsPanel.tsx"
      to: "src/components/tools/results/ScoreGauge.tsx"
      via: "import ScoreGauge"
      pattern: "import.*ScoreGauge"
    - from: "src/components/tools/GhaResultsPanel.tsx"
      to: "src/lib/tools/dockerfile-analyzer/highlight-line.ts"
      via: "import highlightAndScroll"
      pattern: "highlightAndScroll"
    - from: "src/components/tools/GhaValidator.tsx"
      to: "src/components/tools/GhaEditorPanel.tsx"
      via: "import GhaEditorPanel"
      pattern: "import.*GhaEditorPanel"
    - from: "src/pages/tools/gha-validator/index.astro"
      to: "src/components/tools/GhaValidator.tsx"
      via: "client:only='react'"
      pattern: "client:only"
---

<objective>
Build the results panel with score gauge, category breakdown, violation list, empty state, and wire everything together in the GhaValidator React island and Astro page.

Purpose: This is the output side of the tool -- where users see their score, understand category breakdowns, and drill into specific violations. Combined with Plan 02's editor panel, this completes the full user experience.
Output: GhaResultsPanel with subcomponents, GhaValidator wrapper, Astro page at /tools/gha-validator/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-scoring-editor-and-results-panel/78-RESEARCH.md
@.planning/phases/78-scoring-editor-and-results-panel/78-01-SUMMARY.md
@.planning/phases/78-scoring-editor-and-results-panel/78-02-SUMMARY.md

@src/stores/ghaValidatorStore.ts
@src/lib/tools/gha-validator/types.ts
@src/lib/tools/gha-validator/rules/index.ts
@src/components/tools/K8sResultsPanel.tsx
@src/components/tools/k8s-results/K8sViolationList.tsx
@src/components/tools/k8s-results/K8sCategoryBreakdown.tsx
@src/components/tools/k8s-results/K8sEmptyState.tsx
@src/components/tools/K8sAnalyzer.tsx
@src/components/tools/results/ScoreGauge.tsx
@src/components/tools/results/FullscreenToggle.tsx
@src/lib/tools/dockerfile-analyzer/highlight-line.ts
@src/pages/tools/k8s-analyzer/index.astro
</context>

<interfaces>
<!-- Key types the executor needs -->

From src/stores/ghaValidatorStore.ts (after Plan 02):
```typescript
export interface GhaAnalysisResult {
  violations: GhaUnifiedViolation[];
  score: GhaScoreResult;
  pass: 1 | 2;
}
export const ghaResult: WritableAtom<GhaAnalysisResult | null>;
export const ghaAnalyzing: WritableAtom<boolean>;
export const ghaEditorViewRef: WritableAtom<EditorView | null>;
export const ghaResultsStale: WritableAtom<boolean>;
```

From src/lib/tools/gha-validator/types.ts (after Plan 01):
```typescript
export interface GhaScoreResult {
  overall: number; grade: string;
  categories: GhaCategoryScore[]; deductions: GhaScoreDeduction[];
}
export interface GhaCategoryScore {
  category: string; score: number; weight: number; deductions: GhaScoreDeduction[];
}
export interface GhaScoreDeduction {
  ruleId: string; category: string; severity: GhaSeverity; points: number; line: number;
}
```

From src/lib/tools/gha-validator/rules/index.ts:
```typescript
export function getGhaRuleById(id: string): GhaLintRule | undefined;
// GhaLintRule has: id, title, severity, category, explanation, fix: { description, beforeCode, afterCode }
```

From src/components/tools/results/ScoreGauge.tsx:
```typescript
export function ScoreGauge({ score, grade, size }: { score: number; grade: string; size?: number }): JSX.Element;
```

From src/lib/tools/dockerfile-analyzer/highlight-line.ts:
```typescript
export function highlightAndScroll(view: EditorView, line: number): void;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build GhaResultsPanel with subcomponents</name>
  <files>src/components/tools/GhaResultsPanel.tsx, src/components/tools/gha-results/GhaViolationList.tsx, src/components/tools/gha-results/GhaCategoryBreakdown.tsx, src/components/tools/gha-results/GhaEmptyState.tsx</files>
  <action>
    Create 4 component files following K8s results panel patterns:

    **GhaCategoryBreakdown.tsx:**
    Port from `K8sCategoryBreakdown.tsx`. Display per-category sub-scores as horizontal progress bars.
    - Accept `categories: GhaCategoryScore[]` and optional `onCategoryClick` callback
    - GHA category labels: Schema, Security, Semantic, Best Practice, Style
    - GHA category colors: schema=#6366f1 (indigo), security=#ef4444 (red), semantic=#f59e0b (amber), best-practice=#10b981 (emerald), style=#8b5cf6 (purple)
    - Show weight percentage next to category name (e.g., "Security (35%)")
    - Progress bar width = score%, color = category color
    - Score number right-aligned
    - Clickable categories filter the violation list (optional interaction)

    **GhaViolationList.tsx:**
    Port from `K8sViolationList.tsx` but group by CATEGORY (not severity -- this is the key difference per UI-07).
    - Accept `violations: GhaUnifiedViolation[]`, `onNavigate: (line: number) => void`, optional `selectedCategory: string | null`, optional `searchQuery: string`
    - Group violations by category using the 5 GHA categories
    - Each category group is collapsible (default expanded)
    - Each violation row shows: severity icon (error/warning/info), rule ID as link to `/tools/gha-validator/rules/${ruleId}/` (Phase 80 will create these pages), line number (clickable -- calls onNavigate), message
    - Expandable detail section for each violation: look up rule via `getGhaRuleById(v.ruleId)` and show title, explanation, fix description, before/after code blocks (use `<pre><code>` with YAML syntax)
    - Filter by selectedCategory if provided (from category bar click)
    - Filter by searchQuery if provided (match against message, ruleId)
    - Category group headers show count badge

    **GhaEmptyState.tsx:**
    Port from `K8sEmptyState.tsx`. Show when result exists but violations.length === 0.
    - Green checkmark icon (SVG)
    - "No Issues Found" heading
    - "Your GitHub Actions workflow follows all best practices. Great job!" body text
    - Include ScoreGauge showing perfect 100/A+ score
    - Styled with green accent color

    **GhaResultsPanel.tsx:**
    Port from `K8sResultsPanel.tsx`. Tabbed panel with Results and Graph tabs.
    - Import useStore from @nanostores/react, ghaResult/ghaAnalyzing/ghaResultsStale/ghaEditorViewRef from store
    - Import ScoreGauge from results/ScoreGauge (reuse directly -- DO NOT duplicate)
    - Import highlightAndScroll from dockerfile-analyzer/highlight-line (reuse directly)
    - Import GhaCategoryBreakdown, GhaViolationList, GhaEmptyState from gha-results/
    - Import FullscreenToggle from results/FullscreenToggle

    Tab bar: "Results" tab (active by default) and "Graph" tab (placeholder for Phase 79).
    - Graph tab shows "Workflow graph visualization coming soon" centered text

    Results tab content (when result exists):
    - If violations.length === 0: Show GhaEmptyState
    - If violations.length > 0:
      1. ScoreGauge with result.score.overall and result.score.grade (size=80)
      2. Pass indicator: "Pass 1 of 2" or "Complete" based on result.pass
      3. Severity summary line (e.g., "3 errors, 2 warnings, 1 info")
      4. GhaCategoryBreakdown with result.score.categories
      5. GhaViolationList with violations, onNavigate handler using highlightAndScroll

    No-result state: "Paste a workflow and click Analyze to see results" placeholder.
    Analyzing state: "Analyzing..." with spinner.
    Stale state: Yellow banner "Results may be stale -- click Analyze to refresh".

    handleNavigate function: `ghaEditorViewRef.get()` then `highlightAndScroll(view, line)` (UI-06).

    Props: `{ onToggleFullscreen?: () => void; isFullscreen?: boolean }`

    Local state: selectedCategory, searchQuery (both optional for violation filtering).
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/tools/GhaResultsPanel.tsx src/components/tools/gha-results/GhaViolationList.tsx src/components/tools/gha-results/GhaCategoryBreakdown.tsx src/components/tools/gha-results/GhaEmptyState.tsx 2>&1 | head -20</automated>
  </verify>
  <done>GhaResultsPanel renders with tabs (Results/Graph). Results tab shows ScoreGauge, GhaCategoryBreakdown with 5 categories, GhaViolationList grouped by category with expandable details. Empty state renders for clean workflows. Click-to-navigate calls highlightAndScroll. All compile clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire GhaValidator React island and Astro page</name>
  <files>src/components/tools/GhaValidator.tsx, src/pages/tools/gha-validator/index.astro</files>
  <action>
    **GhaValidator.tsx:**
    Port exactly from `K8sAnalyzer.tsx` pattern:
    - Import useState from react
    - Import GhaEditorPanel and GhaResultsPanel (default imports)
    - Fullscreen toggle state: `useState<'editor' | 'results' | null>(null)`
    - Render: `grid grid-cols-1 ${fullscreen ? '' : 'lg:grid-cols-2'} gap-4 lg:gap-6`
    - Editor panel hidden when results fullscreen, results hidden when editor fullscreen
    - Pass onToggleFullscreen and isFullscreen props to both panels
    - Export as default

    **Astro page (src/pages/tools/gha-validator/index.astro):**
    Port from `src/pages/tools/k8s-analyzer/index.astro` pattern:
    - Import Layout from layouts/Layout.astro
    - Import GhaValidator from components/tools/GhaValidator (React component)
    - Layout props:
      - title: "GitHub Actions Workflow Validator | Patryk Golabek"
      - description: "Free online GitHub Actions workflow linter and validator. 40 rules for security, semantic correctness, best practices, and style. Category-weighted scoring with actionlint WASM. 100% browser-based."
      - (Skip ogImage for now -- Phase 81 creates OG images)
    - Page content:
      - `<section class="py-8 sm:py-12">` with max-w-7xl container
      - `<h1>` "GitHub Actions Workflow Validator"
      - Brief description paragraph: "Paste your GitHub Actions workflow YAML to analyze it against 40 rules across security, semantic correctness, best practices, and style. Two-pass analysis: instant schema and custom rules, plus deep actionlint WASM analysis. 100% browser-based -- your code never leaves your device."
      - `<GhaValidator client:only="react" />` (UI-10: React island)
    - Do NOT add JSON-LD, breadcrumbs, or navigation links yet (Phase 81 handles site integration)
    - Do NOT add skill download badge yet (Phase 81)

    **Responsive layout (UI-09):**
    The `grid grid-cols-1 lg:grid-cols-2` in GhaValidator.tsx handles responsive automatically:
    - Mobile: single column, editor stacked above results
    - Desktop (lg+): side-by-side two-column layout
    - Each panel gets equal width at lg breakpoint
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>GhaValidator.tsx renders editor and results panels in responsive grid. Astro page at /tools/gha-validator/ renders with client:only="react" directive. Full build succeeds with no SSR errors. Stacked on mobile, side-by-side on desktop.</done>
</task>

</tasks>

<verification>
```bash
# TypeScript compilation
npx tsc --noEmit

# Full GHA test suite
npx vitest run src/lib/tools/gha-validator/ -x

# Build succeeds (critical -- ensures client:only works, no SSR crashes)
npm run build

# Verify page exists in build output
ls dist/tools/gha-validator/index.html
```
</verification>

<success_criteria>
- GhaResultsPanel shows ScoreGauge with score and letter grade (SCORE-04)
- Tabbed panel with Results and Graph tabs (UI-05), Graph tab shows placeholder
- Per-category breakdown with 5 colored bars and weight labels (SCORE-03 visual)
- Violations grouped by category with expandable details (UI-07)
- Rule ID links point to /tools/gha-validator/rules/{code}/ (UI-07)
- Clicking violation line number scrolls editor to that line (UI-06)
- Clean workflow shows GhaEmptyState with "No issues found" and perfect score gauge (UI-08)
- Responsive layout: stacked mobile, side-by-side desktop (UI-09)
- client:only="react" on Astro page, no SSR rendering (UI-10)
- Full build succeeds with zero errors
- All existing GHA validator tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/78-scoring-editor-and-results-panel/78-03-SUMMARY.md`
</output>
