---
phase: 71-site-wide-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/beauty-index/index.astro
  - src/pages/beauty-index/[slug].astro
  - src/pages/beauty-index/code/index.astro
  - src/pages/beauty-index/justifications/index.astro
  - src/pages/beauty-index/vs/[slug].astro
  - src/pages/index.astro
  - src/components/BeautyIndexJsonLd.astro
  - src/components/beauty-index/RankingBarChart.astro
  - src/components/beauty-index/FeatureMatrix.astro
  - src/lib/og-image.ts
  - src/pages/llms.txt.ts
  - src/pages/llms-full.txt.ts
  - src/data/blog/the-beauty-index.mdx
  - src/data/blog/building-kubernetes-observability-stack.mdx
  - src/data/beauty-index/code-features.ts
  - .planning/PROJECT.md
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-03
  - SITE-04
  - SITE-05
  - SITE-06
  - SITE-07

must_haves:
  truths:
    - "Every page referencing language count shows 26, not 25"
    - "Derived counts are consistent: 156 justifications, 260 code blocks, 650 VS pages"
    - "Blog posts reference 26 languages"
    - "OG images render with 26 languages text"
    - "JSON-LD structured data says 26 languages"
    - "LLMs.txt and llms-full.txt reference 26 languages with correct derived counts"
    - "PROJECT.md documents all v1.11 validated requirements"
    - "Full astro build passes with Lisp detail page, OG image, and VS comparison pages"
  artifacts:
    - path: "src/pages/beauty-index/index.astro"
      provides: "Overview page with 26 languages references"
      contains: "26 programming languages"
    - path: "src/lib/og-image.ts"
      provides: "OG image render with 26 languages"
      contains: "Ranking 26 programming languages"
    - path: "src/components/BeautyIndexJsonLd.astro"
      provides: "JSON-LD with 26 languages description"
      contains: "Ranking 26 programming languages"
    - path: "src/pages/llms.txt.ts"
      provides: "LLMs.txt with 26 languages and 156 justifications"
      contains: "26 programming languages"
    - path: "src/data/blog/the-beauty-index.mdx"
      provides: "Blog post with 26 languages references"
      contains: "26 programming languages"
    - path: ".planning/PROJECT.md"
      provides: "v1.11 validated requirements documentation"
      contains: "v1.11"
  key_links:
    - from: "src/pages/beauty-index/index.astro"
      to: "dist/beauty-index/index.html"
      via: "Astro build renders updated text into HTML"
      pattern: "26 programming languages"
    - from: "src/lib/og-image.ts"
      to: "dist/open-graph/beauty-index/lisp.png"
      via: "Satori renders OG image with updated text"
      pattern: "Ranking 26"
    - from: "src/pages/llms-full.txt.ts"
      to: "dist/llms-full.txt"
      via: "Astro build generates machine-readable file"
      pattern: "650 comparison pages"
---

<objective>
Update every hardcoded "25 languages" reference to "26 languages" across the entire codebase, update derived counts (150->156 justifications, 250->260 code blocks, 600->650 VS pages), update PROJECT.md with v1.11 validated requirements, and verify the full production build passes with all Lisp pages generated.

Purpose: Complete the Lisp language integration by ensuring all user-facing, SEO, machine-readable, and documentation references reflect the correct 26-language count. This is the final phase of the v1.11 milestone.
Output: 16 updated source files, clean production build with Lisp detail page, OG image, and 650 VS comparison pages.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-site-wide-integration/71-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all hardcoded 25->26 references and PROJECT.md documentation</name>
  <files>
    src/pages/beauty-index/index.astro,
    src/pages/beauty-index/[slug].astro,
    src/pages/beauty-index/code/index.astro,
    src/pages/beauty-index/justifications/index.astro,
    src/pages/beauty-index/vs/[slug].astro,
    src/pages/index.astro,
    src/components/BeautyIndexJsonLd.astro,
    src/components/beauty-index/RankingBarChart.astro,
    src/components/beauty-index/FeatureMatrix.astro,
    src/lib/og-image.ts,
    src/pages/llms.txt.ts,
    src/pages/llms-full.txt.ts,
    src/data/blog/the-beauty-index.mdx,
    src/data/blog/building-kubernetes-observability-stack.mdx,
    src/data/beauty-index/code-features.ts,
    .planning/PROJECT.md
  </files>
  <action>
Perform the complete set of 34+ text replacements documented in 71-RESEARCH.md, organized by category. Read each file, make the exact changes listed below, and write back.

**Category 1: Page Components and Meta (14 replacements in 6 files)**

In `src/pages/beauty-index/index.astro`:
- Line ~30: "ranks 25 programming languages" -> "ranks 26 programming languages"
- Line ~32: "Ranking 25 programming languages" -> "Ranking 26 programming languages"
- Line ~47: "Ranking 25 programming languages" -> "Ranking 26 programming languages"
- Line ~124: "Ranking 25 Programming Languages" -> "Ranking 26 Programming Languages"

In `src/pages/beauty-index/[slug].astro`:
- Line ~4 (comment): "Generates 25 static pages" -> "Generates 26 static pages"
- Line ~80: "among 25 programming languages" -> "among 26 programming languages"

In `src/pages/beauty-index/code/index.astro`:
- Line ~5 (comment): "for all 25 languages. All 250 code blocks" -> "for all 26 languages. All 260 code blocks"
- Line ~29: "Compare how 25 programming languages" -> "Compare how 26 programming languages"
- Line ~31: "across 25 programming languages" -> "across 26 programming languages"
- Line ~39: "See how 25 languages" -> "See how 26 languages"

In `src/pages/beauty-index/justifications/index.astro`:
- Line ~5 (comment): "for all 25 languages" -> "for all 26 languages"
- Line ~31: "25 languages, 6 dimensions, 150 justifications" -> "26 languages, 6 dimensions, 156 justifications"
- Line ~33: "for 25 programming languages" -> "for 26 programming languages"

In `src/pages/index.astro`:
- Line ~181: "25 programming languages ranked" -> "26 programming languages ranked"

In `src/components/BeautyIndexJsonLd.astro`:
- Line ~5 (comment): "list all 25 languages" -> "list all 26 languages"
- Line ~23: "Ranking 25 programming languages" -> "Ranking 26 programming languages"

In `src/components/beauty-index/RankingBarChart.astro`:
- Line ~84: "all 25 languages sorted" -> "all 26 languages sorted"

In `src/components/beauty-index/FeatureMatrix.astro`:
- Line ~4 (comment): "25-row x 10-column" -> "26-row x 10-column"

In `src/lib/og-image.ts`:
- Line ~618: "Ranking 25 programming languages" -> "Ranking 26 programming languages"

**Category 2: LLMs.txt (5 replacements in 2 files)**

In `src/pages/llms.txt.ts`:
- Line ~89: "ranks 25 programming languages" -> "ranks 26 programming languages"
- Line ~94: "25 languages compared" -> "26 languages compared"
- Line ~95: "150 editorial justifications" -> "156 editorial justifications"

In `src/pages/llms-full.txt.ts`:
- Line ~143: "ranking of 25 programming languages" -> "ranking of 26 programming languages"
- Line ~184: "600 comparison pages available (25 x 24)" -> "650 comparison pages available (26 x 25)"

**Category 3: Blog Posts (6 replacements in 2 files)**

In `src/data/blog/the-beauty-index.mdx`:
- Line ~3 (frontmatter): "ranks 25 programming languages" -> "ranks 26 programming languages"
- Line ~30: "ranking of 25 programming languages" -> "ranking of 26 programming languages"
- Line ~36: "**25 languages** scored" -> "**26 languages** scored"
- Line ~227: "for all 25 languages" -> "for all 26 languages"
- Line ~229: "all 25 languages side by side" -> "all 26 languages side by side"
- IMPORTANT: Do NOT change line ~16 which contains "2013/03/25" -- this is a date in a RedMonk URL.

In `src/data/blog/building-kubernetes-observability-stack.mdx`:
- Line ~173: "25 languages are scored" -> "26 languages are scored"

**Category 4: Code Comments (3 remaining replacements in 2 files)**

In `src/pages/beauty-index/vs/[slug].astro`:
- Line ~4 (comment): "600 static pages (25 x 24)" -> "650 static pages (26 x 25)"

In `src/data/beauty-index/code-features.ts`:
- Line ~3137 (comment): "up to 25 languages" -> "up to 26 languages"

(Note: Comments in [slug].astro, code/index.astro, justifications/index.astro, BeautyIndexJsonLd.astro, and FeatureMatrix.astro are listed in Category 1 above since they are in the same files.)

**Category 5: PROJECT.md Documentation (7 count updates + validated requirements)**

In `.planning/PROJECT.md`:
- Line ~5: "25 programming languages ranked across 6 aesthetic dimensions" -> "26 programming languages ranked across 6 aesthetic dimensions"
- Line ~84: "for 25 languages across 6 dimensions" -> "for 26 languages across 6 dimensions"
- Line ~95: "25 radar charts in overview grid" -> "26 radar charts in overview grid"
- Line ~96: "for all 25 languages" -> "for all 26 languages"
- Line ~101: "All 25 languages per tab with syntax-highlighted code blocks (240 total)" -> "All 26 languages per tab with syntax-highlighted code blocks (260 total)"
- Line ~104: "overview and all 25 language pages" -> "overview and all 26 language pages"
- Line ~256: Update the summary line to reflect "26 languages ranked...26 detail pages...260 code blocks"

Add validated requirement lines under the v1.11 section (after the existing validated requirements), following the established pattern with checkmark prefix and " -- v1.11" suffix:
- Lisp language entry in languages.json with scores, tier (Handsome/44), character sketch -- v1.11
- Lisp signature code snippet with common-lisp syntax highlighting -- v1.11
- 6 dimension justifications for Lisp differentiating from Clojure -- v1.11
- Lisp registered in ALL_LANGS constant -- v1.11
- 10 code comparison snippets for Lisp across all feature tabs -- v1.11
- CLOS (defclass/defgeneric/defmethod) for Structs tab -- v1.11
- Condition/restart system for Error Handling tab -- v1.11
- All hardcoded "25 languages" references updated to "26 languages" -- v1.11
- Blog posts updated from 25 to 26 language references -- v1.11
- OG image text updated from 25 to 26 -- v1.11
- JSON-LD structured data description updated to 26 languages -- v1.11
- LLMs.txt files updated with 26 languages and derived counts -- v1.11
- PROJECT.md updated with Lisp validated requirements -- v1.11
- Full production build passes with Lisp detail page, OG image, and 650 VS pages -- v1.11

**CRITICAL anti-patterns -- do NOT change these:**
- `numberOfItems` in BeautyIndexJsonLd.astro (already dynamic from sorted.length)
- "2013/03/25" in the-beauty-index.mdx (RedMonk URL date)
- "25 years" in justifications.ts line 173 (C#'s age, not language count)
- "Efficiency 25%" in PROJECT.md line 130 (Dockerfile Analyzer weight)
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && grep -rn "25 languages\|25 programming" src/ .planning/PROJECT.md --include="*.astro" --include="*.ts" --include="*.tsx" --include="*.mdx" --include="*.md" | grep -v node_modules | grep -v "2013/03/25" | grep -v "25 years" | grep -v "Efficiency 25%" | grep -v ".planning/phases/" | grep -v ".planning/research/" | grep -v ".planning/codebase/" | grep -v ".planning/milestones/"</automated>
  </verify>
  <done>All 34+ text replacements applied. grep for "25 languages" or "25 programming" across source files returns zero results (excluding known false positives). PROJECT.md contains v1.11 validated requirements section with all 14 Lisp milestone requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Build verification and final validation</name>
  <files>dist/ (build output, not committed)</files>
  <action>
Run the full Astro production build and verify all Lisp integration artifacts are generated correctly.

1. Run `npx astro build` from the project root. The build must complete with zero errors.

2. Verify Lisp-specific pages exist:
   - `dist/beauty-index/lisp/index.html` (Lisp detail page)
   - `dist/open-graph/beauty-index/lisp.png` (Lisp OG image)

3. Count VS comparison pages: `ls dist/beauty-index/vs/*/index.html | wc -l` should return 650 (26 x 25).

4. Verify zero surviving "25 languages" references in built output:
   - `grep -r "25 languages" dist/` should return zero results
   - `grep -r "25 programming" dist/` should return zero results (excluding any false positives like the RedMonk URL date)

5. Verify "26 languages" appears in key built pages:
   - `grep "26 languages" dist/beauty-index/index.html` (overview page)
   - `grep "26 programming" dist/beauty-index/code/index.html` (code comparison)
   - `grep "26 programming" dist/llms.txt` (LLMs.txt)
   - `grep "156 editorial" dist/llms.txt` or `grep "156" dist/beauty-index/justifications/index.html` (derived count)

If the build fails or any verification step fails, diagnose and fix the root cause before declaring done.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -5 && echo "---LISP PAGE---" && ls dist/beauty-index/lisp/index.html 2>&1 && echo "---LISP OG---" && ls dist/open-graph/beauty-index/lisp.png 2>&1 && echo "---VS COUNT---" && ls dist/beauty-index/vs/*/index.html 2>/dev/null | wc -l && echo "---NO 25 LANGS---" && (grep -rl "25 languages" dist/ 2>/dev/null | head -5 || echo "PASS: zero results") && echo "---26 PRESENT---" && grep -l "26 languages" dist/beauty-index/index.html 2>&1</automated>
  </verify>
  <done>Astro build completes with zero errors. Lisp detail page at dist/beauty-index/lisp/index.html exists. Lisp OG image at dist/open-graph/beauty-index/lisp.png exists. VS comparison page count is 650. grep for "25 languages" in dist/ returns zero results. grep for "26 languages" confirms presence in overview page, code page, and LLMs.txt.</done>
</task>

</tasks>

<verification>
1. Source-level: `grep -rn "25 languages\|25 programming" src/` returns only known false positives (RedMonk URL date, C# age)
2. Build-level: `npx astro build` completes with zero errors
3. Output-level: `grep -r "25 languages" dist/` returns zero results
4. Lisp artifacts: detail page, OG image, and VS pages all generated
5. Derived counts: 156 justifications, 260 code blocks, 650 VS pages all correct
6. Documentation: PROJECT.md contains v1.11 validated requirements
</verification>

<success_criteria>
- All 34+ hardcoded "25" references updated to "26" with correct derived counts
- No false positives modified (RedMonk URL, C# age, scoring weight)
- PROJECT.md has complete v1.11 validated requirements section
- `astro build` passes with zero errors
- Lisp detail page, OG image, and 650 VS comparison pages generated
- Zero "25 languages" matches in built dist/ output
</success_criteria>

<output>
After completion, create `.planning/phases/71-site-wide-integration/71-01-SUMMARY.md`
</output>
