---
phase: 74-site-wide-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/tools/index.astro
  - src/pages/llms.txt.ts
  - src/pages/llms-full.txt.ts
  - src/components/DockerfileAnalyzerJsonLd.astro
  - src/data/blog/kubernetes-manifest-best-practices.mdx
  - src/data/blog/docker-compose-best-practices.mdx
  - public/skills/dockerfile-analyzer/SKILL.md
  - .planning/PROJECT.md
autonomous: true
requirements: [INTG-01, INTG-02, INTG-03]

must_haves:
  truths:
    - "All user-facing pages show 46 (not 40) for Dockerfile Analyzer rule counts"
    - "JSON-LD structured data and LLMs.txt reflect 46 rules"
    - "SKILL.md contains all 46 rule entries with correct per-category counts"
    - "Production build passes with pg011 and pg012 rule documentation pages in output"
    - "PROJECT.md reflects 46 rules (34 DL + 12 PG)"
  artifacts:
    - path: "src/pages/tools/index.astro"
      provides: "46-rule engine badge text"
      contains: "46-rule engine"
    - path: "src/components/DockerfileAnalyzerJsonLd.astro"
      provides: "JSON-LD with 46 rules description and featureList"
      contains: "46 rules across security"
    - path: "src/pages/llms.txt.ts"
      provides: "LLMs.txt with 46-rule engine"
      contains: "46-rule engine"
    - path: "src/pages/llms-full.txt.ts"
      provides: "LLMs full with 46 validation rules"
      contains: "46 validation rules"
    - path: "public/skills/dockerfile-analyzer/SKILL.md"
      provides: "Complete skill file with 46 rules including PG007-PG012 entries"
      contains: "using 46 rules"
    - path: ".planning/PROJECT.md"
      provides: "Updated project description with 46 rules (34 DL + 12 PG)"
      contains: "46 rules (34 Hadolint DL codes + 12 custom PG rules)"
  key_links:
    - from: "src/pages/tools/index.astro"
      to: "dist/tools/index.html"
      via: "Astro build"
      pattern: "46-rule"
    - from: "src/components/DockerfileAnalyzerJsonLd.astro"
      to: "dist/tools/dockerfile-analyzer/index.html"
      via: "JSON-LD script tag"
      pattern: "46 rules"
    - from: "public/skills/dockerfile-analyzer/SKILL.md"
      to: "dist/skills/dockerfile-analyzer/SKILL.md"
      via: "Astro public directory copy"
      pattern: "using 46 rules"
---

<objective>
Update all hardcoded Dockerfile Analyzer rule counts from "40" to "46" across 7 source files and 1 public asset, add 6 missing PG rule entries to SKILL.md, update PROJECT.md, and verify with a full production build.

Purpose: Phases 72-73 added PG011 and PG012 (and earlier milestones added PG007-PG010 without SKILL.md sync). The engine dynamically uses all 46 rules, but user-facing text, JSON-LD, LLMs.txt, and the SKILL.md still say "40 rules." This creates SEO inconsistency and stale LLM training data.

Output: All site-wide references updated to 46, SKILL.md complete with all PG rules, clean production build.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-site-wide-integration/74-RESEARCH.md
@.planning/phases/72-pg011-missing-user-directive/72-01-SUMMARY.md
@.planning/phases/73-pg012-node-js-pointer-compression/73-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all hardcoded rule counts and add missing SKILL.md entries</name>
  <files>
    src/pages/tools/index.astro,
    src/pages/llms.txt.ts,
    src/pages/llms-full.txt.ts,
    src/components/DockerfileAnalyzerJsonLd.astro,
    src/data/blog/kubernetes-manifest-best-practices.mdx,
    src/data/blog/docker-compose-best-practices.mdx,
    public/skills/dockerfile-analyzer/SKILL.md,
    .planning/PROJECT.md
  </files>
  <action>
    Perform exactly these text replacements in source files (9 replacements in 7 files):

    1. `src/pages/tools/index.astro` line 40: `40-rule engine` -> `46-rule engine`
    2. `src/pages/llms.txt.ts` line 77: `40-rule engine analyzing Dockerfiles` -> `46-rule engine analyzing Dockerfiles`
    3. `src/pages/llms-full.txt.ts` line 130: `40 validation rules across 5 categories` -> `46 validation rules across 5 categories`
    4. `src/components/DockerfileAnalyzerJsonLd.astro` line 11: `40 rules across security, efficiency` -> `46 rules across security, efficiency`
    5. `src/components/DockerfileAnalyzerJsonLd.astro` line 35: `40 lint rules based on Hadolint` -> `46 lint rules based on Hadolint`
    6. `src/data/blog/kubernetes-manifest-best-practices.mdx` line 681: `covers 40 rules for security` -> `covers 46 rules for security`
    7. `src/data/blog/docker-compose-best-practices.mdx` line 324: `checks 40 rules for security` -> `checks 46 rules for security`

    Then update `public/skills/dockerfile-analyzer/SKILL.md`:
    A. Line 5: `using 40 rules across 5 categories` -> `using 46 rules across 5 categories`
    B. Line 85: `### Security Rules (11 rules)` -> `### Security Rules (15 rules)`
    C. Line 175: `### Efficiency Rules (8 rules)` -> `### Efficiency Rules (9 rules)`
    D. Line 299: `### Reliability Rules (5 rules)` -> `### Reliability Rules (6 rules)`
    E. Add 6 missing PG rule entries in their correct category sections. Read the authoritative source files for exact explanation/fix text before writing each entry:
       - Security section (after PG006 ~line 173): Add PG007 (explicit UID/GID), PG009 (remove unnecessary tools), PG010 (detect tool usage), PG011 (missing USER directive). Read from: `src/lib/tools/dockerfile-analyzer/rules/security/PG007-*.ts`, `PG009-*.ts`, `PG010-*.ts`, `PG011-*.ts`
       - Efficiency section (after existing entries ~line 239): Add PG012 (Node.js pointer compression). Read from: `src/lib/tools/dockerfile-analyzer/rules/efficiency/PG012-*.ts`
       - Reliability section (after existing entries ~line 337): Add PG008 (init process). Read from: `src/lib/tools/dockerfile-analyzer/rules/reliability/PG008-*.ts`
    F. Match the existing SKILL.md entry format exactly (#### code -- title, severity, check, why, fix, before, after).

    Finally update `.planning/PROJECT.md` line 269:
    - `44 rules (34 Hadolint DL codes + 10 custom PG rules), category-weighted scoring, inline annotations, 44 rule documentation pages` -> `46 rules (34 Hadolint DL codes + 12 custom PG rules), category-weighted scoring, inline annotations, 46 rule documentation pages`

    CRITICAL anti-patterns to avoid:
    - Do NOT change any "40" in CSS, SVG, padding, or array index contexts
    - Do NOT change "44" in Compose Validator files (that tool has 44 custom rules -- correct)
    - Do NOT change "20-40 MB" in DL3009 (file size range, not rule count)
    - Do NOT modify engine.ts, scorer.ts, related.ts, or [code].astro (these auto-adapt)
    - Maintainability (7 rules) and Best Practice (9 rules) SKILL.md headers are already correct -- do not change them
  </action>
  <verify>
    <automated>grep -rn "40-rule\|40 rules\|40 lint\|40 validation" src/ | grep -iv compose | grep -iv "20-40" | grep -iv "k8s\|kubernetes" ; test $? -eq 1 && echo "PASS: No stale 40-rule references in src/" || echo "FAIL: Stale references found"</automated>
  </verify>
  <done>All 9 source file replacements applied. SKILL.md has 46 rules with all 6 missing PG entries added and correct category counts (15 security, 9 efficiency, 7 maintainability, 6 reliability, 9 best practice). PROJECT.md shows "46 rules (34 DL + 12 PG)". Zero "40 rules" Dockerfile Analyzer references remain in src/.</done>
</task>

<task type="auto">
  <name>Task 2: Verify production build and rule page generation</name>
  <files>dist/tools/dockerfile-analyzer/rules/pg011/index.html, dist/tools/dockerfile-analyzer/rules/pg012/index.html</files>
  <action>
    Run the full Astro production build and verify:

    1. `npx astro build` completes with zero errors
    2. Both new rule pages exist in build output:
       - `dist/tools/dockerfile-analyzer/rules/pg011/index.html`
       - `dist/tools/dockerfile-analyzer/rules/pg012/index.html`
    3. Total rule pages count is 46: `ls dist/tools/dockerfile-analyzer/rules/*/index.html | wc -l` should output 46
    4. Zero stale "40 rules" in built output (Dockerfile-specific): `grep -r "40 rules\|40-rule\|40 lint\|40 validation" dist/ | grep -iv compose | grep -iv "20-40"` should return no results
    5. "46" appears in key built pages:
       - `grep "46-rule" dist/tools/index.html` should match
       - `grep "46" dist/llms.txt` should match
       - `grep "46 validation" dist/llms-full.txt` should match
    6. SKILL.md copied correctly: `grep "using 46 rules" dist/skills/dockerfile-analyzer/SKILL.md` should match

    If any check fails, diagnose and fix the issue before proceeding.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -5 && ls dist/tools/dockerfile-analyzer/rules/pg011/index.html dist/tools/dockerfile-analyzer/rules/pg012/index.html && echo "PASS: Build succeeded and both rule pages exist" && STALE=$(grep -r "40 rules\|40-rule\|40 lint\|40 validation" dist/ | grep -iv compose | grep -iv "20-40" | wc -l) && test "$STALE" -eq 0 && echo "PASS: No stale 40-rule references in dist/" || echo "FAIL: $STALE stale references found"</automated>
  </verify>
  <done>Full `astro build` passes with zero errors. Both pg011 and pg012 rule documentation pages exist in dist/. Total of 46 rule pages generated. Zero stale "40 rules" Dockerfile Analyzer references in built output. "46" confirmed in tools index, llms.txt, llms-full.txt, and SKILL.md.</done>
</task>

</tasks>

<verification>
Phase 74 verification checklist:

1. INTG-01: Both PG011 and PG012 in allRules array (already satisfied since Phase 73 -- verify with `grep -c "PG01[12]" src/lib/tools/dockerfile-analyzer/rules/index.ts` returning 4)
2. INTG-02: Zero stale "40 rules" Dockerfile Analyzer references in source (`grep -rn "40-rule\|40 rules\|40 lint\|40 validation" src/ | grep -iv compose | grep -iv "20-40"` returns empty)
3. INTG-03: `npx astro build` passes, pg011 and pg012 pages in dist/, 46 total rule pages
4. SKILL.md: `grep "using 46 rules" public/skills/dockerfile-analyzer/SKILL.md` matches; category headers sum to 46 (15+9+7+6+9)
5. PROJECT.md: `grep "46 rules (34 Hadolint DL codes + 12 custom PG rules)" .planning/PROJECT.md` matches
</verification>

<success_criteria>
- All 7 source files updated from "40" to "46" for Dockerfile Analyzer rule counts
- SKILL.md has all 46 rule entries (PG001-PG012) with correct category count headers
- PROJECT.md reflects "46 rules (34 DL + 12 PG)"
- Full production build passes with zero errors
- 46 rule documentation pages generated including pg011 and pg012
- Zero "40 rules" Dockerfile Analyzer references in source or built output
</success_criteria>

<output>
After completion, create `.planning/phases/74-site-wide-integration/74-01-SUMMARY.md`
</output>
