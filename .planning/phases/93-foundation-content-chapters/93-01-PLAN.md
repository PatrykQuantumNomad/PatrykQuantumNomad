---
phase: 93-foundation-content-chapters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/guides/claude-code/pages/introduction.mdx
  - src/data/guides/claude-code/pages/context-management.mdx
autonomous: true
requirements: [CHAP-01, CHAP-02]

must_haves:
  truths:
    - "A reader with zero Claude Code experience can follow Chapter 1 from installation through first successful agentic interaction"
    - "A reader understands the 3-phase agentic loop (gather context, take action, verify results) after reading Chapter 1"
    - "A reader knows how to install Claude Code via native install script and understands available interfaces after Chapter 1"
    - "A reader understands the CLAUDE.md file hierarchy (managed, project, user) and .claude/rules/ directory after Chapter 2"
    - "A reader can manage context window limits using /compact and /context commands after Chapter 2"
    - "A reader understands file exclusion via respectGitignore and claudeMdExcludes (not a standalone .claudeignore file) after Chapter 2"
  artifacts:
    - path: "src/data/guides/claude-code/pages/introduction.mdx"
      provides: "Chapter 1 -- Introduction and Getting Started"
      contains: "AgenticLoopDiagram"
      min_lines: 250
    - path: "src/data/guides/claude-code/pages/context-management.mdx"
      provides: "Chapter 2 -- Project Context and Memory Management"
      min_lines: 300
  key_links:
    - from: "src/data/guides/claude-code/pages/introduction.mdx"
      to: "src/components/guide/AgenticLoopDiagram.astro"
      via: "MDX import and embed"
      pattern: "import AgenticLoopDiagram"
    - from: "src/data/guides/claude-code/pages/introduction.mdx"
      to: "src/components/guide/CodeBlock.astro"
      via: "MDX import for code snippets"
      pattern: "import CodeBlock"
    - from: "src/data/guides/claude-code/pages/context-management.mdx"
      to: "src/components/guide/CodeBlock.astro"
      via: "MDX import for code snippets"
      pattern: "import CodeBlock"
---

<objective>
Write Chapter 1 (Introduction & Getting Started) and Chapter 2 (Project Context & Memory Management) for the Claude Code guide.

Purpose: These two chapters form the entry point for the guide. Chapter 1 takes a reader from zero knowledge to their first agentic interaction. Chapter 2 teaches how to give Claude Code project-specific context via CLAUDE.md and manage the context window. Together they answer "what is this tool and how do I configure it for my project?"

Output: Two complete MDX chapter files ready for production build
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-foundation-content-chapters/93-RESEARCH.md

@src/data/guides/claude-code/guide.json
@src/data/guides/claude-code/pages/introduction.mdx
@src/components/guide/CodeBlock.astro
@src/components/guide/AgenticLoopDiagram.astro
@src/data/guides/fastapi-production/pages/nfr-introduction.mdx
</context>

<interfaces>
<!-- CodeBlock.astro Props -->
```typescript
interface Props {
  code: string;    // Raw source code to display
  lang: string;    // Language identifier (e.g. "typescript", "bash", "json", "markdown", "text")
  title?: string;  // Optional file path displayed as tab-style header
  caption?: string; // Optional caption displayed below
}
```

<!-- Import paths from src/data/guides/claude-code/pages/ (4 levels deep) -->
```mdx
import CodeBlock from '../../../../components/guide/CodeBlock.astro';
import AgenticLoopDiagram from '../../../../components/guide/AgenticLoopDiagram.astro';
```

<!-- MDX frontmatter schema -->
```yaml
title: string       # Chapter title
description: string # Short description
order: number       # 0-based chapter position
slug: string        # URL slug (must match guide.json)
lastVerified: date  # Content verification date
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write Chapter 1 -- Introduction and Getting Started</name>
  <files>src/data/guides/claude-code/pages/introduction.mdx</files>
  <action>
Replace the existing stub introduction.mdx with full chapter content (300-400 lines target). Structure:

**Frontmatter:** title "Introduction & Getting Started", description from guide.json, order 0, slug "introduction", lastVerified 2026-03-10.

**Imports:** CodeBlock and AgenticLoopDiagram (4-level relative path).

**Sections (use h2 headings):**

1. "What You Will Learn" -- 2-3 sentence overview of chapter scope (agentic loop, installation, interfaces, core tools, checkpoints).

2. "What Is Claude Code?" -- Brief intro: agentic coding tool that runs in your terminal, reads your codebase, edits files, runs commands. Emphasize it operates directly on your filesystem (not a hosted IDE). Note it is available across terminal, IDE extensions, desktop app, web, and Slack.

3. "The Agentic Loop" -- Explain the 3-phase cycle: gather context (reads files, searches code, understands project), take action (edits files, runs commands, creates files), verify results (runs tests, checks output, validates changes). Embed AgenticLoopDiagram after the explanation. Add a brief paragraph explaining the diagram. Mention that Claude Code loops through these phases autonomously until the task is complete or it needs clarification.

4. "Installation" -- Lead with native install script (curl -fsSL https://claude.ai/install.sh | bash) via CodeBlock with title "macOS / Linux / WSL". Show Homebrew alternative (brew install claude-code) via CodeBlock. Show Windows (winget install Anthropic.ClaudeCode) via CodeBlock. Note npm install method exists but is deprecated -- use native installer instead. After installation, show the startup command (`claude`) via CodeBlock.

5. "Available Interfaces" -- Brief list of where Claude Code runs: Terminal CLI (primary), VS Code extension, JetBrains plugin, Desktop app (macOS/Windows), Web (console.anthropic.com), Slack integration. Note that terminal CLI is the core experience and all features are available there. IDE extensions provide inline integration.

6. "Core Tools" -- Explain the 5 tool categories Claude Code uses autonomously: File operations (Read, Write, Edit files), Search (Glob for file patterns, Grep for content search), Execution (Bash for running commands), Web (WebFetch for reading URLs), Code intelligence (codebase understanding, symbol navigation). Use a brief descriptive format, not a table.

7. "Checkpoints and Undo" -- Explain that Claude Code creates checkpoints before making changes. If something goes wrong, press Esc twice (Esc-Esc) to stop and rewind. File snapshots are created before edits so you can always get back to the previous state. This makes it safe to let Claude Code work autonomously.

8. "Your First Session" -- Walk through a first interaction: cd into a project, run `claude`, ask a question about the codebase (show example prompt via CodeBlock with lang="text"), then ask Claude to make a change (show example via CodeBlock). Explain what happens: Claude reads files, proposes changes, asks for permission, applies them.

9. "Best Practices" -- 3-5 bullet points: start with small tasks to build trust, review diffs before approving, use /undo if something goes wrong, keep commits small and focused.

10. "Further Reading" -- Links to official docs (overview, quickstart, how-claude-code-works pages at code.claude.com/docs/en/) and cross-link to Chapter 2 for project context configuration.

**Critical accuracy rules:**
- Native install script is the RECOMMENDED method (not npm)
- The agentic loop has exactly 3 phases: gather context, take action, verify results
- Esc-Esc is the rewind shortcut (two presses, not one)
- Do NOT invent Claude Code commands that do not exist in official docs
- Do NOT mention features that are covered in later chapters (permissions, models, hooks, etc.) except as brief forward references
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <done>introduction.mdx is 250+ lines, builds without errors, imports AgenticLoopDiagram and CodeBlock, covers installation (native first, npm deprecated), agentic loop explanation with diagram, core tools, checkpoints, and first session walkthrough</done>
</task>

<task type="auto">
  <name>Task 2: Write Chapter 2 -- Project Context and Memory Management</name>
  <files>src/data/guides/claude-code/pages/context-management.mdx</files>
  <action>
Create new context-management.mdx file (350-450 lines target). Structure:

**Frontmatter:** title "Project Context & Memory", description from guide.json, order 1, slug "context-management", lastVerified 2026-03-10.

**Imports:** CodeBlock (4-level relative path). No diagram components for this chapter.

**Sections (use h2 headings):**

1. "What You Will Learn" -- 2-3 sentence overview: CLAUDE.md hierarchy, auto-memory, context management, file exclusion mechanisms.

2. "The CLAUDE.md File" -- Explain that CLAUDE.md is the primary way to give Claude Code project-specific instructions. It is loaded automatically at session start. Show a minimal example CLAUDE.md via CodeBlock (lang="markdown", title="CLAUDE.md") containing project description, tech stack, coding conventions, and a few specific rules. Explain that Claude reads this before doing anything.

3. "CLAUDE.md Hierarchy" -- Explain the 3 scopes where CLAUDE.md can exist:
   - Managed policy (enterprise, set by admins) -- highest priority
   - Project (./CLAUDE.md or ./.claude/CLAUDE.md) -- project-specific instructions
   - User (~/.claude/CLAUDE.md) -- personal preferences across all projects
   Show example project CLAUDE.md and user CLAUDE.md via CodeBlock. Explain precedence: managed > project > user. Note that .claude/CLAUDE.md is equivalent to root CLAUDE.md but keeps the root directory clean.

4. "Writing Effective Instructions" -- Best practices: target under 200 lines, use markdown structure (headings, bullets), be specific about conventions (naming, patterns), include what to avoid and why. Show a before/after example: vague instructions vs specific actionable ones via CodeBlock.

5. "Path-Specific Rules with .claude/rules/" -- Explain the .claude/rules/ directory for rules that apply only to certain file paths. Each file has YAML frontmatter with a glob pattern. Show example file via CodeBlock (lang="markdown", title=".claude/rules/api-routes.md") with frontmatter containing `globs: "src/api/**/*.ts"` and rules specific to API files. Explain when to use rules/ vs CLAUDE.md (rules/ for path-specific, CLAUDE.md for project-wide).

6. "The @import Syntax" -- Explain @path/to/file references in CLAUDE.md that pull in content from other files. Show example via CodeBlock. Note the 5-hop recursive depth limit. Use case: keeping CLAUDE.md concise by importing detailed docs.

7. "Auto-Memory System" -- Explain that Claude Code remembers things across sessions via ~/.claude/projects/ directory. The MEMORY.md file has a 200-line limit. Topic-specific memory files are loaded on-demand. Show where memory is stored. Mention the /memory command for browsing loaded files and toggling auto-memory.

8. "Context Window Management" -- Explain the context window concept (how much Claude can "see" at once). Cover:
   - /context command to see current usage
   - Auto-compaction (Claude automatically compresses when window fills up)
   - /compact command with optional focus instruction ("focus on the auth module")
   - /clear to start fresh between unrelated tasks
   Show /compact example via CodeBlock (lang="text").

9. "File Exclusion" -- IMPORTANT: Do NOT describe .claudeignore as a standalone dotfile. The actual mechanisms are:
   - respectGitignore setting (default: true) -- Claude Code respects .gitignore patterns
   - claudeMdExcludes -- patterns specified in CLAUDE.md to exclude files from loading
   Show example of both via CodeBlock. Explain that files matching these patterns will not be read or loaded into context.

10. "Best Practices" -- 5-7 bullets: keep CLAUDE.md under 200 lines, use path-specific rules for team/area conventions, clear context between unrelated tasks, review auto-memory periodically, use @imports for detailed documentation.

11. "Further Reading" -- Links to official docs (memory page at code.claude.com/docs/en/memory) and cross-link to Chapter 3 for model and cost configuration.

**Critical accuracy rules:**
- .claudeignore is NOT a standalone file -- use respectGitignore and claudeMdExcludes
- MEMORY.md has a 200-line limit
- @import has a 5-hop recursive depth limit
- .claude/rules/ files use YAML frontmatter with glob patterns
- Do NOT invent commands or features not in official docs
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <done>context-management.mdx is 300+ lines, builds without errors, imports CodeBlock, covers CLAUDE.md hierarchy (managed/project/user), .claude/rules/ with glob patterns, @import syntax, auto-memory with 200-line limit, context management commands, and file exclusion via respectGitignore + claudeMdExcludes (NOT .claudeignore as standalone file)</done>
</task>

</tasks>

<verification>
1. `npx astro build` completes without errors
2. Both chapter files exist at correct paths with correct frontmatter (slug matching guide.json)
3. introduction.mdx imports and embeds AgenticLoopDiagram
4. Both files import CodeBlock for code examples
5. No references to npm as primary install method (deprecated)
6. No references to .claudeignore as standalone file
7. `ls dist/guides/claude-code/introduction/index.html dist/guides/claude-code/context-management/index.html` both exist after build
</verification>

<success_criteria>
- Both chapters render at their correct URLs (/guides/claude-code/introduction/ and /guides/claude-code/context-management/)
- Chapter 1 is 250-400 lines with AgenticLoopDiagram embedded and native install as primary method
- Chapter 2 is 300-450 lines covering CLAUDE.md hierarchy, auto-memory, context management, and file exclusion via settings (not .claudeignore)
- Production build passes with zero errors
- All code examples use CodeBlock component (not raw markdown fences)
</success_criteria>

<output>
After completion, create `.planning/phases/93-foundation-content-chapters/93-01-SUMMARY.md`
</output>
