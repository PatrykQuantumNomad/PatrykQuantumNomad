---
phase: 108-guided-tours-compare-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai-landscape/tours.ts
  - src/lib/ai-landscape/comparisons.ts
  - src/lib/ai-landscape/routes.ts
  - src/lib/ai-landscape/__tests__/tours.test.ts
  - src/lib/ai-landscape/__tests__/comparisons.test.ts
autonomous: true
requirements: [NAV-03, SEO-08]

must_haves:
  truths:
    - "Three curated tour definitions exist with validated node IDs"
    - "Twelve curated comparison pairs exist with validated concept slugs"
    - "VS page route helper produces correct /ai-landscape/vs/ URLs"
    - "All tour step nodeIds and comparison concept slugs resolve to real nodes in the dataset"
  artifacts:
    - path: "src/lib/ai-landscape/tours.ts"
      provides: "Tour interface and 3 curated TOURS array with TourStep narratives"
      exports: ["Tour", "TourStep", "TOURS"]
    - path: "src/lib/ai-landscape/comparisons.ts"
      provides: "ComparisonPair interface, POPULAR_COMPARISONS array, comparisonSlug helper"
      exports: ["ComparisonPair", "POPULAR_COMPARISONS", "comparisonSlug"]
    - path: "src/lib/ai-landscape/routes.ts"
      provides: "vsPageUrl helper for VS page routing"
      exports: ["vsPageUrl"]
    - path: "src/lib/ai-landscape/__tests__/tours.test.ts"
      provides: "Tour data validation tests"
    - path: "src/lib/ai-landscape/__tests__/comparisons.test.ts"
      provides: "Comparison data validation tests"
  key_links:
    - from: "src/lib/ai-landscape/tours.ts"
      to: "src/data/ai-landscape/nodes.json"
      via: "tour step nodeIds must match node slugs"
      pattern: "nodeId.*slug"
    - from: "src/lib/ai-landscape/comparisons.ts"
      to: "src/data/ai-landscape/nodes.json"
      via: "comparison concept slugs must match node slugs"
      pattern: "concept1.*slug"
---

<objective>
Create the data layer for guided tours and concept comparisons -- tour definitions, curated comparison pairs, route helpers, and comprehensive test coverage validating all node references against the real dataset.

Purpose: Every downstream plan (tour UI, compare UI, VS pages) depends on these data files. Establishing them first with full test coverage prevents broken references and ensures all node IDs are valid.

Output: tours.ts, comparisons.ts, updated routes.ts, and two test files that validate all data against the live dataset.
</objective>

<execution_context>
@/Users/patrykattc/work/git/PatrykQuantumNomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/PatrykQuantumNomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-guided-tours-compare-mode/108-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/ai-landscape/schema.ts:
```typescript
export type AiNode = z.infer<typeof aiNodeSchema>;
// AiNode has: slug, name, cluster, parentId, simpleDescription, technicalDescription, whyItMatters, examples
export type Edge = z.infer<typeof edgeSchema>;
// Edge has: source, target, label, type
export type Cluster = z.infer<typeof clusterSchema>;
// Cluster has: id, name, color, darkColor
```

From src/lib/ai-landscape/routes.ts:
```typescript
export const AI_LANDSCAPE_BASE = '/ai-landscape/';
export function conceptUrl(slug: string): string;
export function ogImageUrl(slug: string): string;
```

From src/lib/ai-landscape/ancestry.ts:
```typescript
export interface AncestryItem { slug: string; name: string; cluster: string; }
export function buildAncestryChain(nodeSlug: string, nodesMap: Map<string, AiNode>): AncestryItem[];
```

Dataset: 51 nodes in src/data/ai-landscape/nodes.json, 66 edges in src/data/ai-landscape/graph.json.
Node id === slug throughout the dataset.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tours.ts and comparisons.ts data files</name>
  <files>src/lib/ai-landscape/tours.ts, src/lib/ai-landscape/comparisons.ts, src/lib/ai-landscape/routes.ts</files>
  <behavior>
    - tours.ts exports Tour and TourStep interfaces and a TOURS array with exactly 3 tours
    - Tour 1 "The Big Picture": steps through AI -> ML -> NN -> DL -> GenAI -> LLMs -> Agentic AI (7 steps)
    - Tour 2 "How ChatGPT Works": steps through AI -> ML -> NN -> DL -> Transformers -> Foundation Models -> LLMs -> Prompt Engineering -> Context Windows -> RLHF/DPO (10 steps)
    - Tour 3 "What is Agentic AI": steps through AI -> ML -> Reinforcement Learning -> Foundation Models -> Reasoning Models -> Agentic AI -> Autonomy -> Tool Use -> MCP (9 steps)
    - Each TourStep has nodeId (valid slug) and narrative (1-2 sentence explanation)
    - comparisons.ts exports ComparisonPair interface, POPULAR_COMPARISONS array (~12 pairs), and comparisonSlug() helper
    - comparisonSlug('deep-learning', 'machine-learning') returns 'deep-learning-vs-machine-learning' (alphabetical order)
    - All comparison slugs in POPULAR_COMPARISONS are canonically ordered (concept1 < concept2 alphabetically)
    - routes.ts gains vsPageUrl(slug) returning /ai-landscape/vs/{slug}/
  </behavior>
  <action>
    Create `src/lib/ai-landscape/tours.ts`:
    - Export `TourStep` interface: `{ nodeId: string; narrative: string }`
    - Export `Tour` interface: `{ id: string; title: string; description: string; steps: TourStep[] }`
    - Export `TOURS` const array with 3 tours as specified in the research (see 108-RESEARCH.md "Tour Data: Three Curated Paths" section for exact node sequences)
    - Each step narrative should be 1-2 sentences explaining what the concept is and why it matters in the tour's context
    - Use single quotes, 2-space indent, trailing commas per project conventions

    Create `src/lib/ai-landscape/comparisons.ts`:
    - Export `ComparisonPair` interface: `{ slug: string; concept1: string; concept2: string; question: string; summary: string }`
    - Export `comparisonSlug(slug1: string, slug2: string): string` that sorts both slugs alphabetically and joins with '-vs-'
    - Export `POPULAR_COMPARISONS` const array with ~12 curated pairs from the research (see 108-RESEARCH.md "Recommended Popular Comparisons" section)
    - Each pair: slug is the canonical `concept1-vs-concept2` (alphabetical), question is SEO-friendly "X vs Y: What's the Difference?", summary is 2-3 sentence meta description
    - CRITICAL: Do NOT include a parseComparisonSlug function -- slug parsing is ambiguous with hyphenated concept names. Use the curated array for lookups instead.

    Update `src/lib/ai-landscape/routes.ts`:
    - Add `export function vsPageUrl(slug: string): string` returning `/ai-landscape/vs/${slug}/`
    - Add `export function vsOgImageUrl(slug: string): string` returning `/open-graph/ai-landscape/vs/${slug}.png`
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-landscape/__tests__/tours.test.ts src/lib/ai-landscape/__tests__/comparisons.test.ts -x</automated>
  </verify>
  <done>Three tour definitions with valid node IDs, twelve comparison pairs with valid concept slugs, and route helpers all exported and passing tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create unit tests for tours and comparisons data</name>
  <files>src/lib/ai-landscape/__tests__/tours.test.ts, src/lib/ai-landscape/__tests__/comparisons.test.ts</files>
  <behavior>
    - tours.test.ts: TOURS has exactly 3 entries; each tour has >= 5 steps; all step nodeIds exist in nodes.json; no duplicate nodeIds within a tour; each step has non-empty narrative
    - comparisons.test.ts: POPULAR_COMPARISONS has >= 10 entries; all concept1/concept2 slugs exist in nodes.json; slugs are canonically ordered (concept1 < concept2); no duplicate slugs; comparisonSlug() returns alphabetical ordering; vsPageUrl() returns correct path; each pair has non-empty question and summary
  </behavior>
  <action>
    NOTE: Write tests FIRST (before Task 1 implementation), then implement Task 1 to make them pass. If Task 1 is already done, run tests to verify.

    Create `src/lib/ai-landscape/__tests__/tours.test.ts`:
    - Import TOURS from '../tours'
    - Import the real nodes.json dataset: `import nodesData from '../../../data/ai-landscape/nodes.json'` (use `{ assert: { type: 'json' } }` if needed, or read the file -- follow the pattern in existing tests like content.test.ts)
    - Build a Set of valid node slugs from the dataset
    - Test: "TOURS has exactly 3 entries"
    - Test: "each tour has a non-empty id, title, and description"
    - Test: "each tour has at least 5 steps" (per NAV-03)
    - Test: "all tour step nodeIds exist in the dataset" (iterate every step in every tour)
    - Test: "no duplicate nodeIds within a single tour"
    - Test: "each step has a non-empty narrative"

    Create `src/lib/ai-landscape/__tests__/comparisons.test.ts`:
    - Import POPULAR_COMPARISONS, comparisonSlug from '../comparisons'
    - Import vsPageUrl, vsOgImageUrl from '../routes'
    - Import the real nodes.json dataset
    - Build a Set of valid node slugs from the dataset
    - Test: "POPULAR_COMPARISONS has at least 10 entries"
    - Test: "all concept1 and concept2 slugs exist in the dataset"
    - Test: "concept1 < concept2 alphabetically in every pair" (canonical ordering)
    - Test: "no duplicate comparison slugs"
    - Test: "each pair has non-empty question and summary"
    - Test: "comparisonSlug() produces alphabetical ordering" (test with reversed inputs)
    - Test: "vsPageUrl() returns /ai-landscape/vs/{slug}/"
    - Test: "vsOgImageUrl() returns /open-graph/ai-landscape/vs/{slug}.png"

    Follow existing test patterns: look at `src/lib/ai-landscape/__tests__/routes.test.ts` and `src/lib/ai-landscape/__tests__/schema.test.ts` for import style and assertion patterns.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai-landscape/__tests__/tours.test.ts src/lib/ai-landscape/__tests__/comparisons.test.ts -x</automated>
  </verify>
  <done>All tour and comparison validation tests pass against the real dataset; zero failing tests</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/ai-landscape/__tests__/tours.test.ts src/lib/ai-landscape/__tests__/comparisons.test.ts` -- all tests pass
- `npx vitest run` -- full test suite still passes (no regressions)
- TypeScript compiles without errors: `npx astro check 2>&1 | tail -5` (or `npx tsc --noEmit`)
</verification>

<success_criteria>
- tours.ts exports 3 tours with verified node IDs and narratives
- comparisons.ts exports 12 curated pairs with verified concept slugs in canonical order
- routes.ts has vsPageUrl() and vsOgImageUrl() helpers
- All tests pass against the real 51-node dataset
- No new dependencies introduced
</success_criteria>

<output>
After completion, create `.planning/phases/108-guided-tours-compare-mode/108-01-SUMMARY.md`
</output>
