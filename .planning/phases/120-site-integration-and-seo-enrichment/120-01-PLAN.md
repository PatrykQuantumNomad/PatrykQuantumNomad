---
phase: 120-site-integration-and-seo-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/blog/[slug].astro
  - src/data/blog/dark-code.mdx
autonomous: true
requirements: [INTG-01, INTG-02, INTG-03, INTG-06, INTG-08]

must_haves:
  truths:
    - "Dark Code post page includes BlogPosting JSON-LD with articleSection 'Code Quality' and about field of type CreativeWork"
    - "Dark Code post page includes FAQPage JSON-LD with 5 question-answer pairs mapping the Dark Code Spectrum dimensions"
    - "OG image auto-generates at /open-graph/blog/dark-code.png after build"
    - "Post appears in sitemap, RSS feed, blog listing, and LLMs.txt after build"
    - "Post is publicly accessible with draft: false"
  artifacts:
    - path: "src/pages/blog/[slug].astro"
      provides: "isDarkCodePost boolean, articleSection, aboutDataset, darkCodeFAQ array, FAQPageJsonLd rendering"
      contains: "isDarkCodePost"
    - path: "src/data/blog/dark-code.mdx"
      provides: "Published blog post"
      contains: "draft: false"
  key_links:
    - from: "src/pages/blog/[slug].astro"
      to: "BlogPostingJsonLd.astro"
      via: "articleSection and about props"
      pattern: "isDarkCodePost.*Code Quality"
    - from: "src/pages/blog/[slug].astro"
      to: "FAQPageJsonLd.astro"
      via: "darkCodeFAQ array conditional rendering"
      pattern: "darkCodeFAQ\\.length > 0"
    - from: "src/data/blog/dark-code.mdx"
      to: "all pipelines (sitemap, RSS, LLMs.txt, OG, blog listing)"
      via: "draft: false flag"
      pattern: "draft: false"
---

<objective>
Wire the Dark Code blog post into all site integration pipelines: JSON-LD structured data enrichment (BlogPosting with articleSection and about fields), FAQ schema injection for rich results, and publish activation by setting draft: false.

Purpose: Make the Dark Code post fully discoverable by search engines (via JSON-LD, FAQ rich results, sitemap, RSS), social platforms (via OG image), AI systems (via LLMs.txt), and human visitors (via blog listing).

Output: Two modified files -- [slug].astro with Dark Code integration data, dark-code.mdx with draft: false.
</objective>

<execution_context>
@/Users/patrykattc/work/git/PatrykQuantumNomad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/PatrykQuantumNomad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-site-integration-and-seo-enrichment/120-RESEARCH.md

<interfaces>
<!-- Key patterns and contracts from the codebase. Executor should follow these exactly. -->

From src/pages/blog/[slug].astro (boolean flag pattern, lines 40-69):
```typescript
// Existing boolean flags (add isDarkCodePost after line 46):
const isClaudeCodeRefreshPost = post.id === 'claude-code-guide-refresh';

// articleSection ternary chain (add before final : undefined at line 54):
  : isClaudeCodeRefreshPost ? 'AI Developer Tools'
  : undefined;

// aboutDataset ternary chain (add before final : undefined at line 69):
  : isClaudeCodeRefreshPost
    ? { type: 'WebPage', name: 'Claude Code Guide', url: 'https://patrykgolabek.dev/guides/claude-code/' }
    : undefined;
```

From src/pages/blog/[slug].astro (FAQ rendering pattern, lines 270-276):
```astro
{beautyIndexFAQ.length > 0 && <FAQPageJsonLd items={beautyIndexFAQ} />}
{k8sFAQ.length > 0 && <FAQPageJsonLd items={k8sFAQ} />}
{composeFAQ.length > 0 && <FAQPageJsonLd items={composeFAQ} />}
{dockerfileFAQ.length > 0 && <FAQPageJsonLd items={dockerfileFAQ} />}
{ghaFAQ.length > 0 && <FAQPageJsonLd items={ghaFAQ} />}
{claudeCodeGuideFAQ.length > 0 && <FAQPageJsonLd items={claudeCodeGuideFAQ} />}
{claudeCodeRefreshFAQ.length > 0 && <FAQPageJsonLd items={claudeCodeRefreshFAQ} />}
```

From src/components/FAQPageJsonLd.astro (props interface):
```typescript
interface Props {
  items: { question: string; answer: string }[];
}
```

From src/data/blog/dark-code.mdx (current frontmatter):
```yaml
draft: true
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add isDarkCodePost boolean, JSON-LD fields, and FAQ array to [slug].astro</name>
  <files>src/pages/blog/[slug].astro</files>
  <action>
Modify src/pages/blog/[slug].astro following the exact boolean-flag pattern used by the 7 existing specialized posts. Make these 4 additions:

1. **Boolean flag** -- Add after line 46 (the isClaudeCodeRefreshPost declaration):
   ```typescript
   const isDarkCodePost = post.id === 'dark-code';
   ```

2. **articleSection** -- Add to the ternary chain BEFORE the final `: undefined` (currently at end of line 54). Insert:
   ```typescript
   : isDarkCodePost ? 'Code Quality'
   ```

3. **aboutDataset** -- Add to the ternary chain BEFORE the final `: undefined` (currently at end of line 69). Insert:
   ```typescript
   : isDarkCodePost
     ? { type: 'CreativeWork', name: 'The Dark Code Spectrum', url: 'https://patrykgolabek.dev/blog/dark-code/' }
   ```
   Use `CreativeWork` (not Article, Dataset, or Thing) -- it is the most semantically accurate Schema.org type for an original framework/methodology.

4. **FAQ array** -- Add after the last existing FAQ array (claudeCodeRefreshFAQ, which ends around line 235). Create:
   ```typescript
   const darkCodeFAQ = isDarkCodePost ? [
     {
       question: 'What is dark code?',
       answer: 'Dark code is code executing in production that no one on the current team understands, no one owns, and no one can safely change. It passes CI, ships to users, and silently accumulates risk until it fails.',
     },
     {
       question: 'What is the Dark Code Spectrum?',
       answer: 'The Dark Code Spectrum is a 5-dimension diagnostic framework for measuring invisible codebase decay: Clone Density, Ownership Vacuum, Comprehension Decay, Refactoring Deficit, and Vulnerability Surface.',
     },
     {
       question: 'How do AI coding assistants accelerate dark code?',
       answer: 'AI assistants increase code output without proportional increases in comprehension or ownership. Studies show a 4x increase in code clones, refactoring collapsing from 25% to under 10%, and AI-assisted developers scoring 17% lower on code comprehension.',
     },
     {
       question: 'How do you measure dark code in a codebase?',
       answer: 'Measure each Dark Code Spectrum dimension: clone-to-unique ratio for Clone Density, files with no active committer for Ownership Vacuum, comprehension assessments for Comprehension Decay, refactoring-to-new-code ratio for Refactoring Deficit, and vulnerability density per KLOC for Vulnerability Surface.',
     },
     {
       question: 'Can dark code be prevented?',
       answer: 'Dark code cannot be eliminated but can be managed through deliberate illumination: mandatory comprehension reviews, ownership registries with accountable maintainers, enforced refactoring budgets, and continuous vulnerability scanning with ownership-aware triage.',
     },
   ] : [];
   ```
   All FAQ answers are under 300 characters for optimal Google rich results display.

5. **FAQ rendering** -- Add after line 276 (the claudeCodeRefreshFAQ rendering line):
   ```astro
   {darkCodeFAQ.length > 0 && <FAQPageJsonLd items={darkCodeFAQ} />}
   ```

CRITICAL: In steps 2 and 3, the new isDarkCodePost conditions MUST be inserted BEFORE the final `: undefined` in each ternary chain, not after it. If placed after, the code will be unreachable.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && grep -q "isDarkCodePost" src/pages/blog/\[slug\].astro && grep -q "Code Quality" src/pages/blog/\[slug\].astro && grep -q "darkCodeFAQ" src/pages/blog/\[slug\].astro && grep -q "CreativeWork" src/pages/blog/\[slug\].astro && echo "PASS: All 4 additions present"</automated>
  </verify>
  <done>
    - isDarkCodePost boolean declared matching post.id === 'dark-code'
    - articleSection returns 'Code Quality' for Dark Code post
    - aboutDataset returns CreativeWork with name 'The Dark Code Spectrum' and correct URL
    - darkCodeFAQ array contains 5 question-answer pairs with answers under 300 chars
    - FAQPageJsonLd renders when darkCodeFAQ has items
  </done>
</task>

<task type="auto">
  <name>Task 2: Set draft: false and verify build with all pipelines</name>
  <files>src/data/blog/dark-code.mdx</files>
  <action>
1. Open src/data/blog/dark-code.mdx and change the frontmatter field `draft: true` to `draft: false`. This is the single toggle that activates ALL integration pipelines simultaneously:
   - Sitemap inclusion (via @astrojs/sitemap with draft filter)
   - RSS feed inclusion (via @astrojs/rss with draft filter)
   - Blog listing page inclusion (via getCollection with draft filter)
   - OG image generation (via [...slug].png.ts with draft filter)
   - LLMs.txt entry (via llms.txt.ts with draft filter)

2. Run `npx astro build` to verify the production build completes with zero errors.

3. After build, verify all 5 integration pipelines activated:
   - Check `dist/open-graph/blog/dark-code.png` exists (INTG-01: OG image)
   - Check `dist/sitemap-0.xml` contains 'dark-code' (INTG-08: sitemap)
   - Check `dist/rss.xml` contains 'dark-code' (INTG-08: RSS)
   - Check `dist/llms.txt` contains 'dark-code' (INTG-06: LLMs.txt)
   - Check `dist/blog/dark-code/index.html` exists (INTG-08: blog listing generates the page)

Do NOT manually add entries to RSS, sitemap, or LLMs.txt -- these are all dynamically generated from the blog collection's draft filter.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -5 && test -f dist/open-graph/blog/dark-code.png && echo "OG: PASS" && grep -q 'dark-code' dist/sitemap-0.xml && echo "Sitemap: PASS" && grep -q 'dark-code' dist/rss.xml && echo "RSS: PASS" && grep -q 'dark-code' dist/llms.txt && echo "LLMs: PASS" && test -f dist/blog/dark-code/index.html && echo "Page: PASS"</automated>
  </verify>
  <done>
    - dark-code.mdx frontmatter has draft: false
    - Production build completes with zero errors
    - OG image exists at dist/open-graph/blog/dark-code.png
    - Sitemap includes dark-code entry
    - RSS feed includes dark-code entry
    - LLMs.txt includes dark-code entry
    - Blog post page generated at dist/blog/dark-code/index.html
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. This plan adds static structured data (JSON-LD) to a statically generated page and toggles a boolean frontmatter flag. No user input, authentication, API calls, or dynamic data flows are involved.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | Information Disclosure | FAQ JSON-LD answers | accept | FAQ content is intentionally public structured data for search engine consumption. No sensitive information exposed. |
| T-120-02 | Tampering | aboutDataset URL | accept | URL is hardcoded to the site's own domain (patrykgolabek.dev). Static site generation means no runtime injection possible. |
</threat_model>

<verification>
After both tasks complete, run a single comprehensive build verification:
1. `npx astro build` completes with zero errors
2. `grep -c 'isDarkCodePost' src/pages/blog/[slug].astro` returns at least 4 (boolean, articleSection, aboutDataset, FAQ guard)
3. `dist/open-graph/blog/dark-code.png` exists
4. `dist/sitemap-0.xml` contains 'dark-code'
5. `dist/rss.xml` contains 'dark-code'
6. `dist/llms.txt` contains 'dark-code'
7. `dist/blog/dark-code/index.html` contains 'FAQPage' (FAQ JSON-LD rendered)
8. `dist/blog/dark-code/index.html` contains 'Code Quality' (articleSection rendered)
</verification>

<success_criteria>
- The Dark Code blog post is live (draft: false) and all auto-discovery pipelines include it
- JSON-LD BlogPosting schema includes articleSection: 'Code Quality' and about: CreativeWork
- FAQ JSON-LD with 5 Dark Code Spectrum dimension Q&A pairs renders on the post page
- OG image generates automatically from the existing pipeline
- Zero new files created -- only 2 existing files modified
- Production build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/120-site-integration-and-seo-enrichment/120-01-SUMMARY.md`
</output>
