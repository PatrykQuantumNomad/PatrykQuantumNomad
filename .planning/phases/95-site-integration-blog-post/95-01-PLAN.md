---
phase: 95-site-integration-blog-post
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Header.astro
  - src/pages/index.astro
autonomous: true
requirements: [SITE-01, SITE-02, SITE-03, SITE-04, SITE-05, SITE-06, SITE-07]

must_haves:
  truths:
    - "The site header navigation includes a 'Claude Code' link visible on all pages"
    - "The homepage shows a Claude Code callout card in the Reference Guides section"
    - "All 12 Claude Code guide pages appear in the sitemap"
    - "LLMs.txt and LLMs-full.txt include Claude Code guide entries"
    - "Every guide page has TechArticle JSON-LD and BreadcrumbList markup"
    - "Build-time OG images exist for landing + 11 chapters"
  artifacts:
    - path: "src/components/Header.astro"
      provides: "Claude Code nav link in navLinks array"
      contains: "claude-code"
    - path: "src/pages/index.astro"
      provides: "Claude Code callout card in Reference Guides grid"
      contains: "Claude Code Guide"
  key_links:
    - from: "src/components/Header.astro"
      to: "/guides/claude-code/"
      via: "navLinks array entry"
      pattern: "claude-code.*Claude Code"
    - from: "src/pages/index.astro"
      to: "/guides/claude-code/"
      via: "anchor tag in Reference Guides section"
      pattern: "href=\"/guides/claude-code/\""
---

<objective>
Add the Claude Code guide to site-wide navigation and the homepage, then verify all infrastructure requirements (SITE-03 through SITE-07) that were already completed in Phase 90.

Purpose: Make the Claude Code guide discoverable from every page via header nav and prominently featured on the homepage. Confirm Phase 90 infrastructure still works correctly.
Output: Updated Header.astro with nav link, updated index.astro with callout card, verified build output for sitemap/LLMs.txt/JSON-LD/OG images.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-site-integration-blog-post/95-RESEARCH.md

@src/components/Header.astro
@src/pages/index.astro
@src/data/guides/claude-code/guide.json
</context>

<interfaces>
<!-- Key patterns the executor needs -->

From src/components/Header.astro (current navLinks array):
```typescript
const navLinks = [
  { href: '/', label: 'Home' },
  { href: '/blog/', label: 'Blog' },
  { href: '/beauty-index/', label: 'Beauty Index' },
  { href: '/db-compass/', label: 'DB Compass' },
  { href: '/eda/', label: 'EDA' },
  { href: '/guides/', label: 'Guides' },
  { href: '/tools/', label: 'Tools' },
  { href: '/projects/', label: 'Projects' },
  { href: '/about/', label: 'About' },
  { href: '/contact/', label: 'Contact' },
];
```

From src/pages/index.astro (FastAPI card pattern, lines 227-248):
```astro
<a href="/guides/fastapi-production/" class="block card-hover rounded-lg border border-[var(--color-border)] no-underline overflow-hidden" data-reveal data-tilt>
  <article>
    <div class="bg-gradient-to-br from-[#009485]/8 to-[#009485]/3 px-6 pt-6 pb-4 flex flex-col items-center justify-center min-h-[148px] gap-2 relative overflow-hidden">
      <div class="absolute inset-4 border border-dashed border-[#009485]/20 rounded-lg" aria-hidden="true">
        <div class="absolute -top-1 -left-1 w-2 h-2 rounded-full bg-[#009485]/40"></div>
        <!-- ... corner dots ... -->
      </div>
      <Image src={fastapiLogo} alt="FastAPI Chassis" width={180} height={52} class="relative z-10" loading="lazy" />
      <span class="relative z-10 font-mono text-[10px] tracking-[0.35em] text-[#009485]/60 uppercase select-none">Production Chassis</span>
    </div>
    <div class="p-6 pt-4">
      <p class="meta-mono text-[var(--color-accent)]">Production Guide</p>
      <h3 class="text-lg font-heading font-bold text-[var(--color-text-primary)] mt-2">FastAPI Production Guide</h3>
      <p class="text-sm text-[var(--color-text-secondary)] mt-2">
        13 chapters covering middleware, authentication, observability, database, Docker, testing, and more. Everything your AI agent inherits.
      </p>
      <span class="inline-block mt-4 text-sm font-medium text-[var(--color-accent)]">Read the guide &rarr;</span>
    </div>
  </article>
</a>
```

Guide accent color: #D97706 (amber) from guide.json
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Claude Code nav link and homepage callout card</name>
  <files>src/components/Header.astro, src/pages/index.astro</files>
  <action>
1. In `src/components/Header.astro`, add a new entry to the `navLinks` array immediately after the `{ href: '/guides/', label: 'Guides' }` entry:
   ```
   { href: '/guides/claude-code/', label: 'Claude Code' },
   ```
   This brings the array to 11 entries. The existing `isActive` logic (`currentPath.startsWith(link.href)`) handles active state correctly -- the more specific `/guides/claude-code/` link will match Claude Code pages while `/guides/` will match the hub page.

2. In `src/pages/index.astro`, add a Claude Code callout card inside the Reference Guides grid (the `<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">` section), after the existing FastAPI card (after line 248, before the closing `</div>` of the grid). The card follows the identical structure as the FastAPI card but uses amber accent #D97706 instead of teal #009485:

   ```astro
   <a href="/guides/claude-code/" class="block card-hover rounded-lg border border-[var(--color-border)] no-underline overflow-hidden" data-reveal data-tilt>
     <article>
       <div class="bg-gradient-to-br from-[#D97706]/8 to-[#D97706]/3 px-6 pt-6 pb-4 flex flex-col items-center justify-center min-h-[148px] gap-2 relative overflow-hidden">
         <div class="absolute inset-4 border border-dashed border-[#D97706]/20 rounded-lg" aria-hidden="true">
           <div class="absolute -top-1 -left-1 w-2 h-2 rounded-full bg-[#D97706]/40"></div>
           <div class="absolute -top-1 -right-1 w-2 h-2 rounded-full bg-[#D97706]/40"></div>
           <div class="absolute -bottom-1 -left-1 w-2 h-2 rounded-full bg-[#D97706]/40"></div>
           <div class="absolute -bottom-1 -right-1 w-2 h-2 rounded-full bg-[#D97706]/40"></div>
         </div>
         <h3 class="relative z-10 text-2xl font-heading font-bold text-[#D97706]">Claude Code Guide</h3>
         <span class="relative z-10 font-mono text-[10px] tracking-[0.35em] text-[#D97706]/60 uppercase select-none">Zero to Hero</span>
       </div>
       <div class="p-6 pt-4">
         <p class="meta-mono text-[var(--color-accent)]">Mastery Guide</p>
         <h3 class="text-lg font-heading font-bold text-[var(--color-text-primary)] mt-2">Claude Code Guide</h3>
         <p class="text-sm text-[var(--color-text-secondary)] mt-2">
           11 chapters from first agentic interaction through multi-agent orchestration. Skills, hooks, MCP, worktrees, and enterprise security.
         </p>
         <span class="inline-block mt-4 text-sm font-medium text-[var(--color-accent)]">Read the guide &rarr;</span>
       </div>
     </article>
   </a>
   ```

   NOTE: Unlike the FastAPI card which uses an `<Image>` component with the FastAPI logo, the Claude Code card uses a text `<h3>` with the guide title (matching the hub page pattern from Phase 90), because Claude Code has no logo asset. The grid stays at `lg:grid-cols-3` -- the 3+2 layout naturally groups reference content on top row and guides on bottom row.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <done>Header.astro has 11 navLinks including "Claude Code" pointing to /guides/claude-code/. Homepage index.astro has 5 cards in the Reference Guides grid with Claude Code using amber #D97706 accent. Build succeeds with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Verify SITE-03 through SITE-07 in build output</name>
  <files></files>
  <action>
After the build from Task 1, verify all 5 already-complete requirements against the build output. Do NOT modify any files -- these are verification-only checks:

1. SITE-01 (new): `grep -c 'claude-code' dist/index.html` -- expect at least 1 (nav link + callout card)
2. SITE-02 (new): `grep 'Claude Code Guide' dist/index.html` -- expect match in callout card
3. SITE-03 (already done): `grep -c 'card-hover' dist/guides/index.html` -- expect 2+ (both guide cards)
4. SITE-04 (already done): `grep -c 'claude-code' dist/sitemap-0.xml` -- expect 12 (landing + 11 chapters)
5. SITE-05 (already done): `grep -c 'claude-code' dist/llms.txt` -- expect 13+ entries
6. SITE-06 (already done): `grep 'TechArticle' dist/guides/claude-code/introduction/index.html` -- expect match
7. SITE-07 (already done): `test -f dist/open-graph/guides/claude-code.png && echo "Landing OG exists"` and `test -f dist/open-graph/guides/claude-code/introduction.png && echo "Chapter OG exists"`

Report all results. If any already-complete requirement fails, investigate but do NOT modify infrastructure code -- the issue is likely a build or path problem.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -5 && grep -c 'claude-code' dist/sitemap-0.xml && grep -c 'claude-code' dist/llms.txt && test -f dist/open-graph/guides/claude-code.png && echo "All SITE verifications passed"</automated>
  </verify>
  <done>All 7 SITE requirements (SITE-01 through SITE-07) verified in build output: nav link present, callout card present, hub page shows both guides, sitemap has 12 claude-code URLs, LLMs.txt has 13+ entries, JSON-LD TechArticle present on chapter pages, OG images exist for landing + chapters.</done>
</task>

</tasks>

<verification>
1. `npx astro build` succeeds with zero errors
2. `grep 'claude-code' dist/index.html` shows nav link and callout card
3. `grep -c 'claude-code' dist/sitemap-0.xml` returns 12
4. `grep -c 'claude-code' dist/llms.txt` returns 13+
5. `grep 'TechArticle' dist/guides/claude-code/introduction/index.html` finds JSON-LD
6. OG images exist at `dist/open-graph/guides/claude-code.png` and `dist/open-graph/guides/claude-code/introduction.png`
</verification>

<success_criteria>
- Header navigation has 11 links including "Claude Code" pointing to /guides/claude-code/
- Homepage Reference Guides grid has 5 cards including Claude Code with amber accent
- Build output confirms SITE-03 through SITE-07 are still working from Phase 90
- Zero build errors
</success_criteria>

<output>
After completion, create `.planning/phases/95-site-integration-blog-post/95-01-SUMMARY.md`
</output>
