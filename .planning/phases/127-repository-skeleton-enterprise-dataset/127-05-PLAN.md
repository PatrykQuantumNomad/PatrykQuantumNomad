---
phase: 127-repository-skeleton-enterprise-dataset
plan: 05
type: execute
wave: 4
depends_on: ["127-04"]
files_modified:
  - "../rag-architecture-patterns/dataset/images/*.png"
  - "../rag-architecture-patterns/dataset/images/*.jpg"
  - "../rag-architecture-patterns/dataset/manifests/figures.json"
  - "../rag-architecture-patterns/dataset/videos/*.mp4"
  - "../rag-architecture-patterns/dataset/manifests/videos.json"
  - "../rag-architecture-patterns/scripts/video_sources.json"
autonomous: false

requirements: [REPO-02]

must_haves:
  truths:
    - "dataset/images/ contains pre-extracted figures from the corpus PDFs (≥30 images, the curator-reviewed survivors of the size/aspect/dedup filter)"
    - "Every image in dataset/images/ is git-lfs-tracked"
    - "dataset/manifests/figures.json maps each image to (figure_id, paper_id, page_number, bbox, caption, filename)"
    - "dataset/videos/ contains 1-2 CC-licensed conference talk clips (~30s each, never CC-BY-ND or CC-BY-NC-ND)"
    - "dataset/manifests/videos.json has license_verified_at + license_verified_by fields filled by the curator"
    - "Figure manifest has caption text filled for the ≥7 figures the multimodal golden Q&A in Plan 06 will reference"
    - "Total dataset/ size (papers + images + videos) is <500 MB"
  artifacts:
    - path: "../rag-architecture-patterns/dataset/images/"
      provides: "Pre-extracted figures from PDFs, ready for Tier 4 (multimodal RAG) indexing"
      min_count: 30
    - path: "../rag-architecture-patterns/dataset/manifests/figures.json"
      provides: "Figure manifest per D-05 schema (figure_id → paper_id → caption → page_number)"
      contains: "figure_id"
    - path: "../rag-architecture-patterns/dataset/videos/"
      provides: "1-2 CC-licensed conference talk clips, 30s each"
      min_count: 1
    - path: "../rag-architecture-patterns/dataset/manifests/videos.json"
      provides: "Video manifest per D-12 schema with verified CC license"
      contains: "license_verified_at"
  key_links:
    - from: "dataset/manifests/figures.json entries"
      to: "dataset/images/{filename}"
      via: "manifest filename matches existing file"
      pattern: "\\.(png|jpg|jpeg)$"
    - from: "dataset/manifests/figures.json entries"
      to: "dataset/manifests/papers.json paper_id"
      via: "every figure's paper_id appears in papers manifest"
      pattern: "paper_id"
    - from: "dataset/manifests/videos.json entries"
      to: "dataset/videos/{filename}"
      via: "manifest filename matches an existing mp4"
      pattern: "\\.mp4$"
    - from: "dataset/videos/*"
      to: "git-lfs"
      via: ".gitattributes filter=lfs"
      pattern: "filter: lfs"
---

<objective>
Run scripts/extract_figures.py against the ~100 PDFs from Plan 04 to produce dataset/images/ + dataset/manifests/figures.json. Then curator manually verifies CC licenses for video clips, runs scripts/cut_video_clips.py to produce dataset/videos/, and writes dataset/manifests/videos.json. The curator also fills caption fields for the ≥7 figures that Plan 06's multimodal golden Q&A will reference.

Purpose: REPO-02 explicitly requires "at least one image" — this plan delivers ≥30 images via PyMuPDF extraction plus 1-2 video clips. Without Plan 05's outputs, Tier 4 (multimodal RAG, Phase 130) has no visual content to index, and the golden_qa multimodal/video questions in Plan 06 cannot be authored.

Output: dataset/images/ + dataset/videos/ committed via LFS with verified figure and video manifests.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/127-repository-skeleton-enterprise-dataset/127-CONTEXT.md
@.planning/phases/127-repository-skeleton-enterprise-dataset/127-RESEARCH.md
@.planning/phases/127-repository-skeleton-enterprise-dataset/127-03-SUMMARY.md
@.planning/phases/127-repository-skeleton-enterprise-dataset/127-04-SUMMARY.md

<interfaces>
<!-- Pulled from Plan 03 — extract_figures.py and cut_video_clips.py -->

extract_figures.py CLI:
```
python scripts/extract_figures.py --all dataset/papers/ \
  --out dataset/images/ \
  --manifest dataset/manifests/figures.json \
  --min-dim 200 --max-aspect-ratio 5
```

cut_video_clips.py CLI:
```
python scripts/cut_video_clips.py --sources scripts/video_sources.json \
  --out dataset/videos/ \
  --manifest dataset/manifests/videos.json
```

<!-- Acceptable CC licenses (D-12, Pitfall 8): BY, BY-SA, BY-NC, BY-NC-SA -->
<!-- Reject: BY-ND, BY-NC-ND -->

<!-- Sources for CC-licensed conference talks (curator's starting point — verify each per clip) -->
- SlidesLive (NeurIPS, ICML, ICLR mirror) — license varies per upload, often "All Rights Reserved" — verify before assuming CC
- The Open Review forums for ICLR/NeurIPS — many videos posted with CC-BY
- ML Conference YouTube channels (NeurIPS Conference, ICML — some uploads are CC-BY)
- The arxiv-sanity / Two Minute Papers / Yannic Kilcher channels — generally NOT CC-licensed (verify case by case)
- Lewis Patrick (RAG paper author) personal talks — check his personal site / Meta AI page for license
- Conference paper page on the official conference site — sometimes lists "video license: CC-BY"

<!-- Heuristic: Lewis Patrick's NeurIPS 2020 RAG talk on slideslive may or may not be CC. -->
<!-- If you cannot confirm a CC license for a clip, REJECT and find another. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run figure extraction across all PDFs and generate manifests/figures.json</name>
  <files>
    ../rag-architecture-patterns/dataset/images/
    ../rag-architecture-patterns/dataset/manifests/figures.json
  </files>
  <action>
All work in ../rag-architecture-patterns/.

1. Pull latest:
   ```
   cd ../rag-architecture-patterns
   source .venv/bin/activate
   git pull origin main
   ```

2. Run extraction in --dry-run first to estimate output size (per Pitfall 7 — heuristic filter prunes ~90% but the curator still reviews):
   ```
   python scripts/extract_figures.py --all dataset/papers/ \
     --out dataset/images/ \
     --manifest dataset/manifests/figures.json \
     --min-dim 200 --max-aspect-ratio 5 \
     --dry-run \
     2>&1 | tee /tmp/extract_dryrun.log
   ```
   The log should print "Would extract N figures from M PDFs" — capture N and M.

3. If `N > 1000` (clearly too many — implies filter is too loose), tighten:
   ```
   python scripts/extract_figures.py --all dataset/papers/ \
     --out dataset/images/ \
     --manifest dataset/manifests/figures.json \
     --min-dim 250 --max-aspect-ratio 4 \
     --dry-run
   ```
   Document the chosen thresholds in SUMMARY.

4. Run the real extraction:
   ```
   python scripts/extract_figures.py --all dataset/papers/ \
     --out dataset/images/ \
     --manifest dataset/manifests/figures.json \
     --min-dim 200 --max-aspect-ratio 5 \
     2>&1 | tee /tmp/extract_realrun.log
   ```
   This is fast (~30-60s for 100 PDFs).

5. Sanity check the output:
   ```
   ls dataset/images/ | wc -l                                          # Expect 30-500 images post-filter
   du -sh dataset/images/                                               # Expect 5-50 MB
   python -c "
   import json
   m = json.load(open('dataset/manifests/figures.json'))
   print(f'Manifest entries: {len(m)}')
   from collections import Counter
   per_paper = Counter(e['paper_id'] for e in m)
   print(f'Papers with figures: {len(per_paper)} / 100')
   print(f'Median figures per paper: {sorted(per_paper.values())[len(per_paper)//2]}')
   print(f'Top 5 papers by figure count:', per_paper.most_common(5))
   "
   ```

6. Verify LFS-tracked:
   ```
   for img in dataset/images/*; do
     git check-attr filter "$img" | grep -q 'filter: lfs' || { echo "FAIL: $img"; exit 1; }
   done
   ```

7. DO NOT commit yet — Task 2 (curator review) may delete garbage images.

Decisions referenced: D-05 (pre-extract figures, manifest schema), D-11 (figure manifest fields), Pitfall 7 (heuristic filtering + manual review), 127-RESEARCH.md Pattern 3.
  </action>
  <verify>
    <automated>
cd ../rag-architecture-patterns &amp;&amp; \
  img_count=$(ls -1 dataset/images/ 2&gt;/dev/null | wc -l | tr -d ' ') &amp;&amp; \
  test "$img_count" -ge 30 &amp;&amp; \
  manifest_count=$(python -c "import json; print(len(json.load(open('dataset/manifests/figures.json'))))") &amp;&amp; \
  test "$manifest_count" -eq "$img_count" &amp;&amp; \
  for img in dataset/images/*; do \
    git check-attr filter "$img" | grep -q 'filter: lfs' || exit 1; \
  done
    </automated>
  </verify>
  <done>
    ≥30 figures extracted under dataset/images/.
    figures.json has matching entries with figure_id, paper_id, page_number, bbox, filename.
    All images LFS-tracked.
    Coverage: most papers in the cluster contributed at least one figure.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Curator reviews extracted figures + selects video clips + verifies CC licenses</name>
  <what-built>
    Task 1 extracted ≥30 figures into dataset/images/ via the size/aspect/xref-dedup filter. Manifest at dataset/manifests/figures.json. Nothing committed yet.
  </what-built>
  <how-to-verify>
    **PART A: Figure curation (manual review per Pitfall 7)**

    1. Open the dataset/images/ directory in a thumbnail-capable viewer (macOS Finder, Preview, or use a quick CLI):
       ```
       open dataset/images/
       ```

    2. Scan for "garbage" survivors of the heuristic filter:
       - Logos that slipped through (publisher logos, university crests)
       - Decorative backgrounds (single-color rectangles, gradient bars)
       - Tiny chart axes labels mistakenly extracted as standalone images
       - Equation rasterizations that PyMuPDF treated as images
       For each garbage image:
       ```
       rm dataset/images/{garbage_filename}
       ```

    3. After deletion, regenerate the manifest to drop the deleted entries:
       ```
       cd ../rag-architecture-patterns
       source .venv/bin/activate
       python -c "
       import json
       from pathlib import Path
       manifest = json.load(open('dataset/manifests/figures.json'))
       existing = set(p.name for p in Path('dataset/images').iterdir() if p.is_file())
       cleaned = [m for m in manifest if m['filename'] in existing]
       print(f'Pruned {len(manifest) - len(cleaned)} entries')
       json.dump(cleaned, open('dataset/manifests/figures.json', 'w'), indent=2)
       "
       ```

    4. Fill caption text for ≥7 figures (per D-04 — multimodal Q&A targets 7 figures). For each, open the source PDF, locate the figure on its page (use bbox if helpful), and copy the caption text into the manifest entry's `caption` field. Use a JSON editor; preserve all other fields. Preferred figures:
       - Architecture diagrams from Lewis 2020 RAG paper
       - LightRAG's pipeline diagram
       - GraphRAG's hierarchical summarization figure
       - Self-RAG's reflection-token diagram
       - Plus any 3 others that show meaningful visual content the multimodal Q&A can probe

    **PART B: Video clip license verification (per D-12, Pitfall 8)**

    5. Open `scripts/video_sources.json`. The current single entry has `license: "TBD_VERIFY_MANUALLY"`. Verify and update:
       - Visit `entry.source_url` (the conference talk page)
       - Find the license attribution. Acceptable: CC-BY, CC-BY-SA, CC-BY-NC, CC-BY-NC-SA. **Reject:** CC-BY-ND, CC-BY-NC-ND.
       - If the talk has NO clear CC license OR is "All Rights Reserved", REJECT this candidate. Find an alternative — search for the same paper's authors giving talks at venues that publish CC-BY (often workshop talks at NeurIPS / ICLR / ICML have explicit CC-BY licenses).
       - Once verified, update the JSON entry:
         ```
         {
           ...
           "license": "CC-BY-4.0",
           "license_verified_at": "2026-04-25",
           "license_verified_by": "Patryk Golabek (manual review of {url})",
           "source_file_local_path": "/Users/patrykattc/Downloads/lewis_neurips2020_rag.mp4",
           ...
         }
         ```
       - Manually download the source video to `source_file_local_path` (yt-dlp / browser save / etc.) before Task 3 runs the cutter. The script does NOT download — it only cuts a local file.

    6. Optionally add a SECOND clip (D-01 says "1-2 short clips"). Same verification protocol. Two clips = stronger video Q&A demonstrations.

    7. Save scripts/video_sources.json with the verified entry/entries.

    **APPROVAL GATE**

    8. Confirm BEFORE proceeding:
       - dataset/images/ contains only meaningful figures (no logos/decorations)
       - dataset/manifests/figures.json has captions for ≥7 figures (the rest can stay empty per Open Question #4)
       - scripts/video_sources.json has at least 1 entry with verified CC license (NOT BY-ND, NOT BY-NC-ND)
       - The user has manually downloaded each video clip's source file to the path in `source_file_local_path`
  </how-to-verify>
  <resume-signal>Type "approved" once figures are curated, captions are filled for ≥7 entries, and ≥1 video clip is license-verified with its source file downloaded locally.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Cut video clips + write videos manifest + commit dataset/images/ and dataset/videos/ via LFS</name>
  <files>
    ../rag-architecture-patterns/dataset/videos/
    ../rag-architecture-patterns/dataset/manifests/videos.json
    ../rag-architecture-patterns/dataset/manifests/figures.json
  </files>
  <action>
All work in ../rag-architecture-patterns/.

1. Run the video clip cutter (it reads scripts/video_sources.json which the curator updated in Task 2):
   ```
   cd ../rag-architecture-patterns
   source .venv/bin/activate
   python scripts/cut_video_clips.py \
     --sources scripts/video_sources.json \
     --out dataset/videos/ \
     --manifest dataset/manifests/videos.json \
     --dry-run
   ```
   Confirm the dry-run prints the ffmpeg commands without errors. The script's safety gate refuses any entry still on `TBD_VERIFY_MANUALLY` or `CC-BY-ND` / `CC-BY-NC-ND` — if it skips an entry, that entry was not curated in Task 2 and the curator must go back.

2. If dry-run is clean, run for real:
   ```
   python scripts/cut_video_clips.py \
     --sources scripts/video_sources.json \
     --out dataset/videos/ \
     --manifest dataset/manifests/videos.json \
     2>&1 | tee /tmp/cut_video_realrun.log
   ```
   Stream-copy is instant (per 127-RESEARCH.md "Don't Hand-Roll" — `ffmpeg -ss -t -c copy`).

3. Verify outputs:
   ```
   ls dataset/videos/                                 # >= 1 mp4 file
   du -sh dataset/videos/                             # Each ~5-15 MB at 30s; total <30 MB for 1-2 clips
   python -c "
   import json
   m = json.load(open('dataset/manifests/videos.json'))
   for entry in m:
       assert entry['license'] not in ('TBD_VERIFY_MANUALLY', 'CC-BY-ND', 'CC-BY-NC-ND'), f'unverified: {entry}'
       assert entry['license_verified_at'], f'no verification timestamp: {entry}'
       assert entry['license_verified_by'], f'no verifier: {entry}'
   print(f'Verified video manifest: {len(m)} entries')
   "
   ```

4. Verify LFS-tracking on videos:
   ```
   for vid in dataset/videos/*.mp4; do
     git check-attr filter "$vid" | grep -q 'filter: lfs' || { echo "FAIL: $vid"; exit 1; }
   done
   ```

5. Final cross-link sanity (figures manifest references valid paper_ids):
   ```
   python -c "
   import json
   papers = {p['paper_id'] for p in json.load(open('dataset/manifests/papers.json'))}
   figures = json.load(open('dataset/manifests/figures.json'))
   videos = json.load(open('dataset/manifests/videos.json'))
   bad_fig_papers = [f['paper_id'] for f in figures if f['paper_id'] not in papers]
   bad_vid_papers = [pr for v in videos for pr in v['papers_referenced'] if pr not in papers]
   assert not bad_fig_papers, f'figures reference unknown paper_ids: {set(bad_fig_papers)}'
   assert not bad_vid_papers, f'videos reference unknown paper_ids: {set(bad_vid_papers)}'
   print('All cross-references valid')
   "
   ```

6. Total dataset size check (papers + images + videos must be <500 MB):
   ```
   du -sh dataset/papers/ dataset/images/ dataset/videos/
   du -sh dataset/                                   # Should be <500 MB total
   ```

7. Commit and push:
   ```
   git add scripts/video_sources.json \
           dataset/images/ \
           dataset/videos/ \
           dataset/manifests/figures.json \
           dataset/manifests/videos.json
   git commit -m "feat: extract figures + cut CC-licensed video clips with verified manifests (REPO-02)"
   git push origin main
   ```

Decision rationale: D-05, D-11 (manifest schemas), D-12 (CC license verification, BY/BY-SA/BY-NC/BY-NC-SA accepted), Pitfall 8 (ND rejection enforced by script).
  </action>
  <verify>
    <automated>
cd ../rag-architecture-patterns &amp;&amp; source .venv/bin/activate &amp;&amp; \
  pytest tests/test_dataset.py -v -m 'not live' &amp;&amp; \
  img_count=$(ls -1 dataset/images/ 2&gt;/dev/null | wc -l | tr -d ' ') &amp;&amp; test "$img_count" -ge 30 &amp;&amp; \
  vid_count=$(ls -1 dataset/videos/*.mp4 2&gt;/dev/null | wc -l | tr -d ' ') &amp;&amp; test "$vid_count" -ge 1 &amp;&amp; \
  python -c "
import json
figures = json.load(open('dataset/manifests/figures.json'))
captioned = [f for f in figures if f.get('caption')]
assert len(captioned) &gt;= 7, f'need &gt;=7 captioned figures, got {len(captioned)}'
" &amp;&amp; \
  git status | grep -q 'up to date'
    </automated>
  </verify>
  <done>
    ≥30 figures committed under dataset/images/ (LFS-tracked).
    1-2 video clips committed under dataset/videos/ (LFS-tracked) with verified non-ND CC license.
    figures.json has captions for ≥7 figures.
    videos.json has license_verified_at + license_verified_by for every entry.
    All manifest cross-references valid (figures.paper_id and videos.papers_referenced exist in papers.json).
    Total dataset/ size <500 MB.
    Pushed to origin.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local→ffmpeg subprocess | Curator-supplied path crosses into shell; validated as Path before subprocess |
| local→GitHub LFS | Image + video bytes pushed; bandwidth quota |
| video source → local download | Manual step by curator; trust the curator's source URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-20 | Information Disclosure | License violation cutting an ND clip | mitigate | Task 2 checkpoint forces curator-side license verification; Task 3 script-side gate refuses TBD/ND entries (Pitfall 8) |
| T-127-21 | Tampering | Garbage figures inflate dataset size | mitigate | Task 2 manual review prunes filter survivors before commit; manifest regenerated to match disk |
| T-127-22 | Tampering | Manifest claims captions that don't match figure | mitigate | Captions are manually copied from source PDFs by curator; bbox + page_number in manifest enable spot-check |
| T-127-23 | Denial of Service | LFS quota exceeded by video size | accept | 1-2 clips × 30s × 5-15 MB = ~30 MB max; well within budget; cumulative dataset/ verified <500 MB before push |
</threat_model>

<verification>
After all 3 tasks complete:

```
cd ../rag-architecture-patterns

# Image / video counts
ls dataset/images/ | wc -l
ls dataset/videos/*.mp4 | wc -l

# All LFS
git lfs ls-files | wc -l
git check-attr filter dataset/images/* dataset/videos/* | grep -v 'filter: lfs' && echo FAIL || echo OK

# Manifests valid + cross-linked + license-clean
python -c "
import json
papers = {p['paper_id'] for p in json.load(open('dataset/manifests/papers.json'))}
figs = json.load(open('dataset/manifests/figures.json'))
vids = json.load(open('dataset/manifests/videos.json'))
assert all(f['paper_id'] in papers for f in figs)
assert all(pr in papers for v in vids for pr in v['papers_referenced'])
assert all(v['license'] not in ('TBD_VERIFY_MANUALLY','CC-BY-ND','CC-BY-NC-ND') for v in vids)
assert sum(1 for f in figs if f.get('caption')) >= 7
print('All manifest invariants hold')
"

# Total size
du -sh dataset/

# Pushed
git log --oneline -1
```
</verification>

<success_criteria>
- ≥30 curator-reviewed figures in dataset/images/ (REPO-02 "at least one image" — exceeded)
- 1-2 CC-licensed video clips in dataset/videos/ (D-01 "1-2 short clips")
- All images and videos git-lfs-tracked
- figures.json has captions for ≥7 figures (covers multimodal golden Q&A in Plan 06)
- videos.json has license_verified_at + license_verified_by per entry; no ND/NC-ND licenses
- Cross-references between manifests are consistent
- Total dataset/ <500 MB
</success_criteria>

<output>
After completion, create `.planning/phases/127-repository-skeleton-enterprise-dataset/127-05-SUMMARY.md` documenting:
- Final figure count + thresholds chosen (--min-dim, --max-aspect-ratio)
- How many images the curator manually pruned in Task 2
- Final video count + each clip's CC license type + verifier
- Total dataset/ size in MB
- Any seed papers from Plan 04 that contributed zero usable figures (informs Q&A authoring in Plan 06)
</output>
