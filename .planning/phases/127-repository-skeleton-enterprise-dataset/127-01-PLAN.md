---
phase: 127-repository-skeleton-enterprise-dataset
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "../rag-architecture-patterns/.git/"
  - "../rag-architecture-patterns/.gitattributes"
  - "../rag-architecture-patterns/.gitignore"
  - "../rag-architecture-patterns/LICENSE"
  - "../rag-architecture-patterns/README.md"
  - "../rag-architecture-patterns/pyproject.toml"
  - "../rag-architecture-patterns/.env.example"
  - "../rag-architecture-patterns/dataset/README.md"
  - "../rag-architecture-patterns/dataset/papers/.gitkeep"
  - "../rag-architecture-patterns/dataset/images/.gitkeep"
  - "../rag-architecture-patterns/dataset/videos/.gitkeep"
  - "../rag-architecture-patterns/dataset/manifests/.gitkeep"
  - "../rag-architecture-patterns/shared/__init__.py"
  - "../rag-architecture-patterns/scripts/.gitkeep"
  - "../rag-architecture-patterns/tests/__init__.py"
  - "../rag-architecture-patterns/tests/conftest.py"
  - "../rag-architecture-patterns/evaluation/README.md"
  - "../rag-architecture-patterns/evaluation/results/costs/.gitkeep"
  - "../rag-architecture-patterns/tier-1-naive/requirements.txt"
  - "../rag-architecture-patterns/tier-2-managed/requirements.txt"
  - "../rag-architecture-patterns/tier-3-graph/requirements.txt"
  - "../rag-architecture-patterns/tier-4-multimodal/requirements.txt"
  - "../rag-architecture-patterns/tier-5-agentic/requirements.txt"
autonomous: true
requirements: [REPO-01, REPO-04, REPO-06]
user_setup:
  - service: github
    why: "Companion repo creation requires authenticated gh CLI"
    env_vars: []
    dashboard_config:
      - task: "Run `gh auth status` to confirm authenticated; if logged out, run `gh auth login` and follow prompts"
        location: "Local terminal"
  - service: github-lfs
    why: "PDFs/images/videos route through git-lfs; bandwidth quota applies"
    env_vars: []
    dashboard_config:
      - task: "After first push, verify LFS quota at https://github.com/settings/billing (Free tier may be 1 GB or 10 GiB — confirm before bulk dataset push in Plan 04/05)"
        location: "GitHub billing settings"

must_haves:
  truths:
    - "Companion repo PatrykQuantumNomad/rag-architecture-patterns exists on GitHub (public)"
    - "Local working copy exists at sibling path ../rag-architecture-patterns/ with origin remote pointing to GitHub"
    - "git-lfs is initialized and tracks *.pdf *.png *.jpg *.jpeg *.mp4 *.webm BEFORE any binary is staged"
    - "Each tier-N/ directory has its own requirements.txt referencing the parent extras"
    - ".env.example documents GEMINI_API_KEY plus all known optional keys; .env is gitignored"
    - "README.md links to https://patrykgolabek.dev/blog/rag-architecture-patterns (placeholder, blog not yet published)"
    - "pyproject.toml installs cleanly with `uv pip install -e \".[shared,curation]\"` (no missing deps)"
  artifacts:
    - path: "../rag-architecture-patterns/.gitattributes"
      provides: "git-lfs tracking patterns committed before any binary"
      contains: "*.pdf"
    - path: "../rag-architecture-patterns/pyproject.toml"
      provides: "Project metadata + optional-dependencies (shared, curation, tier-1..tier-5) + dependency-groups (test, dev)"
      contains: "google-genai"
    - path: "../rag-architecture-patterns/.env.example"
      provides: "Documented placeholders for GEMINI_API_KEY, OPENAI_API_KEY, S2_API_KEY"
      contains: "GEMINI_API_KEY=your_gemini_api_key_here"
    - path: "../rag-architecture-patterns/README.md"
      provides: "Repo overview + blog post placeholder link + setup instructions"
      contains: "patrykgolabek.dev/blog/rag-architecture-patterns"
    - path: "../rag-architecture-patterns/.gitignore"
      provides: "Python ignore rules + .env + .venv + chroma_db + lightrag_*"
      contains: ".env"
    - path: "../rag-architecture-patterns/tier-1-naive/requirements.txt"
      provides: "Tier 1 dep list mirroring [project.optional-dependencies.tier-1]"
      contains: "rag-architecture-patterns"
    - path: "../rag-architecture-patterns/tier-2-managed/requirements.txt"
      provides: "Tier 2 dep list"
    - path: "../rag-architecture-patterns/tier-3-graph/requirements.txt"
      provides: "Tier 3 dep list"
    - path: "../rag-architecture-patterns/tier-4-multimodal/requirements.txt"
      provides: "Tier 4 dep list"
    - path: "../rag-architecture-patterns/tier-5-agentic/requirements.txt"
      provides: "Tier 5 dep list"
    - path: "../rag-architecture-patterns/LICENSE"
      provides: "Apache 2.0 license file"
      contains: "Apache License"
  key_links:
    - from: "../rag-architecture-patterns/.gitattributes"
      to: "git-lfs"
      via: "filter=lfs diff=lfs merge=lfs"
      pattern: "filter=lfs"
    - from: "tier-N/requirements.txt"
      to: "pyproject.toml [project.optional-dependencies.tier-N]"
      via: "either includes `-e ..[tier-N]` or mirrors the same package list"
      pattern: "rag-architecture-patterns\\[|^[a-z]"
    - from: "../rag-architecture-patterns/README.md"
      to: "patrykgolabek.dev/blog/rag-architecture-patterns"
      via: "Markdown link"
      pattern: "\\[.*\\]\\(https://patrykgolabek\\.dev/blog/rag-architecture-patterns"
---

<objective>
Bootstrap the companion repository at PatrykQuantumNomad/rag-architecture-patterns with a complete Python project skeleton, git-lfs configured for binary assets, all per-tier requirements.txt files, and the documented .env.example. This plan creates ZERO Python implementation code — only structural scaffolding. After this plan, every subsequent plan in this phase operates inside ../rag-architecture-patterns/.

Purpose: Lock down LFS tracking before any binary is staged (per Pitfall 1 in 127-RESEARCH.md), establish the pyproject layout that downstream tier phases (128-130) will extend, and satisfy REPO-01, REPO-04, REPO-06 in a single autonomous wave so the rest of the phase can parallelize.

Output: A pushed companion repo skeleton on GitHub plus a clean local clone at ../rag-architecture-patterns/.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-repository-skeleton-enterprise-dataset/127-CONTEXT.md
@.planning/phases/127-repository-skeleton-enterprise-dataset/127-RESEARCH.md

<interfaces>
<!-- Key conventions and templates the executor needs. Sourced from 127-RESEARCH.md. -->
<!-- Treat these as the canonical project shape. Do not improvise structure. -->

Repository structure (from 127-RESEARCH.md "Recommended Project Structure"):
```
rag-architecture-patterns/
├── README.md
├── LICENSE                              # Apache-2.0
├── pyproject.toml
├── .env.example
├── .gitignore
├── .gitattributes                       # git-lfs patterns FIRST
├── dataset/
│   ├── README.md
│   ├── papers/         (LFS, populated by Plan 04)
│   ├── images/         (LFS, populated by Plan 05)
│   ├── videos/         (LFS, populated by Plan 05)
│   └── manifests/      (populated by Plan 04/05)
├── shared/
│   └── __init__.py     (Plan 02 fills modules)
├── evaluation/
│   ├── README.md
│   ├── golden_qa.json  (Plan 06)
│   └── results/costs/.gitkeep
├── scripts/            (Plan 03 fills curation scripts)
├── tests/
│   ├── __init__.py
│   └── conftest.py     (Plan 02 fills smoke_test.py)
├── tier-1-naive/requirements.txt
├── tier-2-managed/requirements.txt
├── tier-3-graph/requirements.txt
├── tier-4-multimodal/requirements.txt
└── tier-5-agentic/requirements.txt
```

pyproject.toml shape (from 127-RESEARCH.md Pattern 7):
```toml
[project]
name = "rag-architecture-patterns"
version = "0.1.0"
description = "Companion repo for the RAG Architecture Patterns blog post"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
dependencies = []

[project.optional-dependencies]
shared = [
  "google-genai>=1.73,<2",
  "pydantic>=2.10,<3",
  "pydantic-settings>=2.10,<3",
  "rich>=14,<16",
  "tiktoken>=0.10,<1",
  "python-dotenv>=1.0,<2",
]
curation = [
  "arxiv>=3.0,<4",
  "semanticscholar>=0.12,<1",
  "pymupdf>=1.27,<2",
]
tier-1 = ["rag-architecture-patterns[shared]"]
tier-2 = ["rag-architecture-patterns[shared]"]
tier-3 = ["rag-architecture-patterns[shared]"]
tier-4 = ["rag-architecture-patterns[shared]"]
tier-5 = ["rag-architecture-patterns[shared]"]

[dependency-groups]
test = ["pytest>=8.4,<9"]
dev = [{include-group = "test"}, "ruff>=0.6"]

[tool.uv]
default-groups = ["dev"]

[tool.pytest.ini_options]
markers = ["live: tests that hit real APIs and incur cost"]
```

.gitattributes (verbatim from 127-RESEARCH.md):
```
*.pdf  filter=lfs diff=lfs merge=lfs -text
*.png  filter=lfs diff=lfs merge=lfs -text
*.jpg  filter=lfs diff=lfs merge=lfs -text
*.jpeg filter=lfs diff=lfs merge=lfs -text
*.mp4  filter=lfs diff=lfs merge=lfs -text
*.webm filter=lfs diff=lfs merge=lfs -text
```

.env.example (verbatim from 127-RESEARCH.md):
```
GEMINI_API_KEY=your_gemini_api_key_here
OPENAI_API_KEY=
S2_API_KEY=
DEFAULT_CHAT_MODEL=gemini-2.5-flash
DEFAULT_EMBEDDING_MODEL=gemini-embedding-001
DATASET_ROOT=dataset
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create companion repo on GitHub and clone locally as a sibling directory</name>
  <files>
    ../rag-architecture-patterns/.git/
    ../rag-architecture-patterns/README.md
    ../rag-architecture-patterns/LICENSE
    ../rag-architecture-patterns/.gitignore
    ../rag-architecture-patterns/.gitattributes
  </files>
  <action>
Operate from the parent directory of this profile repo (i.e., the parent of /Users/patrykattc/work/git/PatrykQuantumNomad). All work in this task happens in the NEW companion repo, NOT in the profile repo.

Steps (in order, idempotent — re-running is safe):

1. Confirm gh auth: run `gh auth status`. If not authenticated, ABORT and surface the error so the user can `gh auth login` (per D-01: companion repo at PatrykQuantumNomad/rag-architecture-patterns).

2. Check whether the GitHub repo already exists: `gh repo view PatrykQuantumNomad/rag-architecture-patterns --json name 2>/dev/null`. If it does NOT exist, create it:
   ```
   gh repo create PatrykQuantumNomad/rag-architecture-patterns \
     --public \
     --description "Companion repo for the RAG Architecture Patterns blog post — 5 RAG architecture tiers indexed against a curated arXiv RAG-literature corpus" \
     --license apache-2.0 \
     --gitignore Python
   ```
   This creates the remote with an initial README, LICENSE (Apache-2.0), and Python .gitignore.

3. Clone (or verify clone of) the repo as a sibling of the profile repo. From the parent directory of /Users/patrykattc/work/git/PatrykQuantumNomad:
   ```
   if [ ! -d rag-architecture-patterns ]; then
     git clone https://github.com/PatrykQuantumNomad/rag-architecture-patterns.git rag-architecture-patterns
   fi
   ```
   The companion repo working copy MUST live at /Users/patrykattc/work/git/rag-architecture-patterns (sibling of this profile repo). Confirm with `realpath`.

4. Inside the companion repo working copy: install git-lfs hooks for THIS repo (per Pitfall 1 — must happen BEFORE any PDF/PNG/JPG/MP4 is staged):
   ```
   cd ../rag-architecture-patterns
   git lfs install
   ```

5. Write `.gitattributes` with EXACTLY the LFS filter lines from <interfaces> above (six entries: pdf, png, jpg, jpeg, mp4, webm). Do NOT add any other patterns.

6. Append to the existing `.gitignore` (created by `gh repo create --gitignore Python`) the project-specific entries — append them, do not replace the Python defaults:
   ```
   # Project
   .env
   .venv/
   chroma_db/
   lightrag_*/
   evaluation/results/costs/*.json
   !evaluation/results/costs/.gitkeep
   ```

7. Replace the auto-generated `README.md` with a curated scaffold containing these sections (per D-15 in CONTEXT — README structure beyond the blog link is Claude's discretion):
   - Title: `# RAG Architecture Patterns`
   - Tagline: One sentence on the meta-recursive framing ("Indexing the RAG literature itself to compare 5 architectures.")
   - **Blog Post** section linking to `https://patrykgolabek.dev/blog/rag-architecture-patterns` (placeholder — note "Coming soon" if needed). This link is REQUIRED by REPO-01.
   - **Architecture Tiers** section listing the five tiers (Tier 1 Naive, Tier 2 Managed File Search, Tier 3 Graph (LightRAG), Tier 4 Multimodal (RAG-Anything), Tier 5 Agentic) with one-line descriptions
   - **Dataset** section: ~100 arXiv papers (RAG citation cluster) + extracted figures + 1-2 CC-licensed video clips, all stored via git-lfs (~300-500MB)
   - **Setup** section: clone with `GIT_LFS_SKIP_SMUDGE=1` for code-only, `git lfs pull` for full corpus, `uv venv && uv pip install -e ".[shared]"`, copy `.env.example` to `.env` and set `GEMINI_API_KEY`
   - **License** section: Apache-2.0 (code), corpus licenses documented per asset in `dataset/README.md`

8. Verify LICENSE file is the Apache-2.0 file `gh repo create --license apache-2.0` produced — do not modify.

9. Stage and commit ONLY these scaffold files (per Pitfall 5 — never `git add -A` to avoid pulling stray binaries):
   ```
   git add .gitattributes .gitignore README.md
   git commit -m "chore: scaffold repo structure with git-lfs tracking"
   git push origin main
   ```

10. Sanity check: `git lfs track` should show the six patterns; `git ls-files | xargs -I {} git check-attr filter {}` should return only paths with `filter: lfs` for any matching file (none yet, but the rule must exist).

Decision rationale (per D-01, D-02 from CONTEXT.md): Companion repo lives at PatrykQuantumNomad/rag-architecture-patterns; ~300-500MB target requires git-lfs from commit 1.
  </action>
  <verify>
    <automated>
cd ../rag-architecture-patterns &amp;&amp; \
  test -f .gitattributes &amp;&amp; \
  grep -q '\*\.pdf  filter=lfs' .gitattributes &amp;&amp; \
  grep -q '\*\.png  filter=lfs' .gitattributes &amp;&amp; \
  grep -q '\*\.mp4  filter=lfs' .gitattributes &amp;&amp; \
  test -f LICENSE &amp;&amp; grep -q 'Apache License' LICENSE &amp;&amp; \
  test -f .gitignore &amp;&amp; grep -q '^\.env$' .gitignore &amp;&amp; \
  test -f README.md &amp;&amp; grep -q 'patrykgolabek\.dev/blog/rag-architecture-patterns' README.md &amp;&amp; \
  git lfs install --skip-smudge --local 2&gt;&amp;1 | grep -q 'Git LFS initialized' &amp;&amp; \
  git ls-remote --exit-code origin main
    </automated>
  </verify>
  <done>
    Companion repo PatrykQuantumNomad/rag-architecture-patterns exists on GitHub (public, Apache-2.0).
    Local sibling clone at ../rag-architecture-patterns/ has .gitattributes (6 LFS patterns), .gitignore (with .env), README.md (with blog link), LICENSE.
    Initial scaffolding commit pushed to origin/main.
    git-lfs is initialized BEFORE any binary is staged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author pyproject.toml + .env.example + per-tier requirements.txt files</name>
  <files>
    ../rag-architecture-patterns/pyproject.toml
    ../rag-architecture-patterns/.env.example
    ../rag-architecture-patterns/tier-1-naive/requirements.txt
    ../rag-architecture-patterns/tier-2-managed/requirements.txt
    ../rag-architecture-patterns/tier-3-graph/requirements.txt
    ../rag-architecture-patterns/tier-4-multimodal/requirements.txt
    ../rag-architecture-patterns/tier-5-agentic/requirements.txt
  </files>
  <action>
All work happens in ../rag-architecture-patterns/. NOT in the profile repo.

1. Write `pyproject.toml` using the exact shape from <interfaces> above. Pin versions per 127-RESEARCH.md "Standard Stack" (verified 2026-04-25):
   - google-genai>=1.73,<2 (NOT google-generativeai — EOL 2025-08-31, per Pitfall 4)
   - pydantic>=2.10,<3
   - pydantic-settings>=2.10,<3
   - rich>=14,<16
   - tiktoken>=0.10,<1
   - python-dotenv>=1.0,<2
   - arxiv>=3.0,<4
   - semanticscholar>=0.12,<1
   - pymupdf>=1.27,<2
   - pytest>=8.4,<9
   - ruff>=0.6

   Include `[tool.pytest.ini_options]` with `markers = ["live: tests that hit real APIs and incur cost"]` (per 127-RESEARCH.md Validation Architecture). Tier 1-5 extras stub out as `["rag-architecture-patterns[shared]"]` only — concrete tier deps are added in Phases 128-130, NOT here. Curation extras include arxiv, semanticscholar, pymupdf.

2. Write `.env.example` verbatim from <interfaces> above. Include the comment headers explaining which keys are required vs optional. Document where to obtain GEMINI_API_KEY (`https://aistudio.google.com/app/apikey`).

3. For each of the five tier directories, create `tier-{N}-{name}/requirements.txt`. Each file contains a SINGLE line that pulls the parent extras via editable install (per 127-RESEARCH.md Pattern 7 — "tier directory mirrors its extras into a requirements.txt"):
   ```
   # tier-1-naive/requirements.txt
   -e ..[tier-1]
   ```
   Repeat for tier-2-managed (`-e ..[tier-2]`), tier-3-graph (`-e ..[tier-3]`), tier-4-multimodal (`-e ..[tier-4]`), tier-5-agentic (`-e ..[tier-5]`).

   Rationale: This is the simplest mirror that satisfies REPO-04 ("per-tier requirements.txt for isolated installation") without duplicating package lists. When Phases 128-130 land, they will EITHER (a) extend the pyproject extras and the requirements.txt automatically picks them up, OR (b) replace the `-e ..[tier-N]` line with explicit pinned packages — that's a Phase 128+ decision per CONTEXT.md "Per-tier README content — handled in each tier's phase".

4. Verify install dry-run before committing:
   ```
   cd ../rag-architecture-patterns
   uv venv --quiet
   uv pip install --dry-run -e ".[shared,curation]" 2>&1 | tail -10
   ```
   This proves the pyproject parses and dependencies resolve without actually downloading. If it fails, fix the toml.

5. Commit:
   ```
   git add pyproject.toml .env.example tier-*/requirements.txt
   git commit -m "chore: add pyproject.toml with tier extras and .env.example"
   git push origin main
   ```

Decisions referenced (CONTEXT.md): D-09 (pyproject.toml extras layout — Claude's discretion, this is the chosen shape), D-06 (shared utilities list — drives the `shared` extras group), pricing+SDK choices in 127-RESEARCH.md "Standard Stack".

DO NOT install actual packages; dry-run only. Plan 02 will install the shared/dev/test extras when implementing modules.
  </action>
  <verify>
    <automated>
cd ../rag-architecture-patterns &amp;&amp; \
  test -f pyproject.toml &amp;&amp; grep -q 'google-genai' pyproject.toml &amp;&amp; \
  ! grep -q 'google-generativeai' pyproject.toml &amp;&amp; \
  grep -q 'pydantic-settings' pyproject.toml &amp;&amp; \
  grep -q 'arxiv' pyproject.toml &amp;&amp; grep -q 'pymupdf' pyproject.toml &amp;&amp; \
  grep -q '\[tool.pytest.ini_options\]' pyproject.toml &amp;&amp; \
  grep -q 'live:' pyproject.toml &amp;&amp; \
  test -f .env.example &amp;&amp; grep -q 'GEMINI_API_KEY=your_gemini_api_key_here' .env.example &amp;&amp; \
  for t in tier-1-naive tier-2-managed tier-3-graph tier-4-multimodal tier-5-agentic; do \
    test -f "$t/requirements.txt" || exit 1; \
    grep -q '\-e \.\.\[' "$t/requirements.txt" || exit 1; \
  done &amp;&amp; \
  uv pip install --dry-run -e ".[shared,curation]" --python python3.11 &gt; /dev/null 2&gt;&amp;1
    </automated>
  </verify>
  <done>
    pyproject.toml resolves cleanly with all 5 tier extras + shared + curation + test/dev groups.
    .env.example documents GEMINI_API_KEY (required) plus OPENAI_API_KEY and S2_API_KEY (optional).
    Five tier-N/requirements.txt files exist, each pulling the parent extras.
    google-generativeai (deprecated) is NOT in any dependency list.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create remaining directory structure with placeholder files and dataset/evaluation READMEs</name>
  <files>
    ../rag-architecture-patterns/dataset/papers/.gitkeep
    ../rag-architecture-patterns/dataset/images/.gitkeep
    ../rag-architecture-patterns/dataset/videos/.gitkeep
    ../rag-architecture-patterns/dataset/manifests/.gitkeep
    ../rag-architecture-patterns/dataset/README.md
    ../rag-architecture-patterns/shared/__init__.py
    ../rag-architecture-patterns/scripts/.gitkeep
    ../rag-architecture-patterns/tests/__init__.py
    ../rag-architecture-patterns/tests/conftest.py
    ../rag-architecture-patterns/evaluation/README.md
    ../rag-architecture-patterns/evaluation/results/costs/.gitkeep
  </files>
  <action>
All work happens in ../rag-architecture-patterns/.

1. Create the empty directory tree with `.gitkeep` placeholders for directories that will be filled by later plans:
   - dataset/papers/.gitkeep (Plan 04 fills with PDFs)
   - dataset/images/.gitkeep (Plan 05 fills with PNGs)
   - dataset/videos/.gitkeep (Plan 05 fills with MP4s)
   - dataset/manifests/.gitkeep (Plan 04/05 fill with JSON manifests)
   - scripts/.gitkeep (Plan 03 fills with curation scripts)
   - evaluation/results/costs/.gitkeep (runtime cost JSON destination, gitignored except this)

2. Write `dataset/README.md` documenting:
   - Provenance: ~100 arXiv papers curated as a citation cluster anchored on RAG / retrieval-augmented LMs literature (meta-recursive framing per D-02)
   - License per asset class:
     - PDFs (papers): arXiv non-exclusive distribution license (https://arxiv.org/licenses/) — papers retain original copyright; redistribution permitted for non-commercial research per arXiv ToU
     - Figures (images): same as parent paper (extracted at curation time, retain source paper's terms)
     - Videos: each clip individually CC-licensed (BY, BY-SA, BY-NC, or BY-NC-SA — never ND); manifest documents per-clip license
   - Storage: all binaries via git-lfs (`.pdf`, `.png`, `.jpg`, `.jpeg`, `.mp4`, `.webm`)
   - Manifests: `manifests/papers.json`, `manifests/figures.json`, `manifests/videos.json`, plus a top-level `manifests/metadata.json` index (assembled in Plan 06)
   - Per Pitfall 1: never bypass LFS — verify with `git check-attr filter dataset/papers/<file>.pdf` before committing.

3. Write `evaluation/README.md` documenting:
   - `golden_qa.json` schema: 30 hand-authored entries (10 single-hop / 10 multi-hop / 7 multimodal / 3 video — locked split per D-04)
   - Each entry has: `id`, `question`, `expected_answer`, `source_papers` (list of paper_ids), `modality_tag` (`text`, `multimodal`, `video`), `hop_count_tag` (`single-hop`, `multi-hop`)
   - `results/costs/`: per-run cost tracker JSON (gitignored except `.gitkeep`); schema is stable (per D-13 — blog Phase 133 ingests this directly)
   - Phase 131 (Evaluation Harness) implements the test runner — Phase 127 only commits the schema and Q&A.

4. Write `shared/__init__.py` as an empty file (Plan 02 will fill modules; this just makes `shared/` a package).

5. Write `tests/__init__.py` as empty.

6. Write `tests/conftest.py` with a single fixture skeleton — full smoke test contents land in Plan 02:
   ```python
   """Pytest configuration. Smoke test fixtures land in Plan 02."""
   ```
   Just a docstring placeholder; Plan 02 expands it.

7. Verify directory structure with `find ../rag-architecture-patterns -maxdepth 3 -type d | sort`. Should match the structure in <interfaces>.

8. Commit:
   ```
   cd ../rag-architecture-patterns
   git add dataset/ evaluation/ shared/ scripts/ tests/
   git commit -m "chore: scaffold dataset/evaluation/shared/scripts/tests directories with provenance docs"
   git push origin main
   ```

Decisions referenced: D-02 (RAG-literature anchor), D-04 (golden_qa balanced split locked), D-13 (cost tracker JSON schema stability), D-12 (CC license filter — BY/BY-SA/BY-NC/BY-NC-SA acceptable, ND rejected).
  </action>
  <verify>
    <automated>
cd ../rag-architecture-patterns &amp;&amp; \
  test -d dataset/papers &amp;&amp; test -d dataset/images &amp;&amp; test -d dataset/videos &amp;&amp; test -d dataset/manifests &amp;&amp; \
  test -d shared &amp;&amp; test -d scripts &amp;&amp; test -d tests &amp;&amp; test -d evaluation/results/costs &amp;&amp; \
  test -f dataset/README.md &amp;&amp; grep -q 'citation cluster' dataset/README.md &amp;&amp; \
  test -f evaluation/README.md &amp;&amp; grep -q 'golden_qa' evaluation/README.md &amp;&amp; \
  grep -q 'single-hop' evaluation/README.md &amp;&amp; grep -q 'multi-hop' evaluation/README.md &amp;&amp; \
  grep -q 'multimodal' evaluation/README.md &amp;&amp; grep -q 'video' evaluation/README.md &amp;&amp; \
  test -f shared/__init__.py &amp;&amp; test -f tests/__init__.py &amp;&amp; test -f tests/conftest.py
    </automated>
  </verify>
  <done>
    Full directory tree exists per 127-RESEARCH.md "Recommended Project Structure".
    dataset/README.md documents corpus provenance and per-asset licensing.
    evaluation/README.md documents the golden_qa.json schema with the locked 10/10/7/3 split.
    All scaffolding committed and pushed; companion repo is ready for Plans 02 and 03 to run in parallel.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local→GitHub (push) | Repo contents become public; .env contents must NEVER cross this boundary |
| local→GitHub (LFS) | LFS objects bandwidth-quota-limited; oversized commits brick the repo for that month |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-01 | Information Disclosure | `.env` (local-only secrets) | mitigate | `.env` listed in `.gitignore` from Task 1 commit; never `git add -A` (use named-file stages); `.env.example` ships placeholders only (Pattern: never the real key) |
| T-127-02 | Tampering | `.gitattributes` LFS rules | mitigate | Commit `.gitattributes` BEFORE any binary is staged (Pitfall 1); verify `git ls-files | xargs git check-attr filter` returns `lfs` for matching paths in Plan 04 verification |
| T-127-03 | Denial of Service | git-lfs bandwidth quota | accept | GitHub Free quota uncertain (1 GB vs 10 GiB — Open Question #1 in 127-RESEARCH.md); user_setup checkpoint flags this for post-push verification; recommend `GIT_LFS_SKIP_SMUDGE=1` clones during dev to conserve bandwidth |
| T-127-04 | Information Disclosure | Apache-2.0 LICENSE choice | accept | Public repo, intended for blog companion; no proprietary content |
| T-127-05 | Tampering | dependency-confusion attack on pyproject deps | mitigate | All deps pinned with upper bounds (`google-genai>=1.73,<2` etc.); `uv.lock` committed in Plan 02 (lockfile guarantees reproducible installs) |
</threat_model>

<verification>
After all 3 tasks complete, run from inside ../rag-architecture-patterns/:

```
# 1. LFS sanity
git lfs install --skip-smudge --local
git lfs track | grep -q '\*\.pdf'

# 2. pyproject resolves
uv pip install --dry-run -e ".[shared,curation]" --python python3.11

# 3. README has blog link (REPO-01 trace)
grep -q 'patrykgolabek\.dev/blog/rag-architecture-patterns' README.md

# 4. .env.example has GEMINI_API_KEY (REPO-06 trace)
grep -q 'GEMINI_API_KEY=your_gemini_api_key_here' .env.example

# 5. All 5 tier requirements.txt exist (REPO-04 trace)
ls tier-{1-naive,2-managed,3-graph,4-multimodal,5-agentic}/requirements.txt

# 6. Pushed to origin
git status | grep -q 'up to date'
```
</verification>

<success_criteria>
- Companion repo PatrykQuantumNomad/rag-architecture-patterns exists on GitHub (public, Apache-2.0)
- Local sibling clone at ../rag-architecture-patterns/ is initialized with git-lfs BEFORE any binary lands
- README.md links to the blog placeholder (REPO-01)
- pyproject.toml + 5 tier requirements.txt files install cleanly via dry-run (REPO-04)
- .env.example documents GEMINI_API_KEY plus optional OPENAI_API_KEY and S2_API_KEY (REPO-06)
- Directory scaffold (dataset/, shared/, scripts/, tests/, evaluation/) exists with provenance/schema documentation
- google-generativeai (deprecated) appears nowhere in the project
- All scaffolding committed and pushed to origin/main
</success_criteria>

<output>
After completion, create `.planning/phases/127-repository-skeleton-enterprise-dataset/127-01-SUMMARY.md` documenting:
- gh CLI commands run + their results (repo URL, initial commit SHA)
- Final pyproject.toml extras layout chosen
- Any deviations from 127-RESEARCH.md "Recommended Project Structure"
- LFS quota discovered post-push (resolves Open Question #1 from research)
- Companion repo absolute path on disk
</output>
