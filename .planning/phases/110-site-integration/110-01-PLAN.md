---
phase: 110-site-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Header.astro
  - astro.config.mjs
  - src/pages/index.astro
autonomous: true

must_haves:
  truths:
    - "The site header navigation includes an 'AI Landscape' link that navigates to /ai-landscape/"
    - "The homepage features a callout card linking to the AI Landscape Explorer"
    - "All AI Landscape pages appear in the sitemap with priority 0.5 and changefreq monthly"
  artifacts:
    - path: "src/components/Header.astro"
      provides: "AI Landscape nav link"
      contains: "ai-landscape"
    - path: "astro.config.mjs"
      provides: "Sitemap serialize rule for AI landscape"
      contains: "ai-landscape"
    - path: "src/pages/index.astro"
      provides: "AI Landscape card in Reference Guides grid"
      contains: "ai-landscape"
  key_links:
    - from: "src/components/Header.astro"
      to: "/ai-landscape/"
      via: "navLinks array entry"
      pattern: "href.*ai-landscape"
    - from: "src/pages/index.astro"
      to: "/ai-landscape/"
      via: "card anchor tag"
      pattern: "href.*ai-landscape"
---

<objective>
Add AI Landscape to site navigation, sitemap, and homepage

Purpose: Make the AI Landscape Explorer discoverable from the header, homepage, and search engine sitemaps. Covers requirements SITE-02, SITE-03, and SITE-04.
Output: Updated Header.astro with nav link, astro.config.mjs with sitemap rule, index.astro with homepage card
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-site-integration/110-RESEARCH.md
@src/components/Header.astro
@src/pages/index.astro
@astro.config.mjs
@src/data/ai-landscape/graph.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AI Landscape to header navigation and sitemap</name>
  <files>src/components/Header.astro, astro.config.mjs</files>
  <action>
  **Header.astro:** Add `{ href: '/ai-landscape/', label: 'AI Landscape' }` to the `navLinks` array. Place it after the `Blog` entry and before `Beauty Index` for alphabetical consistency among content sections. The array is currently 10 items (Home, Blog, Beauty Index, DB Compass, EDA, Guides, Tools, Projects, About, Contact). Adding makes 11. Both desktop and mobile nav render from the same array, so one edit covers both.

  **astro.config.mjs:** In the sitemap `serialize(item)` function, add an AI landscape URL pattern rule. Add it to the existing condition on line 100 that handles `/beauty-index/`, `/db-compass/`, and `/eda/` -- extend the `||` chain to include `item.url.includes('/ai-landscape/')`. This ensures all ~64 AI landscape pages (landing + 51 concepts + 12 VS) get `changefreq: 'monthly'` and `priority: 0.5`. Do NOT create a separate else-if block since the priority/changefreq values are identical to the beauty-index/db-compass/eda rule.
  </action>
  <verify>
  1. `grep -n "ai-landscape" src/components/Header.astro` returns the nav link entry
  2. `grep -n "ai-landscape" astro.config.mjs` returns the sitemap rule
  3. The navLinks array has 11 entries with AI Landscape after Blog and before Beauty Index
  </verify>
  <done>Header nav has "AI Landscape" link pointing to /ai-landscape/ and sitemap serialize function includes AI landscape pattern in the beauty-index/db-compass/eda priority rule</done>
</task>

<task type="auto">
  <name>Task 2: Add AI Landscape card to homepage Reference Guides grid</name>
  <files>src/pages/index.astro</files>
  <action>
  Add a 6th card to the Reference Guides grid in `src/pages/index.astro`. Insert it as the LAST card in the grid (after the Claude Code Guide card, before the closing `</div>` of the grid). This creates a balanced 3+3 layout on desktop (`lg:grid-cols-3`).

  The card follows the exact pattern of the existing EDA card (using an inline SVG for the hero visual). Create a helper function in the frontmatter script (similar to how `generateEdaHeroSvg` is imported and called) that generates an inline SVG with colored circles and connecting lines representing the AI landscape graph. Use 5-6 cluster `darkColor` values from graph.json: `#00696e` (AI), `#c62828` (GenAI), `#4527a0` (DL), `#1565c0` (ML), `#2e7d32` (NN), `#ef6c00` (Agentic). The SVG should render a small abstract node-graph motif: 6-8 circles of different sizes connected by thin lines, scattered in a compact composition. Width 280, height 160. The SVG should use `viewBox` and be responsive.

  Card structure (follow the exact HTML pattern from the EDA card):
  ```html
  <a href="/ai-landscape/" class="block card-hover rounded-lg border border-[var(--color-border)] no-underline overflow-hidden" data-reveal data-tilt>
    <article>
      <div class="bg-[var(--color-surface-alt)] px-6 pt-6 pb-4 flex justify-center items-center" aria-hidden="true" set:html={aiLandscapeCardSvg} />
      <div class="p-6 pt-4">
        <p class="meta-mono text-[var(--color-accent)]">51 Concepts</p>
        <h3 class="text-lg font-heading font-bold text-[var(--color-text-primary)] mt-2">AI Landscape Explorer</h3>
        <p class="text-sm text-[var(--color-text-secondary)] mt-2">
          Visual guide to artificial intelligence — from machine learning fundamentals to generative AI, agentic systems, and developer tools.
        </p>
        <span class="inline-block mt-4 text-sm font-medium text-[var(--color-accent)]">Explore the landscape &rarr;</span>
      </div>
    </article>
  </a>
  ```

  Generate the SVG string inline in the frontmatter script (no separate file needed). Assign it to `const aiLandscapeCardSvg`. The function should produce a compact decorative SVG: circles of varying radius (4-10px) placed at different positions, each filled with one of the cluster darkColors at ~70% opacity, connected by thin gray lines (`stroke: var(--color-border)`, `stroke-width: 0.5`). This visually previews the graph experience without being a literal copy.
  </action>
  <verify>
  1. `grep -c "ai-landscape" src/pages/index.astro` shows the new card reference
  2. The Reference Guides section now has 6 `<a>` cards (previously 5)
  3. The AI Landscape card has `href="/ai-landscape/"`, uses `set:html` for the SVG hero, shows "51 Concepts" and "AI Landscape Explorer"
  4. `npm run build` completes without error (run only if time permits)
  </verify>
  <done>Homepage Reference Guides grid has 6 cards (balanced 3+3 on desktop) with AI Landscape Explorer card last, showing inline SVG node-graph motif and linking to /ai-landscape/</done>
</task>

</tasks>

<verification>
1. Header.astro navLinks array includes `{ href: '/ai-landscape/', label: 'AI Landscape' }` as the 3rd entry (after Home, Blog)
2. astro.config.mjs sitemap serialize includes `/ai-landscape/` in the priority 0.5 rule
3. index.astro Reference Guides grid has exactly 6 card `<a>` elements
4. AI Landscape card links to `/ai-landscape/` with correct content
</verification>

<success_criteria>
- Header shows "AI Landscape" nav link navigating to /ai-landscape/
- Homepage has AI Landscape card in Reference Guides grid
- Sitemap includes AI landscape pages with monthly/0.5 settings
</success_criteria>

<output>
After completion, create `.planning/phases/110-site-integration/110-01-SUMMARY.md`
</output>
