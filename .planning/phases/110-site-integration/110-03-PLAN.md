---
phase: 110-site-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/llms.txt.ts
  - src/pages/llms-full.txt.ts
autonomous: true

must_haves:
  truths:
    - "LLMs.txt includes an AI Landscape Explorer section with concept listing by cluster"
    - "LLMs-full.txt includes expanded AI Landscape content with per-concept descriptions and VS comparisons"
    - "Both files import data dynamically from existing collections and data sources (no hardcoded concept lists)"
  artifacts:
    - path: "src/pages/llms.txt.ts"
      provides: "AI Landscape section in LLMs.txt"
      contains: "AI Landscape"
    - path: "src/pages/llms-full.txt.ts"
      provides: "Expanded AI Landscape section in LLMs-full.txt"
      contains: "AI Landscape"
  key_links:
    - from: "src/pages/llms.txt.ts"
      to: "src/lib/ai-landscape/routes.ts"
      via: "import conceptUrl, vsPageUrl"
      pattern: "conceptUrl|vsPageUrl"
    - from: "src/pages/llms.txt.ts"
      to: "src/lib/ai-landscape/comparisons.ts"
      via: "import POPULAR_COMPARISONS"
      pattern: "POPULAR_COMPARISONS"
    - from: "src/pages/llms-full.txt.ts"
      to: "src/lib/ai-landscape/routes.ts"
      via: "import conceptUrl, vsPageUrl"
      pattern: "conceptUrl|vsPageUrl"
---

<objective>
Add AI Landscape content to LLMs.txt and LLMs-full.txt

Purpose: Make the AI Landscape Explorer discoverable by LLM-based tools that read llms.txt and llms-full.txt. List concepts by cluster, popular comparisons, and guided tours. Covers requirement SITE-05.
Output: Updated llms.txt.ts and llms-full.txt.ts with AI Landscape sections
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-site-integration/110-RESEARCH.md
@src/pages/llms.txt.ts
@src/pages/llms-full.txt.ts
@src/lib/ai-landscape/routes.ts
@src/lib/ai-landscape/comparisons.ts
@src/lib/ai-landscape/tours.ts
@src/data/ai-landscape/graph.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AI Landscape section to llms.txt.ts</name>
  <files>src/pages/llms.txt.ts</files>
  <action>
  Edit `src/pages/llms.txt.ts` to add an AI Landscape section. This is the compact version (not full).

  **Add imports at top:**
  ```typescript
  import { conceptUrl, vsPageUrl } from '../lib/ai-landscape/routes';
  import { POPULAR_COMPARISONS } from '../lib/ai-landscape/comparisons';
  ```

  **In the GET function, add after the existing collections:**
  ```typescript
  const aiNodes = await getCollection('aiLandscape');
  const aiClusters = (await import('../data/ai-landscape/graph.json')).default.clusters;
  ```

  **Add the AI Landscape section to the `lines` array.** Insert it AFTER the Claude Code Guide section (after line ~238, before `'## Blog Posts'`). The section structure:

  ```
  ## AI Landscape Explorer

  Visual guide to 51 key concepts in artificial intelligence organized into 9 clusters — from machine learning fundamentals to generative AI, agentic systems, and developer tools.

  - [AI Landscape Explorer](https://patrykgolabek.dev/ai-landscape/): Interactive graph visualization with guided tours

  ### Clusters

  [For each cluster from aiClusters: cluster name with count of nodes and a few example concept names]
  Example: - Artificial Intelligence (AI): 7 concepts including NLP, Computer Vision, Reinforcement Learning

  ### Popular Comparisons

  [For each of the 12 POPULAR_COMPARISONS: question with link and summary]
  Example: - [AI vs Machine Learning: What's the Difference?](https://patrykgolabek.dev/ai-landscape/vs/artificial-intelligence-vs-machine-learning/): Artificial intelligence is the broad goal...
  ```

  Build the clusters dynamically: for each cluster in `aiClusters`, filter `aiNodes` by `n.data.cluster === cluster.id`, get the count, and list 2-3 example node names. Use `cluster.name` for display.

  Build comparisons dynamically from `POPULAR_COMPARISONS.map(pair => ...)`.

  Do NOT hardcode concept names. Use the data from the collections.
  </action>
  <verify>
  1. `grep -c "AI Landscape" src/pages/llms.txt.ts` returns at least 1
  2. `grep "POPULAR_COMPARISONS" src/pages/llms.txt.ts` confirms the import
  3. `grep "aiLandscape" src/pages/llms.txt.ts` confirms collection fetch
  4. The section is placed between Claude Code Guide and Blog Posts sections
  </verify>
  <done>llms.txt.ts includes AI Landscape section with dynamic cluster listing and popular comparisons, placed between Claude Code Guide and Blog Posts</done>
</task>

<task type="auto">
  <name>Task 2: Add expanded AI Landscape section to llms-full.txt.ts</name>
  <files>src/pages/llms-full.txt.ts</files>
  <action>
  Edit `src/pages/llms-full.txt.ts` to add an expanded AI Landscape section. This is the FULL version with per-concept detail.

  **Add imports at top:**
  ```typescript
  import { conceptUrl, vsPageUrl } from '../lib/ai-landscape/routes';
  import { POPULAR_COMPARISONS } from '../lib/ai-landscape/comparisons';
  import { TOURS } from '../lib/ai-landscape/tours';
  ```

  **In the GET function, add after the existing collections:**
  ```typescript
  const aiNodes = await getCollection('aiLandscape');
  const aiGraphData = (await import('../data/ai-landscape/graph.json')).default;
  const aiClusters = aiGraphData.clusters;
  ```

  **Add the expanded AI Landscape section** BEFORE the `## Blog Posts` section. Structure:

  ```
  ## AI Landscape Explorer

  Visual guide to 51 key concepts in artificial intelligence organized into 9 clusters.
  Interactive graph visualization with guided tours, side-by-side comparisons, and both simple and technical explanations.

  - [AI Landscape Explorer](https://patrykgolabek.dev/ai-landscape/): Landing page with interactive force-directed graph

  ### Guided Tours

  [For each tour in TOURS: title and description]
  - The Big Picture: See how the entire AI landscape fits together...
  - How ChatGPT Works: Trace the technology stack...
  - What is Agentic AI: From basic AI to autonomous agents...

  ### Concepts by Cluster

  [For each cluster in aiClusters:]
  #### {cluster.name} ({count} concepts)

  [For each node in that cluster:]
  - [{node.name}](https://patrykgolabek.dev/ai-landscape/{slug}/): {simpleDescription} | Technical: {technicalDescription truncated to ~200 chars}

  ### All Comparisons

  [For all 12 POPULAR_COMPARISONS:]
  - [{pair.question}](https://patrykgolabek.dev/ai-landscape/vs/{pair.slug}/): {pair.summary}
  ```

  Build everything dynamically. For each cluster, filter aiNodes by cluster ID, then for each node output:
  - Name as link using `conceptUrl(n.data.slug)`
  - `simpleDescription` (full, not truncated)
  - `technicalDescription` truncated to ~200 chars using the `truncate` pattern or simple `.slice(0, 200)`

  For comparisons, use `vsPageUrl(pair.slug)` for the URL.
  For tours, use `TOURS.map(t => ...)`.

  Include `whyItMatters` for each node if available in the data (check the schema -- if node.data has a `whyItMatters` field, include it).
  </action>
  <verify>
  1. `grep -c "AI Landscape" src/pages/llms-full.txt.ts` returns at least 1
  2. `grep "POPULAR_COMPARISONS" src/pages/llms-full.txt.ts` confirms the import
  3. `grep "TOURS" src/pages/llms-full.txt.ts` confirms tours import
  4. `grep "conceptUrl" src/pages/llms-full.txt.ts` confirms route helper usage
  5. The section appears before the Blog Posts section
  </verify>
  <done>llms-full.txt.ts includes expanded AI Landscape section with per-concept details (simple + technical descriptions), all 12 comparisons, 3 guided tours, and cluster-grouped concept listings</done>
</task>

</tasks>

<verification>
1. Both llms.txt.ts and llms-full.txt.ts import from ai-landscape route helpers and comparisons
2. Both files fetch `getCollection('aiLandscape')` and cluster data from graph.json
3. llms.txt has compact cluster summaries and popular comparisons
4. llms-full.txt has per-concept detail with descriptions and all comparisons
5. No hardcoded concept names — all derived from data sources
6. `npm run build` succeeds (if time permits)
</verification>

<success_criteria>
- /llms.txt includes AI Landscape section with cluster overview and popular VS comparisons
- /llms-full.txt includes expanded AI Landscape section with per-concept descriptions, tours, and all comparisons
- All data is dynamically sourced from existing collections (not hardcoded)
</success_criteria>

<output>
After completion, create `.planning/phases/110-site-integration/110-03-SUMMARY.md`
</output>
