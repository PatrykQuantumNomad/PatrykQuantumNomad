---
phase: 14-visual-design-card-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/projects/index.astro
autonomous: true

must_haves:
  truths:
    - "Each category header shows the category name followed by project count in monospace metadata style"
    - "Category count includes ALL projects in the category, including featured projects shown in the hero section"
    - "Count uses the // separator for code/terminal aesthetic (e.g. AI/ML & LLM Agents // 7 projects)"
    - "Singular form used when count is 1 (e.g. // 1 project)"
  artifacts:
    - path: "src/pages/projects/index.astro"
      provides: "Category headers with total project count in meta-mono style"
      contains: "totalCount"
  key_links:
    - from: "src/pages/projects/index.astro"
      to: "src/data/projects.ts"
      via: "projects.filter by category for totalCount (including featured)"
      pattern: "totalCount.*projects\\.filter"
---

<objective>
Add monospace project count metadata to category headers on the projects page.

Purpose: Satisfy VIS-05 requirement -- each category header shows the project count in developer-aesthetic monospace style that matches the site's terminal-inspired brand.

Output: Enhanced projects/index.astro with category headers displaying total project count.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-visual-design-card-components/14-RESEARCH.md
@.planning/phases/13-data-model-bento-grid-layout/13-02-SUMMARY.md
@src/pages/projects/index.astro
@src/data/projects.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add project count metadata to category headers</name>
  <files>src/pages/projects/index.astro</files>
  <action>
Modify the `grouped` variable in the frontmatter to include `totalCount` for each category. The totalCount must include ALL projects in the category (including featured ones that appear in the hero section, not just the items displayed in the category grid). This avoids Pitfall 4 from the research.

Updated grouped computation:
```typescript
const grouped = categories.map((category) => ({
  category,
  items: projects.filter((p) => p.category === category && !p.featured),
  totalCount: projects.filter((p) => p.category === category).length,
}));
```

Then update the category header from a plain `<h2>` to a flex container with the count:

Replace:
```html
<h2 class="text-xl sm:text-2xl font-heading font-bold mb-6" data-word-reveal>
  {category}
</h2>
```

With:
```html
<div class="flex items-baseline gap-3 mb-6">
  <h2 class="text-xl sm:text-2xl font-heading font-bold" data-word-reveal>
    {category}
  </h2>
  <span class="meta-mono text-[var(--color-text-secondary)]">
    // {totalCount} {totalCount === 1 ? 'project' : 'projects'}
  </span>
</div>
```

Note: The `meta-mono` class already provides Fira Code font, 0.75rem, uppercase, letter-spacing 0.15em, and secondary text color. The explicit `text-[var(--color-text-secondary)]` is for specificity reinforcement since meta-mono sets it via CSS class.

IMPORTANT: Do NOT modify the hero section, the grid layout, the gradient dividers, or any other part of the page. Only add totalCount to the grouped data and enhance the category h2 headers.
  </action>
  <verify>Run `npx astro build` -- must succeed. Check the built HTML for /projects/index.html and verify: (1) "AI/ML &amp; LLM Agents" header is followed by "// 7 PROJECTS" (uppercase due to meta-mono text-transform), (2) "Kubernetes &amp; Infrastructure" shows "// 6 PROJECTS", (3) "Platform &amp; DevOps Tooling" shows "// 2 PROJECTS", (4) "Security &amp; Networking" shows "// 1 PROJECT" (singular).</verify>
  <done>Each of the 4 category headers displays the total project count in monospace metadata style with the // separator. Counts are: AI/ML=7, Kubernetes=6, Platform=2, Security=1. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npx astro build` succeeds with 0 errors
2. Built /projects/index.html contains "// 7 projects" adjacent to "AI/ML & LLM Agents"
3. Built /projects/index.html contains "// 6 projects" adjacent to "Kubernetes & Infrastructure"
4. Built /projects/index.html contains "// 2 projects" adjacent to "Platform & DevOps Tooling"
5. Built /projects/index.html contains "// 1 project" adjacent to "Security & Networking" (singular)
6. Category headers use meta-mono class for the count span
7. h2 elements still have data-word-reveal for GSAP animation
8. Hero section, grid layout, gradient dividers unchanged
</verification>

<success_criteria>
- VIS-05: Each category header shows project count in monospace metadata style (e.g., "AI/ML & LLM Agents // 7 projects")
- Counts include featured projects in total (not just grid items)
- Singular/plural grammar correct
- Existing page layout and GSAP attributes preserved
</success_criteria>

<output>
After completion, create `.planning/phases/14-visual-design-card-components/14-02-SUMMARY.md`
</output>
