---
phase: 88-content-authoring
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/guides/fastapi-production/pages/06-testing.mdx
  - src/data/guides/fastapi-production/pages/07-health-checks.mdx
  - src/data/guides/fastapi-production/pages/10-caching.mdx
autonomous: true
requirements: [PAGE-07, PAGE-08, PAGE-11, AGENT-01, AGENT-02]

must_haves:
  truths:
    - "Testing page explains unit/integration test architecture, hermetic helpers, and fixture-based test isolation"
    - "Health Checks page explains readiness vs liveness separation and dependency-aware registry pattern"
    - "Caching page explains the CacheStore abstraction, memory/Redis implementations, and per-key TTL"
    - "All 3 pages open with 'What Your Agent Inherits' and close with 'What the Agent Never Implements'"
    - "All 3 pages include CodeFromRepo snippets from the relevant fastapi-template source files"
  artifacts:
    - path: "src/data/guides/fastapi-production/pages/06-testing.mdx"
      provides: "Testing domain page with test architecture and helpers coverage"
      min_lines: 150
      contains: "slug: \"testing\""
    - path: "src/data/guides/fastapi-production/pages/07-health-checks.mdx"
      provides: "Health Checks domain page with readiness registry coverage"
      min_lines: 120
      contains: "slug: \"health-checks\""
    - path: "src/data/guides/fastapi-production/pages/10-caching.mdx"
      provides: "Caching domain page with store abstraction and TTL coverage"
      min_lines: 120
      contains: "slug: \"caching\""
  key_links:
    - from: "src/data/guides/fastapi-production/pages/06-testing.mdx"
      to: "src/components/guide/CodeFromRepo.astro"
      via: "MDX component import"
      pattern: "import CodeFromRepo from"
    - from: "src/data/guides/fastapi-production/pages/07-health-checks.mdx"
      to: "src/components/guide/CodeFromRepo.astro"
      via: "MDX component import"
      pattern: "import CodeFromRepo from"
    - from: "src/data/guides/fastapi-production/pages/10-caching.mdx"
      to: "src/components/guide/CodeFromRepo.astro"
      via: "MDX component import"
      pattern: "import CodeFromRepo from"
---

<objective>
Author the Testing, Health Checks, and Caching domain pages -- the supporting layers cluster of the FastAPI Production Guide.

Purpose: These chapters cover the template's quality and resilience infrastructure: how it tests itself (hermetic test architecture), how it reports health (readiness vs liveness), and how it caches data (pluggable memory/Redis stores).
Output: Three complete MDX files with full prose, CodeFromRepo snippets, and AI agent narrative framing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-content-authoring/88-RESEARCH.md

Source material (read these files from the fastapi-template):
@/Users/patrykattc/work/git/fastapi-template/tests/conftest.py
@/Users/patrykattc/work/git/fastapi-template/tests/helpers.py
@/Users/patrykattc/work/git/fastapi-template/pyproject.toml (pytest config section)
@/Users/patrykattc/work/git/fastapi-template/src/app/routes/health.py
@/Users/patrykattc/work/git/fastapi-template/src/app/readiness/registry.py
@/Users/patrykattc/work/git/fastapi-template/src/app/db/health.py
@/Users/patrykattc/work/git/fastapi-template/src/app/cache/health.py
@/Users/patrykattc/work/git/fastapi-template/src/app/cache/store.py
@/Users/patrykattc/work/git/fastapi-template/src/app/cache/dependencies.py
@/Users/patrykattc/work/git/fastapi-template/src/app/settings.py (cache_* fields)

Existing patterns:
@src/data/guides/fastapi-production/guide.json
@src/components/guide/CodeFromRepo.astro

<interfaces>
<!-- Import path from MDX -->
import CodeFromRepo from '../../../../components/guide/CodeFromRepo.astro';

<!-- Frontmatter slugs (must match guide.json) -->
testing: order=6, slug="testing"
health-checks: order=7, slug="health-checks"
caching: order=10, slug="caching"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Testing page (PAGE-07)</name>
  <files>src/data/guides/fastapi-production/pages/06-testing.mdx</files>
  <action>
Create the Testing chapter MDX file. Read these fastapi-template source files first:
- `/Users/patrykattc/work/git/fastapi-template/tests/conftest.py` -- shared pytest fixtures, test app factory, async fixtures
- `/Users/patrykattc/work/git/fastapi-template/tests/helpers.py` -- hermetic test helpers (JWT minting, factory functions)
- `/Users/patrykattc/work/git/fastapi-template/pyproject.toml` -- find [tool.pytest.ini_options] and [tool.coverage] sections
- Also scan `/Users/patrykattc/work/git/fastapi-template/tests/unit/` and `/Users/patrykattc/work/git/fastapi-template/tests/integration/` directory listings to understand the test organization

Page structure:
1. **Frontmatter**: title="Testing", description about unit/integration architecture and hermetic helpers, order=6, slug="testing"
2. **Import**: CodeFromRepo only
3. **"What Your Agent Inherits"** (AGENT-01): The agent writes tests against a pre-built test infrastructure with async fixtures, a test app factory, JWT minting helpers, and hermetic database isolation. Every new endpoint gets integration-tested with the full middleware stack.
4. **"Unit vs Integration Architecture" section**: Explain the two-tier test structure. Unit tests: no database, no network, fast. Integration tests: full middleware stack, real (in-memory) database, realistic. Use directory listing (fenced code block is fine here since it is not template source code) to show the test directory layout.
5. **"Test Fixtures" section**: Show the key conftest.py fixtures via CodeFromRepo. Explain the test app factory, how it creates isolated FastAPI instances with test settings.
6. **"Hermetic Helpers" section**: Show helpers.py excerpts via CodeFromRepo. Explain JWT minting for auth-protected routes, factory functions for test data.
7. **"Coverage Configuration" section**: Show the pytest and coverage config from pyproject.toml via CodeFromRepo (lang="toml", filePath="pyproject.toml"). Explain the 98%+ coverage target and which paths are excluded.
8. **"What the Agent Never Implements"** (AGENT-02): Bullet list covering test infrastructure, fixture setup, JWT minting, async test runner configuration, coverage enforcement.

Target: 150-250 lines. Use CodeFromRepo for 4+ template excerpts.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -20</automated>
  </verify>
  <done>06-testing.mdx covers unit/integration architecture, conftest fixtures, hermetic helpers, and coverage config. Includes 4+ CodeFromRepo snippets and agent narrative sections. Renders successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Author Health Checks and Caching pages (PAGE-08, PAGE-11)</name>
  <files>src/data/guides/fastapi-production/pages/07-health-checks.mdx, src/data/guides/fastapi-production/pages/10-caching.mdx</files>
  <action>
Create both MDX files.

**Read source files first:**
- `/Users/patrykattc/work/git/fastapi-template/src/app/routes/health.py` -- health_check() vs readiness_check()
- `/Users/patrykattc/work/git/fastapi-template/src/app/readiness/registry.py` -- ReadinessRegistry, ReadinessCheckResult
- `/Users/patrykattc/work/git/fastapi-template/src/app/db/health.py` -- database readiness check
- `/Users/patrykattc/work/git/fastapi-template/src/app/cache/health.py` -- cache readiness check
- `/Users/patrykattc/work/git/fastapi-template/src/app/cache/store.py` -- CacheStore ABC, MemoryCacheStore, RedisCacheStore, create_cache_store()
- `/Users/patrykattc/work/git/fastapi-template/src/app/cache/dependencies.py` -- FastAPI dependency for cache access
- `/Users/patrykattc/work/git/fastapi-template/src/app/settings.py` -- find cache_* settings fields

**Health Checks page (07-health-checks.mdx):**
1. **Frontmatter**: title="Health Checks", description about readiness vs liveness separation, order=7, slug="health-checks"
2. **Import**: CodeFromRepo only
3. **"What Your Agent Inherits"** (AGENT-01): The agent's endpoints are served only when the application reports ready. Kubernetes liveness and readiness probes are pre-configured, and every dependency (database, cache) automatically registers its health check.
4. **"Liveness vs Readiness" section**: Explain the difference. Liveness: "am I alive? should Kubernetes restart me?" -- always returns 200 unless the process is wedged. Readiness: "can I serve traffic?" -- checks all dependencies before answering. Show `health_check()` and `readiness_check()` from health.py via CodeFromRepo.
5. **"The Readiness Registry" section**: Show `ReadinessRegistry` and `ReadinessCheckResult` from registry.py via CodeFromRepo. Explain how each builder step (setup_database, setup_cache) registers its own readiness check.
6. **"Dependency Health Checks" section**: Show database and cache readiness checks via CodeFromRepo. Explain timeout-protected async pings.
7. **"What the Agent Never Implements"** (AGENT-02): Bullet list.
Target: 120-180 lines.

**Caching page (10-caching.mdx):**
1. **Frontmatter**: title="Caching", description about optional memory/Redis cache with per-key TTL, order=10, slug="caching"
2. **Import**: CodeFromRepo only
3. **"What Your Agent Inherits"** (AGENT-01): The agent can cache any value with a single dependency injection call. The cache layer is optional, pluggable (memory or Redis), and pre-wired with TTL support, key prefix namespacing, and automatic health registration.
4. **"The CacheStore Abstraction" section**: Show the `CacheStore` ABC from store.py via CodeFromRepo. Explain the 7 methods: get, set, delete, exists, clear, ping, close.
5. **"Memory Backend" section**: Show `MemoryCacheStore` via CodeFromRepo. Explain dict-based storage with monotonic TTL checking and max-entry eviction.
6. **"Redis Backend" section**: Show `RedisCacheStore` via CodeFromRepo. Explain Redis client with key prefix namespacing for multi-tenant isolation.
7. **"Factory Function" section**: Show `create_cache_store()` via CodeFromRepo. Explain how settings drive backend selection.
8. **"Dependency Injection" section**: Show the FastAPI dependency from dependencies.py via CodeFromRepo. Explain how route handlers access the cache.
9. **"What the Agent Never Implements"** (AGENT-02): Bullet list.
Target: 150-220 lines.

CRITICAL: Use exact slugs "health-checks" and "caching" in frontmatter. Use CodeFromRepo for all template source excerpts.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -20</automated>
  </verify>
  <done>07-health-checks.mdx covers liveness vs readiness, ReadinessRegistry, and dependency health checks. 10-caching.mdx covers CacheStore abstraction, memory/Redis backends, factory function, and DI. Both include CodeFromRepo snippets and agent narrative sections. Both render successfully.</done>
</task>

</tasks>

<verification>
1. `npx astro build` completes without errors for all 3 new MDX files
2. Frontmatter slugs match guide.json: "testing" (order 6), "health-checks" (order 7), "caching" (order 10)
3. All 3 pages import CodeFromRepo only (no diagrams in these chapters)
4. All 3 pages have agent opener and closer sections
5. All code snippets use CodeFromRepo with correct filePath values
</verification>

<success_criteria>
- Testing page is self-contained: reader understands the two-tier test architecture, fixtures, and hermetic helpers
- Health Checks page is self-contained: reader understands liveness vs readiness, the registry pattern, and dependency checks
- Caching page is self-contained: reader understands the CacheStore interface, both backends, and DI integration
- All 3 pages render at their respective URLs and appear in sidebar navigation
- After this plan completes, all 11 domain pages exist and the guide is complete
</success_criteria>

<output>
After completion, create `.planning/phases/88-content-authoring/88-04-SUMMARY.md`
</output>
