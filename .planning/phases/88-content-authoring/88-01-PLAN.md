---
phase: 88-content-authoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/guides/fastapi-production/pages/00-builder-pattern.mdx
  - src/data/guides/fastapi-production/pages/01-middleware.mdx
autonomous: true
requirements: [PAGE-01, PAGE-03, AGENT-01, AGENT-02]

must_haves:
  truths:
    - "Builder Pattern page explains FastAPIAppBuilder fluent interface, setup_*() composition, and create_app() factory"
    - "Builder Pattern page includes BuilderPatternDiagram showing method composition flow"
    - "Middleware page explains all 6 raw ASGI middlewares with ordering rationale and why not BaseHTTPMiddleware"
    - "Middleware page includes MiddlewareStackDiagram showing 8-layer request flow"
    - "Both pages open with 'What Your Agent Inherits' and close with 'What the Agent Never Implements'"
    - "Both pages include CodeFromRepo snippets with correct filePath and line ranges from fastapi-template"
  artifacts:
    - path: "src/data/guides/fastapi-production/pages/00-builder-pattern.mdx"
      provides: "Builder Pattern domain page with full prose, code snippets, diagram, and AI agent narrative"
      min_lines: 200
      contains: "slug: \"builder-pattern\""
    - path: "src/data/guides/fastapi-production/pages/01-middleware.mdx"
      provides: "Middleware domain page with full prose, code snippets, diagram, and AI agent narrative"
      min_lines: 200
      contains: "slug: \"middleware\""
  key_links:
    - from: "src/data/guides/fastapi-production/pages/00-builder-pattern.mdx"
      to: "src/components/guide/BuilderPatternDiagram.astro"
      via: "MDX component import"
      pattern: "import BuilderPatternDiagram from"
    - from: "src/data/guides/fastapi-production/pages/00-builder-pattern.mdx"
      to: "src/components/guide/CodeFromRepo.astro"
      via: "MDX component import"
      pattern: "import CodeFromRepo from"
    - from: "src/data/guides/fastapi-production/pages/01-middleware.mdx"
      to: "src/components/guide/MiddlewareStackDiagram.astro"
      via: "MDX component import"
      pattern: "import MiddlewareStackDiagram from"
---

<objective>
Author the Builder Pattern and Middleware domain pages -- the two core architecture chapters of the FastAPI Production Guide.

Purpose: These are the foundational chapters (order 0 and 1) that explain how the template composes itself (Builder Pattern) and how requests flow through it (Middleware). They set the stage for all subsequent chapters.
Output: Two complete MDX files with full prose, CodeFromRepo snippets, architecture diagrams, and AI agent narrative framing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-content-authoring/88-RESEARCH.md

Source material (read these files from the fastapi-template to extract code snippets):
@/Users/patrykattc/work/git/fastapi-template/src/app/__init__.py
@/Users/patrykattc/work/git/fastapi-template/src/app/app_builder.py
@/Users/patrykattc/work/git/fastapi-template/main.py
@/Users/patrykattc/work/git/fastapi-template/src/app/middleware/request_id.py
@/Users/patrykattc/work/git/fastapi-template/src/app/middleware/request_logging.py
@/Users/patrykattc/work/git/fastapi-template/src/app/middleware/security_headers.py
@/Users/patrykattc/work/git/fastapi-template/src/app/middleware/rate_limit.py
@/Users/patrykattc/work/git/fastapi-template/src/app/middleware/body_size.py
@/Users/patrykattc/work/git/fastapi-template/src/app/middleware/timeout.py

Existing patterns to follow:
@src/data/guides/fastapi-production/pages/00-builder-pattern.mdx (existing stub -- REPLACE with full content)
@src/data/guides/fastapi-production/guide.json
@src/components/guide/CodeFromRepo.astro (component API)

<interfaces>
<!-- CodeFromRepo props -->
code: string (raw source code to display)
lang: string (e.g., "python", "yaml", "dockerfile")
filePath: string (path within template repo, e.g., "src/app/app_builder.py")
title?: string (defaults to filePath)
templateRepo?: string (defaults to fastapi-template GitHub URL)
versionTag?: string (defaults to "v1.0.0")
startLine?: number (optional line range)
endLine?: number (optional line range)

<!-- Import paths from MDX in src/data/guides/fastapi-production/pages/ -->
import CodeFromRepo from '../../../../components/guide/CodeFromRepo.astro';
import BuilderPatternDiagram from '../../../../components/guide/BuilderPatternDiagram.astro';
import MiddlewareStackDiagram from '../../../../components/guide/MiddlewareStackDiagram.astro';

<!-- Frontmatter pattern -->
---
title: "Chapter Title"
description: "One-line description"
order: N (0-indexed, matches guide.json chapters array position)
slug: "exact-slug-from-guide-json"
---
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Builder Pattern page (PAGE-01)</name>
  <files>src/data/guides/fastapi-production/pages/00-builder-pattern.mdx</files>
  <action>
REPLACE the existing stub MDX file with the complete Builder Pattern chapter. Read these fastapi-template source files first to extract accurate code snippets:
- `/Users/patrykattc/work/git/fastapi-template/src/app/__init__.py` -- `create_app()` factory function (full file, ~73 lines)
- `/Users/patrykattc/work/git/fastapi-template/src/app/app_builder.py` -- `FastAPIAppBuilder` class (~346 lines)
- `/Users/patrykattc/work/git/fastapi-template/main.py` -- module-level `app = create_app()` entrypoint

Page structure:
1. **Frontmatter**: title="Builder Pattern", description about FastAPIAppBuilder composition, order=0, slug="builder-pattern"
2. **Imports**: CodeFromRepo and BuilderPatternDiagram (use exact 4-level relative paths)
3. **"What Your Agent Inherits" section** (AGENT-01): 2-3 sentences explaining the agent inherits a fully wired application -- it writes route handlers, not bootstrap code. The builder ensures every production concern (auth, middleware, observability, caching) is configured before the first request.
4. **"Why a Factory Function?" section**: Explain testability, explicit lifecycle, multiple instances. Use CodeFromRepo to show the `create_app()` function from `src/app/__init__.py` (lines 36-73).
5. **"The Builder Chain" section**: Explain the fluent interface -- each `setup_*()` method returns `Self` for chaining. Show the full builder chain from `create_app()`. Use CodeFromRepo to show the `FastAPIAppBuilder` class definition and its `__init__` method from `src/app/app_builder.py`.
6. **"What Each Step Does" section**: Brief (2-3 sentence) explanation of each `setup_*()` method. Show 2-3 representative `setup_*()` method implementations via CodeFromRepo (e.g., `setup_database()`, `setup_middleware()`, `setup_auth()`).
7. **"The Build Step" section**: Explain `build()` as the final assembly. Show the `build()` method via CodeFromRepo.
8. **Diagram**: Embed `<BuilderPatternDiagram />` after the builder chain section with a contextual sentence.
9. **"The ASGI Entry Point" section**: Show `main.py` with module-level `app = create_app()` via CodeFromRepo. Explain how Uvicorn references this.
10. **"What the Agent Never Implements" section** (AGENT-02): Bullet list of what the template handles (application wiring, middleware registration, service initialization, lifespan management, error handler setup).

Target: 250-350 lines of MDX. Use CodeFromRepo for ALL template source excerpts (not fenced code blocks). Keep code excerpts focused -- show the critical 10-40 lines per snippet, not entire files. Include `startLine`/`endLine` props for accurate source attribution. Use standard fenced code blocks only for shell commands or configuration examples.

CRITICAL: The `code` prop value for CodeFromRepo must be wrapped in `{backtick-string}` syntax. Curly braces in Python f-strings render correctly inside template literals.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -20</automated>
  </verify>
  <done>00-builder-pattern.mdx has full prose covering FastAPIAppBuilder, setup_*() composition, create_app() factory, and main.py entrypoint. Includes 5+ CodeFromRepo snippets, BuilderPatternDiagram, agent opener and closer sections. Renders successfully via astro build.</done>
</task>

<task type="auto">
  <name>Task 2: Author Middleware page (PAGE-03)</name>
  <files>src/data/guides/fastapi-production/pages/01-middleware.mdx</files>
  <action>
Create the Middleware chapter MDX file. Read these fastapi-template source files first:
- `/Users/patrykattc/work/git/fastapi-template/src/app/app_builder.py` -- find the `setup_middleware()` method for registration order
- `/Users/patrykattc/work/git/fastapi-template/src/app/middleware/request_id.py` -- raw ASGI pattern
- `/Users/patrykattc/work/git/fastapi-template/src/app/middleware/request_logging.py` -- structured logging
- `/Users/patrykattc/work/git/fastapi-template/src/app/middleware/security_headers.py` -- header injection
- `/Users/patrykattc/work/git/fastapi-template/src/app/middleware/rate_limit.py` -- fixed-window algorithm
- `/Users/patrykattc/work/git/fastapi-template/src/app/middleware/body_size.py` -- body size limiting
- `/Users/patrykattc/work/git/fastapi-template/src/app/middleware/timeout.py` -- asyncio timeout

Page structure:
1. **Frontmatter**: title="Middleware Stack", description about 6 raw ASGI middlewares with deliberate ordering, order=1, slug="middleware"
2. **Imports**: CodeFromRepo and MiddlewareStackDiagram
3. **"What Your Agent Inherits" section** (AGENT-01): The agent's endpoints run inside a middleware pipeline that has already handled request IDs, rate limiting, body size limits, timeouts, security headers, CORS, and trusted host validation.
4. **"The Registration Order" section**: Explain LIFO ordering (last registered = outermost). Show `setup_middleware()` from app_builder.py via CodeFromRepo. Explain why order matters: security headers wrap everything, request ID is the innermost custom middleware.
5. **"Why Raw ASGI, Not BaseHTTPMiddleware" section**: Key design decision. Explain the 3 problems with BaseHTTPMiddleware: (a) reads entire request body into memory, (b) wraps exceptions in a way that hides the original, (c) prevents streaming responses. Show the raw ASGI `__call__` pattern from request_id.py via CodeFromRepo as the canonical example.
6. **"The Six Middlewares" section**: For each middleware, show the key excerpt via CodeFromRepo and explain:
   - **Request ID**: Generates UUID, sets scope header, propagates correlation ID
   - **Request Logging**: Structured JSON access log with timing, status, path
   - **Security Headers**: HSTS, CSP, referrer policy, permissions policy injection
   - **Rate Limiting**: Fixed-window algorithm, memory/Redis backends, proxy-aware IP
   - **Body Size Limit**: Streaming body counter, early rejection before full read
   - **Timeout**: asyncio.timeout wrapping, cancellation semantics
7. **Diagram**: Embed `<MiddlewareStackDiagram />` showing the 8-layer request flow.
8. **"What the Agent Never Implements" section** (AGENT-02): Bullet list covering all 6 middlewares plus CORS and TrustedHost from Starlette.

Target: 250-350 lines of MDX. Use CodeFromRepo for all 7+ template code excerpts. Show the `__call__` method signature and key logic for each middleware, not the entire file. Include startLine/endLine for source attribution.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -20</automated>
  </verify>
  <done>01-middleware.mdx has full prose covering all 6 raw ASGI middlewares, registration ordering, why not BaseHTTPMiddleware, and per-middleware explanations. Includes 7+ CodeFromRepo snippets, MiddlewareStackDiagram, agent opener and closer sections. Renders successfully via astro build.</done>
</task>

</tasks>

<verification>
1. `npx astro build` completes without errors -- validates MDX syntax, component imports, and frontmatter schema
2. Both files have correct frontmatter slugs matching guide.json: "builder-pattern" (order 0) and "middleware" (order 1)
3. Both files import CodeFromRepo and their respective diagram component
4. Both files have "What Your Agent Inherits" opener and "What the Agent Never Implements" closer
5. Code snippets use CodeFromRepo (not fenced blocks) for all template source excerpts
</verification>

<success_criteria>
- Builder Pattern page is self-contained: a reader unfamiliar with the template understands factory functions, builder pattern, and the full bootstrap chain
- Middleware page is self-contained: a reader understands all 6 middlewares, their ordering, and why raw ASGI was chosen over BaseHTTPMiddleware
- Both pages render at their respective URLs under /guides/fastapi-production/
- Both pages appear correctly in sidebar navigation
</success_criteria>

<output>
After completion, create `.planning/phases/88-content-authoring/88-01-SUMMARY.md`
</output>
