---
phase: 88-content-authoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/guides/fastapi-production/pages/02-authentication.mdx
  - src/data/guides/fastapi-production/pages/08-security-headers.mdx
  - src/data/guides/fastapi-production/pages/09-rate-limiting.mdx
autonomous: true
requirements: [PAGE-02, PAGE-09, PAGE-10, AGENT-01, AGENT-02]

must_haves:
  truths:
    - "Authentication page explains 3-mode JWT validation (shared secret, static key, JWKS) with key resolution logic"
    - "Authentication page includes JwtAuthFlowDiagram showing the 3 validation paths"
    - "Security Headers page explains HSTS, CSP, permissions policy, and trusted hosts with configuration options"
    - "Rate Limiting page explains fixed-window algorithm, memory/Redis backends, and proxy-aware IP extraction"
    - "All 3 pages open with 'What Your Agent Inherits' and close with 'What the Agent Never Implements'"
    - "All 3 pages include CodeFromRepo snippets with correct filePath references"
  artifacts:
    - path: "src/data/guides/fastapi-production/pages/02-authentication.mdx"
      provides: "Authentication domain page with 3-mode JWT, diagram, and AI agent narrative"
      min_lines: 200
      contains: "slug: \"authentication\""
    - path: "src/data/guides/fastapi-production/pages/08-security-headers.mdx"
      provides: "Security Headers domain page with HSTS/CSP/permissions coverage"
      min_lines: 100
      contains: "slug: \"security-headers\""
    - path: "src/data/guides/fastapi-production/pages/09-rate-limiting.mdx"
      provides: "Rate Limiting domain page with algorithm and backend coverage"
      min_lines: 150
      contains: "slug: \"rate-limiting\""
  key_links:
    - from: "src/data/guides/fastapi-production/pages/02-authentication.mdx"
      to: "src/components/guide/JwtAuthFlowDiagram.astro"
      via: "MDX component import"
      pattern: "import JwtAuthFlowDiagram from"
    - from: "src/data/guides/fastapi-production/pages/02-authentication.mdx"
      to: "src/components/guide/CodeFromRepo.astro"
      via: "MDX component import"
      pattern: "import CodeFromRepo from"
---

<objective>
Author the Authentication, Security Headers, and Rate Limiting domain pages -- the security-focused cluster of the FastAPI Production Guide.

Purpose: These three chapters cover the template's security posture: JWT validation (3 modes), HTTP security headers, and request rate limiting. They share a thematic focus on protecting the application.
Output: Three complete MDX files with full prose, CodeFromRepo snippets, one diagram (Auth), and AI agent narrative framing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-content-authoring/88-RESEARCH.md

Source material (read these files from the fastapi-template):
@/Users/patrykattc/work/git/fastapi-template/src/app/auth/service.py
@/Users/patrykattc/work/git/fastapi-template/src/app/auth/dependencies.py
@/Users/patrykattc/work/git/fastapi-template/src/app/auth/models.py
@/Users/patrykattc/work/git/fastapi-template/src/app/middleware/security_headers.py
@/Users/patrykattc/work/git/fastapi-template/src/app/middleware/rate_limit.py
@/Users/patrykattc/work/git/fastapi-template/src/app/settings.py

Existing patterns:
@src/data/guides/fastapi-production/guide.json
@src/components/guide/CodeFromRepo.astro

<interfaces>
<!-- CodeFromRepo props -->
code: string, lang: string, filePath: string, title?: string, startLine?: number, endLine?: number

<!-- Import paths from MDX -->
import CodeFromRepo from '../../../../components/guide/CodeFromRepo.astro';
import JwtAuthFlowDiagram from '../../../../components/guide/JwtAuthFlowDiagram.astro';

<!-- Frontmatter slugs (must match guide.json exactly) -->
authentication: order=2, slug="authentication"
security-headers: order=8, slug="security-headers"
rate-limiting: order=9, slug="rate-limiting"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Authentication page (PAGE-02)</name>
  <files>src/data/guides/fastapi-production/pages/02-authentication.mdx</files>
  <action>
Create the Authentication chapter MDX file. Read these fastapi-template source files first:
- `/Users/patrykattc/work/git/fastapi-template/src/app/auth/service.py` -- JWTAuthService class (~253 lines)
- `/Users/patrykattc/work/git/fastapi-template/src/app/auth/dependencies.py` -- FastAPI dependency injection for auth
- `/Users/patrykattc/work/git/fastapi-template/src/app/auth/models.py` -- Principal dataclass
- `/Users/patrykattc/work/git/fastapi-template/src/app/settings.py` -- find auth_* fields and _validate_auth_settings()

Page structure:
1. **Frontmatter**: title="Authentication (JWT)", description about 3-mode stateless JWT validation, order=2, slug="authentication"
2. **Imports**: CodeFromRepo and JwtAuthFlowDiagram
3. **"What Your Agent Inherits" section** (AGENT-01): The agent's route handlers receive an authenticated `Principal` object via dependency injection. All token parsing, key resolution, signature verification, and claim validation is already handled.
4. **"Three Validation Modes" section**: Explain the 3 key material sources: shared secret (HMAC), static public key (RSA/EC), and JWKS endpoint (auto-rotating keys). Show the `__init__` of JWTAuthService via CodeFromRepo.
5. **"Key Resolution" section**: Explain `_resolve_key()` decision tree. Show the method via CodeFromRepo. Explain the priority order and when each mode is used.
6. **"JWKS with Graceful Degradation" section**: Show `_fetch_jwks()` with lock + stale cache. Explain cache-aside pattern, TTL refresh, and fallback to stale cache on fetch failure. Use CodeFromRepo.
7. **Diagram**: Embed `<JwtAuthFlowDiagram />` after the validation modes explanation.
8. **"The Principal Model" section**: Show the `Principal` dataclass from models.py via CodeFromRepo. Explain normalized claims as domain model.
9. **"Configuration Validation" section**: Show `_validate_auth_settings()` from settings.py. Explain how the validator prevents misconfigured auth at startup.
10. **"Dependency Injection" section**: Show how routes access the authenticated principal via FastAPI dependencies. Show from dependencies.py via CodeFromRepo.
11. **"What the Agent Never Implements" section** (AGENT-02): Bullet list covering token parsing, key management, JWKS rotation, signature verification, claim validation, principal mapping.

Target: 250-350 lines. Use CodeFromRepo for all 6+ template excerpts.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -20</automated>
  </verify>
  <done>02-authentication.mdx has full prose covering 3-mode JWT validation, JWKS caching, Principal model, and FastAPI dependency injection. Includes 6+ CodeFromRepo snippets, JwtAuthFlowDiagram, agent opener and closer. Renders successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Author Security Headers and Rate Limiting pages (PAGE-09, PAGE-10)</name>
  <files>src/data/guides/fastapi-production/pages/08-security-headers.mdx, src/data/guides/fastapi-production/pages/09-rate-limiting.mdx</files>
  <action>
Create both MDX files. These are shorter, focused chapters that complement the security cluster.

**Read source files first:**
- `/Users/patrykattc/work/git/fastapi-template/src/app/middleware/security_headers.py` (~69 lines)
- `/Users/patrykattc/work/git/fastapi-template/src/app/middleware/rate_limit.py` (~204 lines)
- `/Users/patrykattc/work/git/fastapi-template/src/app/settings.py` -- find security_* and rate_limit_* fields

**Security Headers page (08-security-headers.mdx):**
1. **Frontmatter**: title="Security Headers", description about HSTS/CSP/permissions policy, order=8, slug="security-headers"
2. **Import**: CodeFromRepo only (no diagram for this page)
3. **"What Your Agent Inherits"** (AGENT-01): Every response from the agent's endpoints automatically includes HSTS, CSP, referrer policy, and permissions policy headers. The agent never sets security headers manually.
4. **"The Headers" section**: For each header, explain what it does and why it matters: HSTS (forces HTTPS), Content-Security-Policy (prevents XSS), X-Content-Type-Options (prevents MIME sniffing), Referrer-Policy, Permissions-Policy. Show the header injection logic via CodeFromRepo.
5. **"Automatic CSP Relaxation for API Docs" section**: Show `_resolve_csp_for_docs()` from settings.py. Explain how CSP is automatically relaxed when Swagger/ReDoc is enabled so interactive docs work without manual overrides.
6. **"Configuration" section**: Show the security_* settings fields. Explain all are configurable with safe defaults.
7. **"What the Agent Never Implements"** (AGENT-02): Bullet list.
Target: 100-150 lines.

**Rate Limiting page (09-rate-limiting.mdx):**
1. **Frontmatter**: title="Rate Limiting", description about fixed-window algorithm with pluggable backends, order=9, slug="rate-limiting"
2. **Import**: CodeFromRepo only (no diagram)
3. **"What Your Agent Inherits"** (AGENT-01): The agent's endpoints are rate-limited before the request reaches the route handler. Limits are enforced per IP (or per authorization key) using a fixed-window algorithm with configurable windows and thresholds.
4. **"Fixed-Window Algorithm" section**: Explain the algorithm: divide time into windows, count requests per window, reject when threshold exceeded. Show the core `__call__` method from rate_limit.py via CodeFromRepo.
5. **"Memory and Redis Backends" section**: Show `MemoryRateLimitStore` and `RedisRateLimitStore` classes. Explain the store abstraction and when to use each (development/single-instance vs production/multi-instance).
6. **"Client Identification" section**: Show `_build_rate_limit_key()`. Explain proxy trust (X-Forwarded-For), authorization header fallback, and path-exempt configuration.
7. **"Response Headers" section**: Explain X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset, Retry-After headers.
8. **"Configuration" section**: Show rate_limit_* settings fields from settings.py via CodeFromRepo.
9. **"What the Agent Never Implements"** (AGENT-02): Bullet list.
Target: 150-200 lines.

CRITICAL: Use the exact slugs "security-headers" and "rate-limiting" in frontmatter. Use CodeFromRepo for all template source excerpts.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -20</automated>
  </verify>
  <done>08-security-headers.mdx covers HSTS, CSP, permissions policy, and auto-relaxation for docs. 09-rate-limiting.mdx covers fixed-window algorithm, memory/Redis stores, client identification, and response headers. Both include CodeFromRepo snippets and agent narrative sections. Both render successfully.</done>
</task>

</tasks>

<verification>
1. `npx astro build` completes without errors for all 3 new MDX files
2. Frontmatter slugs match guide.json exactly: "authentication" (order 2), "security-headers" (order 8), "rate-limiting" (order 9)
3. Authentication page imports JwtAuthFlowDiagram; other two import CodeFromRepo only
4. All 3 pages have agent opener and closer sections
5. All code snippets use CodeFromRepo with correct filePath values
</verification>

<success_criteria>
- Authentication page is self-contained: reader understands 3-mode JWT, JWKS caching, Principal model, and DI integration
- Security Headers page is self-contained: reader understands each header's purpose and configuration
- Rate Limiting page is self-contained: reader understands fixed-window algorithm, backend selection, and client identification
- All 3 pages render at their respective URLs and appear in sidebar navigation
</success_criteria>

<output>
After completion, create `.planning/phases/88-content-authoring/88-02-SUMMARY.md`
</output>
