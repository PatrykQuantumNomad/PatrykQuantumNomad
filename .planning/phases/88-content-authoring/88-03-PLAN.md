---
phase: 88-content-authoring
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/guides/fastapi-production/pages/03-observability.mdx
  - src/data/guides/fastapi-production/pages/04-database.mdx
  - src/data/guides/fastapi-production/pages/05-docker.mdx
autonomous: true
requirements: [PAGE-04, PAGE-05, PAGE-06, AGENT-01, AGENT-02]

must_haves:
  truths:
    - "Observability page explains OpenTelemetry tracing, Prometheus metrics, and structured JSON logging"
    - "Database page explains async SQLAlchemy, Alembic migrations, and multi-backend URL derivation"
    - "Docker page explains multi-stage builds, tini, unprivileged user, digest-pinned images, and includes DeploymentTopology"
    - "All 3 pages open with 'What Your Agent Inherits' and close with 'What the Agent Never Implements'"
    - "All 3 pages include CodeFromRepo snippets from the relevant fastapi-template source files"
  artifacts:
    - path: "src/data/guides/fastapi-production/pages/03-observability.mdx"
      provides: "Observability domain page with tracing, metrics, and logging coverage"
      min_lines: 150
      contains: "slug: \"observability\""
    - path: "src/data/guides/fastapi-production/pages/04-database.mdx"
      provides: "Database domain page with async SQLAlchemy and Alembic coverage"
      min_lines: 150
      contains: "slug: \"database\""
    - path: "src/data/guides/fastapi-production/pages/05-docker.mdx"
      provides: "Docker domain page with multi-stage build and deployment topology"
      min_lines: 200
      contains: "slug: \"docker\""
  key_links:
    - from: "src/data/guides/fastapi-production/pages/05-docker.mdx"
      to: "src/components/guide/DeploymentTopology.tsx"
      via: "MDX component import with client:visible"
      pattern: "DeploymentTopology client:visible"
    - from: "src/data/guides/fastapi-production/pages/03-observability.mdx"
      to: "src/components/guide/CodeFromRepo.astro"
      via: "MDX component import"
      pattern: "import CodeFromRepo from"
---

<objective>
Author the Observability, Database, and Docker domain pages -- the infrastructure cluster of the FastAPI Production Guide.

Purpose: These chapters cover the template's operational infrastructure: how it observes itself (tracing, metrics, logging), how it manages data (async SQLAlchemy, migrations), and how it runs in containers (multi-stage Docker, deployment topology).
Output: Three complete MDX files with full prose, CodeFromRepo snippets, DeploymentTopology diagram (Docker page), and AI agent narrative framing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-content-authoring/88-RESEARCH.md

Source material (read these files from the fastapi-template):
@/Users/patrykattc/work/git/fastapi-template/src/app/observability/tracing.py
@/Users/patrykattc/work/git/fastapi-template/src/app/log_config/request_context.py
@/Users/patrykattc/work/git/fastapi-template/src/app/logging_setup.py
@/Users/patrykattc/work/git/fastapi-template/src/app/app_builder.py (setup_tracing, setup_metrics, setup_logging methods)
@/Users/patrykattc/work/git/fastapi-template/src/app/db/engine.py
@/Users/patrykattc/work/git/fastapi-template/src/app/db/session.py
@/Users/patrykattc/work/git/fastapi-template/src/app/db/base.py
@/Users/patrykattc/work/git/fastapi-template/src/app/settings.py (database and alembic settings)
@/Users/patrykattc/work/git/fastapi-template/Dockerfile
@/Users/patrykattc/work/git/fastapi-template/docker-compose.yml
@/Users/patrykattc/work/git/fastapi-template/ops/docker-entrypoint.sh

Existing patterns:
@src/data/guides/fastapi-production/guide.json
@src/components/guide/CodeFromRepo.astro

<interfaces>
<!-- Import paths from MDX -->
import CodeFromRepo from '../../../../components/guide/CodeFromRepo.astro';
import DeploymentTopology from '../../../../components/guide/DeploymentTopology';

<!-- DeploymentTopology is React -- MUST use client:visible -->
<DeploymentTopology client:visible />

<!-- Frontmatter slugs (must match guide.json) -->
observability: order=3, slug="observability"
database: order=4, slug="database"
docker: order=5, slug="docker"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Observability page (PAGE-04)</name>
  <files>src/data/guides/fastapi-production/pages/03-observability.mdx</files>
  <action>
Create the Observability chapter MDX file. Read these fastapi-template source files first:
- `/Users/patrykattc/work/git/fastapi-template/src/app/observability/tracing.py` -- configure_tracing(), instrument_fastapi_app(), instrument_database_engine()
- `/Users/patrykattc/work/git/fastapi-template/src/app/app_builder.py` -- find setup_tracing(), setup_metrics(), setup_logging() methods
- `/Users/patrykattc/work/git/fastapi-template/src/app/log_config/request_context.py` -- context variables for request_id/correlation_id
- `/Users/patrykattc/work/git/fastapi-template/src/app/logging_setup.py` -- JSON vs text format configuration

Page structure:
1. **Frontmatter**: title="Observability", description about OpenTelemetry tracing, Prometheus metrics, and structured logging, order=3, slug="observability"
2. **Import**: CodeFromRepo only (no diagram for this page)
3. **"What Your Agent Inherits"** (AGENT-01): Every request the agent handles is automatically traced with OpenTelemetry spans, counted by Prometheus metrics, and logged in structured JSON with request ID correlation. The agent gets observability for free.
4. **"Distributed Tracing" section**: Explain TracerProvider, Resource, BatchSpanProcessor setup. Show `configure_tracing()` via CodeFromRepo. Explain auto-instrumentation of FastAPI and SQLAlchemy. Show `instrument_fastapi_app()` via CodeFromRepo with path exclusions (health, metrics).
5. **"Prometheus Metrics" section**: Explain starlette-exporter integration. Show `setup_metrics()` from app_builder.py via CodeFromRepo. Explain excluded paths to avoid metric noise.
6. **"Structured Logging" section**: Explain JSON vs text format toggle, request context injection (request_id, correlation_id). Show the request context module via CodeFromRepo. Explain how context variables propagate through async code.
7. **"The Three Pillars Together" section**: Brief synthesis of how traces, metrics, and logs correlate via request ID.
8. **"What the Agent Never Implements"** (AGENT-02): Bullet list covering span creation, metric exposition, log formatting, request ID propagation, context variable management.

Target: 200-280 lines. Use CodeFromRepo for 5+ template excerpts.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -20</automated>
  </verify>
  <done>03-observability.mdx covers OpenTelemetry tracing, Prometheus metrics, structured logging, and request ID correlation. Includes 5+ CodeFromRepo snippets and agent narrative sections. Renders successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Author Database and Docker pages (PAGE-05, PAGE-06)</name>
  <files>src/data/guides/fastapi-production/pages/04-database.mdx, src/data/guides/fastapi-production/pages/05-docker.mdx</files>
  <action>
Create both MDX files.

**Read source files first:**
- `/Users/patrykattc/work/git/fastapi-template/src/app/db/engine.py` -- create_database_engine()
- `/Users/patrykattc/work/git/fastapi-template/src/app/db/session.py` -- session factory and session_scope()
- `/Users/patrykattc/work/git/fastapi-template/src/app/db/base.py` -- declarative base
- `/Users/patrykattc/work/git/fastapi-template/src/app/settings.py` -- _derive_database_url(), _derive_alembic_database_url()
- `/Users/patrykattc/work/git/fastapi-template/Dockerfile` (full file, ~89 lines)
- `/Users/patrykattc/work/git/fastapi-template/docker-compose.yml`
- `/Users/patrykattc/work/git/fastapi-template/ops/docker-entrypoint.sh`

**Database page (04-database.mdx):**
1. **Frontmatter**: title="Database", description about async SQLAlchemy and Alembic, order=4, slug="database"
2. **Import**: CodeFromRepo only
3. **"What Your Agent Inherits"** (AGENT-01): The agent writes SQLAlchemy models and queries against a pre-configured async engine. Session lifecycle, connection pooling, and migration infrastructure are already wired.
4. **"Engine Creation" section**: Show `create_database_engine()` via CodeFromRepo. Explain SQLite-first default for development, Postgres pool configuration for production.
5. **"Multi-Backend URL Derivation" section**: Show `_derive_database_url()` from settings.py. Explain how one settings object handles SQLite, Postgres, and custom URLs via a priority chain.
6. **"Async Sessions" section**: Show session factory and `session_scope()` via CodeFromRepo. Explain commit/rollback semantics.
7. **"Alembic Integration" section**: Explain async-to-sync URL mapping for Alembic (which does not support async drivers natively). Show `_derive_alembic_database_url()` via CodeFromRepo.
8. **"What the Agent Never Implements"** (AGENT-02): Bullet list.
Target: 150-220 lines.

**Docker page (05-docker.mdx):**
1. **Frontmatter**: title="Docker & Containerization", description about multi-stage builds and production container hardening, order=5, slug="docker"
2. **Imports**: CodeFromRepo AND DeploymentTopology (React component -- import WITHOUT .tsx extension)
3. **"What Your Agent Inherits"** (AGENT-01): The agent's code runs in a hardened container with a non-root user, PID 1 signal forwarding via tini, digest-pinned base images, and optimized layer caching. The Dockerfile is production-ready out of the box.
4. **"Multi-Stage Build" section**: Show the full Dockerfile via CodeFromRepo (lang="dockerfile", filePath="Dockerfile"). Explain the builder stage (dependencies) vs runtime stage (application) separation. Explain why: smaller images, no build tools in production.
5. **"Container Hardening" section**: Explain each security measure:
   - Digest-pinned base images (reproducible builds)
   - tini as PID 1 (signal forwarding, zombie reaping)
   - Unprivileged user with stable UID/GID
   - HEALTHCHECK directive for container-level liveness
   Show relevant Dockerfile sections via CodeFromRepo with startLine/endLine.
6. **"Deployment Topology" section**: Embed `<DeploymentTopology client:visible />`. CRITICAL: Must use `client:visible` directive. Explain the four services (app, Postgres, Redis, reverse proxy) and their connections.
7. **"Docker Compose" section**: Show docker-compose.yml via CodeFromRepo (lang="yaml", filePath="docker-compose.yml") for local development stack.
8. **"What the Agent Never Implements"** (AGENT-02): Bullet list.
Target: 200-300 lines.

CRITICAL for Docker page: Import DeploymentTopology WITHOUT .tsx extension: `import DeploymentTopology from '../../../../components/guide/DeploymentTopology';` and use `<DeploymentTopology client:visible />`.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx astro build 2>&1 | tail -20</automated>
  </verify>
  <done>04-database.mdx covers async SQLAlchemy, multi-backend URL derivation, session lifecycle, and Alembic integration. 05-docker.mdx covers multi-stage builds, container hardening, DeploymentTopology diagram, and Docker Compose. All include CodeFromRepo snippets and agent narrative sections. Both render successfully.</done>
</task>

</tasks>

<verification>
1. `npx astro build` completes without errors for all 3 new MDX files
2. Frontmatter slugs match guide.json: "observability" (order 3), "database" (order 4), "docker" (order 5)
3. Docker page imports DeploymentTopology with client:visible directive
4. All 3 pages have agent opener and closer sections
5. All code snippets use CodeFromRepo with correct filePath values
</verification>

<success_criteria>
- Observability page is self-contained: reader understands the three pillars (traces, metrics, logs) and how they correlate
- Database page is self-contained: reader understands async engine, sessions, multi-backend support, and Alembic integration
- Docker page is self-contained: reader understands the Dockerfile, container hardening, and deployment topology
- DeploymentTopology interactive diagram renders when scrolled into view on the Docker page
- All 3 pages render at their respective URLs and appear in sidebar navigation
</success_criteria>

<output>
After completion, create `.planning/phases/88-content-authoring/88-03-SUMMARY.md`
</output>
