---
phase: 125-blog-pagination-and-on-page-seo-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/blog/dark-code.mdx
  - src/pages/beauty-index/[slug].astro
  - src/pages/tools/dockerfile-analyzer/index.astro
autonomous: true
requirements:
  - OPSEO-01
  - OPSEO-02
  - OPSEO-03
  - OPSEO-04
phase_num: 125

must_haves:
  truths:
    - "dist/blog/dark-code/index.html <title> tag renders exactly 'Dark Code: The Silent Rot AI Accelerated — Patryk Golabek' (57 chars, between 55-60 inclusive)"
    - "dist/blog/dark-code/index.html <meta name=\"description\"> content is ≤ 160 characters and front-loads 'AI coding assistants'"
    - "All 26 dist/beauty-index/{slug}/index.html pages have <meta name=\"description\"> between 140 and 160 characters inclusive, ending on a word/sentence boundary or ellipsis (no mid-word truncation)"
    - "dist/tools/dockerfile-analyzer/index.html <meta name=\"description\"> content is ≤ 160 characters"
  artifacts:
    - path: "src/data/blog/dark-code.mdx"
      provides: "Updated frontmatter: title (40 chars) + description (152 chars)"
      contains: "Dark Code: The Silent Rot AI Accelerated"
    - path: "src/pages/beauty-index/[slug].astro"
      provides: "Clause-boundary-aware truncator targeting 140-157 chars"
      contains: "truncateDescription"
    - path: "src/pages/tools/dockerfile-analyzer/index.astro"
      provides: "Dockerfile Analyzer page with ≤160-char meta description"
      contains: "46 rules for security"
  key_links:
    - from: "src/data/blog/dark-code.mdx (frontmatter title ≤48 chars)"
      to: "src/pages/blog/[slug].astro (title+suffix truncation at line 224-227)"
      via: "title.length + suffix.length ≤ 65 bypasses truncation branch"
      pattern: "Dark Code: The Silent Rot"
---

<objective>
Land the four on-page SEO content edits in Phase 125:
- OPSEO-01: dark-code title shortened to bypass [slug].astro's 65-char
  truncation guard so the rendered <title> lands in the 55-60 char window.
- OPSEO-02: dark-code description trimmed to ≤ 160 chars with keywords
  front-loaded.
- OPSEO-03: Beauty Index single-language meta description generator
  replaced with a clause-boundary-aware truncator and guaranteed 140-160
  output.
- OPSEO-04: Dockerfile Analyzer description trimmed to ≤ 160 chars.

Purpose: Fix the four on-page SEO content violations from the 2026-04-15
SEO audit. Independent of Plan 01's technical-SEO work (no file overlap →
parallel execution in Wave 1).

Output:
- `src/data/blog/dark-code.mdx` frontmatter title + description updated.
- `src/pages/beauty-index/[slug].astro` `truncateDescription` helper replaces
  regex truncation at lines 80-83.
- `src/pages/tools/dockerfile-analyzer/index.astro` line 10 description
  rewritten.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-blog-pagination-and-on-page-seo-fixes/125-RESEARCH.md

@src/data/blog/dark-code.mdx
@src/pages/blog/[slug].astro
@src/pages/beauty-index/[slug].astro
@src/pages/tools/dockerfile-analyzer/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shorten dark-code title + description (OPSEO-01, OPSEO-02)</name>
  <files>
    src/data/blog/dark-code.mdx
  </files>
  <action>
**CRITICAL CONTEXT:** `src/pages/blog/[slug].astro` lines 224-227 contains:
```astro
const suffix = ' — Patryk Golabek';
const titleTag = (title.length + suffix.length > 65)
  ? `${title.split(':')[0]}${suffix}`
  : `${title}${suffix}`;
```
The current frontmatter title is 64 chars. Adding `" — Patryk Golabek"`
(17 chars, includes the leading space) produces 81 total, which exceeds 65,
triggering the split-at-colon fallback that renders just `"Dark Code — Patryk
Golabek"` (26 chars). The fix is to shorten the frontmatter title to ≤ 48
chars so `title + suffix` ≤ 65 and the truncation branch never fires. Do NOT
touch the truncation logic in [slug].astro — that's a shared fallback used by
every blog post and changing it has unknown blast radius.

**OPSEO-01 edit — `src/data/blog/dark-code.mdx` line 2:**

Current:
```
title: "Dark Code: The Silent Rot AI Accelerated and No One Is Measuring"
```

Replace with (40 chars, yielding final rendered `<title>` = 40 + 17 = 57 chars,
within the 55-60 target window):
```
title: "Dark Code: The Silent Rot AI Accelerated"
```

Rationale: picked from RESEARCH.md § OPSEO-01 candidate #3 — preserves
the original editorial voice while landing safely in range. Research lists
five candidates; this one hits 57 chars rendered (40 title + 17 suffix).
Any of the five candidates would satisfy OPSEO-01, but this one
is selected for editorial continuity.

**OPSEO-02 edit — `src/data/blog/dark-code.mdx` line 3:**

Current (219 chars):
```
description: "AI coding assistants have accelerated a pre-existing problem: codebases filling with code no one understands, no one owns, and no one can safely change. A framework for measuring how much of your codebase has gone dark."
```

Replace with (152 chars — RESEARCH.md § OPSEO-02 candidate #1; front-loads
"AI coding assistants" keyword + "dark code" target phrase):
```
description: "AI coding assistants accelerate dark code: codebases filled with code no one understands, owns, or can safely change. A framework for measuring the rot."
```

Do NOT change `publishedDate`, `tags`, `coverImage`, or `draft`. Leave
everything else in the frontmatter unchanged.
  </action>
  <verify>
Run `npm run build`. Then:

1. `grep -c '<title>Dark Code: The Silent Rot AI Accelerated — Patryk Golabek</title>' dist/blog/dark-code/index.html` → 1.
2. Compute title length: the string inside `<title>...</title>` should be 57 chars. Verify with:
   ```
   node -e 'const t="Dark Code: The Silent Rot AI Accelerated — Patryk Golabek"; console.log(t.length)'
   ```
   (outputs 57; within 55-60 range — "—" is a single UTF-16 code unit.)
3. Verify description: `grep 'name="description"' dist/blog/dark-code/index.html` → the content attribute starts with "AI coding assistants accelerate dark code" and the content value is ≤ 160 chars.
   ```
   node -e 'const d="AI coding assistants accelerate dark code: codebases filled with code no one understands, owns, or can safely change. A framework for measuring the rot."; console.log(d.length)'
   ```
   (outputs 152.)
  </verify>
  <done>
`dist/blog/dark-code/index.html` renders `<title>Dark Code: The Silent Rot AI
Accelerated — Patryk Golabek</title>` (57 chars) and
`<meta name="description" content="AI coding assistants accelerate dark code:
codebases filled with code no one understands, owns, or can safely change. A
framework for measuring the rot.">` (152 chars).
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace Beauty Index description truncator (OPSEO-03)</name>
  <files>
    src/pages/beauty-index/[slug].astro
  </files>
  <action>
**OPSEO-03 edit — `src/pages/beauty-index/[slug].astro` lines 80-83:**

Current:
```astro
const fullDescription = `${language.name} scores ${total}/60 in the Beauty Index, ranking #${rank} among 26 programming languages (${tierLabel} tier). ${language.characterSketch}`;
const metaDescription = fullDescription.length <= 155
  ? fullDescription
  : fullDescription.slice(0, 155).replace(/\s+\S*$/, '') + '…';
```

Replace with a clause-boundary-aware truncator that targets 140-157 chars
and prefers sentence/clause boundaries, falling back to word boundaries
(never mid-word). Taken from RESEARCH.md § OPSEO-03:

```astro
const fullDescription = `${language.name} scores ${total}/60 in the Beauty Index, ranking #${rank} among 26 programming languages (${tierLabel} tier). ${language.characterSketch}`;

/**
 * Bounded truncator: prefers sentence/clause boundary in the [targetMin,
 * targetMax] window; falls back to word boundary; never truncates mid-word.
 * Guarantees result.length ≤ targetMax + 2 (for trailing ' …' or '…').
 * Always returns the original string unchanged if it already fits.
 */
function truncateDescription(full: string, targetMin = 140, targetMax = 157): string {
  if (full.length <= targetMax) return full;
  const slice = full.slice(0, targetMax);
  // Prefer the last clause/sentence boundary inside the target window.
  const boundary = Math.max(
    slice.lastIndexOf('. '),
    slice.lastIndexOf('; '),
    slice.lastIndexOf(', '),
  );
  if (boundary >= targetMin) return slice.slice(0, boundary + 1) + ' …';
  // Fall back to whitespace boundary.
  const wordEnd = slice.lastIndexOf(' ');
  return (wordEnd >= targetMin ? slice.slice(0, wordEnd) : slice.replace(/\s+\S*$/, '')) + '…';
}
const metaDescription = truncateDescription(fullDescription);
```

Do not change the `languageSchema` block below line 85 — it already uses
`fullDescription` (unchanged).
  </action>
  <verify>
Run `npm run build`. Then for all 26 Beauty Index language pages:

```bash
for dir in dist/beauty-index/*/; do
  slug=$(basename "$dir")
  # skip vs/ and any non-language subpaths
  [ "$slug" = "vs" ] && continue
  desc=$(grep -o '<meta name="description" content="[^"]*"' "$dir/index.html" | sed 's/.*content="//;s/"$//')
  len=${#desc}
  echo "$slug: $len chars"
done
```

Each length should fall in `[140, 160]` inclusive (the truncator can produce
`targetMax + ' …'.length` = 157 + 2 = 159 at worst case, still ≤ 160).

Spot-check a known-long case (e.g., python, javascript, rust): description
ends on either `…`, ` …`, a period, or a complete word followed by ellipsis
— never a broken word like `"Python's…"` mid-apostrophe.

Short cases (languages whose full description is already ≤ 157) are
returned verbatim — those may be below 140 by design. Verify by enumerating:
any page whose `fullDescription.length <= 157` returns the full string
unchanged, so the 140 lower bound applies ONLY to truncated entries.

NOTE: the success criterion in the phase goal is "140-160 chars without
mid-word truncation" — for descriptions short enough to not require
truncation, the full ≤ 160 string IS the description. The truncator's job
is to guarantee the output IS 140-160 WHEN truncation occurs.
  </verify>
  <done>
For every Beauty Index language page that requires truncation
(`fullDescription.length > 157`), the rendered meta description falls in
[140, 160] chars and ends at a clause/sentence boundary or after a complete
word + ellipsis. No output contains a truncated apostrophe or partial word.
  </done>
</task>

<task type="auto">
  <name>Task 3: Trim Dockerfile Analyzer description (OPSEO-04)</name>
  <files>
    src/pages/tools/dockerfile-analyzer/index.astro
  </files>
  <action>
**OPSEO-04 edit — `src/pages/tools/dockerfile-analyzer/index.astro` line 10:**

Current (196 chars):
```astro
  description="Free Dockerfile linter and best-practice analyzer by a Kubernetes architect. Dozens of rules for security, efficiency, and maintainability. 100% browser-based — your code never leaves your device."
```

Replace with (157 chars — RESEARCH.md § OPSEO-04 candidate #1; preserves the
"Kubernetes architect" credibility signal and specifies the concrete rule
count of 46 from the authoritative tool source in `src/pages/blog/[slug].astro:139`):
```astro
  description="Free browser-based Dockerfile linter by a Kubernetes architect. 46 rules for security, efficiency, and maintainability — your code never leaves your device."
```

Do NOT change the `title`, `ogImage`, or `ogImageAlt` props. Do NOT touch
anything outside line 10.
  </action>
  <verify>
Run `npm run build`. Then:

1. `grep 'name="description"' dist/tools/dockerfile-analyzer/index.html` — the content attribute contains "Free browser-based Dockerfile linter" and "46 rules".
2. Extract and count the description length:
   ```bash
   grep -o '<meta name="description" content="[^"]*"' dist/tools/dockerfile-analyzer/index.html | sed 's/.*content="//;s/"$//' | awk '{ print length }'
   ```
   Should print `157`.
3. Verify ≤ 160:
   ```
   node -e 'const d="Free browser-based Dockerfile linter by a Kubernetes architect. 46 rules for security, efficiency, and maintainability — your code never leaves your device."; console.log(d.length)'
   ```
   (outputs 157.)
  </verify>
  <done>
`dist/tools/dockerfile-analyzer/index.html` renders `<meta
name="description">` with length 157 (≤ 160) and the new copy.
  </done>
</task>

</tasks>

<verification>
**Plan-level checks after all three tasks complete:**

1. `npm run build` runs clean end-to-end (all 4 existing verifiers still pass).

2. `dist/blog/dark-code/index.html` title is 55-60 chars; description ≤ 160.

3. All 26 `dist/beauty-index/{slug}/index.html` pages have descriptions in [140,160].

4. `dist/tools/dockerfile-analyzer/index.html` description ≤ 160.

5. The OG / Twitter meta tags for each page automatically inherit the new
   values (SEOHead.astro reuses `description` for og:description and
   twitter:description — see lines 41, 54).
</verification>

<success_criteria>
- OPSEO-01: dark-code rendered `<title>` length in [55, 60].
- OPSEO-02: dark-code meta description length ≤ 160, front-loads "AI coding
  assistants" keyword.
- OPSEO-03: all 26 Beauty Index language pages have meta descriptions in
  [140, 160] with no mid-word truncation (word or clause boundary only).
- OPSEO-04: dockerfile-analyzer meta description length ≤ 160.
- `npm run build` green.
</success_criteria>

<output>
After completion, create `.planning/phases/125-blog-pagination-and-on-page-seo-fixes/125-02-SUMMARY.md`
</output>
