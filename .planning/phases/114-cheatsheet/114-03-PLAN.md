---
phase: 114-cheatsheet
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified: []
autonomous: false
must_haves:
  truths:
    - "Cheatsheet page renders correctly at /guides/claude-code/cheatsheet/"
    - "Both SVGs display inline with new commands visible"
    - "Download buttons trigger file download"
    - "Landing page Resources section links to cheatsheet"
    - "OG image renders correctly"
  artifacts: []
  key_links: []
---

<objective>
Build the site and verify all Phase 114 deliverables visually and functionally.

Purpose: Confirm all requirements (CS-01 through CS-06) are met before marking the phase complete.
Output: Verified working cheatsheet page with all features
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-cheatsheet/114-01-SUMMARY.md
@.planning/phases/114-cheatsheet/114-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build site and verify outputs</name>
  <files></files>
  <action>
Run `npm run build` and verify:

1. Build completes without errors or warnings
2. Check build output includes:
   - `guides/claude-code/cheatsheet/index.html` (the cheatsheet page)
   - `open-graph/guides/claude-code/cheatsheet.png` (OG image)
3. Inspect the generated `cheatsheet/index.html` for:
   - `<script type="application/ld+json">` tag containing "TechArticle"
   - `<script type="application/ld+json">` tag containing "BreadcrumbList"
   - `<meta property="og:image"` pointing to the cheatsheet OG image
   - Two `<img>` tags pointing to the SVG files
   - Two `<a>` tags with `download` attribute for SVG downloads
4. Inspect `guides/claude-code/index.html` for the Resources section containing a link to `/guides/claude-code/cheatsheet/`
5. Verify the OG image PNG file was generated (check file size > 0)

If the build fails, diagnose and fix the issue. Common issues:
- Import path errors in cheatsheet.png.ts (relative path depth)
- Missing closing tags in cheatsheet.astro
- TypeScript type errors in Layout or JsonLd props
  </action>
  <verify>
`npm run build` exits with code 0.
`ls dist/guides/claude-code/cheatsheet/index.html` exists.
`ls dist/open-graph/guides/claude-code/cheatsheet.png` exists.
  </verify>
  <done>Site builds cleanly. Cheatsheet page and OG image are in the build output. HTML contains all required structured data, meta tags, SVG images, and download links.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete cheatsheet system: both SVGs updated with /effort, /remote-control, and /loop commands; dedicated page at /guides/claude-code/cheatsheet/ with inline rendering and download buttons; OG image, JSON-LD structured data; Resources section on guide landing page.
  </what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Visit http://localhost:4321/guides/claude-code/cheatsheet/
   - Verify both SVGs display inline (dark interactive version first, light print version below)
   - Find /effort in the MANAGING SESSION section of the dark SVG
   - Find /remote-control in the STATUS & ADMIN section
   - Find /loop in the EXTENDING section
   - Click both download buttons — each should trigger a file download (not open SVG in browser)
3. Visit http://localhost:4321/guides/claude-code/
   - Scroll past the chapter grid
   - Verify a "Resources" section appears with a "Cheatsheet" card
   - Click the card — should navigate to the cheatsheet page
4. View page source of /guides/claude-code/cheatsheet/
   - Look for `application/ld+json` script tags (TechArticle + BreadcrumbList)
   - Look for `og:image` meta tag
5. Open http://localhost:4321/open-graph/guides/claude-code/cheatsheet.png
   - Should render a branded OG image with "Claude Code Cheatsheet" title
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- CS-01: Both SVGs updated with new commands (verified by SVG content + visual check)
- CS-02: Dedicated page at /guides/claude-code/cheatsheet/ with inline rendering
- CS-03: Download buttons serve both SVG files
- CS-04: OG image at /open-graph/guides/claude-code/cheatsheet.png
- CS-05: JSON-LD structured data (TechArticle + BreadcrumbList)
- CS-06: Cheatsheet linked from guide landing page Resources section
</verification>

<success_criteria>
All six requirements (CS-01 through CS-06) verified. Build passes. Visual inspection confirms no layout issues.
</success_criteria>

<output>
After completion, create `.planning/phases/114-cheatsheet/114-03-SUMMARY.md`
</output>
