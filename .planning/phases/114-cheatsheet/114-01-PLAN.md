---
phase: 114-cheatsheet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/images/cheatsheet/claude-code-cheatsheet.svg
  - public/images/cheatsheet/claude-code-cheatsheet-print.svg
requirements: [CS-01]
autonomous: true
must_haves:
  truths:
    - "Both SVG cheatsheets include /effort command entry"
    - "Both SVG cheatsheets include /remote-control command entry"
    - "Both SVG cheatsheets include /loop command entry"
    - "COMMAND COST box in both SVGs lists /effort under MODIFIES"
    - "Interactive (dark) and print (light) SVGs have identical command content with different color palettes"
  artifacts:
    - path: "public/images/cheatsheet/claude-code-cheatsheet.svg"
      provides: "Interactive dark theme cheatsheet with updated commands"
      contains: "/effort"
    - path: "public/images/cheatsheet/claude-code-cheatsheet-print.svg"
      provides: "Print light theme cheatsheet with updated commands"
      contains: "/effort"
  key_links:
    - from: "claude-code-cheatsheet.svg"
      to: "claude-code-cheatsheet-print.svg"
      via: "identical content, different colors"
      pattern: "/effort|/remote-control|/loop"
---

<objective>
Add three new commands (/effort, /remote-control, /loop) to both SVG cheatsheet files and update the COMMAND COST reference box.

Purpose: Both cheatsheets must reflect all interactive commands documented in the finalized guide chapters (Phases 111-113). Three new slash commands are missing from the current SVGs.
Output: Updated claude-code-cheatsheet.svg and claude-code-cheatsheet-print.svg
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-cheatsheet/114-RESEARCH.md
@public/images/cheatsheet/claude-code-cheatsheet.svg
@public/images/cheatsheet/claude-code-cheatsheet-print.svg
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add three new commands to both SVG cheatsheet files</name>
  <files>public/images/cheatsheet/claude-code-cheatsheet.svg, public/images/cheatsheet/claude-code-cheatsheet-print.svg</files>
  <action>
Edit BOTH SVG files to add these three new commands. Always edit both files in the same pass — they share identical content structure but use different color palettes.

**1. Add `/effort` to the MANAGING SESSION section (column 2, x=440)**

In the dark theme SVG, the MANAGING SESSION section currently ends with `/export` at translate(440, 276). Add `/effort` after `/export`:

Dark theme (claude-code-cheatsheet.svg):
- Insert a new keycap group at translate(440, 306) following the exact same pattern as `/export` above it:
  ```xml
  <g transform="translate(440, 306)">
    <rect x="0" y="0" width="56" height="24" rx="5" fill="url(#keycap)" stroke="#333348" stroke-width="0.4" filter="url(#keyShadow)"/>
    <text x="28" y="16" font-family="'JetBrains Mono', monospace" font-size="11" fill="#c8a84e" font-weight="600" text-anchor="middle" style="white-space: pre;">/effort</text>
    <text x="66" y="16" font-family="'DM Sans', sans-serif" font-size="12" fill="#7a7a90" style="white-space: pre;">Reasoning level</text>
  </g>
  ```
- The EXTENDING section header below currently starts at y=328. Shift the EXTENDING header and ALL content below it in column 2 down by 30px (e.g., EXTENDING header moves from y=328 to y=358, its underline from y=336 to y=366, first entry /agents from translate(440, 346) to translate(440, 376), etc.)
- Continue shifting everything in column 2 down by 30px until the end of the column content

Light/print theme (claude-code-cheatsheet-print.svg):
- Same structural changes but using print theme colors: keycap fill="url(#keycap)", stroke="#d0d0d8", text fill="#8a7820" for the command, description fill="#505060"
- Same 30px downward shift for all subsequent column 2 content

**2. Add `/remote-control` to the STATUS & ADMIN section (column 3, x=800)**

In the dark theme SVG, the STATUS & ADMIN section currently ends with `/privacy-settings` at y=284 (text element at x=806, y=284). Add `/remote-control` after it:

Dark theme:
- Insert a new text-only entry (STATUS & ADMIN uses text-only style, not keycap boxes):
  ```xml
  <text x="806" y="302" font-family="'JetBrains Mono', monospace" font-size="11" fill="#555568" style="white-space: pre;">/remote-control</text>
  <text x="940" y="302" font-family="'DM Sans', sans-serif" font-size="11" fill="#444458" style="white-space: pre;">Remote server</text>
  ```
- The VIM MODE section header below currently starts at y=316. Shift VIM MODE header and ALL content below it in column 3 down by 18px (y=316 becomes y=334, etc.)
- Continue shifting all column 3 content below this point down by 18px

Light/print theme:
- Same structural changes using print colors: command fill="#808090", description fill="#707080"
- Same 18px downward shift

**3. Add `/loop` to the EXTENDING section (column 2, x=440)**

After adding `/effort` and shifting EXTENDING down (from step 1), the EXTENDING section's last keycap entry before the CUSTOM COMMANDS box will be `/ide`. Add `/loop` after `/ide`:

Dark theme:
- After the shifted `/ide` entry, insert:
  ```xml
  <g transform="translate(440, NEW_Y)">
    <rect x="0" y="0" width="46" height="24" rx="5" fill="url(#keycap)" stroke="#333348" stroke-width="0.4" filter="url(#keyShadow)"/>
    <text x="23" y="16" font-family="'JetBrains Mono', monospace" font-size="11" fill="#c06888" font-weight="600" text-anchor="middle" style="white-space: pre;">/loop</text>
    <text x="56" y="16" font-family="'DM Sans', sans-serif" font-size="12" fill="#7a7a90" style="white-space: pre;">Recurring tasks</text>
  </g>
  ```
  Where NEW_Y = the shifted /ide y-position + 30
- Shift the CUSTOM COMMANDS box and everything below it in column 2 down by another 30px

Light/print theme:
- Same structural changes using print colors: command fill="#983868", description fill="#505060"
- Same positioning shifts

**4. Update COMMAND COST box — add /effort to MODIFIES line**

In both SVGs, find the COMMAND COST box (column 3). The MODIFIES line currently reads:
```
/compact  /model  /permissions  /memory
```
Change it to:
```
/compact  /model  /permissions  /memory  /effort
```

Dark theme: The text element is at y=622, fill="#444458"
Light/print theme: Same structural position, fill="#808090" or similar print color

**CRITICAL LAYOUT CHECKS after all edits:**
- No content should have y-coordinate above 840 that wasn't already there
- All content must stay below the footer line at y=960
- Column divider lines (x=420, x=780, x=1140) and footer line (y=960) positions must NOT change
- Column 4 (x=1160) content must NOT be modified — only columns 2 and 3 change
- If vertical space is tight after shifts, verify no overlapping elements by checking that consecutive entries maintain at least 26-30px vertical spacing
  </action>
  <verify>
Open both SVG files in a browser. Visually confirm:
1. /effort appears in MANAGING SESSION section of both SVGs
2. /remote-control appears in STATUS & ADMIN section of both SVGs
3. /loop appears in EXTENDING section of both SVGs
4. COMMAND COST MODIFIES line includes /effort in both SVGs
5. No overlapping text or clipped content
6. Footer line and column dividers are intact
7. Dark and light versions have matching content but different color schemes
  </verify>
  <done>/effort, /remote-control, and /loop commands are present in both SVG files with correct styling. COMMAND COST box updated. No layout overflow or overlapping content.</done>
</task>

<task type="auto">
  <name>Task 2: Verify SVG content parity between dark and light versions</name>
  <files>public/images/cheatsheet/claude-code-cheatsheet.svg, public/images/cheatsheet/claude-code-cheatsheet-print.svg</files>
  <action>
Run a content comparison between both SVGs by extracting all command text from each file and diffing them. Use grep to extract all text elements containing slash commands or keyboard shortcuts from both files, sort them, and compare. The command names should be identical — only fill colors and gradient references should differ.

Specifically verify:
1. Both files contain exactly the same set of slash commands (/effort, /remote-control, /loop, /compact, /review, etc.)
2. Both files contain the same keyboard shortcut labels (Ctrl+C, Ctrl+R, etc.)
3. The COMMAND COST MODIFIES line includes /effort in both files
4. Both files have valid XML (no unclosed tags from editing)

If any content mismatch is found, fix the divergent file to match.
  </action>
  <verify>
Grep for "/effort" in both files — both should have matches.
Grep for "/remote-control" in both files — both should have matches.
Grep for "/loop" in both files — both should have matches.
Validate XML: run `xmllint --noout` on both files (or open in browser and check no parsing errors).
  </verify>
  <done>Both SVG files have identical command content. XML is well-formed in both files.</done>
</task>

</tasks>

<verification>
1. Both SVGs render without errors in a browser
2. Three new commands visible in correct sections
3. COMMAND COST box updated with /effort
4. No visual regression — existing commands unchanged
5. Both SVGs have matching content (dark vs light theme only)
</verification>

<success_criteria>
- /effort, /remote-control, and /loop are present in both SVG files
- COMMAND COST MODIFIES category includes /effort
- Both SVGs render cleanly with no layout overflow
- Content parity verified between interactive and print versions
</success_criteria>

<output>
After completion, create `.planning/phases/114-cheatsheet/114-01-SUMMARY.md`
</output>
