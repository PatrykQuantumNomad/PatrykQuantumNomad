---
phase: 111-high-impact-chapter-rewrites
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/guides/claude-code/pages/hooks.mdx
  - src/lib/guides/interactive-data/hook-event-data.ts
  - src/lib/guides/svg-diagrams/hook-lifecycle.ts
  - src/data/guides/claude-code/guide.json
  - src/data/guides/claude-code/pages/environment.mdx
autonomous: true
gap_closure: true
requirements:
  - UPD-08

must_haves:
  truths:
    - "Ch8 reference table has exactly 24 event rows (2 session + 15 loop + 7 standalone)"
    - "All prose event counts in hooks.mdx say 24 total, 15 loop, 7 standalone (not 26/17/7)"
    - "hook-event-data.ts category metadata matches actual node counts (2, 15, 7)"
    - "HookEventVisualizer renders 24 event nodes matching the reference table"
    - "guide.json hooks description says 24 (not 26)"
    - "hook-lifecycle.ts SVG comment says 24 (not 26)"
    - "environment.mdx uses CLAUDE_CODE_EFFORT (not CLAUDE_CODE_EFFORT_LEVEL)"
  artifacts:
    - path: "src/data/guides/claude-code/pages/hooks.mdx"
      provides: "Corrected event counts and complete reference table with 24 rows"
      contains: "24 lifecycle events"
    - path: "src/lib/guides/interactive-data/hook-event-data.ts"
      provides: "Corrected category count metadata matching actual node definitions"
      contains: "count: 15"
    - path: "src/data/guides/claude-code/guide.json"
      provides: "Corrected hooks chapter description"
      contains: "24 lifecycle events"
    - path: "src/data/guides/claude-code/pages/environment.mdx"
      provides: "Corrected env var name matching models-and-costs.mdx"
      contains: "CLAUDE_CODE_EFFORT"
  key_links:
    - from: "hooks.mdx reference table"
      to: "hook-event-data.ts rawNodes"
      via: "event count consistency"
      pattern: "24"
    - from: "environment.mdx"
      to: "models-and-costs.mdx"
      via: "shared env var name"
      pattern: "CLAUDE_CODE_EFFORT"
---

<objective>
Fix event count inconsistencies across Ch8 hooks content and its supporting data files. The reference table, prose, component data, SVG diagram, and guide.json all claim 26 events but only 24 exist. Correct all counts to 24 and add the missing PermissionDenied standalone row to the reference table. Also fix the CLAUDE_CODE_EFFORT_LEVEL vs CLAUDE_CODE_EFFORT cross-chapter env var inconsistency.

Purpose: Close 2 verification gaps (event count mismatch) and 1 anti-pattern (env var name inconsistency) found in 111-VERIFICATION.md.
Output: All event counts consistent at 24 across hooks.mdx, hook-event-data.ts, hook-lifecycle.ts, guide.json. Environment.mdx env var corrected.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-high-impact-chapter-rewrites/111-04-SUMMARY.md
@.planning/phases/111-high-impact-chapter-rewrites/111-VERIFICATION.md

@src/data/guides/claude-code/pages/hooks.mdx
@src/lib/guides/interactive-data/hook-event-data.ts
@src/lib/guides/svg-diagrams/hook-lifecycle.ts
@src/data/guides/claude-code/guide.json
@src/data/guides/claude-code/pages/environment.mdx
@src/data/guides/claude-code/pages/models-and-costs.mdx

<interfaces>
<!-- Key data structures the executor needs to understand -->

From hook-event-data.ts — category nodes that need count corrections:
```typescript
// Line 50: count: 17 must become count: 15
{ id: 'loop-events', type: 'category', data: { label: 'Loop Events', count: 17 } }
// Line 56: count: 7 is already correct
{ id: 'standalone-events', type: 'category', data: { label: 'Standalone Async Events', count: 7 } }
```

From hook-event-data.ts — the PermissionDeniedAsync node (standalone alias, already correct):
```typescript
{ id: 'PermissionDeniedAsync', type: 'event', data: { label: 'PermissionDenied', category: 'standalone' } }
```

From hook-lifecycle.ts — the file comment and SVG open that reference "26":
```typescript
// Line 6: "Generates a build-time SVG showing all 26 hook lifecycle events"
// Line 68: diagramSvgOpen(config, 'Hook lifecycle: 26 event types across session, loop, and async categories')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix event counts in hooks.mdx reference table and prose</name>
  <files>src/data/guides/claude-code/pages/hooks.mdx, src/data/guides/claude-code/guide.json</files>
  <action>
**Root cause:** The original plan said "8 new events" but 3 of those (ConfigChange, WorktreeCreate, WorktreeRemove) already existed in the pre-rewrite 18-event set. Only 5 were truly new (PermissionDenied, CwdChanged, FileChanged, Elicitation, ElicitationResult). 18 + 5 = 23 unique events. PermissionDenied fires in both loop and standalone contexts, giving 24 event-context representations. The correct total is 24 (2 session + 15 loop + 7 standalone where standalone includes PermissionDenied async variant).

**In hooks.mdx, make these specific edits:**

1. **Frontmatter description (line 3):** Change "26 lifecycle events" to "24 lifecycle events"

2. **Keywords (line 8):** No change needed (no number in keywords)

3. **"What You'll Learn" section (line 17):** Change "all 26 lifecycle events" to "all 24 lifecycle events"

4. **"Hook Lifecycle Overview" section (line 33):** Change "Claude Code fires 26 lifecycle events" to "Claude Code fires 24 lifecycle events"

5. **"Event Reference" section (line 70):** Change "Claude Code's 26 lifecycle events" to "Claude Code's 24 lifecycle events"

6. **Reference table (lines 72-96):** Add ONE missing row for PermissionDenied as a Standalone event. Insert after the FileChanged row (line 96), before the table ends:
   ```
   | PermissionDenied | Standalone | Tool call denied (async notification) | tool_name, tool_input, deny_reason, permission_mode | No |
   ```
   This brings the table to 24 rows (2 session + 15 loop + 7 standalone). The existing PermissionDenied row at line 79 stays as-is (it's the Loop category entry).

7. **"Loop Events" section (line 130):** Change "There are 17 of them in total" to "There are 15 of them in total"

8. **"Standalone Async Events" section (line 182):** Change "Seven events fire" to "Seven events fire" (already correct, but verify). The PermissionDenied standalone mention at line 196 is already documented in prose -- keep it.

9. **SVG diagram caption/comment (line 68 in hook-lifecycle.ts):** This is handled in Task 2.

**In guide.json, make this edit:**

Find the hooks chapter entry (slug: "hooks") and change "26 lifecycle events" to "24 lifecycle events" in its description string.

**DO NOT change any other content.** The if field, PermissionDenied, defer, and all other documentation is correct and verified.
  </action>
  <verify>
Run these checks:
1. grep -c "26" src/data/guides/claude-code/pages/hooks.mdx should return 0 (no remaining "26" claims)
2. grep -c "24 lifecycle" src/data/guides/claude-code/pages/hooks.mdx should return 3+ (description, What You'll Learn, Event Reference)
3. grep "There are 15" src/data/guides/claude-code/pages/hooks.mdx should match the loop events sentence
4. Count table rows between the Event/Category header and the blank line after the table -- should be exactly 24
5. grep "24 lifecycle" src/data/guides/claude-code/guide.json should match the hooks entry
  </verify>
  <done>
- hooks.mdx reference table has exactly 24 rows (was 23, added PermissionDenied standalone)
- All prose counts updated: "24 lifecycle events" (was 26), "15 loop events" (was 17)
- guide.json hooks description says "24 lifecycle events" (was 26)
- No remaining "26" event count claims in hooks.mdx
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix event counts in hook-event-data.ts and hook-lifecycle.ts, fix env var in environment.mdx</name>
  <files>src/lib/guides/interactive-data/hook-event-data.ts, src/lib/guides/svg-diagrams/hook-lifecycle.ts, src/data/guides/claude-code/pages/environment.mdx</files>
  <action>
**In hook-event-data.ts, make these edits:**

1. **File comment (line 5):** Change "Defines 26 hook events" to "Defines 24 hook events"

2. **Loop Events category node (line 50):** Change `count: 17` to `count: 15`. The rawNodes array already has exactly 15 loop-category event nodes (UserPromptSubmit, PreToolUse, PermissionRequest, PostToolUse, PostToolUseFailure, Notification, SubagentStart, SubagentStop, Stop, TeammateIdle, TaskCompleted, PreCompact, PermissionDenied, Elicitation, ElicitationResult). The metadata must match.

3. **Detail content comment (line 249):** Change "Detail content for each of the 26 hook events" to "Detail content for each of the 24 hook events"

4. **No changes to rawNodes or rawEdges arrays.** The node definitions and edges are already correct at 24 event representations (2 session + 15 loop + 7 standalone including PermissionDeniedAsync). The detail content entries are complete for all 24 events.

**In hook-lifecycle.ts, make these edits:**

1. **File comment (line 6):** Change "Generates a build-time SVG showing all 26 hook lifecycle events" to "Generates a build-time SVG showing all 24 hook lifecycle events"

2. **Category comment (line 7):** Change "Session Events (2), Loop Events (17)" to "Session Events (2), Loop Events (15)"

3. **SVG accessible title (line 68):** Change `'Hook lifecycle: 26 event types across session, loop, and async categories'` to `'Hook lifecycle: 24 event types across session, loop, and async categories'`

4. **Loop events array comment (line 73):** If there's a comment saying "12" (original count), ensure it's removed or updated. The actual loopEvents array at lines 98-114 already has 15 entries -- this is correct.

**In environment.mdx, make this edit:**

1. **Line 174:** Change `export CLAUDE_CODE_EFFORT_LEVEL=medium` to `export CLAUDE_CODE_EFFORT=medium`. The authoritative name is `CLAUDE_CODE_EFFORT` as documented in models-and-costs.mdx lines 106-109 (where the effort level feature has its primary documentation). The `_LEVEL` suffix is incorrect and creates cross-chapter inconsistency.
  </action>
  <verify>
Run these checks:
1. grep -c "26" src/lib/guides/interactive-data/hook-event-data.ts should return 0
2. grep "count: 15" src/lib/guides/interactive-data/hook-event-data.ts should match the loop events category
3. grep "count: 7" src/lib/guides/interactive-data/hook-event-data.ts should match the standalone events category
4. grep "count: 2" src/lib/guides/interactive-data/hook-event-data.ts should match the session events category
5. grep -c "26" src/lib/guides/svg-diagrams/hook-lifecycle.ts should return 0
6. grep "24 event types" src/lib/guides/svg-diagrams/hook-lifecycle.ts should match
7. grep "CLAUDE_CODE_EFFORT_LEVEL" src/data/guides/claude-code/pages/environment.mdx should return 0 (no more _LEVEL suffix)
8. grep "CLAUDE_CODE_EFFORT" src/data/guides/claude-code/pages/environment.mdx should match line 174
  </verify>
  <done>
- hook-event-data.ts category count metadata matches actual nodes: 2 session, 15 loop, 7 standalone
- hook-lifecycle.ts comments and SVG title say 24 (was 26)
- No remaining "26" claims in either data file
- environment.mdx uses CLAUDE_CODE_EFFORT (matching models-and-costs.mdx), not CLAUDE_CODE_EFFORT_LEVEL
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected. This plan modifies documentation content (MDX, TypeScript data files, JSON metadata). No user input processing, API endpoints, or authentication logic is involved.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-07-01 | Information Disclosure | guide.json | accept | Public documentation metadata, no sensitive data |
</threat_model>

<verification>
1. Count reference table rows in hooks.mdx: must be exactly 24 (2 + 15 + 7)
2. Grep for "26" across all 5 modified files: must return 0 matches for event count claims
3. Grep for "24 lifecycle" in hooks.mdx: must appear in description, intro, and event reference
4. Grep for "count: 15" in hook-event-data.ts: must match loop events category
5. Grep for "CLAUDE_CODE_EFFORT_LEVEL" in environment.mdx: must return 0
6. Grep for "CLAUDE_CODE_EFFORT" in environment.mdx: must return 1+ matches
7. Verify hook-event-data.ts rawNodes has exactly 24 event-type nodes (excluding 3 category nodes)
</verification>

<success_criteria>
- All event count references across hooks.mdx, hook-event-data.ts, hook-lifecycle.ts, and guide.json are internally consistent at 24
- The hooks.mdx reference table has exactly 24 rows including PermissionDenied in both Loop and Standalone categories
- The hook-event-data.ts category metadata (count fields) matches actual node definitions
- The environment.mdx env var name matches models-and-costs.mdx (CLAUDE_CODE_EFFORT)
- No remaining "26" event count claims in any modified file
- All other Ch8 content (if field, defer, PermissionDenied docs, examples) is untouched
</success_criteria>

<output>
After completion, create `.planning/phases/111-high-impact-chapter-rewrites/111-07-SUMMARY.md`
</output>
