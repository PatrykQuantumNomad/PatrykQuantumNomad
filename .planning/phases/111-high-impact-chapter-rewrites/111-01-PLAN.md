---
phase: 111-high-impact-chapter-rewrites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/guides/claude-code/pages/models-and-costs.mdx
autonomous: true

must_haves:
  truths:
    - "Ch3 documents 1M Opus context as production-ready (not beta)"
    - "Ch3 documents the /effort slash command alongside settings.json and env var methods"
    - "Ch3 documents 6 permission modes including Auto Mode with classifier behavior"
    - "Ch3 documents Plan Mode's 5-option approval flow"
    - "Ch3 documents updated dontAsk and acceptEdits mode behaviors"
    - "No deprecated features appear (thinking summaries default, stale effort default)"
  artifacts:
    - path: "src/data/guides/claude-code/pages/models-and-costs.mdx"
      provides: "Complete Ch3 rewrite with Auto Mode, 1M context, /effort, 6 permission modes"
      contains: "auto"
  key_links:
    - from: "models-and-costs.mdx"
      to: "security.mdx"
      via: "inline cross-reference"
      pattern: "/guides/claude-code/security/"
    - from: "models-and-costs.mdx"
      to: "hooks.mdx"
      via: "inline cross-reference"
      pattern: "/guides/claude-code/hooks/"
---

<objective>
Rewrite Ch3 (Models, Cost Economics & Permissions) to document 1M Opus context, restructured effort levels, /effort command, Auto Mode as the 6th permission mode, Plan Mode approval flow, and updated dontAsk/acceptEdits behaviors. RESTRUCTURE the chapter flow to accommodate Auto Mode as a major addition.

Purpose: Ch3 is the most user-facing chapter for cost and permission decisions. Auto Mode is the largest new feature requiring dedicated coverage of the classifier, what it blocks/allows, fallback behavior, and availability requirements.
Output: Fully rewritten models-and-costs.mdx
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-high-impact-chapter-rewrites/111-RESEARCH.md
@.planning/research/FEATURES-claude-code-guide-refresh.md

@src/data/guides/claude-code/pages/models-and-costs.mdx
@src/components/guide/PermissionFlowExplorer.tsx
@src/components/guide/PermissionModelDiagram.astro
@src/lib/guides/svg-diagrams/permission-model.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read existing Ch3 and dependent components</name>
  <files>src/data/guides/claude-code/pages/models-and-costs.mdx, src/components/guide/PermissionFlowExplorer.tsx, src/lib/guides/svg-diagrams/permission-model.ts</files>
  <action>
Read the full existing Ch3 models-and-costs.mdx (353 lines), PermissionFlowExplorer.tsx (120 lines), and permission-model.ts (324 lines) to understand current structure, component APIs, and cross-references.

Specifically note:
- Current effort level default and how it's described
- Current permission modes section structure (currently 5 modes)
- How the PermissionFlowExplorer component is imported and invoked
- Current "Extended Context" section wording (1M is currently beta)
- All cross-references to other chapters (slugs must stay valid)

Also read the feature research file (.planning/research/FEATURES-claude-code-guide-refresh.md) for exact behavioral details of Auto Mode, /effort command, and permission mode updates. Look for feature numbers #1, #9, #22 specifically.

Verify: Does the PermissionFlowExplorer need data changes for Auto Mode? Per research, Auto Mode is a permission MODE, not a permission RULE. The explorer shows rule evaluation which is the same in auto mode -- auto mode just changes which tool calls reach the evaluator. If the explorer does NOT need changes, confirm this. If it DOES need changes, note them for inclusion in this plan.
  </action>
  <verify>All three files read successfully. Clear understanding of current structure, component imports, and what needs to change.</verify>
  <done>Full mental model of existing Ch3 content, component dependencies, and change scope documented</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Ch3 models-and-costs.mdx</name>
  <files>src/data/guides/claude-code/pages/models-and-costs.mdx</files>
  <action>
Write the COMPLETE rewritten chapter as a full MDX file replacement. Do NOT do surgical find-and-replace -- write the chapter fresh using the existing file as structural reference.

**Frontmatter updates:**
- Bump `lastVerified` and `updatedDate` to current date
- Update `description` to mention 6 permission modes and Auto Mode
- Update `keywords` to include "auto mode", "permission classifier", "/effort"

**Structural reorganization (RESTRUCTURE per research recommendation):**

Reorganize into clear sections flowing logically:
1. What You'll Learn (updated to mention Auto Mode, /effort)
2. Choosing a Model (Sonnet 4, Opus 4.6, Haiku -- update context window info, Opus now has 1M production context, not beta)
3. Effort Levels (restructure: verify current default, document /effort slash command alongside settings.json and CLAUDE_CODE_EFFORT env var methods)
4. Extended Context (Opus 1M is production-ready, Sonnet[1m] variant exists)
5. Cost Tracking (largely unchanged)
6. Cost Reduction Strategies (largely unchanged)
7. Permission System (deny/ask/allow rules -- this section stays as the rule evaluation logic)
8. Permission Modes (EXPAND from 5 to 6 modes):
   - default (ask for everything)
   - plan (with updated 5-option approval flow: proceed, proceed different, skip step, replan, exit)
   - autoEdit / acceptEdits (updated: now auto-approves mkdir, touch, rm, mv, cp, sed in working directory)
   - fullAutoEdit / fullAuto (unchanged)
   - dontAsk (updated: auto-denies unpermitted tools, designed for CI)
   - **auto** (NEW -- the major addition): dedicated subsection covering:
     * What it does: classifier auto-decides safe tool calls
     * What it blocks by default
     * What it allows by default
     * Fallback behavior when classifier is uncertain
     * Availability requirements (Team/Enterprise/API plan, Sonnet/Opus 4.6, API only)
     * Configuration: `permissions.disableAutoMode`, `autoMode.environment`
     * Example settings.json configuration
9. Interactive Permission Explorer (keep existing PermissionFlowExplorer component import)
10. Settings Precedence (largely unchanged)
11. Best Practices (update to include auto mode guidance)
12. Further Reading (update if needed)

**Deprecation sweep (silent removal):**
- If "thinking summaries" are mentioned as on by default, remove or correct to "off by default in interactive sessions" with `showThinkingSummaries: true` to restore
- Verify effort default is correctly stated (check feature research)
- Remove any mention of /tag or /vim if present
- Remove any reference to "5 permission modes" -- now 6

**Code examples:**
- All settings.json examples must be valid, complete JSON objects
- Use CodeBlock component with lang="json" and title=".claude/settings.json"
- Auto Mode config example: complete JSON enabling auto mode
- /effort command example: inline code mention, not a full CodeBlock
- Representative examples only (3-5 patterns) per CONTEXT.md

**Cross-references:**
- Keep all existing cross-references to other chapters using current slugs
- Add inline cross-reference to Ch11 Security for enterprise Auto Mode governance: "see [Security & Enterprise Administration](/guides/claude-code/security/)"
- Forward reference to Ch8 Hooks for PermissionDenied event if mentioning auto mode denials
- NO hyperlinks to not-yet-written chapters (Ch12-14). Text-only mentions if needed.

**Style:**
- No "What's New" callouts -- everything reads seamlessly as if always written this way
- No parenthetical "(formerly X)" mentions
- Professional but approachable first-person tone matching existing guide style
- Use `##` headings for main sections (these auto-generate TOC anchors)
  </action>
  <verify>
1. File is valid MDX (no unclosed tags, no JSX syntax errors)
2. All CodeBlock components have required props (code, lang)
3. All imports at top of file match components used in body
4. Cross-reference links use correct slugs: /guides/claude-code/hooks/, /guides/claude-code/security/, /guides/claude-code/environment/
5. No occurrences of "5 permission modes" (should be 6)
6. No occurrences of /tag or /vim
7. "auto" appears as a documented permission mode
8. /effort command is documented
9. 1M context described as production-ready for Opus (not beta)
  </verify>
  <done>
- Ch3 fully rewritten with all 6 permission modes including Auto Mode
- 1M Opus context documented as production-ready
- /effort command documented
- Plan Mode 5-option approval flow documented
- Updated dontAsk and acceptEdits behaviors documented
- All deprecated items silently removed
- Cross-references intact and correct
- File is ~450-500 lines (expanded from 353 to accommodate Auto Mode)
  </done>
</task>

</tasks>

<verification>
1. Build check: Run `npm run build` or `npx astro check` to verify MDX compiles without errors
2. Content check: Grep for "auto" mode, "/effort", "1M", "6" permission modes in the rewritten file
3. Deprecation check: Grep the file for deprecated terms: /tag, /vim, "5 permission modes", ANTHROPIC_SMALL_FAST_MODEL
4. Cross-reference check: All href/link targets point to existing slugs
</verification>

<success_criteria>
- Ch3 documents 1M Opus context as production-ready
- Ch3 documents /effort command
- Ch3 documents Auto Mode with classifier behavior, availability, and configuration
- Ch3 lists 6 permission modes (not 5)
- Ch3 documents Plan Mode 5-option approval flow
- Ch3 documents updated dontAsk (auto-deny for CI) and acceptEdits (mkdir, touch, rm, mv, cp, sed) behaviors
- No deprecated features present
- File compiles without MDX errors
</success_criteria>

<output>
After completion, create `.planning/phases/111-high-impact-chapter-rewrites/111-01-SUMMARY.md`
</output>
