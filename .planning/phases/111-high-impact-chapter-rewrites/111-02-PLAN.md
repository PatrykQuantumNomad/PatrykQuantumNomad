---
phase: 111-high-impact-chapter-rewrites
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/guides/claude-code/pages/environment.mdx
autonomous: true

must_haves:
  truths:
    - "Ch4 documents managed-settings.d/ directory for layered enterprise policy fragments"
    - "Ch4 documents --bare flag for minimal startup"
    - "Ch4 documents CLAUDE_CODE_NO_FLICKER, CLAUDE_CODE_DISABLE_MOUSE, CLAUDE_CODE_SCROLL_SPEED env vars"
    - "Ch4 documents transcript mode (Ctrl+O)"
    - "Ch4 does NOT mention deprecated ANTHROPIC_SMALL_FAST_MODEL env var"
  artifacts:
    - path: "src/data/guides/claude-code/pages/environment.mdx"
      provides: "Complete Ch4 rewrite with managed-settings.d/, --bare, NO_FLICKER env vars"
      contains: "managed-settings.d"
  key_links:
    - from: "environment.mdx"
      to: "models-and-costs.mdx"
      via: "inline cross-reference"
      pattern: "/guides/claude-code/models-and-costs/"
---

<objective>
Rewrite Ch4 (Environment Sandboxing & Customization) to document managed-settings.d/ directory, --bare flag, NO_FLICKER and related env vars, transcript mode, and PowerShell tool mention. ADDITIVE structure per research -- current layout works well, just adding new content to existing sections.

Purpose: Ch4 is the configuration reference chapter. New env vars and the managed-settings.d/ directory are critical for enterprise deployments and developer experience customization.
Output: Fully rewritten environment.mdx
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-high-impact-chapter-rewrites/111-RESEARCH.md
@.planning/research/FEATURES-claude-code-guide-refresh.md

@src/data/guides/claude-code/pages/environment.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read existing Ch4 and feature research</name>
  <files>src/data/guides/claude-code/pages/environment.mdx</files>
  <action>
Read the full existing Ch4 environment.mdx (363 lines) to understand current structure.

Specifically note:
- Current "Configuration Scopes" section structure (4 tiers)
- Current "Managed Settings" section -- this is where managed-settings.d/ addition goes
- Current "Environment Variables" section and the table/list format used -- this is where NO_FLICKER and friends go
- Location of ANTHROPIC_SMALL_FAST_MODEL mentions (research says lines 138, 147) -- mark for silent removal
- Current "Sandboxing" section structure
- All cross-references to other chapters
- All CodeBlock component usage patterns

Also read .planning/research/FEATURES-claude-code-guide-refresh.md for exact behavioral details of features #7 (fullscreen/NO_FLICKER), #8 (transcript mode), #20 (managed-settings.d/), #26 (PowerShell tool).
  </action>
  <verify>File read successfully. Clear understanding of current structure, env var table format, and where each addition slots in.</verify>
  <done>Full mental model of existing Ch4 content and change scope documented</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Ch4 environment.mdx</name>
  <files>src/data/guides/claude-code/pages/environment.mdx</files>
  <action>
Write the COMPLETE rewritten chapter as a full MDX file replacement. ADDITIVE structure -- keep the existing flow, add new content where it fits naturally.

**Frontmatter updates:**
- Bump `lastVerified` and `updatedDate` to current date
- Update `description` to mention managed-settings.d/ and display customization
- Update `keywords` to include "managed-settings.d", "--bare", "NO_FLICKER"

**Additions to existing sections:**

1. **Configuration Scopes / Managed Settings section:**
   - Add managed-settings.d/ directory alongside managed-settings.json
   - Explain: drop-in directory for layered enterprise policy fragments
   - Files merged alphabetically, later files override earlier for same key
   - Complete JSON example showing a policy fragment file
   - Cross-reference to Ch11 Security for enterprise governance details

2. **Environment Variables section:**
   - Add CLAUDE_CODE_NO_FLICKER=1 (disables TUI flickering, forces fullscreen rendering)
   - Add CLAUDE_CODE_DISABLE_MOUSE=1 (disables mouse event capture in terminal)
   - Add CLAUDE_CODE_SCROLL_SPEED=N (adjusts scroll speed in fullscreen mode)
   - Add CLAUDE_CODE_USE_POWERSHELL_TOOL=1 (Windows: uses PowerShell instead of Bash)
   - REMOVE ANTHROPIC_SMALL_FAST_MODEL silently -- only show ANTHROPIC_DEFAULT_HAIKU_MODEL as the current var name
   - Keep the existing env var table format

3. **New subsection or integration into existing CLI section:**
   - --bare flag: minimal startup mode, skips loading CLAUDE.md files, useful for scripted automation
   - Example: `claude --bare "Quick question about this file"`

4. **Display/UI section (new or integrated into status line section):**
   - Transcript mode: Ctrl+O for searchable transcript view
   - Brief mention alongside the fullscreen rendering env vars

**Deprecation sweep (silent removal):**
- Remove ALL mentions of ANTHROPIC_SMALL_FAST_MODEL -- replace with ANTHROPIC_DEFAULT_HAIKU_MODEL only
- Remove any mention of /tag or /vim if present
- No parenthetical "(formerly X)" for the renamed env var

**Code examples:**
- managed-settings.d/ example: complete JSON file showing a policy fragment
- --bare flag example: bash command
- Env var examples: inline code or env var table entries (match existing format)

**Cross-references:**
- Keep existing cross-references to Ch3 for permissions
- Keep "Further Reading" next-chapter link to Remote & Headless
- Add cross-reference to Ch11 Security for managed-settings.d/ enterprise governance
- NO hyperlinks to not-yet-written chapters

**Style:**
- No "What's New" callouts
- No "(formerly ANTHROPIC_SMALL_FAST_MODEL)" -- just use the new name
- Match existing chapter tone and formatting patterns
  </action>
  <verify>
1. File is valid MDX
2. All CodeBlock components have required props
3. No occurrences of "ANTHROPIC_SMALL_FAST_MODEL" anywhere in the file
4. managed-settings.d/ is documented
5. --bare flag is documented
6. CLAUDE_CODE_NO_FLICKER is documented
7. Transcript mode (Ctrl+O) is mentioned
8. Cross-references use correct slugs
9. No mentions of /tag or /vim
  </verify>
  <done>
- Ch4 fully rewritten with managed-settings.d/ directory documented
- --bare flag documented
- NO_FLICKER and related display env vars documented
- Transcript mode documented
- ANTHROPIC_SMALL_FAST_MODEL silently removed, only current name used
- Cross-references intact
- File is ~410-430 lines (moderate expansion from 363)
  </done>
</task>

</tasks>

<verification>
1. Build check: MDX compiles without errors
2. Content check: Grep for "managed-settings.d", "--bare", "NO_FLICKER", "Ctrl+O" in the rewritten file
3. Deprecation check: Grep for ANTHROPIC_SMALL_FAST_MODEL (must NOT appear), /tag, /vim
4. Cross-reference check: All href targets point to existing slugs
</verification>

<success_criteria>
- Ch4 documents managed-settings.d/ directory with example
- Ch4 documents --bare flag with example
- Ch4 documents CLAUDE_CODE_NO_FLICKER, CLAUDE_CODE_DISABLE_MOUSE, CLAUDE_CODE_SCROLL_SPEED
- Ch4 documents transcript mode (Ctrl+O)
- Ch4 mentions CLAUDE_CODE_USE_POWERSHELL_TOOL for Windows
- ANTHROPIC_SMALL_FAST_MODEL does NOT appear anywhere in the file
- No other deprecated features present
- File compiles without MDX errors
</success_criteria>

<output>
After completion, create `.planning/phases/111-high-impact-chapter-rewrites/111-02-SUMMARY.md`
</output>
