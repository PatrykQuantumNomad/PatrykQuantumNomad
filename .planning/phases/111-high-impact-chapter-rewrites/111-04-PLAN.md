---
phase: 111-high-impact-chapter-rewrites
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/guides/claude-code/pages/hooks.mdx
  - src/components/guide/HookEventVisualizer.tsx
  - src/lib/guides/svg-diagrams/hook-lifecycle.ts
autonomous: true

must_haves:
  truths:
    - "Ch8 documents all 26 hook events (was 18) with a full reference table"
    - "Ch8 documents the conditional if field on hook handlers with permission rule syntax"
    - "Ch8 documents PermissionDenied event with retry option and Auto Mode connection"
    - "Ch8 documents defer as a new PreToolUse decision value with --resume workflow"
    - "Ch8 documents hook output size cap (50K) with disk save behavior"
    - "HookEventVisualizer component shows all 26 events (not 18)"
    - "No deprecated hook fields (old decision/reason) appear anywhere"
  artifacts:
    - path: "src/data/guides/claude-code/pages/hooks.mdx"
      provides: "Complete Ch8 rewrite with 26 events, if field, PermissionDenied, defer"
      contains: "PermissionDenied"
    - path: "src/components/guide/HookEventVisualizer.tsx"
      provides: "Updated interactive event explorer with all 26 events"
      contains: "PermissionDenied"
    - path: "src/lib/guides/svg-diagrams/hook-lifecycle.ts"
      provides: "Updated SVG diagram generator with 26 event nodes"
      contains: "PermissionDenied"
  key_links:
    - from: "hooks.mdx"
      to: "HookEventVisualizer.tsx"
      via: "client:visible import"
      pattern: "HookEventVisualizer"
    - from: "hooks.mdx"
      to: "HookLifecycleDiagram.astro"
      via: "component import"
      pattern: "HookLifecycleDiagram"
    - from: "hooks.mdx"
      to: "models-and-costs.mdx"
      via: "inline cross-reference for Auto Mode"
      pattern: "/guides/claude-code/models-and-costs/"
---

<objective>
Rewrite Ch8 (Hooks & Lifecycle Automation) to document all 26 hook events, conditional `if` field, PermissionDenied event, defer decision, and hook output size cap. Also update the HookEventVisualizer React component and hook-lifecycle SVG diagram generator to include the 8 new events. RESTRUCTURE the chapter to accommodate the expanded event reference table.

Purpose: Ch8 has the most structural change in Phase 111. The event count grows from 18 to 26, requiring an expanded reference table, component data updates, and new sections for the if field and defer decision. This is the largest plan in the phase.
Output: Rewritten hooks.mdx + updated HookEventVisualizer.tsx + updated hook-lifecycle.ts
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-high-impact-chapter-rewrites/111-RESEARCH.md
@.planning/research/FEATURES-claude-code-guide-refresh.md

@src/data/guides/claude-code/pages/hooks.mdx
@src/components/guide/HookEventVisualizer.tsx
@src/components/guide/HookLifecycleDiagram.astro
@src/lib/guides/svg-diagrams/hook-lifecycle.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read all existing Ch8 files and feature research</name>
  <files>src/data/guides/claude-code/pages/hooks.mdx, src/components/guide/HookEventVisualizer.tsx, src/lib/guides/svg-diagrams/hook-lifecycle.ts, src/components/guide/HookLifecycleDiagram.astro</files>
  <action>
Read ALL four files in full:
- hooks.mdx (503 lines): Understand current event tables, handler types, configuration format, decision control, JSON output format
- HookEventVisualizer.tsx (124 lines): Understand the data structure for events (names, descriptions, categories, payload fields). Note exactly how events are defined so new ones can be added in the same format.
- hook-lifecycle.ts (187 lines): Understand the SVG generator layout, node placement algorithm, and how adding 8 more event nodes affects the diagram. Determine if nodes can be added without layout restructuring or if the layout algorithm needs adjustment.
- HookLifecycleDiagram.astro (13 lines): Understand how it imports and renders the SVG generator output.

Specifically document:
- Current event list (all 18 event names and categories)
- Current data structure in HookEventVisualizer (field names, types)
- Current SVG layout approach in hook-lifecycle.ts (hardcoded positions? algorithm? grid?)
- Location of deprecated `decision`/`reason` fields in hooks.mdx (research says lines 417, 496)
- All cross-references to other chapters

Also read .planning/research/FEATURES-claude-code-guide-refresh.md for exact behavioral details of features #10 (26 events), #4 (if field), #33 (output size cap).
  </action>
  <verify>All four files read successfully. Clear understanding of event data structures, SVG layout approach, and the 8 new events to add.</verify>
  <done>Full mental model of existing Ch8 content, component internals, SVG generator layout, and change scope documented</done>
</task>

<task type="auto">
  <name>Task 2: Update HookEventVisualizer component and hook-lifecycle SVG generator</name>
  <files>src/components/guide/HookEventVisualizer.tsx, src/lib/guides/svg-diagrams/hook-lifecycle.ts</files>
  <action>
**HookEventVisualizer.tsx update:**

Add the 8 new events to the component's data array/object, matching the exact data structure found in Task 1. The 8 new events are:
1. PermissionDenied -- fires on auto mode denials, has retry option
2. CwdChanged -- fires when working directory changes
3. FileChanged -- fires when file system changes detected
4. ConfigChange -- fires when settings change (may already exist as standalone async)
5. WorktreeCreate -- fires when new worktree is created
6. WorktreeRemove -- fires when worktree is removed
7. Elicitation -- fires before MCP elicitation prompt
8. ElicitationResult -- fires after user responds to elicitation

For each new event, provide:
- Event name (exact casing from Claude Code docs)
- Category assignment (Session, Loop, or Standalone Async -- categorize based on when they fire)
- Brief description (1 sentence)
- Trigger description
- Key payload fields
- Match the exact structure used by existing events

**hook-lifecycle.ts update:**

Add the 8 new events to the SVG diagram. Based on Task 1 analysis of the layout approach:
- If hardcoded positions: add new nodes with positions that maintain visual clarity
- If algorithmic: ensure the algorithm handles 26 nodes without overlapping
- Maintain the existing visual style (colors, fonts, connector lines)
- Group new events into their respective categories in the diagram
- Ensure the SVG viewBox is large enough to accommodate additional nodes

Keep the existing 18 events exactly as they are -- only ADD the 8 new ones. Do not rename or recategorize existing events.
  </action>
  <verify>
1. HookEventVisualizer.tsx: Count event entries -- must be exactly 26
2. hook-lifecycle.ts: Count event nodes -- must be exactly 26
3. No TypeScript compilation errors in either file
4. All 8 new events present: PermissionDenied, CwdChanged, FileChanged, ConfigChange, WorktreeCreate, WorktreeRemove, Elicitation, ElicitationResult
5. Existing 18 events unchanged
  </verify>
  <done>
- HookEventVisualizer shows all 26 events with descriptions, categories, and payloads
- hook-lifecycle.ts generates SVG diagram with all 26 event nodes
- Both files compile without errors
- Existing 18 events are unmodified
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite Ch8 hooks.mdx</name>
  <files>src/data/guides/claude-code/pages/hooks.mdx</files>
  <action>
Write the COMPLETE rewritten chapter as a full MDX file replacement. RESTRUCTURE per research recommendation to accommodate the expanded event set and new features.

**Frontmatter updates:**
- Bump `lastVerified` and `updatedDate` to current date
- Update `description`: change "18 lifecycle events" to "26 lifecycle events", mention conditional if field
- Update `keywords` to include "PermissionDenied", "defer", "conditional hooks", "if field"

**Structural reorganization:**

1. What You'll Learn (updated for 26 events, if field, defer)
2. Hook Architecture Overview (keep existing, update event count)
3. **Event Reference** (MAJOR EXPANSION):
   - Per CONTEXT.md decision: "Reference content like the 26 hook events uses a full table with all columns (event name, trigger, payload, example)"
   - Create a comprehensive reference table with columns: Event Name, Category, Trigger, Key Payload Fields
   - Group into categories:
     * Session Events: SessionStart, SessionEnd
     * Loop Events: PreToolUse, PostToolUse, PostToolUseFailure, PermissionRequest, PermissionDenied (NEW), Notification, SubagentStart, SubagentEnd, Stop, CwdChanged (NEW), FileChanged (NEW)
     * Standalone Async Events: ConfigChange, WorktreeCreate (NEW), WorktreeRemove (NEW), Elicitation (NEW), ElicitationResult (NEW)
     * (Verify exact categorization against feature research)
   - After the table, provide 3-5 representative prose descriptions covering the main patterns (not all 26). Per CONTEXT.md: "Representative examples for repetitive content (3-5 well-chosen hook event examples covering the main patterns, not all 26)"
   - Good pattern choices: PreToolUse (most common), PermissionDenied (new + auto mode), PostToolUse (output processing), Elicitation (MCP integration), SessionStart (setup)
4. Handler Types (4 types, keep existing structure)
5. Configuration Format (update to include the `if` field):
   - Add the `if` field alongside existing matcher, command, timeout fields
   - `if` uses permission rule syntax to narrow beyond matcher
   - Supported on: PreToolUse, PostToolUse, PostToolUseFailure, PermissionRequest, PermissionDenied
   - Complete JSON configuration example showing matcher + if combination
6. **Decision Control** (update to include `defer`):
   - Existing: allow, deny, ask (via hookSpecificOutput.permissionDecision)
   - NEW: `defer` -- pauses `-p` sessions at tool call, exits with `deferred_tool_use` payload
   - Resume with `--resume` flag for SDK applications
   - REMOVE old top-level `decision`/`reason` fields SILENTLY -- only show hookSpecificOutput with permissionDecision and permissionDecisionReason
7. **PermissionDenied Event** (dedicated subsection or integrated into events):
   - Fires when auto mode classifier denies a tool call
   - Has `retry: true` option in hookSpecificOutput
   - Connection to Auto Mode (cross-ref to Ch3)
   - Example hook script showing PermissionDenied handler
8. Hook Output Size Cap:
   - Output over 50K saved to disk with path + preview instead of injected into context
   - Brief mention in the handler output section
9. Matcher Patterns (keep existing)
10. Exit Codes (keep existing)
11. Hook Locations/Scopes (keep existing)
12. /hooks Management (keep existing)
13. HookLifecycleDiagram component (keep import, component renders updated SVG)
14. HookEventVisualizer component (keep import, component shows updated 26 events)
15. Best Practices (update for new events and if field)
16. Further Reading (update if needed, keep next-chapter link to Worktrees)

**Deprecation sweep (silent removal):**
- REMOVE all mentions of old top-level `decision` and `reason` fields
- Only show `hookSpecificOutput` with `permissionDecision` and `permissionDecisionReason`
- Change ALL instances of "18" events to "26"
- Remove any "deprecated" labels on the old fields -- just don't mention them at all
- Remove any mentions of /tag or /vim

**Code examples:**
- Configuration with `if` field: complete JSON showing matcher + if combination
- PermissionDenied handler: complete bash script
- Defer decision: complete JSON output showing deferred_tool_use payload
- Use 3-5 representative event examples, not all 26
- All examples complete and copy-pasteable

**Cross-references:**
- Keep existing link to Ch7 Skills for frontmatter hooks
- Keep existing link to Ch9 Worktrees for next chapter
- Add cross-reference to Ch3 Models & Costs for Auto Mode context (for PermissionDenied)
- Add cross-reference to Ch11 Security for hook governance
- NO hyperlinks to not-yet-written chapters
  </action>
  <verify>
1. File is valid MDX
2. All component imports match: CodeBlock, TerminalRecording, HookLifecycleDiagram, HookEventVisualizer
3. Event reference table has 26 entries
4. "26" appears where event count is mentioned (not "18")
5. `if` field is documented with permission rule syntax
6. `defer` decision is documented
7. PermissionDenied event is documented
8. Hook output size cap (50K) is mentioned
9. No occurrences of old top-level `decision`/`reason` pattern
10. No occurrences of "18 lifecycle events" or "18 events"
11. Cross-references use correct slugs
12. No mentions of /tag or /vim
  </verify>
  <done>
- Ch8 fully rewritten with all 26 hook events documented in comprehensive reference table
- Conditional if field documented with examples
- PermissionDenied event documented with Auto Mode connection
- defer decision documented with --resume workflow
- Hook output size cap documented
- Old decision/reason fields silently removed
- HookEventVisualizer and HookLifecycleDiagram imports maintained
- File is ~650-750 lines (expanded from 503)
  </done>
</task>

</tasks>

<verification>
1. Build check: Run `npm run build` or `npx astro check` -- MDX and TypeScript must compile
2. Content check: Grep hooks.mdx for all 8 new event names
3. Component check: Grep HookEventVisualizer.tsx for all 8 new event names
4. SVG check: Grep hook-lifecycle.ts for all 8 new event names
5. Deprecation check: Grep hooks.mdx for "18 lifecycle", "18 events", old `decision`/`reason` pattern, /tag, /vim
6. Consistency check: Event count in prose, table, component, and SVG all match (26)
</verification>

<success_criteria>
- Ch8 documents all 26 hook events in a full reference table
- Ch8 documents the conditional if field with permission rule syntax
- Ch8 documents PermissionDenied event with retry and Auto Mode connection
- Ch8 documents defer as a PreToolUse decision value with --resume
- Ch8 documents hook output size cap (50K)
- HookEventVisualizer.tsx shows all 26 events
- hook-lifecycle.ts generates diagram with all 26 events
- No deprecated fields (old decision/reason) appear anywhere
- All files compile without errors
</success_criteria>

<output>
After completion, create `.planning/phases/111-high-impact-chapter-rewrites/111-04-SUMMARY.md`
</output>
