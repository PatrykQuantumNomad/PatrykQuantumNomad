---
phase: 111-high-impact-chapter-rewrites
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/guides/claude-code/pages/custom-skills.mdx
autonomous: true

must_haves:
  truths:
    - "Ch7 documents paths frontmatter field with glob pattern examples"
    - "Ch7 documents shell frontmatter field for PowerShell support"
    - "Ch7 has a Content Lifecycle and Compaction section documenting 5K token re-attach, 25K combined budget, and recency prioritization"
    - "Ch7 mentions Plugins as the distribution mechanism for skills with namespaced invocation"
    - "Ch7 documents disableSkillShellExecution managed setting"
  artifacts:
    - path: "src/data/guides/claude-code/pages/custom-skills.mdx"
      provides: "Complete Ch7 rewrite with paths/shell frontmatter, lifecycle section, Plugins mention"
      contains: "paths"
  key_links:
    - from: "custom-skills.mdx"
      to: "hooks.mdx"
      via: "inline cross-reference"
      pattern: "/guides/claude-code/hooks/"
    - from: "custom-skills.mdx"
      to: "security.mdx"
      via: "inline cross-reference for disableSkillShellExecution"
      pattern: "/guides/claude-code/security/"
---

<objective>
Rewrite Ch7 (Custom Skills) to document paths and shell frontmatter fields, content lifecycle/compaction behavior, Plugins relationship, and disableSkillShellExecution security setting. ADDITIVE structure with one new section (lifecycle/compaction) per research recommendation.

Purpose: Ch7 is the skills reference. The new paths frontmatter enables conditional skill activation, lifecycle docs explain how skills survive context compaction, and the Plugins mention prepares readers for the upcoming Ch12.
Output: Fully rewritten custom-skills.mdx
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-high-impact-chapter-rewrites/111-RESEARCH.md
@.planning/research/FEATURES-claude-code-guide-refresh.md

@src/data/guides/claude-code/pages/custom-skills.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read existing Ch7 and feature research</name>
  <files>src/data/guides/claude-code/pages/custom-skills.mdx</files>
  <action>
Read the full existing Ch7 custom-skills.mdx (366 lines) to understand current structure.

Specifically note:
- Current frontmatter fields reference section (10 fields listed) -- paths and shell will be added here
- Current "Where Skills Live" section (4 storage locations) -- Plugins mention goes here
- Current "Running Skills in Subagents" section -- lifecycle section goes after this
- Current "Bundled Skills" section -- verify if the bundled skills list has changed
- Current "Restricting Skill Access" section -- disableSkillShellExecution goes here
- All cross-references to other chapters
- All CodeBlock usage patterns

Also read .planning/research/FEATURES-claude-code-guide-refresh.md for exact behavioral details of features #41 (paths frontmatter), #23 (lifecycle/compaction), #19 (shell frontmatter), #6 (Plugins), #21 (disableSkillShellExecution).
  </action>
  <verify>File read successfully. Clear understanding of current structure and where each addition slots in.</verify>
  <done>Full mental model of existing Ch7 content and change scope documented</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Ch7 custom-skills.mdx</name>
  <files>src/data/guides/claude-code/pages/custom-skills.mdx</files>
  <action>
Write the COMPLETE rewritten chapter as a full MDX file replacement. ADDITIVE structure -- keep the flow, add new fields and one new section.

**Frontmatter updates:**
- Bump `lastVerified` and `updatedDate` to current date
- Update `description` to mention conditional activation and Plugins
- Update `keywords` to include "paths", "compaction", "plugins", "lifecycle"

**Additions to existing sections:**

1. **Frontmatter Reference section (currently 10 fields, becomes 12):**
   - Add `paths` field: Array of glob patterns for conditional skill activation
     * Example: `paths: ["src/api/**/*.ts", "src/middleware/**"]`
     * When set, skill auto-loads only when matching files are read or modified
     * Useful for context-specific skills (API conventions, test patterns)
   - Add `shell` field: Shell override for inline `!command` execution
     * Example: `shell: powershell` (Windows PowerShell support)
     * Default is system shell (bash/zsh on Unix, cmd on Windows)

2. **Where Skills Live section:**
   - Add brief "Skills and Plugins" subsection
   - Plugins are the distribution mechanism for skills
   - Plugin skills are namespaced: `/plugin-name:skill-name`
   - Point to Plugins chapter with TEXT-ONLY mention (no hyperlink -- chapter doesn't exist yet)
   - Example: "The Plugins system packages and distributes skills as installable units. Skills installed via plugins are namespaced as `/plugin-name:skill-name`. See the Plugins chapter for full details."

3. **NEW section: Content Lifecycle and Compaction** (after Running Skills in Subagents):
   - Skills survive context compaction
   - First 5,000 tokens of each invoked skill are re-attached after compaction
   - Combined budget: 25,000 tokens across all invoked skills
   - Most recently invoked skills are prioritized when budget is constrained
   - Practical implications: keep critical content in the first 5K tokens of the skill
   - No code example needed -- this is behavioral documentation

4. **Running Skills in Subagents section update:**
   - Skills with `context: fork` run in subagents
   - Subagents with `skills` field can preload skill content
   - Document bidirectional relationship

5. **Restricting Skill Access section:**
   - Add `disableSkillShellExecution` managed setting
   - Blocks inline shell (`!command`) execution from within skills
   - Enterprise security control
   - Cross-reference to Ch11 Security: "see [Security & Enterprise Administration](/guides/claude-code/security/) for managed setting deployment"

**Deprecation sweep:**
- Skills chapter has no deprecated items per research (clean slate)
- Verify no mentions of /tag or /vim

**Code examples:**
- paths frontmatter: complete SKILL.md header showing the paths field
- shell frontmatter: complete SKILL.md header showing the shell field
- Plugin namespaced invocation: inline code mention `/plugin-name:skill-name`

**Cross-references:**
- Keep existing link to Ch8 Hooks for frontmatter hooks
- Keep existing link to Ch5 Remote & Headless for /loop
- Add cross-reference to Ch11 Security for disableSkillShellExecution
- TEXT-ONLY mention of Plugins chapter (no hyperlink)
- NO hyperlinks to not-yet-written chapters

**Style:**
- No "What's New" callouts
- Match existing chapter tone
- Use `##` headings for the new Lifecycle section
  </action>
  <verify>
1. File is valid MDX
2. All CodeBlock components have required props
3. `paths` frontmatter field is documented with glob pattern example
4. `shell` frontmatter field is documented
5. "Content Lifecycle and Compaction" section exists with 5K/25K token details
6. Plugins mention exists WITHOUT hyperlink to /guides/claude-code/plugins/
7. disableSkillShellExecution is documented
8. Cross-references use correct slugs
9. No mentions of /tag or /vim
10. No hyperlinks pointing to non-existent chapter slugs (plugins, agent-sdk, computer-use)
  </verify>
  <done>
- Ch7 fully rewritten with paths and shell frontmatter fields (12 fields total)
- Content Lifecycle and Compaction section added
- Plugins relationship documented with text-only forward reference
- disableSkillShellExecution documented with cross-reference to Ch11
- Skills-subagents bidirectional relationship documented
- Cross-references intact
- File is ~430-460 lines (expanded from 366)
  </done>
</task>

</tasks>

<verification>
1. Build check: MDX compiles without errors
2. Content check: Grep for "paths", "shell", "compaction", "5,000", "25,000", "Plugins", "disableSkillShellExecution"
3. Forward reference check: Grep for href.*plugins -- must NOT find any hyperlinks to non-existent plugins chapter
4. Deprecation check: Grep for /tag, /vim
</verification>

<success_criteria>
- Ch7 documents paths frontmatter with glob pattern examples
- Ch7 documents shell frontmatter for PowerShell
- Ch7 has Content Lifecycle and Compaction section with 5K/25K token details
- Ch7 mentions Plugins as distribution mechanism (text-only, no hyperlink)
- Ch7 documents disableSkillShellExecution with cross-reference to Ch11
- Ch7 documents skills-subagents bidirectional relationship
- File compiles without MDX errors
</success_criteria>

<output>
After completion, create `.planning/phases/111-high-impact-chapter-rewrites/111-03-SUMMARY.md`
</output>
