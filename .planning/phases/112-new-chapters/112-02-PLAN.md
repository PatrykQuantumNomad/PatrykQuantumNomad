---
phase: 112-new-chapters
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/guides/claude-code/pages/agent-sdk.mdx
autonomous: true

must_haves:
  truths:
    - "Reader can install the Agent SDK in Python and TypeScript"
    - "Reader understands the query() API and can write a basic agent script"
    - "Reader knows all built-in tools available through the SDK"
    - "Reader understands hooks as native callback functions (vs JSON config)"
    - "Reader knows how to create subagents programmatically"
    - "Reader knows how to integrate MCP servers, permission modes, and session management"
    - "Reader understands when to use the SDK vs the CLI"
    - "Reader sees one-line mention of rename from Claude Code SDK, then chapter moves on"
  artifacts:
    - path: "src/data/guides/claude-code/pages/agent-sdk.mdx"
      provides: "Complete Agent SDK chapter with Python + TypeScript APIs, hooks, subagents, MCP, sessions"
      contains: "claude_agent_sdk"
  key_links:
    - from: "agent-sdk.mdx"
      to: "hooks.mdx"
      via: "inline cross-reference"
      pattern: "/guides/claude-code/hooks/"
    - from: "agent-sdk.mdx"
      to: "custom-skills.mdx"
      via: "inline cross-reference"
      pattern: "/guides/claude-code/custom-skills/"
    - from: "agent-sdk.mdx"
      to: "mcp.mdx"
      via: "inline cross-reference"
      pattern: "/guides/claude-code/mcp/"
    - from: "agent-sdk.mdx"
      to: "plugins.mdx"
      via: "inline cross-reference"
      pattern: "/guides/claude-code/plugins/"
---

<objective>
Author Ch13 (Agent SDK) as a complete, self-contained guide chapter covering the Python and TypeScript SDKs for embedding Claude Code's agent loop into production applications. Cover the query() API, built-in tools, hooks as callbacks, subagents, MCP integration, permission modes, session management, and authentication providers.

Purpose: The Agent SDK chapter bridges the gap between interactive CLI usage and programmatic automation. Readers building CI/CD pipelines, custom apps, or production agents need a dedicated reference.
Output: src/data/guides/claude-code/pages/agent-sdk.mdx (~450-500 lines)
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-new-chapters/112-RESEARCH.md
@.planning/research/FEATURES-claude-code-guide-refresh.md

@src/data/guides/claude-code/pages/custom-skills.mdx
@src/data/guides/claude-code/pages/hooks.mdx
@src/data/guides/claude-code/pages/mcp.mdx
@src/data/guides/claude-code/guide.json
@src/components/guide/CodeBlock.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read reference materials and SDK feature details</name>
  <files>src/data/guides/claude-code/pages/custom-skills.mdx, src/data/guides/claude-code/pages/hooks.mdx, src/data/guides/claude-code/pages/mcp.mdx</files>
  <action>
Read Ch7 custom-skills.mdx (486 lines) for chapter structure patterns: frontmatter, imports, section voice, CodeBlock usage, Best Practices format, Further Reading format.

Read Ch8 hooks.mdx for how hook events are documented -- the SDK chapter needs to reference these events since SDK hooks use the same event system but as native callback functions instead of JSON config.

Read Ch6 mcp.mdx briefly for how MCP servers are explained -- the SDK chapter references MCP integration via the `mcp_servers` option.

Read the feature research file (.planning/research/FEATURES-claude-code-guide-refresh.md) lines 283-315 for exact Agent SDK details:
- Installation commands (pip install claude-agent-sdk, npm install @anthropic-ai/claude-agent-sdk)
- Core query() API with ClaudeAgentOptions
- Built-in tools list (Read, Write, Edit, Bash, Monitor, Glob, Grep, WebSearch, WebFetch, AskUserQuestion)
- Key capabilities: hooks as callbacks, subagents via AgentDefinition, MCP servers, permission modes, session resume, skills via setting_sources, plugins option
- Authentication: ANTHROPIC_API_KEY, Bedrock, Vertex AI, Azure Foundry
- SDK vs CLI comparison

Read the research file (.planning/phases/112-new-chapters/112-RESEARCH.md) for the section outline, Python example pattern, and anti-patterns.

**Naming note from research:** The SDK packages retained original names (`claude-agent-sdk` for Python, `@anthropic-ai/claude-agent-sdk` for TypeScript). Only the marketing name changed to "Agent SDK." Chapter title uses "Agent SDK", code examples use actual package names.
  </action>
  <verify>Full understanding of chapter patterns, SDK API surface, and all feature details needed for authoring.</verify>
  <done>Complete mental model of existing chapter patterns and all Agent SDK feature details</done>
</task>

<task type="auto">
  <name>Task 2: Author agent-sdk.mdx chapter</name>
  <files>src/data/guides/claude-code/pages/agent-sdk.mdx</files>
  <action>
Create the new file `src/data/guides/claude-code/pages/agent-sdk.mdx` as a complete MDX chapter (~450-500 lines).

**Frontmatter:**
```yaml
---
title: "Agent SDK"
description: "Use the Python and TypeScript SDKs to embed Claude Code's agent loop, tools, and context management into production applications and CI/CD pipelines."
order: 12
slug: "agent-sdk"
lastVerified: 2026-04-12
updatedDate: 2026-04-12
keywords: ["claude code sdk", "agent sdk", "claude agent sdk", "python sdk", "typescript sdk", "query api", "programmatic claude", "ci cd automation", "agent loop"]
---
```

**Import:**
```
import CodeBlock from '../../../../components/guide/CodeBlock.astro';
```

**Section structure (in order):**

1. **What You Will Learn** -- Brief paragraph. Include the rename mention per CONTEXT.md: one natural sentence like "The Agent SDK (previously known as the Claude Code SDK) lets you embed Claude Code's full agent loop..." then move on. Do NOT dwell on the rename or add parenthetical notes elsewhere.

2. **Quick Start** -- NEW CONVENTION. "Run your first SDK agent in under two minutes." Show Python installation and a minimal query() call that does something realistic (e.g., "Find all TODO comments in this project"). Use CodeBlock with lang="python" and title="quick_start.py". Show expected output structure briefly.

3. **What Is the Agent SDK?** -- Explain that the SDK exposes the same agent loop, tools, and context management available in the CLI as a library. When to use SDK vs CLI: SDK for CI/CD pipelines, custom apps, production automation; CLI for interactive development. Keep self-contained: briefly explain what the "agent loop" is for readers who jumped here.

4. **Installation** -- Side-by-side Python and TypeScript installation commands. Two CodeBlock examples:
   - Python: `pip install claude-agent-sdk` with lang="bash" title="Python"
   - TypeScript: `npm install @anthropic-ai/claude-agent-sdk` with lang="bash" title="TypeScript"

5. **The query() API** -- Core section. Show the full query() signature with ClaudeAgentOptions. Cover:
   - `prompt` parameter
   - `options` object: allowed_tools, permission_mode, max_turns, system_prompt
   - Streaming messages (async for message in query(...))
   - Message types (text, tool_use, tool_result)
   Lead with Python, follow immediately with TypeScript equivalent. Use realistic example: "Find and fix the bug in auth.py". Use CodeBlock for both.

6. **Built-in Tools** -- List all 10 tools: Read, Write, Edit, Bash, Monitor (note: new addition), Glob, Grep, WebSearch, WebFetch, AskUserQuestion. Brief description of each in a table or definition list. Show how to restrict tools via `allowed_tools` option.

7. **Hooks as Callbacks** -- KEY DIFFERENTIATOR from CLI hooks. Explain that in the CLI, hooks are JSON config files (link to [Hooks & Lifecycle Automation](/guides/claude-code/hooks/) for that approach). In the SDK, hooks are native callback functions. Show a Python example of a hook callback that logs tool usage. Briefly explain HookMatcher for filtering. Show both Python and TypeScript examples.

8. **Subagents** -- Explain AgentDefinition objects for creating programmatic subagents. Show how to define a subagent with specific tools and system prompt. Realistic example: a code review subagent. Use CodeBlock with Python.

9. **MCP Integration** -- Explain the `mcp_servers` option. Link to [Model Context Protocol](/guides/claude-code/mcp/) for MCP server setup. Show a brief example of passing an MCP server config to query(). Keep short -- the MCP chapter covers setup details.

10. **Permission Modes** -- Explain the `permission_mode` option. List available modes: default, plan, autoEdit, fullAutoEdit, dontAsk, auto. Brief description of each (link to Ch3 for full details). Show CodeBlock example with permission_mode="auto" for CI/CD use case.

11. **Session Management** -- Cover:
    - `resume: session_id` for continuing a previous session
    - Session forking for parallel exploration
    Show a brief example of session resume. Use CodeBlock with Python.

12. **Authentication** -- Cover the four providers:
    - ANTHROPIC_API_KEY (default, environment variable)
    - Amazon Bedrock
    - Google Vertex AI
    - Azure Foundry
    Show env var setup for the default case. Briefly mention the cloud provider options with links to official docs.

13. **SDK vs CLI Comparison** -- Create a comparison table using markdown table syntax (not CodeBlock):
    | Capability | CLI | SDK |
    Show: interactive sessions, CI/CD automation, hooks format, session management, MCP support, tool access, permission modes. Conclusion: same capabilities, different interfaces.

14. **Best Practices** -- 5-7 bullet points with bold lead-in. Cover: start with small allowed_tools set, use permission_mode="auto" for unattended CI, implement hooks for observability, resume sessions instead of starting fresh for iterative tasks, handle streaming messages properly, test with permission_mode="plan" before deploying with "auto".

15. **Further Reading** -- Link to official Agent SDK docs (https://code.claude.com/docs/en/agent-sdk). Next chapter link: "Next: [Computer Use](/guides/claude-code/computer-use/) -- control native desktop applications through Claude Code."

**Language balance (Claude's discretion per CONTEXT.md):**
Lead with Python for each section (more common in automation/CI use case). Show TypeScript equivalent immediately after for core sections (Installation, query() API, Hooks). For other sections, Python only with a note that TypeScript API mirrors Python.

**Style rules:**
- Professional but approachable, first-person "we'll" voice
- All CodeBlock components have required props: code (template literal), lang, title
- Cross-references: `[Chapter Name](/guides/claude-code/slug/)`
- Self-contained: briefly explain agent loop, hooks, MCP, skills on first mention
- Code examples are complete and runnable, not fragments
- One-line rename mention in "What You Will Learn" only, nowhere else
  </action>
  <verify>
1. File exists at src/data/guides/claude-code/pages/agent-sdk.mdx
2. Frontmatter has order: 12, slug: "agent-sdk"
3. CodeBlock import is correct
4. Quick Start section present with runnable Python example
5. Both Python and TypeScript shown for core APIs (Installation, query(), Hooks)
6. All 10 built-in tools listed
7. Rename mention appears exactly once, in "What You Will Learn"
8. Cross-references to Ch6 (/guides/claude-code/mcp/), Ch7 (/guides/claude-code/custom-skills/), Ch8 (/guides/claude-code/hooks/), Ch12 (/guides/claude-code/plugins/) present
9. SDK vs CLI comparison table present
10. File is approximately 450-500 lines
11. No JSX syntax errors or unclosed tags
  </verify>
  <done>
- agent-sdk.mdx created with all 15 sections
- Quick Start provides runnable Python example
- Python and TypeScript shown side-by-side for core APIs
- Hooks as callbacks clearly differentiated from CLI JSON hooks
- Subagents, MCP, permission modes, session management all covered
- Authentication providers documented (ANTHROPIC_API_KEY, Bedrock, Vertex, Azure)
- SDK vs CLI comparison table present
- One-line rename mention, then chapter moves on
- Self-contained with cross-references to Ch6, Ch7, Ch8, Ch12
- File is ~450-500 lines
  </done>
</task>

</tasks>

<verification>
1. Build check: File has valid MDX syntax (no unclosed JSX tags, proper CodeBlock props)
2. Frontmatter check: order=12, slug="agent-sdk", all required fields present
3. Content check: All 15 sections present in correct order
4. Quick Start check: Contains runnable Python installation + query() example
5. Language balance check: Python leads each section, TypeScript shown for core APIs
6. Cross-reference check: Links to Ch6, Ch7, Ch8, Ch12
7. Rename check: "Claude Code SDK" mentioned exactly once in What You Will Learn
8. Self-contained check: Agent loop, hooks, MCP, skills each briefly explained on first mention
</verification>

<success_criteria>
- Agent SDK chapter covers Python and TypeScript installation
- Agent SDK chapter documents the query() API with ClaudeAgentOptions
- Agent SDK chapter lists all 10 built-in tools
- Agent SDK chapter explains hooks as native callbacks (vs CLI JSON config)
- Agent SDK chapter covers subagents via AgentDefinition
- Agent SDK chapter covers MCP integration, permission modes, session management
- Agent SDK chapter documents all 4 authentication providers
- Agent SDK chapter includes SDK vs CLI comparison table
- Rename from Claude Code SDK mentioned once, then moved on
- Quick Start section is copy-pasteable and produces a working agent
- Chapter is self-contained with cross-references to Ch6, Ch7, Ch8, Ch12
</success_criteria>

<output>
After completion, create `.planning/phases/112-new-chapters/112-02-SUMMARY.md`
</output>
