---
phase: 112-new-chapters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/guides/claude-code/pages/plugins.mdx
autonomous: true

must_haves:
  truths:
    - "Reader understands what plugins are and how they relate to skills"
    - "Reader can create a complete working plugin from scratch using the Quick Start"
    - "Reader knows the full plugin.json manifest format and all plugin components"
    - "Reader knows how bin/ executables work and how Claude adds them to PATH"
    - "Reader knows how userConfig provides settings and keychain-backed secrets"
    - "Reader knows how to develop plugins locally and publish to the marketplace"
    - "Reader knows the security restrictions on plugin subagents"
  artifacts:
    - path: "src/data/guides/claude-code/pages/plugins.mdx"
      provides: "Complete Plugins chapter with manifest, bin/, userConfig, lifecycle, publishing"
      contains: "plugin.json"
  key_links:
    - from: "plugins.mdx"
      to: "custom-skills.mdx"
      via: "inline cross-reference"
      pattern: "/guides/claude-code/custom-skills/"
    - from: "plugins.mdx"
      to: "security.mdx"
      via: "inline cross-reference"
      pattern: "/guides/claude-code/security/"
    - from: "plugins.mdx"
      to: "hooks.mdx"
      via: "inline cross-reference"
      pattern: "/guides/claude-code/hooks/"
---

<objective>
Author Ch12 (Plugins) as a complete, self-contained guide chapter covering plugin authoring: manifest format, directory structure, bin/ executables, userConfig, namespacing, local development, marketplace publishing, and security restrictions. Include a Quick Start that produces a copy-pasteable working plugin.

Purpose: Plugins chapter fills the gap between Ch7 Skills (local instruction sets) and the shareable distribution ecosystem. Readers coming from Ch7 need a natural next step; readers jumping directly here need self-contained context.
Output: src/data/guides/claude-code/pages/plugins.mdx (~450-500 lines)
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-new-chapters/112-RESEARCH.md
@.planning/research/FEATURES-claude-code-guide-refresh.md

@src/data/guides/claude-code/pages/custom-skills.mdx
@src/data/guides/claude-code/pages/hooks.mdx
@src/data/guides/claude-code/guide.json
@src/components/guide/CodeBlock.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read reference materials and existing chapter patterns</name>
  <files>src/data/guides/claude-code/pages/custom-skills.mdx, src/data/guides/claude-code/pages/hooks.mdx, src/components/guide/CodeBlock.astro</files>
  <action>
Read the full Ch7 custom-skills.mdx (486 lines) to understand:
- Frontmatter schema (title, description, order, slug, lastVerified, updatedDate, keywords)
- CodeBlock import statement: `import CodeBlock from '../../../../components/guide/CodeBlock.astro';`
- "What You Will Learn" section structure and voice
- How code examples are formatted with CodeBlock (code prop, lang prop, title prop, caption prop)
- "Best Practices" section format
- "Further Reading" section format with next-chapter link pattern
- Cross-reference inline link format (e.g., `[Chapter Name](/guides/claude-code/slug/)`)

Read Ch7 specifically for how it mentions Plugins (around line 213) to ensure Ch12 bridges cleanly from that forward reference.

Read the feature research file (.planning/research/FEATURES-claude-code-guide-refresh.md) lines 339-363 for the exact Plugin system details: directory structure, plugin.json manifest, key behaviors (namespacing, --plugin-dir, /reload-plugins, userConfig, security restrictions, marketplace publishing).

Also read the research file (.planning/phases/112-new-chapters/112-RESEARCH.md) for the section outline, Quick Start pattern, and anti-patterns to avoid.
  </action>
  <verify>Full understanding of chapter structure, CodeBlock API, cross-reference format, and plugin feature details.</verify>
  <done>Complete mental model of existing chapter patterns and all plugin feature details needed to author Ch12</done>
</task>

<task type="auto">
  <name>Task 2: Author plugins.mdx chapter</name>
  <files>src/data/guides/claude-code/pages/plugins.mdx</files>
  <action>
Create the new file `src/data/guides/claude-code/pages/plugins.mdx` as a complete MDX chapter (~450-500 lines).

**Frontmatter:**
```yaml
---
title: "Plugins"
description: "Build shareable plugin packages with manifests, bin/ executables, userConfig, hooks, and marketplace publishing for distribution across teams and the community."
order: 11
slug: "plugins"
lastVerified: 2026-04-12
updatedDate: 2026-04-12
keywords: ["claude code plugins", "plugin manifest", "plugin marketplace", "bin executables", "userConfig", "plugin publishing", "plugin development", "plugin structure"]
---
```

**Import:**
```
import CodeBlock from '../../../../components/guide/CodeBlock.astro';
```

**Section structure (in order):**

1. **What You Will Learn** -- Brief paragraph listing what the chapter covers. Match existing chapter voice (professional, approachable, first-person "we'll"). Mention relationship to skills as one-liner: "Skills are local instruction sets -- Plugins are shareable packages that bundle skills, agents, hooks, MCP servers, and executables for distribution."

2. **Quick Start** -- NEW CONVENTION. "Get a working plugin running in under two minutes." Provide a complete, copy-pasteable bash script that creates a minimal plugin with:
   - `.claude-plugin/plugin.json` manifest
   - `skills/format-md/SKILL.md` skill file
   - Show how to load it: `claude --plugin-dir ./my-plugin`
   Use realistic "markdown-formatter" scenario per CONTEXT.md. Use CodeBlock with lang="bash" and title="Terminal".

3. **What Are Plugins?** -- Explain the concept. Bridge from Ch7 Skills: "If you've read [Custom Skills](/guides/claude-code/custom-skills/), you know how SKILL.md files extend Claude Code locally. Plugins take that concept further..." Explain that plugins are the distribution unit. Keep self-contained: briefly re-explain what skills are for readers who jumped here directly.

4. **Plugin Structure** -- Show the full directory layout from the feature research:
   ```
   my-plugin/
     .claude-plugin/
       plugin.json
     skills/
     commands/
     agents/
     hooks/
       hooks.json
     .mcp.json
     .lsp.json
     bin/
     settings.json
   ```
   Use CodeBlock with lang="text" and title="Plugin Directory Structure". Briefly describe each directory's purpose.

5. **The Plugin Manifest** -- Detail the `plugin.json` fields: name, description, version, author. Show a complete JSON example using CodeBlock with lang="json" and title=".claude-plugin/plugin.json". Explain each field.

6. **Plugin Components** -- Walk through each component type:
   - `skills/` -- SKILL.md directories (link to Ch7 for full skill anatomy)
   - `commands/` -- Legacy flat Markdown skills (backward compatible)
   - `agents/` -- AGENT.md files for plugin-specific agent personas
   - `hooks/hooks.json` -- Hook configurations using the same schema as project hooks (link to Ch8 for hook event reference)
   - `.mcp.json` -- MCP server configurations
   - `.lsp.json` -- LSP server configurations
   - `settings.json` -- Default settings (currently only `agent` key)

7. **bin/ Executables** -- Explain how Claude Code adds the plugin's `bin/` directory to PATH. Show a complete example: create a `bin/format-md` executable script that the skill invokes via Bash. Use CodeBlock with lang="bash" and title="bin/format-md". Explain the pattern: skill gives instructions, bin/ provides the tooling.

8. **userConfig** -- Explain settings at enable time. Cover keychain-backed secrets for API keys. Show a realistic example of a plugin that needs a GitHub token. Use CodeBlock for the config schema example.

9. **Namespacing** -- Explain the `/plugin-name:skill-name` pattern. Cover precedence rules when local skills conflict with plugin skills. Brief, focused section.

10. **Local Development** -- Cover the development workflow:
    - `--plugin-dir` flag for loading a local plugin directory
    - `/reload-plugins` to pick up changes without restarting
    Show both commands with CodeBlock examples.

11. **Publishing to Marketplace** -- Explain submission via claude.ai or Console. Keep high-level since the marketplace UX may change. Mention that published plugins are discoverable by the community.

12. **Security Restrictions** -- Important section. Cover: plugin subagents cannot use `hooks`, `mcpServers`, or `permissionMode`. Link to [Security & Enterprise Administration](/guides/claude-code/security/) for enterprise plugin governance (strictKnownMarketplaces, blockedMarketplaces, pluginTrustMessage).

13. **Best Practices** -- 5-7 bullet points with bold lead-in. Cover: start with skills only, add bin/ when shell tooling needed, keep manifests descriptive, test with --plugin-dir before publishing, use userConfig for secrets (not hardcoded), follow namespacing conventions.

14. **Further Reading** -- External links to official Plugins docs (https://code.claude.com/docs/en/plugins). Next chapter link: "Next: [Agent SDK](/guides/claude-code/agent-sdk/) -- embed Claude Code's agent loop into production applications."

**Style rules:**
- Professional but approachable, first-person "we'll" voice matching Ch7
- All CodeBlock components have required props: code (template literal), lang, title
- Cross-references use format: `[Chapter Name](/guides/claude-code/slug/)`
- No "What's New" callouts -- reads as if always written this way
- Self-contained: briefly re-explain any concept from prior chapters on first mention
- Code examples are complete and copy-pasteable, not fragments
- Use realistic scenarios (markdown-formatter plugin) per CONTEXT.md
  </action>
  <verify>
1. File exists at src/data/guides/claude-code/pages/plugins.mdx
2. Frontmatter has order: 11, slug: "plugins"
3. CodeBlock import is correct: `import CodeBlock from '../../../../components/guide/CodeBlock.astro';`
4. Quick Start section present with complete copy-pasteable plugin example
5. All CodeBlock components have code, lang, and title props
6. Cross-references to Ch7 (/guides/claude-code/custom-skills/), Ch8 (/guides/claude-code/hooks/), Ch11 (/guides/claude-code/security/) present
7. No undefined jargon -- skills, hooks, MCP explained briefly on first use
8. File is approximately 450-500 lines
9. No JSX syntax errors or unclosed tags
  </verify>
  <done>
- plugins.mdx created with all 14 sections
- Quick Start provides complete working plugin example
- Plugin manifest, bin/ executables, userConfig, namespacing, local dev, publishing all covered
- Security restrictions documented with link to Ch11
- Self-contained: all cross-chapter concepts briefly explained
- Cross-references to Ch7, Ch8, Ch11 present
- File is ~450-500 lines matching Ch7 length
  </done>
</task>

</tasks>

<verification>
1. Build check: File has valid MDX syntax (no unclosed JSX tags, proper CodeBlock props)
2. Frontmatter check: order=11, slug="plugins", all required fields present
3. Content check: All 14 sections present in correct order
4. Quick Start check: Contains complete bash commands to create and load a plugin
5. Cross-reference check: Links to /guides/claude-code/custom-skills/, /guides/claude-code/hooks/, /guides/claude-code/security/
6. Self-contained check: Skills, hooks, MCP each briefly explained on first mention
7. No deprecated terms or "What's New" callouts
</verification>

<success_criteria>
- Plugins chapter covers manifest format with complete JSON example
- Plugins chapter covers all plugin components (skills, commands, agents, hooks, MCP, LSP, bin/, settings)
- Plugins chapter covers bin/ executables with working example
- Plugins chapter covers userConfig with keychain-backed secrets
- Plugins chapter covers namespacing (/plugin-name:skill-name)
- Plugins chapter covers local development (--plugin-dir, /reload-plugins)
- Plugins chapter covers marketplace publishing
- Plugins chapter covers security restrictions on plugin subagents
- Quick Start section is copy-pasteable and produces a working plugin
- Chapter is self-contained with cross-references to Ch7, Ch8, Ch11
</success_criteria>

<output>
After completion, create `.planning/phases/112-new-chapters/112-01-SUMMARY.md`
</output>
