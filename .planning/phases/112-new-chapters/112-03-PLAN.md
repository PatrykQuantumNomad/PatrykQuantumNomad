---
phase: 112-new-chapters
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/guides/claude-code/pages/computer-use.mdx
autonomous: true

must_haves:
  truths:
    - "Reader understands the safety model before seeing any practical usage"
    - "Reader knows the per-app approval flow and what the prompt looks like"
    - "Reader knows the three app control tiers (view-only, click-only, full)"
    - "Reader can enable Computer Use via CLI (/mcp) and Desktop (Settings > General)"
    - "Reader understands CLI vs Desktop differences (macOS-only vs macOS+Windows)"
    - "Reader knows about screenshot auto-downscaling for Retina displays"
    - "Reader sees a prominent safety callout box linking to Ch11 Security"
  artifacts:
    - path: "src/data/guides/claude-code/pages/computer-use.mdx"
      provides: "Complete Computer Use chapter with safety model, approval flow, CLI/Desktop setup"
      contains: "per-app approval"
  key_links:
    - from: "computer-use.mdx"
      to: "security.mdx"
      via: "Callout component cross-reference"
      pattern: "/guides/claude-code/security/"
    - from: "computer-use.mdx"
      to: "mcp.mdx"
      via: "inline cross-reference"
      pattern: "/guides/claude-code/mcp/"
    - from: "computer-use.mdx"
      to: "models-and-costs.mdx"
      via: "inline cross-reference"
      pattern: "/guides/claude-code/models-and-costs/"
---

<objective>
Author Ch14 (Computer Use) as a complete, self-contained guide chapter with safety-first structure: per-app approval flow and safety model come before practical usage. Cover CLI and Desktop GUI setup, app control tiers, screenshots, and practical examples. Include a prominent safety callout box using the Callout.astro component linking to Ch11 Security.

Purpose: Computer Use gives Claude direct control over native desktop applications. The chapter must lead with safety because this feature operates outside the sandbox, unlike every other Claude Code tool.
Output: src/data/guides/claude-code/pages/computer-use.mdx (~350-400 lines)
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-new-chapters/112-RESEARCH.md
@.planning/research/FEATURES-claude-code-guide-refresh.md

@src/data/guides/claude-code/pages/custom-skills.mdx
@src/data/guides/claude-code/pages/security.mdx
@src/data/guides/claude-code/pages/mcp.mdx
@src/data/guides/claude-code/guide.json
@src/components/guide/CodeBlock.astro
@src/components/blog/Callout.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read reference materials and Computer Use feature details</name>
  <files>src/data/guides/claude-code/pages/custom-skills.mdx, src/data/guides/claude-code/pages/security.mdx, src/components/blog/Callout.astro</files>
  <action>
Read Ch7 custom-skills.mdx (486 lines) for chapter structure patterns: frontmatter, imports, section voice, CodeBlock usage, Best Practices format, Further Reading format.

Read Ch11 security.mdx briefly for how it references Computer Use and the permission model -- Ch14 needs to link back to this. Note the specific section headings/anchors for the cross-reference.

Read the Callout.astro component (28 lines) to confirm:
- Props: type ('info' | 'warning' | 'tip' | 'important'), title (string)
- Usage: `<Callout type="warning" title="...">content</Callout>`
- Import path from guide chapter MDX: `import Callout from '../../../../components/blog/Callout.astro';`

Read the feature research file (.planning/research/FEATURES-claude-code-guide-refresh.md) lines 249-281 for exact Computer Use details:
- CLI setup steps (4 steps: /mcp, enable computer-use server, grant macOS permissions, use)
- Per-session app approval (each app, warning tiers, session scope)
- Safety model (per-app approval, terminal excluded, Esc abort, machine-wide lock, hidden apps, NOT sandboxed)
- App control tiers (browsers+trading: view-only, terminals+IDEs: click-only, everything else: full)
- Screenshots auto-downscaling for Retina
- CLI vs Desktop differences (CLI: macOS only via /mcp, Desktop: macOS+Windows via Settings > General)

Read the research file (.planning/phases/112-new-chapters/112-RESEARCH.md) for section outline, safety callout pattern, and anti-patterns.
  </action>
  <verify>Full understanding of chapter patterns, Callout API, and all Computer Use feature details.</verify>
  <done>Complete mental model of existing chapter patterns, Callout component usage, and all Computer Use feature details</done>
</task>

<task type="auto">
  <name>Task 2: Author computer-use.mdx chapter</name>
  <files>src/data/guides/claude-code/pages/computer-use.mdx</files>
  <action>
Create the new file `src/data/guides/claude-code/pages/computer-use.mdx` as a complete MDX chapter (~350-400 lines).

**Frontmatter:**
```yaml
---
title: "Computer Use"
description: "Control native desktop applications through Claude Code with per-app approval, safety-first design, and CLI or Desktop GUI setup on macOS and Windows."
order: 13
slug: "computer-use"
lastVerified: 2026-04-12
updatedDate: 2026-04-12
keywords: ["claude code computer use", "desktop control", "gui automation", "per-app approval", "safety model", "screenshot", "mouse control", "keyboard automation"]
---
```

**Imports (TWO imports needed):**
```
import CodeBlock from '../../../../components/guide/CodeBlock.astro';
import Callout from '../../../../components/blog/Callout.astro';
```

**Section structure -- SAFETY-FIRST per CONTEXT.md decision:**

1. **What You Will Learn** -- Brief paragraph. Emphasize that Computer Use gives Claude direct control over native desktop applications (mouse, keyboard, screenshots). Mention the safety-first approach: "We'll start with the safety model and approval flow before covering practical usage."

2. **Quick Start** -- NEW CONVENTION. Brief numbered steps to get Computer Use working:
   1. Run `/mcp` in Claude Code
   2. Enable the `computer-use` server
   3. Grant macOS Accessibility and Screen Recording permissions when prompted
   4. Ask Claude to interact with a GUI app
   Use CodeBlock with lang="bash" title="Terminal" for the /mcp command. Keep this section short (the detail comes later).

3. **Safety Callout Box** -- PROMINENT. Use the Callout component IMMEDIATELY after Quick Start, before any detailed content:
   ```
   <Callout type="warning" title="Safety First: Computer Use Controls Real Applications">
     Computer Use gives Claude direct control over native desktop applications -- mouse clicks, keyboard input, and screenshots on your actual machine. Unlike the sandboxed Bash tool, Computer Use operates outside the sandbox. Every app requires explicit per-session approval, and you can press Escape at any time to abort. Review the full permission model in <a href="/guides/claude-code/security/">Security & Enterprise Administration</a> before enabling Computer Use in any production environment.
   </Callout>
   ```
   Note: Use an `<a>` tag inside the Callout since Markdown links may not render inside Astro component slots. Test both approaches in the verify step -- if Markdown links work in Callout slots, prefer `[Security & Enterprise Administration](/guides/claude-code/security/)`.

4. **Safety Model** -- Detailed section covering ALL safety mechanisms:
   - Per-app approval required each session (apps don't carry over)
   - Machine-wide lock: only one Computer Use session at a time
   - Escape key aborts from anywhere (consumed by Claude, not passed to dialogs)
   - Terminal excluded from screenshots (prevents feedback loops)
   - Other apps hidden during control (focus isolation)
   - NOT sandboxed like the Bash tool -- emphasize this difference
   Format as a bullet list with brief explanations after each point.

5. **Per-App Approval Flow** -- Explain what the user sees:
   - Claude requests access to a specific application
   - Prompt shows which apps Claude wants and extra permissions needed
   - Warning tiers with specific messages:
     * Terminals: "Equivalent to shell access"
     * Finder: "Can read/write any file"
     * System Settings: "Can change system settings"
   - Approval is per-session (resets when session ends)
   Use CodeBlock to show an example approval prompt (lang="text" title="Approval Prompt").

6. **App Control Tiers** -- Explain the three tiers clearly:
   - **View-only**: Browsers and trading platforms (Claude can see but not interact)
   - **Click-only**: Terminals and IDEs (Claude can click but not type)
   - **Full control**: Everything else (mouse + keyboard + screenshots)
   Present as a markdown table for scanability.

7. **Enabling Computer Use** -- Two subsections:
   - **CLI Setup** (macOS only): `/mcp` command, find and enable `computer-use` server, grant Accessibility + Screen Recording permissions. Step-by-step with CodeBlock.
   - **Desktop Setup** (macOS + Windows): Settings > General, toggle Computer Use on. Brief description since the GUI is self-explanatory.

8. **CLI vs Desktop Differences** -- Brief comparison:
   - CLI: macOS only, enable via `/mcp` command, terminal-integrated
   - Desktop: macOS + Windows, enable in Settings > General, standalone GUI
   Present as a markdown table.

9. **Screenshots and Retina Handling** -- Explain:
   - Claude takes screenshots to verify GUI state
   - Retina displays produce high-resolution screenshots (e.g., 3456x2234)
   - Auto-downscaled for the model (e.g., to ~1372x887)
   - This is automatic, no configuration needed
   Brief section, 5-8 lines.

10. **Practical Examples** -- CLI-focused examples per CONTEXT.md:
    - Example 1: Ask Claude to open a specific app and perform an action
    - Example 2: GUI testing scenario (verify a button exists in an app)
    Use CodeBlock with lang="bash" title="Terminal" showing the claude command and expected interaction.

11. **Best Practices** -- 5-7 bullet points with bold lead-in. Cover: always review the approval prompt, use view-only tiers for monitoring tasks, close sensitive apps before enabling, prefer CLI tools when available (Computer Use is for GUI-only tasks), test on non-critical apps first, press Escape immediately if something goes wrong.

12. **Further Reading** -- Link to official Computer Use docs (https://code.claude.com/docs/en/computer-use). Previous chapter link: "Previous: [Agent SDK](/guides/claude-code/agent-sdk/) -- embed Claude Code into production applications."

**Style rules:**
- Professional but approachable, first-person "we'll" voice
- SAFETY-FIRST: Safety model and approval flow BEFORE practical usage (sections 3-6 before 7-10)
- All CodeBlock components have required props: code (template literal), lang, title
- Cross-references: `[Chapter Name](/guides/claude-code/slug/)`
- Self-contained: briefly explain MCP, permissions, sandbox on first mention
- CLI-focused examples per CONTEXT.md (commands, flags, terminal output)
- Callout component used for safety warning (type="warning")
  </action>
  <verify>
1. File exists at src/data/guides/claude-code/pages/computer-use.mdx
2. Frontmatter has order: 13, slug: "computer-use"
3. Both imports present: CodeBlock AND Callout
4. Safety callout box rendered with Callout component (type="warning")
5. Safety callout links to /guides/claude-code/security/
6. Safety model section comes BEFORE practical usage sections
7. Per-app approval flow documented with warning tiers
8. Three app control tiers documented (view-only, click-only, full)
9. CLI and Desktop setup both documented
10. Quick Start section present
11. File is approximately 350-400 lines
12. No JSX syntax errors or unclosed tags
13. Cross-references to Ch3 (/guides/claude-code/models-and-costs/), Ch6 (/guides/claude-code/mcp/), Ch11 (/guides/claude-code/security/) present
  </verify>
  <done>
- computer-use.mdx created with all 12 sections
- Safety-first structure: callout and safety model precede practical usage
- Callout.astro component imported and used for prominent safety warning
- Per-app approval flow documented with warning tiers
- Three app control tiers in table format
- CLI and Desktop setup instructions
- CLI vs Desktop comparison table
- Screenshot/Retina auto-downscaling documented
- CLI-focused practical examples
- Self-contained with cross-references to Ch3, Ch6, Ch11
- File is ~350-400 lines
  </done>
</task>

</tasks>

<verification>
1. Build check: File has valid MDX syntax, Callout component renders without error
2. Frontmatter check: order=13, slug="computer-use", all required fields present
3. Content check: All 12 sections present in correct order
4. Safety-first check: Sections 3-6 (safety callout, safety model, approval flow, control tiers) come before sections 7-10 (setup, differences, screenshots, examples)
5. Callout check: Callout component imported and used with type="warning" and link to Ch11
6. Cross-reference check: Links to Ch3, Ch6, Ch11
7. Self-contained check: MCP, permissions, sandbox explained on first mention
</verification>

<success_criteria>
- Computer Use chapter has prominent safety callout box using Callout.astro linking to Ch11
- Computer Use chapter documents all safety model mechanisms (per-app approval, machine lock, Esc abort, terminal exclusion, hidden apps, no sandbox)
- Computer Use chapter documents per-app approval flow with warning tiers
- Computer Use chapter documents three app control tiers (view-only, click-only, full)
- Computer Use chapter documents both CLI and Desktop setup procedures
- Computer Use chapter documents CLI vs Desktop differences
- Computer Use chapter documents screenshot auto-downscaling
- Safety sections precede practical usage sections (safety-first structure)
- Quick Start section present with enabling steps
- Chapter is self-contained with cross-references to Ch3, Ch6, Ch11
</success_criteria>

<output>
After completion, create `.planning/phases/112-new-chapters/112-03-SUMMARY.md`
</output>
