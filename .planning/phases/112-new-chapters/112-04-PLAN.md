---
phase: 112-new-chapters
plan: 04
type: execute
wave: 2
depends_on: ["112-01", "112-02", "112-03"]
files_modified:
  - src/data/guides/claude-code/guide.json
  - src/pages/index.astro
  - src/pages/guides/claude-code/index.astro
  - src/pages/guides/claude-code/[slug].astro
autonomous: true

must_haves:
  truths:
    - "All three new chapters appear in guide.json with correct slugs matching their MDX frontmatter"
    - "Guide description says 14 chapters (not 11)"
    - "Site-wide references to '11 chapters' are updated to '14 chapters'"
    - "Sidebar navigation shows Plugins, Agent SDK, Computer Use after Security"
    - "OG images auto-generate for all three new chapters at build time"
  artifacts:
    - path: "src/data/guides/claude-code/guide.json"
      provides: "Updated guide registry with 14 chapter entries"
      contains: "plugins"
  key_links:
    - from: "guide.json"
      to: "plugins.mdx"
      via: "slug match"
      pattern: "\"slug\": \"plugins\""
    - from: "guide.json"
      to: "agent-sdk.mdx"
      via: "slug match"
      pattern: "\"slug\": \"agent-sdk\""
    - from: "guide.json"
      to: "computer-use.mdx"
      via: "slug match"
      pattern: "\"slug\": \"computer-use\""
---

<objective>
Register all three new chapters in guide.json and update every site-wide "11 chapters" reference to "14 chapters." Verify that the complete guide structure is consistent: slugs match, ordering is correct, OG route auto-discovers new pages, and no stale chapter counts remain.

Purpose: Without guide.json registration, the new MDX files exist but don't appear in the sidebar, navigation, or OG image generation. Without updating the chapter count references, the site displays stale information.
Output: Updated guide.json + 3 site files with corrected chapter counts + verification pass
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-new-chapters/112-RESEARCH.md

@src/data/guides/claude-code/guide.json
@src/pages/index.astro
@src/pages/guides/claude-code/index.astro
@src/pages/guides/claude-code/[slug].astro
@src/pages/open-graph/guides/claude-code/[slug].png.ts

# Need to verify slugs match the MDX files created by Plans 01-03
@.planning/phases/112-new-chapters/112-01-SUMMARY.md
@.planning/phases/112-new-chapters/112-02-SUMMARY.md
@.planning/phases/112-new-chapters/112-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register new chapters in guide.json and update chapter count references</name>
  <files>src/data/guides/claude-code/guide.json, src/pages/index.astro, src/pages/guides/claude-code/index.astro, src/pages/guides/claude-code/[slug].astro</files>
  <action>
**Step 1: Verify MDX slugs.** Read the frontmatter of all three new MDX files to confirm their exact slugs:
- `src/data/guides/claude-code/pages/plugins.mdx` -- expect slug: "plugins"
- `src/data/guides/claude-code/pages/agent-sdk.mdx` -- expect slug: "agent-sdk"
- `src/data/guides/claude-code/pages/computer-use.mdx` -- expect slug: "computer-use"

**Step 2: Update guide.json.** Edit `src/data/guides/claude-code/guide.json`:

a) Update the top-level `description` field from "11 practitioner-driven chapters" to "14 practitioner-driven chapters". Also add "plugins, agent SDK, and computer use" to the description text. New description:
```
"The definitive zero-to-hero guide for Claude Code, Anthropic's agentic coding tool. Master CLAUDE.md, MCP integrations, custom skills, hooks, worktrees, agent teams, plugins, the Agent SDK, and computer use across 14 practitioner-driven chapters."
```

b) Add three new chapter entries to the `chapters` array, AFTER the existing security entry (last in current array). Maintain the exact JSON format of existing entries. The slugs MUST match the MDX frontmatter exactly:
```json
{ "slug": "plugins", "title": "Plugins", "description": "Build shareable plugin packages with manifests, bin/ executables, userConfig, hooks, and marketplace publishing for distribution across teams and the community." },
{ "slug": "agent-sdk", "title": "Agent SDK", "description": "Use the Python and TypeScript SDKs to embed Claude Code's agent loop, tools, and context management into production applications and CI/CD pipelines." },
{ "slug": "computer-use", "title": "Computer Use", "description": "Control native desktop applications through Claude Code with per-app approval, safety-first design, and CLI or Desktop GUI setup on macOS and Windows." }
```

**Step 3: Update "11 chapters" references site-wide.** The following files contain hardcoded "11 chapters" or "11" references that need updating:

a) `src/pages/index.astro` (line ~289): Change "11 chapters" to "14 chapters" in the guide callout text.

b) `src/pages/guides/claude-code/[slug].astro` (line ~49): Change "all 11 chapters" to "all 14 chapters" in the companion blog post label.

c) `src/pages/guides/claude-code/index.astro` (line ~5): Change "all 11 chapters" to "all 14 chapters" in the comment.

**Do NOT modify** the blog post file (`src/data/blog/claude-code-guide.mdx`) -- that blog post was written when there were 11 chapters and refers to its historical content. The Phase 115 blog post will handle any blog updates. Changing the existing blog post is out of scope for this phase.

**Step 4: Verify OG image route.** Read `src/pages/open-graph/guides/claude-code/[slug].png.ts` to confirm it uses `getCollection('claudeCodePages')` or similar dynamic discovery. Confirm no hardcoded list of slugs. If it uses dynamic discovery (expected), no changes needed.
  </action>
  <verify>
1. guide.json has exactly 14 entries in the chapters array
2. guide.json slugs "plugins", "agent-sdk", "computer-use" match MDX frontmatter slugs exactly
3. guide.json top-level description says "14 practitioner-driven chapters"
4. guide.json is valid JSON (no trailing commas, proper formatting)
5. src/pages/index.astro says "14 chapters" (not 11)
6. src/pages/guides/claude-code/[slug].astro says "all 14 chapters" (not 11)
7. src/pages/guides/claude-code/index.astro comment says "all 14 chapters" (not 11)
8. No remaining "11 chapters" references in src/ (except the blog post which is intentionally unchanged)
9. OG image route confirmed to use dynamic discovery (no changes needed)
  </verify>
  <done>
- guide.json updated with 3 new chapter entries (slugs verified against MDX files)
- guide.json description updated from "11" to "14" practitioner-driven chapters
- All site-wide "11 chapters" references updated to "14 chapters" (3 files)
- Blog post intentionally left unchanged (Phase 115 scope)
- OG image route confirmed dynamic (no changes needed)
  </done>
</task>

<task type="auto">
  <name>Task 2: Cross-chapter verification sweep</name>
  <files>src/data/guides/claude-code/pages/plugins.mdx, src/data/guides/claude-code/pages/agent-sdk.mdx, src/data/guides/claude-code/pages/computer-use.mdx, src/data/guides/claude-code/guide.json</files>
  <action>
Run a comprehensive verification sweep across all new content and integration:

**Slug consistency check:**
- Grep all three new MDX files for their `slug:` frontmatter value
- Verify each matches the corresponding guide.json chapter slug exactly
- Verify each `order:` value: plugins=11, agent-sdk=12, computer-use=13

**Cross-reference integrity check:**
- Grep plugins.mdx for links to /guides/claude-code/custom-skills/, /guides/claude-code/hooks/, /guides/claude-code/security/
- Grep agent-sdk.mdx for links to /guides/claude-code/hooks/, /guides/claude-code/custom-skills/, /guides/claude-code/mcp/, /guides/claude-code/plugins/
- Grep computer-use.mdx for links to /guides/claude-code/security/, /guides/claude-code/mcp/, /guides/claude-code/models-and-costs/
- Verify all linked slugs exist in guide.json

**Quick Start check:**
- Verify all three chapters have a "## Quick Start" section

**Self-contained check:**
- Grep plugins.mdx for brief explanations of "skills" and "hooks" concepts
- Grep agent-sdk.mdx for brief explanations of "agent loop", "hooks", and "MCP"
- Grep computer-use.mdx for brief explanation of "sandbox" and "MCP"

**Safety-first check (Computer Use):**
- Verify the Callout component import exists in computer-use.mdx
- Verify the Callout component is used with type="warning"
- Verify the safety callout appears before practical usage sections

**Chapter count check:**
- Grep entire src/ directory for "11 chapters" or "11 practitioner" -- should only appear in the blog post file
- Verify guide.json description says "14"

**Build attempt:**
- Run `npx astro check` or `npm run build` to verify all MDX files compile without errors
- If build fails due to MDX syntax issues in any of the three new files, fix the syntax error in that file

**If any issue found:** Fix it directly. Common fixes:
- Slug mismatch: update guide.json to match MDX (MDX is source of truth)
- Missing cross-reference: add the link in the appropriate section
- Missing Quick Start: this indicates a major plan execution error -- flag in summary
- Build error: fix the MDX syntax (unclosed tags, wrong CodeBlock props, import issues)
  </action>
  <verify>
1. All three slugs match between MDX and guide.json
2. All cross-references point to valid slugs
3. All three chapters have Quick Start sections
4. Computer Use has Callout import and safety-first structure
5. No "11 chapters" references remain (except blog post)
6. Build passes without errors
  </verify>
  <done>
- All slugs verified consistent between MDX frontmatter and guide.json
- All cross-references verified pointing to valid chapter slugs
- All three Quick Start sections confirmed present
- Computer Use safety-first structure confirmed
- No stale "11 chapters" references outside blog post
- Build passes clean
  </done>
</task>

</tasks>

<verification>
1. guide.json: 14 entries in chapters array, all slugs valid, description says "14"
2. Site-wide: No "11 chapters" in rendered pages (blog post excepted)
3. Build: All 14 chapters compile, OG images generate for new chapters
4. Navigation: New chapters appear after Security in sidebar order
5. Cross-references: All internal links point to valid slugs
6. Self-contained: Each new chapter explains cross-chapter concepts on first use
</verification>

<success_criteria>
- guide.json has 14 chapter entries with correct slugs, titles, and descriptions
- guide.json top-level description references 14 chapters
- Site-wide "11 chapters" references updated to "14" (blog post excepted)
- All three new chapter slugs match between MDX frontmatter and guide.json
- OG images auto-generate for plugins, agent-sdk, computer-use at build time
- Build passes with no MDX compilation errors
- Cross-references between all chapters verified
</success_criteria>

<output>
After completion, create `.planning/phases/112-new-chapters/112-04-SUMMARY.md`
</output>
