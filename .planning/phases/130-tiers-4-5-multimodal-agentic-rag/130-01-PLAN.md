---
phase: 130-tiers-4-5-multimodal-agentic-rag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "../rag-architecture-patterns/pyproject.toml"
  - "../rag-architecture-patterns/.env.example"
  - "../rag-architecture-patterns/.gitignore"
  - "../rag-architecture-patterns/tier-4-multimodal/requirements.txt"
  - "../rag-architecture-patterns/tier-5-agentic/requirements.txt"
autonomous: true
requirements: [TIER-04, TIER-05, REPO-04]

must_haves:
  truths:
    - "`uv pip install --dry-run -e \".[tier-4]\"` resolves cleanly with raganything==1.2.10, lightrag-hku==1.4.15 (already from [tier-3]), openai>=1.50,<3, Pillow>=10,<12 — exact pins per Pitfall 2 (raganything churn mirrors LightRAG)"
    - "`uv pip install --dry-run -e \".[tier-5]\"` resolves cleanly with openai-agents[litellm]==0.14.6, chromadb>=1.5.8,<2 (Tier 1 reuse), openai>=1.50,<3"
    - "tests/test_tier_requirements.py::test_lockfile_does_not_contain_deprecated_sdk still passes — raganything 1.2.10 transitive deps do NOT pull `google-generativeai`; openai-agents 0.14.6 transitive deps do NOT pull `google-generativeai`"
    - ".env.example documents OPENROUTER_API_KEY as REQUIRED for Tier 1 + Tier 3 + Tier 4 + Tier 5"
    - ".gitignore covers `rag_anything_storage/`, `tier-4-multimodal/.cache/`, `tier-4-multimodal/output/` so RAG-Anything's working dir + MineRU intermediate output never get committed"
    - "tier-4-multimodal/requirements.txt and tier-5-agentic/requirements.txt remain `-e ..[tier-N]` mirrors (single source of truth in pyproject)"
    - "shared/embeddings.py, shared/llm.py, shared/pricing.py NOT modified — Phase 127/128/129 contracts preserved; existing pricing slugs cover all Phase 130 models (google/gemini-2.5-flash, openai/text-embedding-3-small)"
  artifacts:
    - path: "../rag-architecture-patterns/pyproject.toml"
      provides: "Concrete tier-4 (raganything==1.2.10, lightrag-hku==1.4.15, openai, Pillow) and tier-5 (openai-agents[litellm]==0.14.6, chromadb, openai) extras"
      contains: "raganything==1.2.10"
    - path: "../rag-architecture-patterns/.env.example"
      provides: "Updated comment block — OPENROUTER_API_KEY now required for Tier 4 (RAG-Anything) and Tier 5 (Agents SDK via LiteLLM)"
      contains: "Tier 4"
    - path: "../rag-architecture-patterns/.gitignore"
      provides: "rag_anything_storage/ + MineRU intermediate dirs ignored"
      contains: "rag_anything_storage"
  key_links:
    - from: "tier-4-multimodal/requirements.txt"
      to: "pyproject.toml [project.optional-dependencies.tier-4]"
      via: "editable install of parent extras"
      pattern: "-e \\.\\.\\[tier-4\\]"
    - from: "tier-5-agentic/requirements.txt"
      to: "pyproject.toml [project.optional-dependencies.tier-5]"
      via: "editable install of parent extras"
      pattern: "-e \\.\\.\\[tier-5\\]"
    - from: "pyproject.toml [tier-4]"
      to: "raganything==1.2.10 + lightrag-hku==1.4.15"
      via: "RAG-Anything composes a LightRAG instance; Phase 129's pin transfers"
      pattern: "raganything==1.2.10"
---

<objective>
Concretize the empty `[tier-4]` and `[tier-5]` extras into pinned dependency sets per 130-RESEARCH.md Standard Stack. Pin `raganything==1.2.10` and `openai-agents[litellm]==0.14.6` exactly (Pitfall 2 — both projects ship dot releases that break APIs every few weeks; mirrors Phase 129's `lightrag-hku==1.4.15` decision). Update `.gitignore` for `rag_anything_storage/` (RAG-Anything's working_dir) and MineRU intermediate dirs. Promote `OPENROUTER_API_KEY` documentation in `.env.example` from "Tier 1 + Tier 3" to "Tier 1 + Tier 3 + Tier 4 + Tier 5". This is the Phase 130 foundation plan — every downstream plan depends on this lock landing.

Purpose: Lock the Phase 130 dependency contract before any Tier 4 / Tier 5 code lands. Mirrors Phase 128 Plan 01 + Phase 129 Plan 01 — extras-first, code-second. Phase 127 Plan 01 established this pattern; the lockfile guard in `tests/test_tier_requirements.py` enforces no `google-generativeai` regression on every test run.

Output: 5 files modified — `pyproject.toml`, `.env.example`, `.gitignore`, `tier-4-multimodal/requirements.txt`, `tier-5-agentic/requirements.txt`.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-tiers-4-5-multimodal-agentic-rag/130-RESEARCH.md

<interfaces>
<!-- All values locked from 130-RESEARCH.md "Standard Stack" + "User Constraints" + Pitfalls 2 + 9 -->

`pyproject.toml` `[project.optional-dependencies]` updates:

```toml
tier-4 = [
  "rag-architecture-patterns[shared]",
  "raganything==1.2.10",
  "lightrag-hku==1.4.15",      # already in [tier-3]; explicit here for clarity
  "openai>=1.50,<3",            # for OpenRouter routing through lightrag.llm.openai
  "Pillow>=10,<12",             # img_path validation in insert_content_list (Pitfall 4)
]
tier-5 = [
  "rag-architecture-patterns[shared]",
  "openai-agents[litellm]==0.14.6",
  "chromadb>=1.5.8,<2",         # read Tier 1's collection
  "openai>=1.50,<3",            # transitive via litellm + Tier 1 embedder reuse
]
```

`.gitignore` ADDITIONS (append; do not replace existing entries):

```
# Phase 130 — Tier 4 RAG-Anything working dir + MineRU intermediate dirs
rag_anything_storage/
tier-4-multimodal/.cache/
tier-4-multimodal/output/
```

(Verify: `lightrag_storage/` and `chroma_db/` should already be gitignored from Phases 128/129.)

`.env.example` UPDATE (find and update the OPENROUTER_API_KEY comment block):

Before:
```
# OPENROUTER_API_KEY: required for Tier 1 (Phase 128) + Tier 3 (Phase 129).
```

After:
```
# OPENROUTER_API_KEY: required for Tier 1 (Phase 128), Tier 3 (Phase 129),
# Tier 4 (Phase 130 — RAG-Anything LLM/vision/embed), Tier 5 (Phase 130 — Agents SDK via LiteLLM).
```

`tier-4-multimodal/requirements.txt` and `tier-5-agentic/requirements.txt` already contain `-e ..[tier-N]`. Verify both unchanged.

Pitfalls re-checked:
- **Pitfall 2** (raganything version churn): EXACT pin `raganything==1.2.10`. NOT `>=1.2,<2`.
- **Pitfall 9** (ChromaDB collision): no work here — Tier 5 will read-only Tier 1's path; this plan only ensures `chromadb` is available.
- **Lockfile guard** (`tests/test_tier_requirements.py`): the test fails if `google-generativeai` appears in `uv.lock`. Both `raganything==1.2.10` (built atop `lightrag-hku`, which uses `google-genai` not the deprecated sibling) and `openai-agents==0.14.6` (no Google deps) are clean — verified via PyPI dependency graph in research. After this plan's `uv sync`, re-run the lockfile guard test to confirm.

Decisions referenced (130-RESEARCH.md): Standard Stack table; Required `pyproject.toml` updates section; Required `.gitignore` updates section; Required `.env.example` updates section; Pitfall 2 (raganything churn).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Concretize [tier-4] + [tier-5] extras + update .gitignore + .env.example</name>
  <files>
    ../rag-architecture-patterns/pyproject.toml
    ../rag-architecture-patterns/.env.example
    ../rag-architecture-patterns/.gitignore
  </files>
  <action>
All work happens in `../rag-architecture-patterns/`.

1. Open `pyproject.toml` and find the `[project.optional-dependencies]` section. Locate the existing `tier-4 = [...]` and `tier-5 = [...]` stubs (each currently `["rag-architecture-patterns[shared]"]` only). Replace each with the multi-line lists from `<interfaces>`. Preserve formatting — match the indentation pattern of the existing `[tier-1]` and `[tier-3]` entries (4 spaces, one dep per line, trailing comma).

2. Open `.gitignore` and append the Phase 130 block from `<interfaces>` (append at the END of the file, with a blank-line separator from prior entries). Verify `lightrag_storage/` and `chroma_db/` are already covered by prior phases — if not, do NOT add them here (out of scope; that's a different plan's regression).

3. Open `.env.example` and find the `OPENROUTER_API_KEY` comment block. Update the "required for" sentence to include Tier 4 and Tier 5 per `<interfaces>`. Do NOT change the secret VALUE (empty string) and do NOT add new env vars.

4. Run dry-run resolution to validate both extras resolve cleanly without conflicts:

   ```
   cd ../rag-architecture-patterns
   uv pip install --dry-run -e ".[tier-4]" 2>&1 | tail -5
   uv pip install --dry-run -e ".[tier-5]" 2>&1 | tail -5
   ```

   Expect: "Would install N packages" with no resolution errors. If raganything==1.2.10 has a conflicting transitive (e.g. an `openai>=2` upper bound clash), STOP and surface the conflict — do not weaken pins.

5. Run the lockfile-guard regression to ensure `google-generativeai` did not sneak back in via raganything's transitives:

   ```
   uv sync --extra tier-4 --extra tier-5 2>&1 | tail -5
   uv run pytest tests/test_tier_requirements.py -v
   ```

   Expect: 5/5 tests pass (4 existing + 1 lockfile guard).

6. Commit + push:

   ```
   git add pyproject.toml .gitignore .env.example
   git commit -m "deps(130-01): concretize [tier-4] + [tier-5] extras (raganything==1.2.10, openai-agents[litellm]==0.14.6)"
   git push origin main
   ```

7. **Lockfile follow-on** (Rule 3): if `uv sync` regenerated `uv.lock`, commit it as a separate atomic chore commit per Phase 128/129 Plan 01 precedent:

   ```
   git status
   # If uv.lock is dirty:
   git add uv.lock
   git commit -m "chore(130-01): regenerate uv.lock after [tier-4]+[tier-5] concretization"
   git push origin main
   ```

DO NOT amend the deps commit — keeps the deps-vs-lockfile diff narrow per GSD git protocol.
  </action>
  <verify>
    <automated>
cd ../rag-architecture-patterns &amp;&amp; \
  grep -q 'raganything==1.2.10' pyproject.toml &amp;&amp; \
  grep -q 'openai-agents\[litellm\]==0.14.6' pyproject.toml &amp;&amp; \
  grep -q 'rag_anything_storage' .gitignore &amp;&amp; \
  grep -q 'Tier 4' .env.example &amp;&amp; \
  grep -q 'Tier 5' .env.example &amp;&amp; \
  uv pip install --dry-run -e ".[tier-4]" &gt; /dev/null 2&gt;&amp;1 &amp;&amp; \
  uv pip install --dry-run -e ".[tier-5]" &gt; /dev/null 2&gt;&amp;1 &amp;&amp; \
  uv run pytest tests/test_tier_requirements.py -q
    </automated>
  </verify>
  <done>
    `[tier-4]` and `[tier-5]` extras concretized with exact pins (raganything==1.2.10, openai-agents[litellm]==0.14.6) + supporting deps (lightrag-hku==1.4.15, openai, Pillow, chromadb).
    `.gitignore` covers `rag_anything_storage/` + MineRU intermediate dirs.
    `.env.example` documents OPENROUTER_API_KEY as required for Tier 4 + Tier 5.
    Both extras dry-run resolve cleanly.
    Lockfile guard test still passes (no `google-generativeai` regression).
    Commits pushed (deps + lockfile follow-on if needed).
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify per-tier requirements.txt mirrors are unchanged + smoke import</name>
  <files>
    ../rag-architecture-patterns/tier-4-multimodal/requirements.txt
    ../rag-architecture-patterns/tier-5-agentic/requirements.txt
  </files>
  <action>
All work happens in `../rag-architecture-patterns/`.

1. Verify both per-tier requirements.txt files contain the editable-parent mirror (single source of truth in pyproject). They were created in Phase 127 and should remain unchanged in this plan:

   ```
   cat tier-4-multimodal/requirements.txt
   # expect: -e ..[tier-4]
   cat tier-5-agentic/requirements.txt
   # expect: -e ..[tier-5]
   ```

   If either file deviates from this pattern, restore it. Do NOT inline pins; the pyproject is the single source of truth.

2. Run a smoke install + import to confirm the new deps actually load (this is NOT a code-execution test — just `python -c "import X"`):

   ```
   uv sync --extra tier-4 --extra tier-5 2>&1 | tail -5
   uv run python -c "import raganything; print('raganything', raganything.__version__)"
   uv run python -c "import agents; print('agents', getattr(agents, '__version__', 'n/a'))"
   uv run python -c "from agents.extensions.models.litellm_model import LitellmModel; print('LitellmModel OK')"
   uv run python -c "import chromadb; print('chromadb', chromadb.__version__)"
   ```

   Expect:
   - `raganything 1.2.10`
   - `agents` import succeeds (version may print as `n/a` since 0.x SDKs don't always expose `__version__`)
   - `LitellmModel OK` printed
   - `chromadb 1.5.x`

   If `import raganything` fails with a missing-extra error like `mineru not installed`, that's a TIER-04 install issue — surface it; don't paper over by adding `mineru` separately to `[tier-4]` (it's a transitive of `raganything`).

3. **Plan-level verification (no commit needed for Task 2)** — this task is a validation-only step. If anything in step 2 fails, fix it in Task 1 (re-pin or adjust the extras) and re-run; do NOT commit a workaround in Task 2 because Task 1 owns the deps file.

4. **Optional: confirm rag_anything_storage/ does not exist yet** (no early-bird artifacts):

   ```
   test ! -d rag_anything_storage || echo "WARNING: rag_anything_storage/ exists already; was Tier 4 ingested before deps were concretized?"
   ```

   If the directory exists from a stale local run, leave it alone — it's gitignored after Task 1 lands.

Decisions referenced (130-RESEARCH.md): User Constraints "Per-tier requirements.txt files for isolated dependency installation" (REPO-04 contract from Phase 127); Pitfall 2 (exact pins on rapidly-churning HKUDS releases).
  </action>
  <verify>
    <automated>
cd ../rag-architecture-patterns &amp;&amp; \
  grep -q '\-e \.\.\[tier-4\]' tier-4-multimodal/requirements.txt &amp;&amp; \
  grep -q '\-e \.\.\[tier-5\]' tier-5-agentic/requirements.txt &amp;&amp; \
  uv run python -c "import raganything; assert raganything.__version__ == '1.2.10'" &amp;&amp; \
  uv run python -c "from agents.extensions.models.litellm_model import LitellmModel" &amp;&amp; \
  uv run python -c "import chromadb"
    </automated>
  </verify>
  <done>
    `tier-4-multimodal/requirements.txt` is `-e ..[tier-4]` (verbatim).
    `tier-5-agentic/requirements.txt` is `-e ..[tier-5]` (verbatim).
    `import raganything` succeeds; version pin holds.
    `import agents` + `LitellmModel` import OK.
    `import chromadb` OK.
    No commit on this task (Task 1 owns the dependency change set).
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run from inside `../rag-architecture-patterns/`:

```
# 1. pyproject extras concretized + lockfile clean
grep -A2 '^tier-4 ' pyproject.toml
grep -A2 '^tier-5 ' pyproject.toml
uv pip install --dry-run -e ".[tier-4]"
uv pip install --dry-run -e ".[tier-5]"
uv run pytest tests/test_tier_requirements.py -q

# 2. .env.example + .gitignore updates
grep -A1 'OPENROUTER_API_KEY' .env.example | head -3
grep 'rag_anything_storage' .gitignore

# 3. Smoke imports
uv run python -c "import raganything, agents, chromadb; print('ALL OK')"
```
</verification>

<success_criteria>
- `[tier-4]` extras: `raganything==1.2.10`, `lightrag-hku==1.4.15`, `openai>=1.50,<3`, `Pillow>=10,<12`, plus shared.
- `[tier-5]` extras: `openai-agents[litellm]==0.14.6`, `chromadb>=1.5.8,<2`, `openai>=1.50,<3`, plus shared.
- Both extras resolve cleanly with `uv pip install --dry-run`.
- Lockfile guard test (`tests/test_tier_requirements.py::test_lockfile_does_not_contain_deprecated_sdk`) still passes.
- `.gitignore` covers `rag_anything_storage/` + MineRU intermediate dirs.
- `.env.example` documents OPENROUTER_API_KEY as REQUIRED for Tier 4 + Tier 5.
- `tier-4-multimodal/requirements.txt` and `tier-5-agentic/requirements.txt` remain `-e ..[tier-N]` mirrors.
- `import raganything`, `import agents`, `import chromadb` all succeed in the synced venv.
- Atomic commit (deps) + optional Rule-3 follow-on (lockfile) both pushed.
</success_criteria>

<output>
After completion, create `.planning/phases/130-tiers-4-5-multimodal-agentic-rag/130-01-SUMMARY.md` documenting:
- Final `[tier-4]` and `[tier-5]` resolution sizes (number of packages added by `uv pip install --dry-run`)
- Whether `uv.lock` was regenerated (and the follow-on commit SHA if so)
- Smoke-import output (raganything version, agents import status, LitellmModel import OK, chromadb version)
- Confirmation that the lockfile guard test still passes (no `google-generativeai` regression)
- Any deviations from the locked dependency list
</output>
