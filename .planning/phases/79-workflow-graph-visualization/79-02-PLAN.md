---
phase: 79-workflow-graph-visualization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/tools/gha-results/GhaGraphSkeleton.tsx
  - src/components/tools/gha-results/gha-graph.css
  - src/components/tools/gha-results/GhaTriggerNode.tsx
  - src/components/tools/gha-results/GhaJobNode.tsx
  - src/components/tools/gha-results/GhaStepNode.tsx
  - src/components/tools/gha-results/GhaDependencyEdge.tsx
autonomous: true
requirements:
  - GRAPH-01
  - GRAPH-04
  - GRAPH-05

must_haves:
  truths:
    - "Trigger node displays event name with indigo border and source handle on right"
    - "Job node displays job name with color-coded border (green/amber/red) and contains step children"
    - "Step node displays step label with color-coded left border inside parent job"
    - "Dependency edge shows smooth step path with cycle edges highlighted in red"
    - "Graph skeleton shows loading animation while React Flow lazy-loads"
    - "Dark theme CSS overrides match existing compose-graph and k8s-graph patterns"
  artifacts:
    - path: "src/components/tools/gha-results/GhaTriggerNode.tsx"
      provides: "Custom React Flow trigger event node"
      exports: ["GhaTriggerNode", "GhaTriggerNodeData", "GhaTriggerNodeType"]
    - path: "src/components/tools/gha-results/GhaJobNode.tsx"
      provides: "Custom React Flow job container node"
      exports: ["GhaJobNode", "GhaJobNodeData", "GhaJobNodeType"]
    - path: "src/components/tools/gha-results/GhaStepNode.tsx"
      provides: "Custom React Flow step node"
      exports: ["GhaStepNode", "GhaStepNodeData", "GhaStepNodeType"]
    - path: "src/components/tools/gha-results/GhaDependencyEdge.tsx"
      provides: "Custom React Flow dependency edge with labels and cycle highlighting"
      exports: ["GhaDependencyEdge", "GhaDependencyEdgeData", "GhaDependencyEdgeType"]
    - path: "src/components/tools/gha-results/GhaGraphSkeleton.tsx"
      provides: "Loading skeleton for Suspense fallback"
      exports: ["GhaGraphSkeleton"]
    - path: "src/components/tools/gha-results/gha-graph.css"
      provides: "Dark theme CSS overrides for React Flow"
  key_links:
    - from: "src/components/tools/gha-results/GhaTriggerNode.tsx"
      to: "@xyflow/react"
      via: "Handle, Position, NodeProps imports"
      pattern: "import.*Handle.*Position.*from.*@xyflow/react"
    - from: "src/components/tools/gha-results/GhaDependencyEdge.tsx"
      to: "@xyflow/react"
      via: "BaseEdge, EdgeLabelRenderer, getSmoothStepPath imports"
      pattern: "import.*BaseEdge.*from.*@xyflow/react"
---

<objective>
Create all custom React Flow node components, edge component, loading skeleton, and dark theme CSS for the workflow graph.

Purpose: These are the visual building blocks that GhaWorkflowGraph.tsx (Plan 03) will compose. Creating them separately ensures Plan 03 focuses on layout logic and wiring rather than component markup. Runs parallel with Plan 01 (data extractor) since they share no files.

Output: 6 new files in `src/components/tools/gha-results/` -- three custom node types, one custom edge, one skeleton, and one CSS file.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-workflow-graph-visualization/79-RESEARCH.md

@src/components/tools/compose-results/ServiceNode.tsx
@src/components/tools/compose-results/DependencyEdge.tsx
@src/components/tools/compose-results/GraphSkeleton.tsx
@src/components/tools/k8s-results/K8sGraphSkeleton.tsx
@src/components/tools/k8s-results/k8s-graph.css
@src/components/tools/compose-results/dependency-graph.css

<interfaces>
<!-- React Flow v12 types used by custom nodes/edges -->

From @xyflow/react (v12.10.1):
```typescript
import { Handle, Position, type NodeProps, type Node } from '@xyflow/react';
import { BaseEdge, EdgeLabelRenderer, getSmoothStepPath, type EdgeProps, type Edge } from '@xyflow/react';
```

Node data pattern (from ServiceNode.tsx):
```typescript
export type ServiceNodeData = { label: string; image?: string; ports?: string; networks: string[]; networkColor?: string; };
export type ServiceNodeType = Node<ServiceNodeData, 'service'>;
export function ServiceNode({ data }: NodeProps<ServiceNodeType>) { ... }
```

Edge data pattern (from DependencyEdge.tsx):
```typescript
export type DependencyEdgeData = { condition?: string; isCycle?: boolean; };
export type DependencyEdgeType = Edge<DependencyEdgeData, 'dependency'>;
export function DependencyEdge({ sourceX, sourceY, targetX, targetY, sourcePosition, targetPosition, data, markerEnd }: EdgeProps<DependencyEdgeType>) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create custom node components and graph skeleton</name>
  <files>
    src/components/tools/gha-results/GhaTriggerNode.tsx
    src/components/tools/gha-results/GhaJobNode.tsx
    src/components/tools/gha-results/GhaStepNode.tsx
    src/components/tools/gha-results/GhaGraphSkeleton.tsx
  </files>
  <action>
    Create four React components following the existing ServiceNode.tsx and K8sResourceNode.tsx patterns.

    **GhaTriggerNode.tsx:**
    - Export types: `GhaTriggerNodeData` (label: string), `GhaTriggerNodeType = Node<GhaTriggerNodeData, 'gha-trigger'>`
    - Export function: `GhaTriggerNode({ data }: NodeProps<GhaTriggerNodeType>)`
    - Visual: 140px wide, rounded-lg border in indigo (#6366f1), dark bg using `var(--color-surface-alt, rgba(0,0,0,0.4))`
    - Shows "trigger" label in 10px uppercase mono text-indigo-400, then bold event name
    - Single `Handle type="source" position={Position.Right}` (triggers only connect outward)
    - Handle className: `!bg-[var(--color-accent,#c44b20)]`

    **GhaJobNode.tsx:**
    - Export types: `GhaJobNodeData` (label: string, violationStatus: 'clean' | 'warning' | 'error', stepCount: number), `GhaJobNodeType = Node<GhaJobNodeData, 'gha-job'>`
    - Export function: `GhaJobNode({ data }: NodeProps<GhaJobNodeType>)`
    - Visual: rounded-lg border color from STATUS_COLORS map (clean: #10b981, warning: #f59e0b, error: #ef4444), dark bg
    - Header section with border-bottom showing "job" label in status color (10px uppercase mono) and bold job name
    - Both `Handle type="target" position={Position.Left}` and `Handle type="source" position={Position.Right}`
    - This is a group/container node -- step children render inside it via React Flow parentId. The component only renders the header chrome, not the step children.

    **GhaStepNode.tsx:**
    - Export types: `GhaStepNodeData` (label: string, violationStatus: 'clean' | 'warning' | 'error', stepIndex: number), `GhaStepNodeType = Node<GhaStepNodeData, 'gha-step'>`
    - Export function: `GhaStepNode({ data }: NodeProps<GhaStepNodeType>)`
    - Visual: compact (px-2 py-1.5), rounded, xs text, bg from STATUS_BG map (10% opacity versions of status colors), 3px solid left border from STATUS_BORDER map (30% opacity)
    - Truncated label with title attribute for full text
    - NO handles -- step nodes are purely visual within their parent job container
    - Width: set via style `width: 'calc(100% - 16px)'` to fill parent job minus padding

    **GhaGraphSkeleton.tsx:**
    - Export function: `GhaGraphSkeleton()`
    - Port from K8sGraphSkeleton.tsx pattern -- same SVG with three connected circles
    - Change loading text to "Loading workflow graph..."
    - 300px height, animate-pulse, centered layout

    All STATUS_COLORS/STATUS_BG/STATUS_BORDER maps should be defined as module-level const objects (not inside components).
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/tools/gha-results/GhaTriggerNode.tsx src/components/tools/gha-results/GhaJobNode.tsx src/components/tools/gha-results/GhaStepNode.tsx src/components/tools/gha-results/GhaGraphSkeleton.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    - GhaTriggerNode renders event name with indigo border and right-side source handle
    - GhaJobNode renders job name with status-colored border, has both target (left) and source (right) handles
    - GhaStepNode renders step label with status-colored left border, no handles
    - GhaGraphSkeleton shows animated loading state with graph-suggesting SVG
    - All components export their data types and node types for use in nodeTypes constant
  </done>
</task>

<task type="auto">
  <name>Task 2: Create custom dependency edge and dark theme CSS</name>
  <files>
    src/components/tools/gha-results/GhaDependencyEdge.tsx
    src/components/tools/gha-results/gha-graph.css
  </files>
  <action>
    **GhaDependencyEdge.tsx:**
    - Port from compose-results/DependencyEdge.tsx pattern
    - Export types: `GhaDependencyEdgeData` (label?: string, isCycle?: boolean), `GhaDependencyEdgeType = Edge<GhaDependencyEdgeData, 'gha-dependency'>`
    - Export function: `GhaDependencyEdge({ sourceX, sourceY, targetX, targetY, sourcePosition, targetPosition, data, markerEnd }: EdgeProps<GhaDependencyEdgeType>)`
    - Use `getSmoothStepPath` for path calculation
    - Normal edges: stroke `var(--color-text-secondary, #888)`, strokeWidth 1.5
    - Cycle edges (data.isCycle): stroke `#ef4444` (red), strokeWidth 2.5
    - Show label via EdgeLabelRenderer when data.label is present (for trigger event names on trigger edges). Label styled as small pill: text-xs, px-1.5 py-0.5, rounded, surface bg, border, secondary text color
    - Do NOT show labels on needs: edges (cleaner visual per research recommendation)

    **gha-graph.css:**
    - Copy k8s-graph.css pattern exactly, changing the wrapper class prefix from `.k8s-graph` to `.gha-graph`
    - Same CSS variable overrides: --xy-background-color transparent, node/edge colors, controls button styling, attribution hidden, arrowhead colors
    - Include minimap styles (same as k8s-graph.css)
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/tools/gha-results/GhaDependencyEdge.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    - GhaDependencyEdge renders smooth step paths with cycle-aware coloring
    - Edge labels appear for trigger edges but not needs: edges
    - gha-graph.css provides dark theme overrides matching k8s-graph.css and dependency-graph.css patterns
    - All 6 files in gha-results/ directory compile without TypeScript errors
  </done>
</task>

</tasks>

<verification>
All files compile: `npx tsc --noEmit` passes (or at minimum no errors from the 6 new files)
</verification>

<success_criteria>
- 6 new files created in src/components/tools/gha-results/
- Three custom node components with correct Handle positions and status coloring
- One custom edge component with cycle highlighting and optional labels
- One loading skeleton matching existing GraphSkeleton pattern
- One CSS file matching existing dark theme pattern
- All exported types match the naming convention for use in Plan 03's nodeTypes/edgeTypes constants
</success_criteria>

<output>
After completion, create `.planning/phases/79-workflow-graph-visualization/79-02-SUMMARY.md`
</output>
