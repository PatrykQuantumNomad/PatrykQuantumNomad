---
phase: 96-notebook-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/eda/notebooks/registry/types.ts
  - src/lib/eda/notebooks/registry/index.ts
  - src/lib/eda/notebooks/registry/normal-random-numbers.ts
  - src/lib/eda/notebooks/registry/uniform-random-numbers.ts
  - src/lib/eda/notebooks/registry/heat-flow-meter.ts
  - src/lib/eda/notebooks/registry/filter-transmittance.ts
  - src/lib/eda/notebooks/registry/cryothermometry.ts
  - src/lib/eda/notebooks/registry/fatigue-life.ts
  - src/lib/eda/notebooks/registry/standard-resistor.ts
  - src/lib/eda/notebooks/registry/beam-deflections.ts
  - src/lib/eda/notebooks/registry/random-walk.ts
  - src/lib/eda/notebooks/registry/ceramic-strength.ts
  - src/lib/eda/notebooks/requirements.ts
  - src/lib/eda/notebooks/theme.ts
  - src/lib/eda/notebooks/__tests__/registry.test.ts
  - src/lib/eda/notebooks/__tests__/requirements.test.ts
autonomous: true
requirements:
  - NBGEN-03
  - NBGEN-04

must_haves:
  truths:
    - "Registry maps all 10 case study slugs to their NIST .DAT filenames and analysis parameters"
    - "Each registry config has slug, dataFile, skipRows, expectedRows, columns, responseVariable, and expectedStats"
    - "JAHANMI2.DAT (ceramic-strength) config specifies skipRows: 50 and 15 columns"
    - "DZIUBA1.DAT (standard-resistor) config specifies 4 columns (Month, Day, Year, Resistance)"
    - "Requirements.txt template specifies numpy, scipy, pandas, matplotlib, seaborn with floor version pins (>=)"
    - "Theme module exports Python code strings for matplotlib/seaborn dark theme matching Quantum Explorer aesthetic"
  artifacts:
    - path: "src/lib/eda/notebooks/registry/types.ts"
      provides: "CaseStudyConfig interface"
      exports: ["CaseStudyConfig"]
    - path: "src/lib/eda/notebooks/registry/index.ts"
      provides: "Registry lookup by slug"
      exports: ["CASE_STUDY_REGISTRY", "getCaseStudyConfig"]
    - path: "src/lib/eda/notebooks/registry/normal-random-numbers.ts"
      provides: "Normal random numbers case study config"
    - path: "src/lib/eda/notebooks/registry/ceramic-strength.ts"
      provides: "Ceramic strength case study config with DOE factors"
    - path: "src/lib/eda/notebooks/requirements.ts"
      provides: "Requirements.txt template string"
      exports: ["REQUIREMENTS_TXT"]
    - path: "src/lib/eda/notebooks/theme.ts"
      provides: "Quantum Explorer dark theme Python code"
      exports: ["THEME_SETUP_CODE"]
    - path: "src/lib/eda/notebooks/__tests__/registry.test.ts"
      provides: "Unit tests for registry completeness and correctness"
    - path: "src/lib/eda/notebooks/__tests__/requirements.test.ts"
      provides: "Unit tests for requirements.txt content"
  key_links:
    - from: "src/lib/eda/notebooks/registry/index.ts"
      to: "src/lib/eda/notebooks/registry/*.ts"
      via: "imports all 10 config files"
      pattern: "import.*from.*\\./[a-z]"
    - from: "src/lib/eda/notebooks/registry/*.ts"
      to: "src/lib/eda/notebooks/registry/types.ts"
      via: "import type { CaseStudyConfig }"
      pattern: "CaseStudyConfig"
---

<objective>
Create the case study registry (10 per-study config files), requirements.txt template, and matplotlib/seaborn dark theme module.

Purpose: The registry provides all the metadata that notebook generators in phases 97 and 100 need to produce case-study-specific notebooks -- dataset filenames, parsing parameters, expected statistics for validation, and plot titles. The requirements.txt and theme modules provide shared Python setup code used by every notebook.

Output: 10 registry config files mapping case studies to their NIST datasets, registry index with lookup function, CaseStudyConfig type, requirements.txt template, theme setup code, plus unit tests.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-notebook-foundation/96-RESEARCH.md
@.planning/phases/96-notebook-foundation/96-CONTEXT.md
@vitest.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create case study registry with 10 config files and tests</name>
  <files>src/lib/eda/notebooks/registry/types.ts, src/lib/eda/notebooks/registry/index.ts, src/lib/eda/notebooks/registry/normal-random-numbers.ts, src/lib/eda/notebooks/registry/uniform-random-numbers.ts, src/lib/eda/notebooks/registry/heat-flow-meter.ts, src/lib/eda/notebooks/registry/filter-transmittance.ts, src/lib/eda/notebooks/registry/cryothermometry.ts, src/lib/eda/notebooks/registry/fatigue-life.ts, src/lib/eda/notebooks/registry/standard-resistor.ts, src/lib/eda/notebooks/registry/beam-deflections.ts, src/lib/eda/notebooks/registry/random-walk.ts, src/lib/eda/notebooks/registry/ceramic-strength.ts, src/lib/eda/notebooks/__tests__/registry.test.ts</files>
  <behavior>
    registry.test.ts:
    - CASE_STUDY_REGISTRY has exactly 10 entries
    - All 10 slugs are present: normal-random-numbers, uniform-random-numbers, heat-flow-meter, filter-transmittance, cryothermometry, fatigue-life, standard-resistor, beam-deflections, random-walk, ceramic-strength
    - getCaseStudyConfig('normal-random-numbers') returns a config (not undefined)
    - getCaseStudyConfig('nonexistent') returns undefined
    - Every config has required fields: slug, title, nistSection, dataFile, skipRows, expectedRows, columns (non-empty array), responseVariable, expectedStats (with mean, stdDev, min, max), githubRawUrl, plotTitles
    - ceramic-strength config has skipRows === 50 (not 25)
    - ceramic-strength config has 15 columns
    - ceramic-strength config has doeFactors defined
    - standard-resistor config has 4 columns
    - All other configs have skipRows === 25
    - All githubRawUrls start with 'https://raw.githubusercontent.com/PatrykQuantumNomad/'
    - Each config's slug matches its key in the registry
    - beam-deflections config has modelParams with type === 'sinusoidal'
    - random-walk config has modelParams with type === 'ar1'
  </behavior>
  <action>
**Step 1 (RED):** Create the test file first.

Create `src/lib/eda/notebooks/__tests__/registry.test.ts` with all the behavior tests listed above. Import `CASE_STUDY_REGISTRY` and `getCaseStudyConfig` from `../registry/index`. Import `CaseStudyConfig` type from `../registry/types`.

Run tests -- they MUST fail.

**Step 2 (GREEN):** Create all registry files.

Create `src/lib/eda/notebooks/registry/types.ts` with the `CaseStudyConfig` interface:
- `slug: string` -- URL-friendly slug matching MDX filename
- `title: string` -- human-readable title
- `nistSection: string` -- NIST section reference (e.g., '1.4.2.1')
- `dataFile: string` -- .DAT filename (e.g., 'RANDN.DAT')
- `skipRows: number` -- lines to skip in .DAT header
- `expectedRows: number` -- expected data row count for assertion
- `columns: string[]` -- column names for pd.read_fwf()
- `responseVariable: string` -- response variable name
- `expectedStats: { mean: number; stdDev: number; min: number; max: number; median?: number }` -- NIST-verified values
- `nistUrl: string` -- NIST handbook URL
- `githubRawUrl: string` -- GitHub raw URL for Colab fallback
- `plotTitles: { fourPlot: string; runSequence: string; lagPlot: string; histogram: string; probabilityPlot: string }` -- plot titles
- `testParams?: { targetMean?: number; alpha?: number }` -- hypothesis test parameters
- `modelParams?: { type: 'sinusoidal' | 'ar1'; [key: string]: unknown }` -- model fitting params (beam-deflections, random-walk)
- `doeFactors?: { name: string; column: string; levels: (string | number)[] }[]` -- DOE factors (ceramic-strength)
- `valuesPerLine?: number` -- for multi-value-per-line .DAT files (RANDN=10, RANDU=5, SOULEN=5)
- `description?: string` -- brief description of the case study

Per user decision: single base type with optional fields, NOT discriminated union.

Create 10 config files, each exporting a `const config: CaseStudyConfig`. Use the exact data from the RESEARCH.md registry reference table:

1. `normal-random-numbers.ts`: RANDN.DAT, skip 25, 500 rows, ['Y'], valuesPerLine: 10
2. `uniform-random-numbers.ts`: RANDU.DAT, skip 25, 500 rows, ['Y'], valuesPerLine: 5
3. `heat-flow-meter.ts`: ZARR13.DAT, skip 25, 195 rows, ['Y']
4. `filter-transmittance.ts`: MAVRO.DAT, skip 25, 50 rows, ['Y']
5. `cryothermometry.ts`: SOULEN.DAT, skip 25, 700 rows, ['Y'], valuesPerLine: 5
6. `fatigue-life.ts`: BIRNSAUN.DAT, skip 25, 101 rows, ['Y']
7. `standard-resistor.ts`: DZIUBA1.DAT, skip 25, 1000 rows, ['Month', 'Day', 'Year', 'Resistance']
8. `beam-deflections.ts`: LEW.DAT, skip 25, 200 rows, ['Y'], modelParams: { type: 'sinusoidal' }
9. `random-walk.ts`: RANDWALK.DAT, skip 25, 500 rows, ['Y'], modelParams: { type: 'ar1' }
10. `ceramic-strength.ts`: JAHANMI2.DAT, skip **50** (NOT 25), 480 rows, 15 columns, doeFactors defined

For `expectedStats` values: use values from the existing EDA case study pages in the codebase. Look at the NIST .DAT file headers or the case study MDX pages for verified mean, stdDev, min, max values. If exact values are uncertain, use reasonable placeholder values from the NIST handbook with a comment noting they should be verified.

GitHub raw URL pattern: `https://raw.githubusercontent.com/PatrykQuantumNomad/PatrykQuantumNomad/main/handbook/datasets/{FILENAME}`

Create `src/lib/eda/notebooks/registry/index.ts`:
- Import all 10 config files
- Export `CASE_STUDY_REGISTRY: Record<string, CaseStudyConfig>` mapping slug to config
- Export `getCaseStudyConfig(slug: string): CaseStudyConfig | undefined`
- Export `ALL_CASE_STUDY_SLUGS: string[]` for iteration
- Re-export `CaseStudyConfig` type from `./types`

Run tests -- they MUST pass.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx vitest run src/lib/eda/notebooks/__tests__/registry.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Registry exports configs for all 10 case study slugs. Each config has all required fields. JAHANMI2.DAT has skipRows: 50. DZIUBA1.DAT has 4 columns. beam-deflections has sinusoidal modelParams. random-walk has ar1 modelParams. ceramic-strength has doeFactors. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create requirements.txt template and dark theme module with tests</name>
  <files>src/lib/eda/notebooks/requirements.ts, src/lib/eda/notebooks/theme.ts, src/lib/eda/notebooks/__tests__/requirements.test.ts</files>
  <behavior>
    requirements.test.ts:
    - REQUIREMENTS_TXT contains 'numpy>=2.0.0'
    - REQUIREMENTS_TXT contains 'scipy>=1.14.0'
    - REQUIREMENTS_TXT contains 'pandas>=2.2.0'
    - REQUIREMENTS_TXT contains 'matplotlib>=3.9.0'
    - REQUIREMENTS_TXT contains 'seaborn>=0.13.0'
    - REQUIREMENTS_TXT has exactly 5 non-empty lines
    - REQUIREMENTS_TXT ends with newline
    - REQUIREMENTS_TXT uses >= (floor pins), not == (exact pins)
    - THEME_SETUP_CODE contains 'plt.rcParams'
    - THEME_SETUP_CODE contains '#0f1117' (background color)
    - THEME_SETUP_CODE contains 'sns.set_theme'
    - THEME_SETUP_CODE contains '#e06040' (accent color)
    - DEPENDENCY_CHECK_CODE contains 'try' and 'import'
    - DEPENDENCY_CHECK_CODE contains 'pip install'
  </behavior>
  <action>
**Step 1 (RED):** Create test file first.

Create `src/lib/eda/notebooks/__tests__/requirements.test.ts`:
- Import `REQUIREMENTS_TXT` from `../requirements`
- Import `THEME_SETUP_CODE`, `DEPENDENCY_CHECK_CODE` from `../theme`
- Test REQUIREMENTS_TXT contains all 5 packages with floor version pins
- Test REQUIREMENTS_TXT has exactly 5 non-empty lines
- Test REQUIREMENTS_TXT ends with trailing newline
- Test no exact pins (no `==` in the string)
- Test THEME_SETUP_CODE contains Quantum Explorer colors: '#0f1117' (background), '#1a1d27' (surface), '#e06040' (accent), '#00a3a3' (teal)
- Test THEME_SETUP_CODE contains 'plt.rcParams' and 'sns.set_theme'
- Test DEPENDENCY_CHECK_CODE contains try/except import pattern and pip install fallback

Run tests -- they MUST fail.

**Step 2 (GREEN):** Create the source files.

Create `src/lib/eda/notebooks/requirements.ts`:
- Export `REQUIREMENTS_TXT: string` -- a newline-joined string of floor-pinned packages:
  ```
  numpy>=2.0.0
  scipy>=1.14.0
  pandas>=2.2.0
  matplotlib>=3.9.0
  seaborn>=0.13.0
  ```
- Must end with trailing newline
- Add comment explaining floor pins are used because these are educational notebooks, not production applications (per user decision and research)

Create `src/lib/eda/notebooks/theme.ts`:
- Export `QUANTUM_COLORS: Record<string, string>` -- TypeScript object mapping color names to hex values matching the site's Quantum Explorer dark theme CSS custom properties:
  - background: '#0f1117'
  - surface: '#1a1d27'
  - accent: '#e06040'
  - teal: '#00a3a3'
  - text: '#e8e8f0'
  - textSecondary: '#9ca3af'
  - border: '#2a2d3a'
  - gradientStart: '#e06040'
  - gradientEnd: '#00a3a3'

- Export `QUANTUM_PALETTE: string[]` -- color cycle for multiple data series: ['#e06040', '#00a3a3', '#f0c040', '#a080e0', '#60c0a0', '#e080a0']

- Export `THEME_SETUP_CODE: string[]` -- array of Python source lines (for use with codeCell factory) that configures matplotlib rcParams and seaborn theme. This is the Python code that will appear in the theme setup cell of every notebook. Include:
  - QUANTUM_COLORS dict
  - QUANTUM_PALETTE list
  - plt.rcParams.update() with figure.facecolor, axes.facecolor, axes.edgecolor, axes.labelcolor, axes.titlecolor, tick colors, grid, font sizes, and axes.prop_cycle
  - sns.set_theme() with darkgrid style and matching rc overrides
  - Use the exact Python code from RESEARCH.md "Dark Theme for Matplotlib/Seaborn" section

- Export `DEPENDENCY_CHECK_CODE: string[]` -- array of Python source lines for the dependency check + install cell (per user decision: try/except import checks with !pip install fallback). Pattern:
  ```python
  try:
      import numpy as np
      import scipy
      import pandas as pd
      import matplotlib.pyplot as plt
      import seaborn as sns
  except ImportError:
      !pip install numpy scipy pandas matplotlib seaborn
      import numpy as np
      import scipy
      import pandas as pd
      import matplotlib.pyplot as plt
      import seaborn as sns
  ```

Run tests -- they MUST pass.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/PatrykQuantumNomad && npx vitest run src/lib/eda/notebooks/__tests__/requirements.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>REQUIREMENTS_TXT contains all 5 packages with floor version pins. THEME_SETUP_CODE contains Quantum Explorer colors and configures both matplotlib and seaborn. DEPENDENCY_CHECK_CODE has try/except import pattern with pip install fallback. All tests pass.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- TypeScript compiles with no errors
2. `npx vitest run src/lib/eda/notebooks/` -- all tests pass (registry + requirements)
3. Registry has exactly 10 entries matching all case study slugs
4. JAHANMI2.DAT has skipRows: 50, all others have skipRows: 25
5. REQUIREMENTS_TXT has 5 floor-pinned packages ending with newline
6. THEME_SETUP_CODE uses Quantum Explorer dark theme colors
</verification>

<success_criteria>
- Registry maps all 10 case study slugs with correct .DAT filenames, skipRows, expectedRows, columns
- Each config has expectedStats, githubRawUrl, plotTitles
- beam-deflections and random-walk have modelParams; ceramic-strength has doeFactors
- REQUIREMENTS_TXT specifies numpy, scipy, pandas, matplotlib, seaborn with >= floor pins
- THEME_SETUP_CODE produces valid Python for matplotlib/seaborn Quantum Explorer dark theme
- DEPENDENCY_CHECK_CODE handles import failures with pip install fallback
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/96-notebook-foundation/96-02-SUMMARY.md`
</output>
