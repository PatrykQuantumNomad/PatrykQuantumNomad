---
phase: 100-advanced-case-study-notebooks
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/eda/notebooks/templates/advanced/ceramic-strength.ts
  - src/lib/eda/notebooks/templates/sections/doe/batch-effect.ts
  - src/lib/eda/notebooks/templates/sections/doe/factor-analysis.ts
  - src/lib/eda/notebooks/templates/sections/doe/anova.ts
  - src/lib/eda/notebooks/__tests__/ceramic-strength-notebook.test.ts
autonomous: true
requirements:
  - NBADV-03

must_haves:
  truths:
    - "buildCeramicStrengthNotebook() produces a valid nbformat v4.5 notebook"
    - "Notebook contains batch effect analysis with bihistogram, box plot, and two-sample t-test"
    - "Notebook contains factor rankings via DOE mean plots per batch"
    - "Notebook contains interaction effect plots for factor pairs per batch"
    - "Notebook contains one-way ANOVA via scipy.stats.f_oneway"
    - "Notebook uses custom DOE intro goals instead of standard 5 EDA goals"
    - "Notebook skips standard hypothesis tests (goes directly from 4-plot to batch effect analysis)"
  artifacts:
    - path: "src/lib/eda/notebooks/templates/advanced/ceramic-strength.ts"
      provides: "Ceramic strength notebook builder"
      exports: ["buildCeramicStrengthNotebook"]
    - path: "src/lib/eda/notebooks/templates/sections/doe/batch-effect.ts"
      provides: "Batch effect analysis section builder"
      exports: ["buildBatchEffect"]
    - path: "src/lib/eda/notebooks/templates/sections/doe/factor-analysis.ts"
      provides: "DOE factor analysis section builder (mean plots, SD plots, rankings)"
      exports: ["buildFactorAnalysis"]
    - path: "src/lib/eda/notebooks/templates/sections/doe/anova.ts"
      provides: "One-way ANOVA section builder"
      exports: ["buildAnova"]
    - path: "src/lib/eda/notebooks/__tests__/ceramic-strength-notebook.test.ts"
      provides: "Tests for ceramic strength notebook"
  key_links:
    - from: "src/lib/eda/notebooks/templates/advanced/ceramic-strength.ts"
      to: "src/lib/eda/notebooks/templates/sections/doe/batch-effect.ts"
      via: "import buildBatchEffect"
      pattern: "import.*buildBatchEffect"
    - from: "src/lib/eda/notebooks/templates/advanced/ceramic-strength.ts"
      to: "src/lib/eda/notebooks/templates/sections/doe/factor-analysis.ts"
      via: "import buildFactorAnalysis"
      pattern: "import.*buildFactorAnalysis"
    - from: "src/lib/eda/notebooks/templates/advanced/ceramic-strength.ts"
      to: "src/lib/eda/notebooks/templates/sections/doe/anova.ts"
      via: "import buildAnova"
      pattern: "import.*buildAnova"
    - from: "src/lib/eda/notebooks/templates/advanced/ceramic-strength.ts"
      to: "src/lib/eda/notebooks/templates/sections/setup.ts"
      via: "import buildSetup (reuse standard)"
      pattern: "import.*buildSetup"
---

<objective>
Build the Ceramic Strength advanced notebook template with DOE multi-factor analysis, batch effects, and ANOVA via TDD.

Purpose: NBADV-03 requires a specialized notebook for a 2^4 full factorial DOE case study. The ceramic strength data (JAHANMI2.DAT, 480 rows, 15 columns) exhibits a dominant batch effect (~75 units) that must be analyzed before factor effects. The notebook covers batch effects, lab effects, DOE mean/SD plots, interaction plots, and one-way ANOVA -- a fundamentally different analysis path from the standard univariate template.

Output: `buildCeramicStrengthNotebook()` function producing a valid nbformat v4.5 notebook with 11 sections: custom-intro, setup, data-loading, summary-stats, 4-plot, batch-effect, lab-effect, factor-analysis, interaction-effects, anova, conclusions.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-advanced-case-study-notebooks/100-RESEARCH.md

@src/lib/eda/notebooks/templates/standard.ts
@src/lib/eda/notebooks/templates/sections/intro.ts
@src/lib/eda/notebooks/templates/sections/data-loading.ts
@src/lib/eda/notebooks/cells.ts
@src/lib/eda/notebooks/notebook.ts
@src/lib/eda/notebooks/types.ts
@src/lib/eda/notebooks/registry/types.ts
@src/lib/eda/notebooks/registry/ceramic-strength.ts
</context>

<feature>
  <name>Ceramic Strength Advanced Notebook</name>
  <files>
    src/lib/eda/notebooks/templates/advanced/ceramic-strength.ts,
    src/lib/eda/notebooks/templates/sections/doe/batch-effect.ts,
    src/lib/eda/notebooks/templates/sections/doe/factor-analysis.ts,
    src/lib/eda/notebooks/templates/sections/doe/anova.ts,
    src/lib/eda/notebooks/__tests__/ceramic-strength-notebook.test.ts
  </files>
  <behavior>
    Test: buildCeramicStrengthNotebook() returns object with nbformat=4, nbformat_minor=5
    Test: Notebook cells array has at least 25 cells (11 sections, each 2+ cells)
    Test: First markdown cell contains "Ceramic Strength" (title)
    Test: Second markdown cell contains DOE goals ("strongest factor" or "factor rankings" or "optimal settings") -- NOT the standard 5 EDA goals
    Test: No code cell contains "shapiro" or "anderson" or "grubbs" (no standard hypothesis tests)
    Test: At least one code cell contains "ttest_ind" (two-sample t-test for batch means)
    Test: At least one code cell contains "f_oneway" (one-way ANOVA)
    Test: At least one code cell contains "groupby" and "mean" (DOE mean plots)
    Test: At least one code cell contains "unstack" or "pivot" (interaction plots)
    Test: At least one code cell contains "boxplot" or "sns.boxplot" (batch box plots)
    Test: At least one markdown cell contains "688.9987" (NIST Batch 1 mean)
    Test: At least one markdown cell contains "611.1559" (NIST Batch 2 mean)
    Test: At least one markdown cell contains "13.3806" (NIST batch t-test T statistic)
    Test: At least one markdown cell mentions factor rankings differ by batch (X1 dominant in batch 1, X2 dominant in batch 2)
    Test: All cells have unique IDs
    Test: Notebook data loading section references 480 rows (assert len(df) == 480)
  </behavior>
  <implementation>
    1. Create `src/lib/eda/notebooks/templates/sections/doe/batch-effect.ts`:
       - Export `buildBatchEffect(config, slug, startIndex)` following the `{ cells, nextIndex }` pattern
       - Markdown cell: "## Batch Effect Analysis" explaining why batch is the first thing to investigate (bimodal histogram in 4-plot)
       - Code cell: split data by Batch column, compute batch means, SDs, Ns
       - Code cell: bihistogram (two overlaid histograms with alpha transparency) of batch1 vs batch2
       - Code cell: side-by-side box plots by batch using sns.boxplot
       - Code cell: two-sample t-test via `ttest_ind(batch1_y, batch2_y)` and F-test via `f_oneway`
       - Markdown cell: comparison table with NIST values -- Batch1 mean=688.9987, SD=65.5491, N=240; Batch2 mean=611.1559, SD=61.8543, N=240; T=13.3806, batch effect ~75-100 units

    2. Create `src/lib/eda/notebooks/templates/sections/doe/factor-analysis.ts`:
       - Export `buildFactorAnalysis(config, slug, startIndex)` following the `{ cells, nextIndex }` pattern
       - Markdown cell: "## Primary Factor Analysis" explaining DOE mean/SD plots
       - Code cell: DOE mean plots for X1, X2, X3 (overall and per batch) using `df.groupby(factor)['Y'].mean()`
       - Code cell: DOE SD plots for X1, X2, X3 per batch
       - Markdown cell: Factor ranking summary -- Batch 1: X1 (table speed, effect=-30.77) dominant; Batch 2: X2 (down feed, effect=18.22) dominant
       - Code cell: interaction plots for (X1,X2), (X1,X3), (X2,X3) per batch using `df.groupby([f1, f2])['Y'].mean().unstack()` and line plots
       - Markdown cell: interpretation of interaction effects

    3. Create `src/lib/eda/notebooks/templates/sections/doe/anova.ts`:
       - Export `buildAnova(config, slug, startIndex)` following the `{ cells, nextIndex }` pattern
       - Markdown cell: "## One-Way ANOVA"
       - Code cell: for each factor X1, X2, X3, per batch: `groups = [g['Y'].values for _, g in batch_data.groupby(factor)]` then `f_stat, p_val = f_oneway(*groups)`
       - Code cell: summary ANOVA results table (factor, batch, F-stat, p-value)
       - Markdown cell: interpretation -- significant factors differ by batch

    4. Create `src/lib/eda/notebooks/templates/advanced/ceramic-strength.ts`:
       - Export `buildCeramicStrengthNotebook()` that:
         a. Gets config via `getCaseStudyConfig('ceramic-strength')`
         b. Builds CUSTOM intro section inline (NOT reusing buildIntro):
            - Title cell: "# Ceramic Strength" with NIST section reference
            - Background cell with DOE-specific goals:
              1. Determine the most important factor affecting ceramic strength
              2. Estimate factor effect magnitudes
              3. Determine optimal factor settings for maximum strength
              4. Assess batch-to-batch variability (nuisance factor)
         c. Reuses standard sections: buildSetup, buildDataLoading, buildSummaryStats, buildFourPlot
         d. Adds lab effect analysis inline: box plots by lab, by lab within each batch
         e. Adds batch effect section via buildBatchEffect
         f. Adds factor analysis section via buildFactorAnalysis (includes interaction plots)
         g. Adds ANOVA section via buildAnova
         h. Adds conclusions inline: batch dominates (~75 units), factor rankings differ by batch, X1 strongest in batch 1, X2 strongest in batch 2, interaction effects present
         i. Returns createNotebook(allCells)
       - Note: ceramic does NOT call buildIntro or buildHypothesisTests or buildTestSummary or buildInterpretation -- it goes from 4-plot directly to DOE analysis
       - The narrative should explain that only longitudinal data (X4=-1) are used (480 rows)
  </implementation>
</feature>

<verification>
```bash
npx vitest run src/lib/eda/notebooks/__tests__/ceramic-strength-notebook.test.ts --reporter=verbose
```
</verification>

<success_criteria>
- buildCeramicStrengthNotebook() produces valid nbformat v4.5 notebook
- Notebook uses custom DOE intro goals, not standard 5 EDA goals
- Notebook contains batch effect analysis with t-test and NIST reference values
- Notebook contains DOE mean plots, factor rankings, and interaction plots
- Notebook contains one-way ANOVA via f_oneway
- No standard hypothesis tests (shapiro, anderson, grubbs) present
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/100-advanced-case-study-notebooks/100-03-SUMMARY.md`
</output>
