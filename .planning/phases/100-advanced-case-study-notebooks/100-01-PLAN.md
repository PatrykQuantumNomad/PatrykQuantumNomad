---
phase: 100-advanced-case-study-notebooks
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/eda/notebooks/templates/advanced/beam-deflections.ts
  - src/lib/eda/notebooks/templates/sections/model-fitting/sinusoidal.ts
  - src/lib/eda/notebooks/templates/sections/model-fitting/residual-validation.ts
  - src/lib/eda/notebooks/__tests__/beam-deflections-notebook.test.ts
autonomous: true
requirements:
  - NBADV-01

must_haves:
  truths:
    - "buildBeamDeflectionsNotebook() produces a valid nbformat v4.5 notebook"
    - "Notebook contains a sinusoidal model fitting code cell using scipy.optimize.curve_fit with NIST starting values p0=[-177.44, -390, 0.3025, 1.5]"
    - "Notebook contains a residual 4-plot code cell that validates the sinusoidal fit"
    - "Notebook contains markdown cells explaining why standard EDA reveals non-randomness (circular lag plot) and why sinusoidal model is appropriate"
    - "Notebook includes parameter comparison table with NIST reference values (C=-178.786, AMP=-361.766, FREQ=0.302596, PHASE=1.46536, ResidSD=155.8484)"
  artifacts:
    - path: "src/lib/eda/notebooks/templates/advanced/beam-deflections.ts"
      provides: "Beam deflections notebook builder"
      exports: ["buildBeamDeflectionsNotebook"]
    - path: "src/lib/eda/notebooks/templates/sections/model-fitting/sinusoidal.ts"
      provides: "Sinusoidal model fitting section builder"
      exports: ["buildSinusoidalFit"]
    - path: "src/lib/eda/notebooks/templates/sections/model-fitting/residual-validation.ts"
      provides: "Residual 4-plot validation section builder"
      exports: ["buildResidualValidation"]
    - path: "src/lib/eda/notebooks/__tests__/beam-deflections-notebook.test.ts"
      provides: "Tests for beam deflections notebook"
  key_links:
    - from: "src/lib/eda/notebooks/templates/advanced/beam-deflections.ts"
      to: "src/lib/eda/notebooks/templates/sections/model-fitting/sinusoidal.ts"
      via: "import buildSinusoidalFit"
      pattern: "import.*buildSinusoidalFit"
    - from: "src/lib/eda/notebooks/templates/advanced/beam-deflections.ts"
      to: "src/lib/eda/notebooks/templates/sections/intro.ts"
      via: "import buildIntro (reuse standard)"
      pattern: "import.*buildIntro"
    - from: "src/lib/eda/notebooks/templates/sections/model-fitting/sinusoidal.ts"
      to: "src/lib/eda/notebooks/cells.ts"
      via: "import markdownCell, codeCell"
      pattern: "import.*codeCell"
---

<objective>
Build the Beam Deflections advanced notebook template with sinusoidal model fitting and residual validation via TDD.

Purpose: NBADV-01 requires a specialized notebook that goes beyond the standard 4-plot EDA to demonstrate nonlinear curve fitting (Y = C + A*sin(2*pi*freq*t + phi)) using scipy.optimize.curve_fit, with residual 4-plot validation showing the model adequately captures the sinusoidal pattern in the data.

Output: `buildBeamDeflectionsNotebook()` function producing a valid nbformat v4.5 notebook with 10 sections: intro, setup, data-loading, summary-stats, 4-plot, initial-interpretation, sinusoidal-fit, residual-4-plot, residual-interpretation, conclusions.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-advanced-case-study-notebooks/100-RESEARCH.md

@src/lib/eda/notebooks/templates/standard.ts
@src/lib/eda/notebooks/templates/sections/intro.ts
@src/lib/eda/notebooks/templates/sections/four-plot.ts
@src/lib/eda/notebooks/cells.ts
@src/lib/eda/notebooks/notebook.ts
@src/lib/eda/notebooks/types.ts
@src/lib/eda/notebooks/registry/types.ts
@src/lib/eda/notebooks/registry/beam-deflections.ts
</context>

<feature>
  <name>Beam Deflections Advanced Notebook</name>
  <files>
    src/lib/eda/notebooks/templates/advanced/beam-deflections.ts,
    src/lib/eda/notebooks/templates/sections/model-fitting/sinusoidal.ts,
    src/lib/eda/notebooks/templates/sections/model-fitting/residual-validation.ts,
    src/lib/eda/notebooks/__tests__/beam-deflections-notebook.test.ts
  </files>
  <behavior>
    Test: buildBeamDeflectionsNotebook() returns object with nbformat=4, nbformat_minor=5
    Test: Notebook cells array has at least 20 cells (5 reused standard sections + 5 new sections each produce 2+ cells)
    Test: At least one code cell source contains "curve_fit" (sinusoidal fitting)
    Test: At least one code cell source contains "sinusoidal_model" (model definition function)
    Test: At least one code cell source contains "p0 = " with starting values (initial guesses)
    Test: At least one code cell source contains "residuals" (residual computation)
    Test: At least one markdown cell source contains "Residual" (residual interpretation section)
    Test: At least one code cell produces a 4-plot of residuals (contains "fig, axes" or "subplot" after the model fit)
    Test: At least one markdown cell contains "-178.786" (NIST reference C value)
    Test: At least one markdown cell contains "155.8484" (NIST residual SD reference)
    Test: All cells have unique IDs
    Test: All cells have valid cell_type (markdown or code)
    Test: Notebook contains the standard intro section (first markdown cell contains "Beam Deflections")
    Test: Notebook contains the standard setup section (a code cell contains "import numpy" or "import matplotlib")
  </behavior>
  <implementation>
    1. Create `src/lib/eda/notebooks/templates/sections/model-fitting/sinusoidal.ts`:
       - Export `buildSinusoidalFit(config, slug, startIndex)` following the `{ cells, nextIndex }` pattern
       - Markdown cell explaining the sinusoidal model Y(i) = C + A*sin(2*pi*freq*t + phi) + E(i)
       - Code cell defining `sinusoidal_model` function
       - Code cell calling `curve_fit(sinusoidal_model, t, y, p0=[-177.44, -390, 0.3025, 1.5])`
       - Code cell computing residuals and residual SD (ddof=4 for 4 estimated parameters)
       - Markdown cell with parameter comparison table vs NIST values: C=-178.786 +/- 11.02, AMP=-361.766 +/- 26.19, FREQ=0.302596 +/- 0.000151, PHASE=1.46536 +/- 0.04909, Residual SD=155.8484

    2. Create `src/lib/eda/notebooks/templates/sections/model-fitting/residual-validation.ts`:
       - Export `buildResidualValidation(config, slug, startIndex)` following the `{ cells, nextIndex }` pattern
       - Markdown cell "Residual 4-Plot Analysis"
       - Code cell generating 4-plot of residuals (run sequence, lag plot, histogram, normal probability plot) using the same matplotlib layout as the standard four-plot.ts section but on residuals
       - Markdown cell interpreting that residuals should appear random (no pattern in run sequence, no autocorrelation in lag plot)

    3. Create `src/lib/eda/notebooks/templates/advanced/beam-deflections.ts`:
       - Export `buildBeamDeflectionsNotebook()` that:
         a. Gets config via `getCaseStudyConfig('beam-deflections')`
         b. Composes standard sections: buildIntro, buildSetup, buildDataLoading, buildSummaryStats, buildFourPlot
         c. Adds initial EDA interpretation markdown (circular lag plot reveals sinusoidal pattern)
         d. Adds sinusoidal fit section via buildSinusoidalFit
         e. Adds residual validation section via buildResidualValidation
         f. Adds residual interpretation markdown (SD reduced from 277 to 156, model captures pattern)
         g. Adds conclusions section with specialized beam deflections summary
         h. Returns createNotebook(allCells)
       - Thread startIndex through all section builders via the { cells, nextIndex } pattern

    4. The initial EDA interpretation and residual interpretation are inline markdown cells in the main builder (2-3 cells each, not separate section files), since they are specific to beam deflections and not reusable.

    5. The conclusions section is also inline (not reusing standard conclusions which reference hypothesis tests that beam deflections does not perform).
  </implementation>
</feature>

<verification>
```bash
npx vitest run src/lib/eda/notebooks/__tests__/beam-deflections-notebook.test.ts --reporter=verbose
```
</verification>

<success_criteria>
- buildBeamDeflectionsNotebook() produces valid nbformat v4.5 notebook
- Notebook contains sinusoidal curve_fit with NIST starting values
- Notebook contains residual 4-plot validation
- Notebook contains NIST reference value comparisons
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/100-advanced-case-study-notebooks/100-01-SUMMARY.md`
</output>
