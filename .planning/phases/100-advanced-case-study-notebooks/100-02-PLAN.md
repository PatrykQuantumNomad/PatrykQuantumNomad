---
phase: 100-advanced-case-study-notebooks
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/eda/notebooks/templates/advanced/random-walk.ts
  - src/lib/eda/notebooks/templates/sections/model-fitting/ar1.ts
  - src/lib/eda/notebooks/__tests__/random-walk-notebook.test.ts
autonomous: true
requirements:
  - NBADV-02

must_haves:
  truths:
    - "buildRandomWalkNotebook() produces a valid nbformat v4.5 notebook"
    - "Notebook contains AR(1) model fitting code cell using scipy.stats.linregress(y[:-1], y[1:])"
    - "Notebook contains residual 4-plot and residual distribution analysis"
    - "Notebook contains markdown cells explaining why the lag plot shows strong linear autocorrelation and why AR(1) model is appropriate"
    - "Notebook includes parameter comparison with NIST reference values (A0=0.050165, A1=0.987087, ResidSD=0.2931)"
  artifacts:
    - path: "src/lib/eda/notebooks/templates/advanced/random-walk.ts"
      provides: "Random walk notebook builder"
      exports: ["buildRandomWalkNotebook"]
    - path: "src/lib/eda/notebooks/templates/sections/model-fitting/ar1.ts"
      provides: "AR(1) model fitting section builder"
      exports: ["buildAR1Model"]
    - path: "src/lib/eda/notebooks/__tests__/random-walk-notebook.test.ts"
      provides: "Tests for random walk notebook"
  key_links:
    - from: "src/lib/eda/notebooks/templates/advanced/random-walk.ts"
      to: "src/lib/eda/notebooks/templates/sections/model-fitting/ar1.ts"
      via: "import buildAR1Model"
      pattern: "import.*buildAR1Model"
    - from: "src/lib/eda/notebooks/templates/advanced/random-walk.ts"
      to: "src/lib/eda/notebooks/templates/sections/intro.ts"
      via: "import buildIntro (reuse standard)"
      pattern: "import.*buildIntro"
    - from: "src/lib/eda/notebooks/templates/sections/model-fitting/ar1.ts"
      to: "src/lib/eda/notebooks/cells.ts"
      via: "import markdownCell, codeCell"
      pattern: "import.*codeCell"
---

<objective>
Build the Random Walk advanced notebook template with AR(1) model development and residual analysis via TDD.

Purpose: NBADV-02 requires a specialized notebook that develops an AR(1) autoregressive model Y(i) = A0 + A1*Y(i-1) + E(i) using scipy.stats.linregress, validates via residual analysis showing the uniform distribution of residuals (7x variability reduction from 2.079 to 0.293).

Output: `buildRandomWalkNotebook()` function producing a valid nbformat v4.5 notebook with 11 sections: intro, setup, data-loading, summary-stats, 4-plot, initial-interpretation, ar1-model, predicted-vs-original, residual-4-plot, residual-distribution, conclusions.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-advanced-case-study-notebooks/100-RESEARCH.md

@src/lib/eda/notebooks/templates/standard.ts
@src/lib/eda/notebooks/templates/sections/intro.ts
@src/lib/eda/notebooks/cells.ts
@src/lib/eda/notebooks/notebook.ts
@src/lib/eda/notebooks/types.ts
@src/lib/eda/notebooks/registry/types.ts
@src/lib/eda/notebooks/registry/random-walk.ts
</context>

<feature>
  <name>Random Walk Advanced Notebook</name>
  <files>
    src/lib/eda/notebooks/templates/advanced/random-walk.ts,
    src/lib/eda/notebooks/templates/sections/model-fitting/ar1.ts,
    src/lib/eda/notebooks/__tests__/random-walk-notebook.test.ts
  </files>
  <behavior>
    Test: buildRandomWalkNotebook() returns object with nbformat=4, nbformat_minor=5
    Test: Notebook cells array has at least 20 cells
    Test: At least one code cell source contains "linregress" (AR(1) fitting)
    Test: At least one code cell source contains "y[:-1]" and "y[1:]" (lag regression)
    Test: At least one code cell source contains "residuals" (residual computation)
    Test: At least one code cell source contains "ddof=2" (2 parameters estimated for residual SD)
    Test: At least one markdown cell contains "0.050165" (NIST A0 reference)
    Test: At least one markdown cell contains "0.987087" (NIST A1 reference)
    Test: At least one markdown cell contains "0.2931" (NIST residual SD reference)
    Test: At least one code cell produces predicted vs original overlay plot (contains "y_pred" and "plot" or "scatter")
    Test: At least one markdown cell mentions "uniform" distribution for residuals
    Test: At least one code cell generates a uniform probability plot of residuals
    Test: All cells have unique IDs
    Test: First markdown cell contains "Random Walk"
  </behavior>
  <implementation>
    1. Create `src/lib/eda/notebooks/templates/sections/model-fitting/ar1.ts`:
       - Export `buildAR1Model(config, slug, startIndex)` following the `{ cells, nextIndex }` pattern
       - Markdown cell explaining AR(1) model: Y(i) = A0 + A1*Y(i-1) + E(i)
       - Code cell: `slope, intercept, r_value, p_value, std_err = linregress(y[:-1], y[1:])`
       - Code cell computing predicted values: `y_pred = intercept + slope * y[:-1]`
       - Code cell computing residuals: `residuals = y[1:] - y_pred` with `resid_sd = np.std(residuals, ddof=2)`
       - Markdown cell with parameter table comparing to NIST: A0=0.050165 +/- 0.024171, A1=0.987087 +/- 0.006313, A1 t-value=156.350, Residual SD=0.2931
       - Note: linregress returns (slope=A1, intercept=A0). Make this mapping explicit in the markdown.

    2. Create `src/lib/eda/notebooks/templates/advanced/random-walk.ts`:
       - Export `buildRandomWalkNotebook()` that:
         a. Gets config via `getCaseStudyConfig('random-walk')`
         b. Composes standard sections: buildIntro, buildSetup, buildDataLoading, buildSummaryStats, buildFourPlot
         c. Adds initial EDA interpretation markdown: lag plot shows strong linear relationship Y(i) vs Y(i-1), strong positive autocorrelation, data are NOT random
         d. Adds AR(1) model section via buildAR1Model
         e. Adds predicted vs original plot: overlay of y_pred and y on run sequence
         f. Adds residual 4-plot: same layout as standard four-plot but on residuals (reuse buildResidualValidation from Plan 01 if available, otherwise inline -- BUT since Plan 01 and 02 run in parallel, inline the residual 4-plot code cells directly in the builder or in ar1.ts)
         g. Adds residual distribution section: uniform probability plot using `scipy.stats.uniform` and `scipy.stats.probplot` with custom dist
         h. Adds conclusions: AR(1) captures autocorrelation, residual SD reduced 7x (2.079 to 0.293), residuals are uniform (not normal)
         i. Returns createNotebook(allCells)

    3. IMPORTANT: Since Plan 01 and Plan 02 run in parallel (Wave 1), do NOT import from Plan 01's residual-validation.ts. Instead, inline residual 4-plot code cells within the random-walk builder or within ar1.ts. The residual 4-plot pattern is: fig with 4 subplots (run sequence of residuals, lag plot of residuals, histogram of residuals, normal probability plot of residuals).

    4. The predicted vs original plot, residual distribution (uniform probability plot), and conclusions are inline in the main builder file since they are specific to random-walk.
  </implementation>
</feature>

<verification>
```bash
npx vitest run src/lib/eda/notebooks/__tests__/random-walk-notebook.test.ts --reporter=verbose
```
</verification>

<success_criteria>
- buildRandomWalkNotebook() produces valid nbformat v4.5 notebook
- Notebook contains AR(1) linregress fitting with correct lag indexing y[:-1], y[1:]
- Notebook contains predicted vs original overlay plot
- Notebook contains residual 4-plot and uniform distribution analysis
- Notebook contains NIST reference value comparisons
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/100-advanced-case-study-notebooks/100-02-SUMMARY.md`
</output>
