# Milestone v1.14: DevOps Skills Publishing

**Status:** SHIPPED 2026-03-05
**Phases:** 82-84
**Total Plans:** 3

## Overview

Packaged and published 4 DevOps validator skills (Dockerfile Analyzer, Docker Compose Validator, Kubernetes Manifest Analyzer, GitHub Actions Workflow Validator) to the skills.sh open agent skills ecosystem. Restructured skill directories to repo root with symlink bridge for dual-consumer architecture, verified CLI discovery and installation, and updated README with install commands and benchmark highlights.

## Phases

### Phase 82: Directory Restructure

**Goal**: Skills directories live at repo root for skills.sh CLI discovery while Astro build continues serving them via symlink
**Depends on**: Nothing (first phase of v1.14)
**Plans**: 1 plan

Plans:
- [x] 82-01: Atomic directory restructure with symlink bridge + build verification

**Details:**
- Moved 4 skill directories (dockerfile-analyzer, compose-validator, gha-validator, k8s-analyzer) from `public/skills/` to `skills/` at repo root
- Created symlink `public/skills -> ../skills` for Astro build compatibility
- Relocated benchmark artifacts to `public/benchmarks/`
- Removed stale `--help/` directory
- Verified executable permissions and Astro build output

### Phase 83: Discovery Verification

**Goal**: All 4 skills are discoverable and installable via the skills CLI, and the first install seeds skills.sh listing
**Depends on**: Phase 82 (skills must be in correct directory structure and pushed to remote)
**Plans**: 1 plan

Plans:
- [x] 83-01: Local pre-validation, push to remote, CLI discovery and install verification

**Details:**
- Validated all 4 SKILL.md frontmatter names pass regex and match directory names
- Verified `npx skills add --list` discovers exactly 4 skills from remote
- Verified all 4 individual `--skill` installs succeed
- Seeded skills.sh telemetry listing via live install
- Added skills CLI artifacts to .gitignore

### Phase 84: Documentation

**Goal**: README.md showcases the skills suite with install commands so visitors can discover and install them
**Depends on**: Phase 83 (skills verified and listed before promotion)
**Plans**: 1 plan

Plans:
- [x] 84-01: Add Agent Skills section, GHA Validator to Interactive Tools, and fix stale path references

**Details:**
- Added GitHub Actions Workflow Validator (48 rules) to Interactive Tools table
- Added Agent Skills section with npx install command, 4-skill table, and benchmark callout (98.8% pass rate, +42.4% improvement)
- Updated test-validator-hooks.sh to reference skills/ instead of stale public/skills/ paths
- Verified all 32 hook tests pass with corrected paths

---

## Milestone Summary

**Decimal Phases:**

None -- all phases were integer phases.

**Key Decisions:**

- Single atomic commit for directory restructure to avoid broken intermediate state
- Symlink bridge pattern (`public/skills -> ../skills`) for dual-consumer architecture (skills.sh CLI + Astro build)
- Relative symlink for cross-clone portability
- Added .agents/, .claude/skills/, skills-lock.json to .gitignore for skills CLI artifact hygiene
- Used 48 rules for GHA Validator in Interactive Tools (blog/codebase count) vs 46 in Agent Skills table (SKILL.md frontmatter)
- Linked skills to skills.sh pages rather than raw GitHub SKILL.md files
- Single --skill install example to keep README concise

**Issues Resolved:**

- Stale `public/skills/` path references in test scripts resolved via path update
- `--help/` stale directory removed during restructure

**Issues Deferred:**

None.

**Technical Debt Incurred:**

None.

---

_For current project status, see .planning/ROADMAP.md_
